// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Module } from '@nestjs/common';
import { RouterModule, Routes } from '@nestjs/core';
import { LookupXxxModule } from '../../lookup-xxx.module';
import { LookupXxxProjectorModule } from '../../lookup-xxx-projector.module';
/**
 * Lookup xxx Router Configuration
 *
 * Configures nested routing for lookup-xxx endpoints to follow
 * the module monolith structure: /core/lookup/api/v1/lookup-xxxs
 *
 * This replaces flat routing (/api/v1/lookup-xxxs) with contextual routing
 * that reflects the bounded context hierarchy.
 */

// Define the routing structure for the lookup-xxx bounded context
const lookupXxxRoutes: Routes = [
  {
    path: 'core',
    children: [
      {
        path: 'lookup',
        children: [
          {
            path: 'lookup-xxxs',
            module: LookupXxxModule,
          },
          {
            path: 'health',
            module: LookupXxxProjectorModule,
          },
        ],
      },
    ],
  },
];

/**
 * Lookup xxx Router Module
 *
 * Provides nested routing configuration for lookup-xxx endpoints.
 * This module should be imported in the main app module to enable
 * the nested route structure.
 *
 * Routes configured:
 * - /core/lookup/api/v1/lookup-xxxs (GET, POST)
 * - /core/lookup/api/v1/lookup-xxxs/:code (GET, PUT, DELETE)
 * - /core/lookup/health/projectors/lookupXxx (GET)
 * - /core/lookup/health/projectors/lookup-xxx/metrics (GET)
 */
@Module({
  imports: [
    LookupXxxModule,
    LookupXxxProjectorModule,
    RouterModule.register(lookupXxxRoutes),
  ],
  exports: [LookupXxxModule, LookupXxxProjectorModule],
})
export class LookupXxxRouterModule {}
