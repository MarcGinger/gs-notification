// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  Controller,
  Get,
  Query,
  Post,
  Body,
  Put,
  Delete,
  HttpStatus,
  HttpCode,
  Param,
  UseInterceptors,
  UseGuards,
} from '@nestjs/common';
import {
  ApiTags,
  ApiOperation,
  ApiResponse,
  ApiNoContentResponse,
  ApiCreatedResponse,
  ApiBody,
  ApiHeader,
  ApiParam,
  ApiOkResponse,
  ApiBearerAuth,
} from '@nestjs/swagger';
import { IdempotencyKey } from 'src/shared/interfaces/decorators';
import { IdempotencyKeyPipe } from 'src/shared/interfaces/pipes';
import { LookupXxxApplicationService } from '../../../application/services';
import { CurrentUser, IUserToken, JwtAuthGuard } from 'src/shared/security';
import {
  DetailLookupXxxResponse,
  CreateLookupXxxRequest,
  UpdateLookupXxxRequest,
  LookupXxxPageResponse,
  ListLookupXxxFilterRequest,
  ListLookupXxxResponse,
} from '../../../application/dtos';
import { Result, ResultInterceptor, DomainError } from 'src/shared/errors';
import { PaginatedResponse } from 'src/shared/application/dtos';
import {
  LookupXxxReadResource,
  LookupXxxCreateResource,
  LookupXxxUpdateResource,
  LookupXxxDeleteResource,
} from '../../lookup-xxx.resource';
import { ApiCommonErrors } from 'src/shared/interfaces/http';

@Controller()
@ApiTags('LookupXxxs')
@ApiBearerAuth()
@UseGuards(JwtAuthGuard)
@UseInterceptors(ResultInterceptor)
export class LookupXxxController {
  constructor(
    private readonly lookupXxxApplicationService: LookupXxxApplicationService,
  ) {}

  @Get(':lookupType')
  @LookupXxxReadResource()
  @ApiOperation({
    summary: 'List Lookup xxxs',
    description:
      'Retrieves a list of Lookup xxxs with optional filtering. Requires READ permission (LOW risk).',
  })
  @ApiParam({
    name: 'lookupType',
    type: 'string',
    description: '',
    example: 'COUNTRY',
  })
  @ApiOkResponse({
    description: 'List of Lookup xxxs retrieved successfully',
    type: LookupXxxPageResponse,
  })
  @ApiCommonErrors()
  async list(
    @CurrentUser() user: IUserToken,
    @Param('lookupType') lookupType: string,
    @Query() pageRequest?: ListLookupXxxFilterRequest,
  ): Promise<Result<PaginatedResponse<ListLookupXxxResponse>, DomainError>> {
    const result = await this.lookupXxxApplicationService.listLookupXxxs(
      user,
      lookupType,
      pageRequest,
    );
    return result;
  }

  @Get(':lookupType/attributes/:code')
  @LookupXxxReadResource()
  @ApiOperation({
    summary: 'Get Lookup xxx by code',
    description:
      'Retrieves a single Lookup xxx by its unique identifier. Requires READ permission (LOW risk).',
  })
  @ApiParam({
    name: 'lookupType',
    type: 'string',
    description: '',
    example: 'COUNTRY',
  })
  @ApiParam({
    name: 'code',
    type: 'string',
    description: '',
    format: 'uuid',
    example: '123e4567-e89b-12d3-a456-426614174000',
  })
  @ApiOkResponse({
    description: 'LookupXxx details retrieved successfully',
    type: DetailLookupXxxResponse,
  })
  @ApiCommonErrors()
  async get(
    @CurrentUser() user: IUserToken,
    @Param('lookupType') lookupType: string,
    @Param('code') code: string,
  ): Promise<Result<DetailLookupXxxResponse, DomainError>> {
    const result = await this.lookupXxxApplicationService.getLookupXxxById(
      user,
      lookupType,
      code,
    );

    return result;
  }
  // ========================================
  // Core CRUD Operations
  // ========================================

  @Post(':lookupType')
  @LookupXxxCreateResource()
  @HttpCode(HttpStatus.CREATED)
  @ApiOperation({
    summary: 'Create a new Lookup xxx',
    description:
      'Creates a new Lookup xxx in the catalog. Requires CREATE permission (MEDIUM risk).',
  })
  @ApiHeader({
    name: 'Idempotency-Key',
    required: false,
    description:
      'Ensures the request is processed only once. Accepts both Idempotency-Key and x-idempotency-key headers.',
  })
  @ApiParam({
    name: 'lookupType',
    type: 'string',
    description: '',
    example: 'COUNTRY',
  })
  @ApiCreatedResponse({
    type: DetailLookupXxxResponse,
    description: 'LookupXxx successfully created',
    headers: {
      Location: {
        description: 'URI of the newly created resource',
        schema: {
          type: 'string',
          example: '/lookup-xxxs/123e4567-e89b-12d3-a456-426614174000',
        },
      },
    },
  })
  @ApiCommonErrors()
  @ApiBody({ type: CreateLookupXxxRequest })
  async create(
    @CurrentUser() user: IUserToken,
    @Param('lookupType') lookupType: string,
    @Body() createLookupXxxRequest: CreateLookupXxxRequest,
    @IdempotencyKey(new IdempotencyKeyPipe())
    idempotencyKey: string | undefined,
  ): Promise<Result<DetailLookupXxxResponse, DomainError>> {
    // idempotencyKey is validated by the IdempotencyKeyPipe

    // Call service with options including idempotency key
    const result = await this.lookupXxxApplicationService.createLookupXxx(
      user,
      lookupType,
      createLookupXxxRequest,
      {
        idempotencyKey,
      },
    );

    return result;
  }

  @Put(':lookupType/attributes/:code')
  @LookupXxxUpdateResource()
  @ApiOperation({
    summary: 'Update a Lookup xxx',
    description:
      'Updates an existing Lookup xxx with new data. Supports partial updates. Requires UPDATE permission (MEDIUM risk).',
  })
  @ApiParam({
    name: 'lookupType',
    type: 'string',
    description: '',
    example: 'COUNTRY',
  })
  @ApiParam({
    name: 'code',
    type: 'string',
    description: '',
    format: 'uuid',
    example: '123e4567-e89b-12d3-a456-426614174000',
  })
  @ApiResponse({
    status: HttpStatus.OK,
    description: 'LookupXxx updated successfully',
    type: DetailLookupXxxResponse,
  })
  @ApiCommonErrors({
    include422: true,
    extra: [
      { status: 429, description: 'Too Many Requests' },
      { status: 503, description: 'Upstream dependency unavailable' },
    ],
  })
  @ApiBody({
    type: UpdateLookupXxxRequest,
    description: 'LookupXxx update data. Only provided fields will be updated.',
  })
  async update(
    @CurrentUser() user: IUserToken,
    @Param('lookupType') lookupType: string,
    @Param('code') code: string,
    @Body() updateLookupXxxRequest: UpdateLookupXxxRequest,
  ): Promise<Result<DetailLookupXxxResponse, DomainError>> {
    const result = await this.lookupXxxApplicationService.updateLookupXxx(
      user,
      lookupType,
      code,
      updateLookupXxxRequest,
    );

    return result;
  }

  @Delete(':lookupType/attributes/:code')
  @LookupXxxDeleteResource()
  @HttpCode(HttpStatus.NO_CONTENT)
  @ApiOperation({
    summary: 'Delete a Lookup xxx',
    description:
      'Marks a Lookup xxx as deleted so it is no longer active. Implementation may use soft-delete internally. Requires DELETE permission (HIGH risk, justification required).',
  })
  @ApiParam({
    name: 'lookupType',
    type: 'string',
    description: '',
    example: 'COUNTRY',
  })
  @ApiParam({
    name: 'code',
    type: 'string',
    description: '',
    format: 'uuid',
    example: '123e4567-e89b-12d3-a456-426614174000',
  })
  @ApiCommonErrors()
  @ApiNoContentResponse({
    description: 'LookupXxx deleted successfully. No content returned.',
  })
  async remove(
    @CurrentUser() user: IUserToken,
    @Param('lookupType') lookupType: string,
    @Param('code') code: string,
  ): Promise<Result<void, DomainError>> {
    const result = await this.lookupXxxApplicationService.deleteLookupXxx(
      user,
      lookupType,
      code,
    );
    return result;
  }
}
