// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

/**
 * Simple Generic Store
 *
 * Direct, no-nonsense storage for any projections.
 * Used by both projectors (write) and repositories (read).
 */

import { BaseProjection, SimpleStore } from 'src/shared/infrastructure/stores';
import { DetailLookupXxxResponse } from '../../application/dtos';

/**
 * LookupXxx projection data
 */
export interface LookupXxxProjection
  extends DetailLookupXxxResponse,
    BaseProjection {
  lastStreamRevision: string;
}

/**
 * LookupXxx-specific store instance - shared across projectors and repositories
 */
export const lookupXxxStore = new SimpleStore<LookupXxxProjection>(
  5000,
  'lookupXxx',
);
