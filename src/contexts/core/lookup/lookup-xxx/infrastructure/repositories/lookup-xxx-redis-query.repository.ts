// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Injectable, Inject } from '@nestjs/common';
import { Redis } from 'ioredis';
import { APP_LOGGER, Log, componentLogger, Logger } from 'src/shared/logging';
import { CorrelationUtil } from 'src/shared/utilities/correlation.util';
import { Clock, CLOCK } from 'src/shared/infrastructure/time';
import {
  RepositoryLoggingUtil,
  RepositoryLoggingConfig,
  handleRepositoryError,
  safeParseJSON,
  safeParseJSONArray,
  RepositoryOptions,
} from 'src/shared/infrastructure/repositories';
import { Result, DomainError, err, ok } from 'src/shared/errors';
import { Option } from 'src/shared/domain/types';
import { ActorContext } from 'src/shared/application/context';
import { RepositoryErrorFactory } from 'src/shared/domain/errors/repository.error';
import { CacheMetricsCollector } from 'src/shared/infrastructure/projections/cache-optimization';
import { LOOKUP_DI_TOKENS } from '../../../lookup.constants';
import { LookupXxxProjectionKeys } from '../../lookup-xxx-projection-keys';
import {
  DetailLookupXxxResponse,
  LookupXxxPageResponse,
  ListLookupXxxFilterRequest,
  ListLookupXxxResponse,
} from '../../application/dtos';
import { PaginationMetaResponse } from 'src/shared/application/dtos';
import { ILookupXxxQuery } from '../../application/ports';

/**
 * Internal lookupXxx data structure for Redis operations
 * Extends DetailLookupXxxResponse with additional fields needed for caching and versioning
 */
interface LookupXxxCacheData extends DetailLookupXxxResponse {
  version: number;
  createdAt: Date;
  updatedAt: Date;
}

/**
 * LookupXxx Query Repository - Interface Segregation Principle Implementation
 *
 * Bounded Context: Core/LookupXxx
 * Handles complex query operations including pagination, filtering, and search.
 * This repository focuses solely on advanced data retrieval operations using Redis projections.
 *
 * ISP Benefits:
 * - Clients that need simple reads don't depend on complex query methods
 * - Can be optimized independently using Redis indexing strategies
 * - Easier to implement caching strategies for query operations
 * - Supports analytical and reporting use cases with Redis sorted sets
 *
 * Redis Features Used:
 * - Hash-based lookupXxx storage with cluster-safe keys
 * - Sorted set indexing for efficient pagination and sorting
 * - Pattern matching for code and name filtering
 * - SCAN operations for tenant isolation
 * - Production-ready caching with metrics collection
 *
 * @domain Core Context - LookupXxx Query Repository (Redis)
 * @layer Infrastructure
 * @pattern Repository Pattern + Interface Segregation Principle + Redis Projector
 */
@Injectable()
export class LookupXxxQueryRepository implements ILookupXxxQuery {
  private readonly logger: Logger;
  private readonly loggingConfig: RepositoryLoggingConfig;
  private readonly metricsCollector = new CacheMetricsCollector();

  constructor(
    @Inject(APP_LOGGER) baseLogger: Logger,
    @Inject(CLOCK) private readonly clock: Clock,
    @Inject(LOOKUP_DI_TOKENS.IO_REDIS)
    private readonly redis: Redis,
  ) {
    this.loggingConfig = {
      serviceName: 'LookupXxxConfigService',
      component: 'LookupXxxQueryRepository',
    };
    this.logger = componentLogger(baseLogger, this.loggingConfig.component);

    Log.info(
      this.logger,
      'LookupXxxQueryRepository initialized with Redis backend',
      {
        component: this.loggingConfig.component,
        redisStatus: this.redis.status,
        clusterSafe: true,
        cacheOptimized: true,
      },
    );
  }

  /**
   * Find a single lookupXxx by code using Redis hash lookup
   *
   * Leverages the established Redis patterns from LookupXxxProjector
   * with cluster-safe keys and production-ready caching.
   *
   * Features:
   * - Cluster-safe Redis keys with hash tags for co-location
   * - Direct hash lookup for optimal performance (O(1))
   * - Tenant isolation using Redis key patterns
   * - Comprehensive logging and error handling
   * - Production-ready metrics collection
   * - Soft delete awareness
   *
   * @param actor - The actor context containing authentication and request metadata.
   * @param code - The lookupXxx code to search for.
   * @param options - Optional repository options (e.g., timeout, correlation).
   * @returns A promise resolving to a Result containing the LookupXxx response or a DomainError.
   */
  async findById(
    actor: ActorContext,
    lookupType: string,
    code: string,
    options?: RepositoryOptions,
  ): Promise<Result<Option<DetailLookupXxxResponse>, DomainError>> {
    const operation = 'findById';
    const correlationId =
      options?.correlationId ??
      CorrelationUtil.generateForOperation('lookup-xxx-query-findById');

    const logContext = this.createLogContext(operation, correlationId, actor, {
      lookupXxxCode: code,
      dataSource: 'redis-projector',
    });

    // Validate actor context with enhanced security logging
    const validation = RepositoryLoggingUtil.validateActorContext(
      this.logger,
      actor,
      logContext,
    );
    if (!validation.ok) return err(validation.error);

    // Guard tenant explicitly
    if (!actor.tenant) {
      return err(
        RepositoryErrorFactory.validationError('tenant', 'Missing tenant id'),
      );
    }

    // Log successful authorization
    RepositoryLoggingUtil.logAuthorizationSuccess(
      this.logger,
      operation,
      logContext,
      {
        operationType: 'lookup-xxx_query_findById',
        scope: 'redis_single_lookup',
        tenant: actor.tenant,
      },
    );

    try {
      // Generate cluster-safe Redis key
      const lookupXxxKey = this.generateLookupXxxKey(actor.tenant, code);

      Log.debug(this.logger, 'Executing Redis hash lookup', {
        ...logContext,
        queryDetails: {
          scope: 'redis_hgetall',
          method: 'redis.hgetall',
          key: lookupXxxKey,
          clusterSafe: true,
        },
      });

      // Fetch lookupXxx hash from Redis
      const hashData = await this.redis.hgetall(lookupXxxKey);

      if (!hashData || Object.keys(hashData).length === 0) {
        Log.debug(this.logger, 'LookupXxx not found in Redis', logContext);
        return ok(Option.none());
      }

      // Parse Redis hash to lookupXxx data
      const lookupXxx = this.parseRedisHashToLookupXxx(hashData);

      if (!lookupXxx) {
        // LookupXxx exists but is soft deleted or malformed
        Log.debug(
          this.logger,
          'LookupXxx exists but is soft deleted or malformed',
          {
            ...logContext,
            softDeleted: !!hashData.deletedAt,
          },
        );
        return ok(Option.none());
      }

      // Transform to DetailLookupXxxResponse DTO (excluding internal fields)
      const detailResponse: DetailLookupXxxResponse = {
        code: lookupXxx.code,
        lookupType: lookupXxx.lookupType,
        name: lookupXxx.name,
        description: lookupXxx.description,
        enabled: lookupXxx.enabled,
        attributes: lookupXxx.attributes,
      };

      Log.debug(this.logger, 'LookupXxx found successfully in Redis', {
        ...logContext,
        resultData: {
          lookupXxxCode: detailResponse.code,
          lookupXxxName: detailResponse.name,
          cacheHit: true,
        },
      });

      return ok(Option.some(detailResponse));
    } catch (error) {
      // Log operation error using shared utility
      RepositoryLoggingUtil.logOperationError(
        this.logger,
        operation,
        logContext,
        error as Error,
        'MEDIUM',
      );

      // Handle and return the classified error using shared utility
      return handleRepositoryError(error);
    }
  }

  /**
   * Generate cluster-safe Redis keys using centralized LookupXxxProjectionKeys
   * Ensures consistency with projector key patterns
   */
  private generateLookupXxxKey(tenant: string, id: string): string {
    // ✅ Use centralized key generation for consistency
    return LookupXxxProjectionKeys.getRedisLookupXxxKey(tenant, id);
  }

  /**
   * Generate tenant index key for pagination and sorting using centralized keys
   */
  private generateTenantIndexKey(tenant: string): string {
    return LookupXxxProjectionKeys.getRedisLookupXxxIndexKey(tenant);
  }

  /**
   * Parse Redis hash data into DetailLookupXxxResponse DTO
   * Uses DTO instead of domain props for CQRS compliance
   */
  private parseRedisHashToLookupXxx(
    hashData: Record<string, string>,
  ): LookupXxxCacheData | null {
    try {
      if (!hashData || Object.keys(hashData).length === 0) {
        return null;
      }

      // Check for soft deletion
      if (hashData.deletedAt) {
        return null;
      }

      // Parse array fields using safeParseJSONArray utility

      // Parse object fields using safeParseJSON utility
      const attributes = safeParseJSON<Record<string, unknown>>(
        hashData.attributes,
        'attributes',
      );
      // Extract basic fields directly from hash data

      return {
        lookupType: hashData.lookupType,
        code: hashData.code,
        name: hashData.name,
        description: hashData.description || undefined,
        enabled: hashData.enabled === 'true',
        attributes,
        version: parseInt(hashData.version, 10),
        createdAt: new Date(hashData.createdAt),
        updatedAt: new Date(hashData.updatedAt),
      };
    } catch (error) {
      Log.error(
        this.logger,
        'Failed to parse Redis hash data to LookupXxxSnapshot',
        {
          method: 'parseRedisHashToLookupXxx',
          error: (error as Error).message,
          code: hashData?.code,
        },
      );
      return null;
    }
  }

  /**
   * Transform LookupXxx CacheData to ListLookupXxxResponse DTO
   */
  private toListResponse(lookupXxx: LookupXxxCacheData): ListLookupXxxResponse {
    return {
      lookupType: lookupXxx.lookupType,
      code: lookupXxx.code,
      name: lookupXxx.name,
      description: lookupXxx.description,
      enabled: lookupXxx.enabled,
      attributes: lookupXxx.attributes,
    } as ListLookupXxxResponse;
  }

  /**
   * Apply filters to lookupXxx data using Redis pattern matching and client-side filtering
   */
  private matchesFilter(
    lookupXxx: LookupXxxCacheData,
    filter?: ListLookupXxxFilterRequest,
  ): boolean {
    if (!filter) return true;

    // Filter by name (partial match)
    if (filter.name) {
      if (lookupXxx.name?.toLowerCase().includes(filter.name.toLowerCase())) {
        return false;
      }
    }
    // Filter by code (partial match)
    if (filter.code) {
      if (lookupXxx.code?.toLowerCase().includes(filter.code.toLowerCase())) {
        return false;
      }
    }

    return true;
  }

  /**
   * Sort lookupXxxs based on sort criteria
   * Handles undefined/null values properly and provides stable sorting
   */
  private sortLookupXxxs(
    lookupXxxs: LookupXxxCacheData[],
    sortBy?: Record<string, string>,
  ): LookupXxxCacheData[] {
    if (!sortBy || Object.keys(sortBy).length === 0) {
      // No sorting criteria provided, return as-is
      return lookupXxxs;
    }

    return lookupXxxs.sort((a, b) => {
      for (const [field, direction] of Object.entries(sortBy)) {
        let aVal: number | Date | string | undefined;
        let bVal: number | Date | string | undefined;

        switch (field) {
          case 'name':
            aVal = a.name;
            bVal = b.name;
            break;
          case 'code':
            aVal = a.code;
            bVal = b.code;
            break;
          case 'createdAt':
            aVal = a.createdAt;
            bVal = b.createdAt;
            break;
          case 'updatedAt':
            aVal = a.updatedAt;
            bVal = b.updatedAt;
            break;
          default:
            continue;
        }

        // Handle undefined/null values - put them at the end
        if (aVal == null && bVal == null) continue;
        if (aVal == null) return 1; // null values go to the end
        if (bVal == null) return -1; // null values go to the end

        if (aVal === bVal) continue;

        let comparison: number;

        // Handle different types properly
        if (aVal instanceof Date && bVal instanceof Date) {
          comparison = aVal.getTime() - bVal.getTime() < 0 ? -1 : 1;
        } else if (typeof aVal === 'number' && typeof bVal === 'number') {
          comparison = aVal - bVal < 0 ? -1 : 1;
        } else {
          // String comparison (convert to string if needed)
          const aStr = String(aVal).toLowerCase();
          const bStr = String(bVal).toLowerCase();
          comparison = aStr < bStr ? -1 : 1;
        }

        return direction?.toLowerCase() === 'desc' ? -comparison : comparison;
      }
      return 0;
    });
  }
  /**
   * Helper to create consistent logging context using shared utilities
   */
  private createLogContext(
    operation: string,
    correlationId: string,
    actor: ActorContext,
    additionalContext?: Record<string, unknown>,
  ): Record<string, unknown> {
    return RepositoryLoggingUtil.createLogContext(
      this.loggingConfig,
      this.clock,
      operation,
      correlationId,
      actor,
      additionalContext,
    );
  }

  /**
   * Find LookupXxx records with pagination and filtering using Redis projections.
   *
   * Leverages the established Redis patterns from LookupXxxProjector and LookupXxxReaderRepository
   * with cluster-safe keys and production-ready caching.
   *
   * Features:
   * - Cluster-safe Redis keys with hash tags for co-location
   * - Client-side filtering for code patterns and name search
   * - Efficient in-memory sorting with configurable directions
   * - Pagination with total count calculation
   * - Tenant isolation using Redis key patterns
   * - Comprehensive logging and error handling
   * - Production-ready metrics collection
   *
   * @param actor - The actor context containing authentication and request metadata.
   * @param filter - Optional filter criteria for the lookup-xxx search.
   * @param options - Optional repository options (e.g., pagination, sorting).
   * @returns A promise resolving to a Result containing paginated LookupXxx responses or a DomainError.
   */
  async findPaginated(
    actor: ActorContext,
    lookupType: string,
    filter?: ListLookupXxxFilterRequest,
    options?: RepositoryOptions,
  ): Promise<Result<LookupXxxPageResponse, DomainError>> {
    const operation = 'findPaginated';
    const correlationId =
      options?.correlationId ??
      CorrelationUtil.generateForOperation('lookup-xxx-query-paginated');

    const logContext = this.createLogContext(operation, correlationId, actor, {
      filterName: filter?.name,
      filterCode: filter?.code,
      page: filter?.page,
      size: filter?.size,
      sortBy: filter?.getSortByRecord?.() || {},
      dataSource: 'redis-projector',
    });

    // Validate actor context with enhanced security logging
    const validation = RepositoryLoggingUtil.validateActorContext(
      this.logger,
      actor,
      logContext,
    );
    if (!validation.ok) return err(validation.error);

    // Guard tenant explicitly
    if (!actor.tenant) {
      return err(
        RepositoryErrorFactory.validationError('tenant', 'Missing tenant id'),
      );
    }

    // Log successful authorization
    RepositoryLoggingUtil.logAuthorizationSuccess(
      this.logger,
      operation,
      logContext,
      {
        operationType: 'lookup-xxx_query_paginated',
        scope: 'redis_projection_search',
        tenant: actor.tenant,
      },
    );

    try {
      const page = filter?.page ?? 1;
      const size = Math.min(filter?.size ?? 20, 100); // Cap at 100 items per page

      // Generate the proper Redis key pattern using centralized LookupXxxProjectionKeys
      const pattern = LookupXxxProjectionKeys.getRedisTenantLookupXxxPattern(
        actor.tenant,
      );

      Log.debug(this.logger, 'Scanning Redis for tenant lookup-xxxs', {
        ...logContext,
        queryDetails: {
          scope: 'redis_scan',
          method: 'redis.scan',
          pattern,
          clusterSafe: true,
        },
      });

      // Use Redis SCAN to find all lookup-xxx keys for the tenant
      const lookupXxxKeys: string[] = [];
      const scanIterator = this.redis.scanStream({
        match: pattern,
        count: 1000, // Batch size for scanning
      });

      for await (const keys of scanIterator) {
        lookupXxxKeys.push(...(keys as string[]));
      }

      if (lookupXxxKeys.length === 0) {
        // No lookup-xxxs found for tenant
        const meta = new PaginationMetaResponse({
          page,
          size,
          totalItems: 0,
          totalPages: 0,
          hasNextPage: false,
          hasPreviousPage: false,
        });

        const response = LookupXxxPageResponse.create([], meta);

        Log.debug(this.logger, 'No lookup-xxxs found for tenant', {
          ...logContext,
          totalItems: 0,
        });

        return ok(response);
      }

      // Batch fetch all lookup-xxx hashes using pipeline for efficiency
      const pipeline = this.redis.pipeline();
      lookupXxxKeys.forEach((key) => {
        pipeline.hgetall(key);
      });

      const results = await pipeline.exec();

      if (!results) {
        throw new Error('Redis pipeline execution failed');
      }

      // Parse all lookup-xxxs from Redis hashes
      const allLookupXxxs: LookupXxxCacheData[] = [];
      for (let i = 0; i < results.length; i++) {
        const [error, hashData] = results[i];
        if (!error && hashData) {
          const lookupXxx = this.parseRedisHashToLookupXxx(
            hashData as Record<string, string>,
          );
          if (lookupXxx) {
            allLookupXxxs.push(lookupXxx);
          }
        }
      }

      // Apply client-side filtering
      const filteredLookupXxxs = allLookupXxxs.filter((lookupXxx) =>
        this.matchesFilter(lookupXxx, filter),
      );

      // Apply client-side sorting
      const sortedLookupXxxs = this.sortLookupXxxs(
        filteredLookupXxxs,
        filter?.getSortByRecord?.(),
      );

      // Calculate pagination metadata
      const totalItems = sortedLookupXxxs.length;
      const totalPages = Math.ceil(totalItems / size);
      const hasNextPage = page < totalPages;
      const hasPreviousPage = page > 1;

      // Apply pagination
      const startIndex = (page - 1) * size;
      const endIndex = startIndex + size;
      const paginatedLookupXxxs = sortedLookupXxxs.slice(startIndex, endIndex);

      // Transform to DTOs
      const lookupXxxResponses = paginatedLookupXxxs.map((lookupXxx) =>
        this.toListResponse(lookupXxx),
      );

      const meta = new PaginationMetaResponse({
        page,
        size,
        totalItems,
        totalPages,
        hasNextPage,
        hasPreviousPage,
      });

      const response = LookupXxxPageResponse.create(lookupXxxResponses, meta);

      // Log successful query metrics
      RepositoryLoggingUtil.logQueryMetrics(
        this.logger,
        operation,
        logContext,
        {
          resultCount: paginatedLookupXxxs.length,
          dataQuality: paginatedLookupXxxs.length > 0 ? 'good' : 'empty',
          sampleData: {
            totalItems,
            totalKeysScanned: lookupXxxKeys.length,
            filteredCount: filteredLookupXxxs.length,
            page,
            size,
            hasFilters: !!(filter?.name || filter?.code),
            sortFields: Object.keys(filter?.getSortByRecord?.() ?? {}),
          },
        },
      );

      return ok(response);
    } catch (error) {
      // Log operation error using shared utility
      RepositoryLoggingUtil.logOperationError(
        this.logger,
        operation,
        logContext,
        error as Error,
        'HIGH',
      );

      // Handle and return the classified error using shared utility
      return handleRepositoryError(error);
    }
  }
}
