// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DocumentBuilder, SwaggerModule } from '@nestjs/swagger';
import { INestApplication } from '@nestjs/common';
import { SwaggerConfigUtil } from 'src/docs/swagger-config.util';
import { LookupXxxModule } from '../lookup-xxx.module';
/**
 * LookupXxx documentation endpoints interface
 */
export interface LookupXxxDocumentationUrls {
  lookupXxx: string;
}

/**
 * LookupXxx Documentation
 * This module handles the Swagger documentation for lookup_xxxs
 *
 */
export class LookupXxxDocumentation {
  static setup(app: INestApplication, port: string | number): void {
    const config = new DocumentBuilder()
      .addBearerAuth(
        { type: 'http', scheme: 'bearer', bearerFormat: 'JWT' },
        'bearer',
      )
      .setTitle('📱 LookupXxx Management API')
      .setDescription(
        `
## bounded context: core
[← Back to documentation](/api/docs/core)

### application: lookup
[← Back to documentation](/api/docs/core/lookup)
`,
      )
      .setVersion('1.0.0')
      .addTag('LookupXxxs', `Configuration for lookup_xxx table`);

    // Add dynamic server configuration
    SwaggerConfigUtil.addServers(config, port);

    const document = SwaggerModule.createDocument(app, config.build(), {
      include: [LookupXxxModule],
      extraModels: [],
    });

    SwaggerModule.setup('api/docs/core/lookup/lookup-xxx', app, document);
  }

  /**
   * Setup all lookupXxx-related documentation endpoints
   * This method centralizes the setup of all lookupXxx module documentation
   */
  static setupAll(
    app: INestApplication,
    port: string | number,
  ): LookupXxxDocumentationUrls {
    // Setup main lookupXxx documentation
    LookupXxxDocumentation.setup(app, port);

    // Setup sub-module documentation

    // Return all endpoint URLs
    return {
      lookupXxx: LookupXxxDocumentation.getEndpoint(port),
    };
  }

  static getEndpoint(port: string | number): string {
    return `${SwaggerConfigUtil.getServerUrl(port)}/api/docs/core/lookup/lookup-xxx`;
  }
}
