// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DomainError, Result, err, withContext, ok } from 'src/shared/errors';
import { Clock } from 'src/shared/domain/clock';
import { EventMetadata } from 'src/shared/domain/events';
import { LookupXxxAggregate } from '../aggregates';
import { CreateLookupXxxProps } from '../props';
import { LookupXxxDomainState } from '../state';
import {
  LookupXxxCreatedAt,
  LookupXxxUpdatedAt,
  LookupXxxVersion,
  LookupXxxLookupType,
  LookupXxxCode,
  LookupXxxName,
  LookupXxxDescription,
  LookupXxxEnabled,
  LookupXxxAttributes,
} from '../value-objects';

/**
 * Enhanced lookupXxx entity factory with comprehensive validation and security context
 * Moved from application layer to properly separate business concerns
 */
export function createLookupXxxAggregateFromProps(
  props: CreateLookupXxxProps,
  metadata: EventMetadata,
  clock: Clock,
  // validatedEntities?: {
  //  channels?: ChannelReference[];
  // },
): Result<LookupXxxAggregate, DomainError> {
  // Validate each property by creating value objects
  // Generate a validate identity column UUID
  const codeResult = LookupXxxCode.generate();
  if (!codeResult.ok) {
    return err(
      withContext(codeResult.error, {
        ...codeResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_lookup_xxx',
      }),
    );
  }

  const lookupTypeResult = LookupXxxLookupType.from(props.lookupType);
  if (!lookupTypeResult.ok) {
    return err(
      withContext(lookupTypeResult.error, {
        ...lookupTypeResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_lookup_xxx',
        lookupType: props.lookupType,
      }),
    );
  }

  const nameResult = LookupXxxName.from(props.name);
  if (!nameResult.ok) {
    return err(
      withContext(nameResult.error, {
        ...nameResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_lookup_xxx',
        name: props.name,
      }),
    );
  }

  const descriptionResult = LookupXxxDescription.from(props.description);
  if (!descriptionResult.ok) {
    return err(
      withContext(descriptionResult.error, {
        ...descriptionResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_lookup_xxx',
        description: props.description,
      }),
    );
  }

  const enabledResult = LookupXxxEnabled.from(props.enabled);
  if (!enabledResult.ok) {
    return err(
      withContext(enabledResult.error, {
        ...enabledResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_lookup_xxx',
        enabled: props.enabled,
      }),
    );
  }

  const attributesResult = LookupXxxAttributes.from(props.attributes);
  if (!attributesResult.ok) {
    return err(
      withContext(attributesResult.error, {
        ...attributesResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_lookup_xxx',
        attributes: props.attributes,
      }),
    );
  }

  const createdAtResult = LookupXxxCreatedAt.create(clock.now());
  if (!createdAtResult.ok) {
    return err(createdAtResult.error);
  }

  const updatedAtResult = LookupXxxUpdatedAt.create(clock.now());
  if (!updatedAtResult.ok) {
    return err(updatedAtResult.error);
  }

  const versionResult = LookupXxxVersion.create(1); // Initial version is 1
  if (!versionResult.ok) {
    return err(versionResult.error);
  }

  // Create the entity properties with validated value objects
  const entityProps: LookupXxxDomainState = {
    code: codeResult.value,
    lookupType: lookupTypeResult.value,
    name: nameResult.value,
    description: descriptionResult.value,
    enabled: enabledResult.value,
    attributes: attributesResult.value,
    createdAt: createdAtResult.value,
    updatedAt: updatedAtResult.value,
    version: versionResult.value,
  };

  // Create the entity using the constructor
  return LookupXxxAggregate.create(entityProps, clock, metadata);
}
