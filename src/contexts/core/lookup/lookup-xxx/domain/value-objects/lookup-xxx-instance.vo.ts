// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DomainError, Result, ok, err } from 'src/shared/errors';
import {
  createIntegerVO,
  createIntegerVOErrors,
  IntegerVOInstance,
  createDateTimeVO,
  createDateTimeVOErrors,
  DateTimeVOInstance,
} from 'src/shared/domain/value-objects';
import { Clock } from 'src/shared/domain/clock';
import { LookupXxxErrors } from '../errors/lookup-xxx.errors';

/**
 * LookupXxx Instance Value Objects
 *
 * Instance VOs for LookupXxx aggregate lifecycle management.
 * Includes creation timestamps, update timestamps, and version tracking.
 * Follows the Clock injection pattern for deterministic testing.
 */

// ===========================================
// LookupXxx Created At Instance VO
// ===========================================

const LookupXxxCreatedAtErrors = createDateTimeVOErrors(
  LookupXxxErrors.CREATED_AT_REQUIRED,
  'LookupXxx Created At',
);

export const LookupXxxCreatedAt = createDateTimeVO({
  name: 'LookupXxxCreatedAt',
  allowFuture: false, // Cannot create lookup_xxx in the future
  allowPast: true, // Can create with past timestamps (data migration)
  customValidation: (date: Date): Result<void, DomainError> => {
    // Optional: Add domain-specific creation time validation
    // For example, cannot be more than 1 year in the past
    const oneYearAgo = new Date();
    oneYearAgo.setFullYear(oneYearAgo.getFullYear() - 1);

    if (date < oneYearAgo) {
      return err({
        ...LookupXxxErrors.CREATED_AT_REQUIRED,
        detail:
          'LookupXxx creation date cannot be more than 1 year in the past',
        context: { value: date, maxPastDate: oneYearAgo },
      } as DomainError);
    }

    return ok(undefined);
  },
  errors: LookupXxxCreatedAtErrors,
});

export type LookupXxxCreatedAt = DateTimeVOInstance;

// ===========================================
// LookupXxx Updated At Instance VO
// ===========================================

const LookupXxxUpdatedAtErrors = createDateTimeVOErrors(
  LookupXxxErrors.UPDATED_AT_REQUIRED,
  'LookupXxx Updated At',
);

export const LookupXxxUpdatedAt = createDateTimeVO({
  name: 'LookupXxxUpdatedAt',
  allowFuture: false, // Cannot update lookup_xxx in the future
  allowPast: true, // Can update with past timestamps (data correction)
  errors: LookupXxxUpdatedAtErrors,
});

export type LookupXxxUpdatedAt = DateTimeVOInstance;

// ===========================================
// LookupXxx Version Instance VO
// ===========================================

const LookupXxxVersionErrors = createIntegerVOErrors(
  LookupXxxErrors.INVALID_CODE_DATA, // Reusing existing error until specific version error is added
  'LookupXxx Version',
);

export const LookupXxxVersion = createIntegerVO({
  name: 'LookupXxxVersion',
  min: 0, // Version starts at 0
  allowNegative: false,
  errors: LookupXxxVersionErrors,
});

export type LookupXxxVersion = IntegerVOInstance;

// ===========================================
// Clock-Based Helper Functions (Explicit Dependencies)
// ===========================================

/**
 * Create LookupXxxCreatedAt from Clock (explicit dependency)
 * Deterministic and testable - pass a fake clock in tests
 */
export const createdAtNow = (
  clock: Clock,
): Result<LookupXxxCreatedAt, DomainError> =>
  LookupXxxCreatedAt.create(clock.now());

/**
 * Create LookupXxxUpdatedAt from Clock (explicit dependency)
 * Deterministic and testable - pass a fake clock in tests
 */
export const updatedAtNow = (
  clock: Clock,
): Result<LookupXxxUpdatedAt, DomainError> =>
  LookupXxxUpdatedAt.create(clock.now());

/**
 * Bump version by 1 (domain business rule)
 */
export const bumpVersion = (
  currentVersion: LookupXxxVersion,
): Result<LookupXxxVersion, DomainError> =>
  LookupXxxVersion.create(currentVersion.value + 1);

/**
 * Create initial version (starts at 1 for new aggregates)
 */
export const initialVersion = (): Result<LookupXxxVersion, DomainError> =>
  LookupXxxVersion.create(1);

// ===========================================
// Direct Creation Functions (Non-Clock Based)
// ===========================================

/**
 * Create LookupXxxCreatedAt from Date
 */
export const createLookupXxxCreatedAt = (
  date: Date,
): Result<LookupXxxCreatedAt, DomainError> => LookupXxxCreatedAt.create(date);

/**
 * Create LookupXxxUpdatedAt from Date
 */
export const createLookupXxxUpdatedAt = (
  date: Date,
): Result<LookupXxxUpdatedAt, DomainError> => LookupXxxUpdatedAt.create(date);

/**
 * Create LookupXxxVersion from number
 */
export const createLookupXxxVersion = (
  version: number,
): Result<LookupXxxVersion, DomainError> => LookupXxxVersion.create(version);

// ===========================================
// Domain Invariants & Validation
// ===========================================

/**
 * Validate that updatedAt is not before createdAt (monotonic time)
 */
export const validateMonotonicTime = (
  createdAt: LookupXxxCreatedAt,
  updatedAt: LookupXxxUpdatedAt,
): Result<void, DomainError> => {
  if (updatedAt.value < createdAt.value) {
    return err({
      ...LookupXxxErrors.UPDATED_AT_REQUIRED,
      detail: 'Updated at cannot be before created at',
      context: {
        createdAt: createdAt.value.toISOString(),
        updatedAt: updatedAt.value.toISOString(),
      },
    } as DomainError);
  }
  return ok(undefined);
};

// ===========================================
// Legacy Compatibility (DEPRECATED)
// ===========================================

/**
 * @deprecated Use createdAtNow(clock) instead for better testability
 * This function hides the Date dependency and makes testing harder
 */
export function createLookupXxxCreatedAtNow(): Result<
  LookupXxxCreatedAt,
  DomainError
> {
  return LookupXxxCreatedAt.create(new Date());
}

/**
 * @deprecated Use updatedAtNow(clock) instead for better testability
 * This function hides the Date dependency and makes testing harder
 */
export function createLookupXxxUpdatedAtNow(): Result<
  LookupXxxUpdatedAt,
  DomainError
> {
  return LookupXxxUpdatedAt.create(new Date());
}
