// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { LookupXxxErrors } from '../errors/lookup-xxx.errors';

/**
 * Description Value Object
 * Represents a validated description with business rules
 */
export const LookupXxxDescription = createStringVO({
  name: 'Description',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    LookupXxxErrors.INVALID_DESCRIPTION,
    'Description',
  ),
});

/**
 * Public instance type for Description
 */
export type LookupXxxDescription = StringVOInstance;

// Convenience creators
export const createLookupXxxDescription = (s: string) =>
  LookupXxxDescription.create(s);
export const lookupXxxDescriptionFrom = (v: unknown) =>
  LookupXxxDescription.from(v);
