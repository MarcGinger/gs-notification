// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  RecordVOInstance,
  createRecordVO,
  createRecordVOErrors,
} from 'src/shared/domain/value-objects';
import { LookupXxxErrors } from '../errors/lookup-xxx.errors';

/**
 * Attributes Record Value Object
 * Validates a plain object (Record<string, unknown>); deepFreeze.
 */
export const LookupXxxAttributes = createRecordVO({
  name: 'Attributes',
  // JSON key validation - must start with letter/underscore, contain only alphanumeric, underscore
  keyPattern: /^[a-zA-Z_][a-zA-Z0-9_]*$/,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createRecordVOErrors(
    LookupXxxErrors.INVALID_ATTRIBUTES,
    'Attributes',
  ),
});

/** Public instance type for Attributes */
export type LookupXxxAttributes = RecordVOInstance;

// Convenience creators
export const createLookupXxxAttributes = (v: Record<string, unknown>) =>
  LookupXxxAttributes.create(v);

export const lookupXxxAttributesFrom = (v: unknown) =>
  LookupXxxAttributes.from(v);
