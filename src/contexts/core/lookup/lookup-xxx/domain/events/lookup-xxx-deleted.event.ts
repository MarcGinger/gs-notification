// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { EventMetadata } from 'src/shared/domain/events';

/**
 * LookupXxx Deleted Event Payload
 * Contains identifying information about the deleted lookupXxx
 */
export interface LookupXxxDeletedEventPayload {
  code: string;
  deletedAt: Date;
  version: number;
  metadata: EventMetadata;
}

/**
 * LookupXxx Deleted Domain Event
 *
 * Emitted when a lookupXxx is successfully deleted.
 * Contains identifying information and deletion metadata.
 */
export class LookupXxxDeletedEvent {
  public readonly eventType = 'CoreLookupLookupXxxDeleted.v1';
  public readonly eventVersion = 'v1';

  constructor(public readonly payload: LookupXxxDeletedEventPayload) {}

  // Factory method with security context
  static create(
    data: Omit<LookupXxxDeletedEventPayload, 'metadata'>,
    baseMetadata: EventMetadata,
  ): LookupXxxDeletedEvent {
    const metadata: EventMetadata = {
      ...baseMetadata,
      eventVersion: '1.0',
      source: 'core.domain.lookup-xxx',
    };

    return new LookupXxxDeletedEvent({
      ...data,
      metadata,
    });
  }

  get code(): string {
    return this.payload.code;
  }

  get deletedAt(): Date {
    return this.payload.deletedAt;
  }

  get version(): number {
    return this.payload.version;
  }

  get metadata(): EventMetadata {
    return this.payload.metadata;
  }
}
