// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { applyDecorators } from '@nestjs/common';
import { ApiProperty } from '@nestjs/swagger';
import { IsUUID, IsOptional, IsNotEmpty } from 'class-validator';

/**
 * Options for property decorators
 */
interface PropOptions {
  required?: boolean;
}

/**
 * Primary key and unique identifier for the lookup entry. Used as the canonical reference value when this lookup is selected or referenced by other entities.
 *
 *
 */
export function ApiLookupXxxCode(options: PropOptions = {}): PropertyDecorator {
  const { required = false } = options;

  return applyDecorators(
    ApiProperty({
      type: String,
      description:
        'Primary key and unique identifier for the lookup entry. Used as the canonical reference value when this lookup is selected or referenced by other entities.',
      example: 'USA',
      required,
    }),

    IsUUID(4),
    required ? IsNotEmpty() : IsOptional(),
  );
}
