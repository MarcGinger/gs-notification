// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { CommandHandler, ICommandHandler } from '@nestjs/cqrs';
import { Inject } from '@nestjs/common';
import { Result, DomainError } from 'src/shared/errors';
import { DeleteLookupXxxUseCase } from '../use-cases';
import { DeleteLookupXxxCommand } from '../commands';

@CommandHandler(DeleteLookupXxxCommand)
export class DeleteLookupXxxHandler
  implements ICommandHandler<DeleteLookupXxxCommand>
{
  constructor(
    @Inject(DeleteLookupXxxUseCase)
    private readonly deleteLookupXxxUseCase: DeleteLookupXxxUseCase,
  ) {}

  async execute(
    command: DeleteLookupXxxCommand,
  ): Promise<Result<void, DomainError>> {
    return await this.deleteLookupXxxUseCase.execute({
      user: command.user,
      lookupType: command.lookupType,
      code: command.code,
      correlationId: command.correlationId,
      authorizationReason: 'CQRS Command Handler',
    });
  }
}
