// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { CommandHandler, ICommandHandler } from '@nestjs/cqrs';
import { Inject } from '@nestjs/common';
import { Result, DomainError } from 'src/shared/errors';
import { APP_LOGGER, componentLogger, Logger } from 'src/shared/logging';
import { CommandHandlerUtil } from 'src/shared/application';
import { DetailLookupXxxResponse } from '../dtos';
import { CreateLookupXxxUseCase } from '../use-cases';
import { CreateLookupXxxCommand } from '../commands';

/**
 * Enhanced Create LookupXxx Handler with security context support
 */
@CommandHandler(CreateLookupXxxCommand)
export class CreateLookupXxxHandler
  implements ICommandHandler<CreateLookupXxxCommand>
{
  private readonly logger: Logger;

  constructor(
    @Inject(CreateLookupXxxUseCase)
    private readonly createLookupXxxUseCase: CreateLookupXxxUseCase,
    @Inject(APP_LOGGER) moduleLogger: Logger,
  ) {
    this.logger = componentLogger(moduleLogger, 'CreateLookupXxxHandler');
  }

  /**
   * Executes the create lookupXxx command with enhanced security and validation
   */
  async execute(
    command: CreateLookupXxxCommand,
  ): Promise<Result<DetailLookupXxxResponse, DomainError>> {
    const commandName = 'CreateLookupXxxCommand';

    // Log command execution start
    CommandHandlerUtil.logCommandStart(this.logger, commandName, command, {
      application: 'lookup',
      component: 'CreateLookupXxxHandler',
      extractCommandData: (cmd: CreateLookupXxxCommand) => ({
        propsKeys: Object.keys(cmd.props || {}),
      }),
    });

    // Delegate to use case with full command context
    const result = await this.createLookupXxxUseCase.execute({
      user: command.user,
      props: command.props,
      correlationId: command.correlationId,
      authorizationReason: 'CQRS Command Handler',
    });

    // Log result with enhanced context
    if (result.ok) {
      CommandHandlerUtil.logCommandSuccess(
        this.logger,
        commandName,
        command,
        result.value,
        {
          application: 'lookup',
          component: 'CreateLookupXxxHandler',
        },
      );
    } else {
      // Use shared error handling with custom context
      CommandHandlerUtil.logValidationError(
        this.logger,
        commandName,
        result.error,
        command,
        {
          application: 'lookup',
          component: 'CreateLookupXxxHandler',
          extractErrorContext: (cmd: CreateLookupXxxCommand) => ({
            inputStructure: {
              hasProps: !!cmd.props,
              propsKeys: cmd.props ? Object.keys(cmd.props) : [],
            },
          }),
        },
      );
    }

    return result;
  }
}
