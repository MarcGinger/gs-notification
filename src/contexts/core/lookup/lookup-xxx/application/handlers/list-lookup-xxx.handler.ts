// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { QueryHandler, IQueryHandler } from '@nestjs/cqrs';
import { Injectable } from '@nestjs/common';
import { DomainError, Result } from 'src/shared/errors';
import { ListLookupXxxQuery } from '../queries';
import { ListLookupXxxUseCase } from '../use-cases';
import { LookupXxxPageResponse } from '../dtos';

/**
 * ✅ Enhanced CQRS Query Handler with use case delegation
 * Follows the pattern: Handler → Use Case → Repository
 */
@Injectable()
@QueryHandler(ListLookupXxxQuery)
export class ListLookupXxxHandler implements IQueryHandler<ListLookupXxxQuery> {
  constructor(private readonly listLookupXxxUseCase: ListLookupXxxUseCase) {}

  /**
   * Handle the list lookupXxx query by delegating to the use case
   */
  async execute(
    query: ListLookupXxxQuery,
  ): Promise<Result<LookupXxxPageResponse, DomainError>> {
    // ✅ Delegate to use case for business logic
    return await this.listLookupXxxUseCase.execute({
      user: query.user,
      lookupType: query.lookupType,
      filter: query.filter,
      correlationId: query.correlationId || 'unknown',
    });
  }
}
