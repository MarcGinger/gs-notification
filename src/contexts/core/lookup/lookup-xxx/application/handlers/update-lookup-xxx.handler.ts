// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { CommandHandler, ICommandHandler } from '@nestjs/cqrs';
import { Inject } from '@nestjs/common';
import { Result, DomainError } from 'src/shared/errors';
import { APP_LOGGER, componentLogger, Logger } from 'src/shared/logging';
import { CommandHandlerUtil } from 'src/shared/application';
import { DetailLookupXxxResponse } from '../dtos';
import { UpdateLookupXxxUseCase } from '../use-cases';
import { UpdateLookupXxxCommand } from '../commands';

/**
 * Enhanced Update LookupXxx Handler with security context support
 */
@CommandHandler(UpdateLookupXxxCommand)
export class UpdateLookupXxxHandler
  implements ICommandHandler<UpdateLookupXxxCommand>
{
  private readonly logger: Logger;

  constructor(
    @Inject(UpdateLookupXxxUseCase)
    private readonly updateLookupXxxUseCase: UpdateLookupXxxUseCase,
    @Inject(APP_LOGGER) moduleLogger: Logger,
  ) {
    this.logger = componentLogger(moduleLogger, 'UpdateLookupXxxHandler');
  }

  /**
   * Executes the update lookupXxx command with enhanced security and validation
   */
  async execute(
    command: UpdateLookupXxxCommand,
  ): Promise<Result<DetailLookupXxxResponse, DomainError>> {
    const commandName = 'UpdateLookupXxxCommand';

    // Log command execution start
    CommandHandlerUtil.logCommandStart(this.logger, commandName, command, {
      application: 'lookup',
      component: 'UpdateLookupXxxHandler',
      extractCommandData: (cmd: UpdateLookupXxxCommand) => ({
        propsKeys: Object.keys(cmd.props || {}),
      }),
    });

    // Transform command to use case parameters
    const result = await this.updateLookupXxxUseCase.execute({
      user: command.user,
      code: command.code,
      props: command.props,
      correlationId: command.correlationId,
      authorizationReason: 'CQRS Command Handler',
    });

    // Log result with enhanced context
    if (result.ok) {
      CommandHandlerUtil.logCommandSuccess(
        this.logger,
        commandName,
        command,
        result.value,
        {
          application: 'lookup',
          component: 'UpdateLookupXxxHandler',
        },
      );
    } else {
      // Use shared error handling with custom context
      CommandHandlerUtil.logValidationError(
        this.logger,
        commandName,
        result.error,
        command,
        {
          application: 'lookup',
          component: 'UpdateLookupXxxHandler',
          extractErrorContext: (cmd: UpdateLookupXxxCommand) => ({
            inputStructure: {
              hasProps: !!cmd.props,
              propsKeys: cmd.props ? Object.keys(cmd.props) : [],
            },
          }),
        },
      );
    }

    return result;
  }
}
