// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { QueryHandler, IQueryHandler } from '@nestjs/cqrs';
import { DetailLookupXxxResponse } from '../dtos';
import { GetLookupXxxUseCase } from '../use-cases';
import { GetLookupXxxQuery } from '../queries';
import { Result, DomainError } from 'src/shared/errors';

@QueryHandler(GetLookupXxxQuery)
export class GetLookupXxxHandler implements IQueryHandler<GetLookupXxxQuery> {
  constructor(private readonly getLookupXxxUseCase: GetLookupXxxUseCase) {}

  async execute(
    query: GetLookupXxxQuery,
  ): Promise<Result<DetailLookupXxxResponse | null, DomainError>> {
    return this.getLookupXxxUseCase.execute({
      user: query.user,
      lookupType: query.lookupType,
      code: query.code,
      correlationId: query.correlationId || 'query-handler',
    });
  }
}
