// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { LookupXxxPermission } from '../../domain/permissions/lookup-xxx.permissions';

/**
 * Field-level permission matrix for LookupXxx updates.
 * Maps lookupXxx fields to the required permissions for modification.
 */
export class LookupXxxFieldPermissionMatrix {
  private static readonly fieldPermissions: Record<
    string,
    LookupXxxPermission[]
  > = {
    // Basic lookupXxx information fields - require standard update permission
    name: [LookupXxxPermission.DOMAIN_LOOKUP_XXX_UPDATE],
    description: [LookupXxxPermission.DOMAIN_LOOKUP_XXX_UPDATE],
    category: [LookupXxxPermission.DOMAIN_LOOKUP_XXX_UPDATE],

    // Status fields - may require special permissions or admin rights
    active: [
      LookupXxxPermission.DOMAIN_LOOKUP_XXX_UPDATE,
      LookupXxxPermission.DOMAIN_LOOKUP_XXX_ADMIN,
    ],
    status: [
      LookupXxxPermission.DOMAIN_LOOKUP_XXX_UPDATE,
      LookupXxxPermission.DOMAIN_LOOKUP_XXX_ADMIN,
    ],

    // Financial fields - typically require higher permissions
    fees: [LookupXxxPermission.DOMAIN_LOOKUP_XXX_UPDATE],
    rates: [LookupXxxPermission.DOMAIN_LOOKUP_XXX_UPDATE],
    pricing: [LookupXxxPermission.DOMAIN_LOOKUP_XXX_UPDATE],

    // Configuration fields
    channelCodes: [LookupXxxPermission.DOMAIN_LOOKUP_XXX_UPDATE],
    railCodes: [LookupXxxPermission.DOMAIN_LOOKUP_XXX_UPDATE],
    configuration: [LookupXxxPermission.DOMAIN_LOOKUP_XXX_UPDATE],

    // Administrative fields - require admin permissions
    tenant: [LookupXxxPermission.DOMAIN_LOOKUP_XXX_ADMIN],
    auditInfo: [LookupXxxPermission.DOMAIN_LOOKUP_XXX_ADMIN],
    metadata: [LookupXxxPermission.DOMAIN_LOOKUP_XXX_UPDATE],
  };

  /**
   * Get required permissions for a specific field.
   * Returns empty array if field has no special permission requirements.
   */
  static getRequiredPermissions(fieldName: string): LookupXxxPermission[] {
    return this.fieldPermissions[fieldName] || [];
  }

  /**
   * Check if a field has specific permission requirements.
   */
  static hasSpecialPermissions(fieldName: string): boolean {
    return fieldName in this.fieldPermissions;
  }

  /**
   * Get all fields that require a specific permission.
   */
  static getFieldsForPermission(permission: LookupXxxPermission): string[] {
    return Object.entries(this.fieldPermissions)
      .filter(([, perms]) => perms.includes(permission))
      .map(([field]) => field);
  }

  /**
   * Get all defined field permissions.
   */
  static getAllFieldPermissions(): Record<string, LookupXxxPermission[]> {
    return { ...this.fieldPermissions };
  }

  /**
   * Add or update permission requirements for a field.
   */
  static setFieldPermissions(
    fieldName: string,
    permissions: LookupXxxPermission[],
  ): void {
    this.fieldPermissions[fieldName] = [...permissions];
  }
}
