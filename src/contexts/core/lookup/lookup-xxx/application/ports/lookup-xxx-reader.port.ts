// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Result, DomainError } from 'src/shared/errors';
import { RepositoryOptions } from 'src/shared/infrastructure/repositories';
import { ActorContext } from 'src/shared/application/context';
import { Option } from 'src/shared/domain/types';
import { LookupXxxSnapshotProps } from '../../domain/props';
import { LookupXxxCode } from '../../domain/value-objects';

/**
 * Token for injecting ILookupXxxReader port implementation
 */
export const LOOKUP_XXX_READER_TOKEN = 'ILookupXxxReader' as const;

/**
 * LookupXxx Reader Port - Interface Segregation Principle Implementation
 *
 * Handles basic read operations for LookupXxx aggregates.
 * This interface focuses solely on simple retrieval operations.
 *
 * ISP Benefits:
 * - Clients only depend on methods they actually use
 * - Easier to mock and test specific read scenarios
 * - Can be implemented independently from write operations
 * - Supports read-only implementations (e.g., reporting services)
 *
 * @domain Core Context - LookupXxx Reader Port
 * @layer Application Ports
 * @pattern Interface Segregation Principle
 */
export interface ILookupXxxReader {
  /**
   * Find a LookupXxx by its unique identifier
   * @param actor - The authenticated user context
   * @param code - The unique identifier of the LookupXxx
   * @param options - Optional repository options
   * @returns Result containing the LookupXxx snapshot or null if not found
   */
  findById(
    actor: ActorContext,
    lookupType: string,
    code: LookupXxxCode,
    options?: RepositoryOptions,
  ): Promise<Result<Option<LookupXxxSnapshotProps>, DomainError>>;

  /**
   * Check if a lookupXxx exists by code (for write-path validation)
   * @param actor - The authenticated user context
   * @param code - The unique identifier of the LookupXxx
   * @param options - Optional repository options
   * @returns Result containing boolean indicating existence
   */
  exists(
    actor: ActorContext,
    lookupType: string,
    code: LookupXxxCode,
    options?: RepositoryOptions,
  ): Promise<Result<boolean, DomainError>>;

  /**
   * Get lookupXxx version for optimistic concurrency control
   * @param actor - The authenticated user context
   * @param code - The unique identifier of the LookupXxx
   * @param options - Optional repository options
   * @returns Result containing version number or null if not found
   */
  getVersion(
    actor: ActorContext,
    lookupType: string,
    code: LookupXxxCode,
    options?: RepositoryOptions,
  ): Promise<Result<Option<number>, DomainError>>;

  /**
   * Get minimal lookupXxx data for write-path operations
   * @param actor - The authenticated user context
   * @param code - The unique identifier of the LookupXxx
   * @param options - Optional repository options
   * @returns Result containing minimal lookupXxx data or null if not found
   */
  getMinimal(
    actor: ActorContext,
    lookupType: string,
    code: LookupXxxCode,
    options?: RepositoryOptions,
  ): Promise<Result<Option<{ code: string; version: number }>, DomainError>>;
}
