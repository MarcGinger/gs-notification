// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

/**
 * Abstract Use Case Contracts for LookupXxx Operations
 *
 * These abstract classes are used instead of interfaces to work with NestJS DI.
 * TypeScript interfaces are erased at runtime and cannot be injected.
 */

import { Result, DomainError } from 'src/shared/errors';
import type { IUserToken } from 'src/shared/security';
import type {
  CreateLookupXxxProps,
  UpdateLookupXxxProps,
} from '../../domain/props';
import type {
  DetailLookupXxxResponse,
  LookupXxxPageResponse,
  ListLookupXxxFilterRequest,
} from '../dtos';

export abstract class ICreateLookupXxxUseCase {
  abstract execute(params: {
    user: IUserToken;
    props: CreateLookupXxxProps;
    correlationId: string;
    authorizationReason: string;
  }): Promise<Result<DetailLookupXxxResponse, DomainError>>;
}

export abstract class IUpdateLookupXxxUseCase {
  abstract execute(params: {
    user: IUserToken;
    code: string;
    props: UpdateLookupXxxProps;
    correlationId: string;
    authorizationReason: string;
  }): Promise<Result<DetailLookupXxxResponse, DomainError>>;
}

export abstract class IDeleteLookupXxxUseCase {
  abstract execute(params: {
    user: IUserToken;
    lookupType: string;
    code: string;
    correlationId: string;
    authorizationReason: string;
  }): Promise<Result<void, DomainError>>;
}

export abstract class IGetLookupXxxUseCase {
  abstract execute(params: {
    user: IUserToken;
    lookupType: string;
    code: string;
    correlationId: string;
  }): Promise<Result<DetailLookupXxxResponse, DomainError>>;
}
export abstract class IListLookupXxxUseCase {
  abstract execute(params: {
    user: IUserToken;
    lookupType: string;
    filter?: ListLookupXxxFilterRequest;
    correlationId: string;
  }): Promise<Result<LookupXxxPageResponse, DomainError>>;
}
