// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  Controller,
  Get,
  Query,
  Post,
  Body,
  Put,
  Delete,
  HttpStatus,
  HttpCode,
  Param,
  UseInterceptors,
  UseGuards,
} from '@nestjs/common';
import {
  ApiTags,
  ApiOperation,
  ApiResponse,
  ApiNoContentResponse,
  ApiCreatedResponse,
  ApiBody,
  ApiHeader,
  ApiParam,
  ApiOkResponse,
  ApiBearerAuth,
} from '@nestjs/swagger';
import { IdempotencyKey } from 'src/shared/interfaces/decorators';
import { IdempotencyKeyPipe } from 'src/shared/interfaces/pipes';
import { LookupTypeApplicationService } from '../../../application/services';
import { CurrentUser, IUserToken, JwtAuthGuard } from 'src/shared/security';
import {
  DetailLookupTypeResponse,
  CreateLookupTypeRequest,
  UpdateLookupTypeRequest,
  LookupTypePageResponse,
  ListLookupTypeFilterRequest,
  ListLookupTypeResponse,
} from '../../../application/dtos';
import { Result, ResultInterceptor, DomainError } from 'src/shared/errors';
import { PaginatedResponse } from 'src/shared/application/dtos';
import {
  LookupTypeReadResource,
  LookupTypeCreateResource,
  LookupTypeUpdateResource,
  LookupTypeDeleteResource,
} from '../../lookup-type.resource';
import { ApiCommonErrors } from 'src/shared/interfaces/http';

@Controller()
@ApiTags('LookupTypes')
@ApiBearerAuth()
@UseGuards(JwtAuthGuard)
@UseInterceptors(ResultInterceptor)
export class LookupTypeController {
  constructor(
    private readonly lookupTypeApplicationService: LookupTypeApplicationService,
  ) {}

  @Get(':lookupType')
  @LookupTypeReadResource()
  @ApiOperation({
    summary: 'List Lookup types',
    description:
      'Retrieves a list of Lookup types with optional filtering. Requires READ permission (LOW risk).',
  })
  @ApiParam({
    name: 'lookupType',
    type: 'string',
    description: '',
    example: 'COUNTRY',
  })
  @ApiOkResponse({
    description: 'List of Lookup types retrieved successfully',
    type: LookupTypePageResponse,
  })
  @ApiCommonErrors()
  async list(
    @CurrentUser() user: IUserToken,
    @Param('lookupType') lookupType: string,
    @Query() pageRequest?: ListLookupTypeFilterRequest,
  ): Promise<Result<PaginatedResponse<ListLookupTypeResponse>, DomainError>> {
    const result = await this.lookupTypeApplicationService.listLookupTypes(
      user,
      lookupType,
      pageRequest,
    );
    return result;
  }

  @Get(':lookupType/attributes/:code')
  @LookupTypeReadResource()
  @ApiOperation({
    summary: 'Get Lookup type by code',
    description:
      'Retrieves a single Lookup type by its unique identifier. Requires READ permission (LOW risk).',
  })
  @ApiParam({
    name: 'lookupType',
    type: 'string',
    description: '',
    example: 'COUNTRY',
  })
  @ApiParam({
    name: 'code',
    type: 'string',
    description: '',
    example: 'USA',
  })
  @ApiOkResponse({
    description: 'LookupType details retrieved successfully',
    type: DetailLookupTypeResponse,
  })
  @ApiCommonErrors()
  async get(
    @CurrentUser() user: IUserToken,
    @Param('lookupType') lookupType: string,
    @Param('code') code: string,
  ): Promise<Result<DetailLookupTypeResponse, DomainError>> {
    const result = await this.lookupTypeApplicationService.getLookupTypeById(
      user,
      lookupType,
      code,
    );

    return result;
  }
  // ========================================
  // Core CRUD Operations
  // ========================================

  @Post(':lookupType')
  @LookupTypeCreateResource()
  @HttpCode(HttpStatus.CREATED)
  @ApiOperation({
    summary: 'Create a new Lookup type',
    description:
      'Creates a new Lookup type in the catalog. Requires CREATE permission (MEDIUM risk).',
  })
  @ApiHeader({
    name: 'Idempotency-Key',
    required: false,
    description:
      'Ensures the request is processed only once. Accepts both Idempotency-Key and x-idempotency-key headers.',
  })
  @ApiParam({
    name: 'lookupType',
    type: 'string',
    description: '',
    example: 'COUNTRY',
  })
  @ApiCreatedResponse({
    type: DetailLookupTypeResponse,
    description: 'LookupType successfully created',
    headers: {
      Location: {
        description: 'URI of the newly created resource',
        schema: {
          type: 'string',
          example: '/lookup-types/123e4567-e89b-12d3-a456-426614174000',
        },
      },
    },
  })
  @ApiCommonErrors()
  @ApiBody({ type: CreateLookupTypeRequest })
  async create(
    @CurrentUser() user: IUserToken,
    @Param('lookupType') lookupType: string,
    @Body() createLookupTypeRequest: CreateLookupTypeRequest,
    @IdempotencyKey(new IdempotencyKeyPipe())
    idempotencyKey: string | undefined,
  ): Promise<Result<DetailLookupTypeResponse, DomainError>> {
    // idempotencyKey is validated by the IdempotencyKeyPipe

    // Call service with options including idempotency key
    const result = await this.lookupTypeApplicationService.createLookupType(
      user,
      lookupType,
      createLookupTypeRequest,
      {
        idempotencyKey,
      },
    );

    return result;
  }

  @Put(':lookupType/attributes/:code')
  @LookupTypeUpdateResource()
  @ApiOperation({
    summary: 'Update a Lookup type',
    description:
      'Updates an existing Lookup type with new data. Supports partial updates. Requires UPDATE permission (MEDIUM risk).',
  })
  @ApiParam({
    name: 'lookupType',
    type: 'string',
    description: '',
    example: 'COUNTRY',
  })
  @ApiParam({
    name: 'code',
    type: 'string',
    description: '',
    example: 'USA',
  })
  @ApiResponse({
    status: HttpStatus.OK,
    description: 'LookupType updated successfully',
    type: DetailLookupTypeResponse,
  })
  @ApiCommonErrors({
    include422: true,
    extra: [
      { status: 429, description: 'Too Many Requests' },
      { status: 503, description: 'Upstream dependency unavailable' },
    ],
  })
  @ApiBody({
    type: UpdateLookupTypeRequest,
    description:
      'LookupType update data. Only provided fields will be updated.',
  })
  async update(
    @CurrentUser() user: IUserToken,
    @Param('lookupType') lookupType: string,
    @Param('code') code: string,
    @Body() updateLookupTypeRequest: UpdateLookupTypeRequest,
  ): Promise<Result<DetailLookupTypeResponse, DomainError>> {
    const result = await this.lookupTypeApplicationService.updateLookupType(
      user,
      lookupType,
      code,
      updateLookupTypeRequest,
    );

    return result;
  }

  @Delete(':lookupType/attributes/:code')
  @LookupTypeDeleteResource()
  @HttpCode(HttpStatus.NO_CONTENT)
  @ApiOperation({
    summary: 'Delete a Lookup type',
    description:
      'Marks a Lookup type as deleted so it is no longer active. Implementation may use soft-delete internally. Requires DELETE permission (HIGH risk, justification required).',
  })
  @ApiParam({
    name: 'lookupType',
    type: 'string',
    description: '',
    example: 'COUNTRY',
  })
  @ApiParam({
    name: 'code',
    type: 'string',
    description: '',
    example: 'USA',
  })
  @ApiCommonErrors()
  @ApiNoContentResponse({
    description: 'LookupType deleted successfully. No content returned.',
  })
  async remove(
    @CurrentUser() user: IUserToken,
    @Param('lookupType') lookupType: string,
    @Param('code') code: string,
  ): Promise<Result<void, DomainError>> {
    const result = await this.lookupTypeApplicationService.deleteLookupType(
      user,
      lookupType,
      code,
    );
    return result;
  }
}
