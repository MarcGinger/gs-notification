// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { QueryHandler, IQueryHandler } from '@nestjs/cqrs';
import { DetailLookupTypeResponse } from '../dtos';
import { GetLookupTypeUseCase } from '../use-cases';
import { GetLookupTypeQuery } from '../queries';
import { Result, DomainError } from 'src/shared/errors';

@QueryHandler(GetLookupTypeQuery)
export class GetLookupTypeHandler implements IQueryHandler<GetLookupTypeQuery> {
  constructor(private readonly getLookupTypeUseCase: GetLookupTypeUseCase) {}

  async execute(
    query: GetLookupTypeQuery,
  ): Promise<Result<DetailLookupTypeResponse | null, DomainError>> {
    return this.getLookupTypeUseCase.execute({
      user: query.user,
      lookupType: query.lookupType,
      code: query.code,
      correlationId: query.correlationId || 'query-handler',
    });
  }
}
