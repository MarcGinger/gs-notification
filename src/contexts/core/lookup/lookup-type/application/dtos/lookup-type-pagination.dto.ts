// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

/**
 * LookupType list request DTO with pagination and filtering
 * Combines standardized pagination with lookup-type-specific filters
 * Now extends shared base classes for consistency across domains
 */

import {
  BaseSortableListFilterRequest,
  SortFieldConfig,
  PaginationMetaResponse,
} from 'src/shared/application/dtos';
import { ApiListOf } from 'src/shared/application/decorators';
import { ApiPropertyOptional } from '@nestjs/swagger';
import {
  ApiLookupTypeCode,
  ApiLookupTypeListMeta,
  ApiLookupTypeName,
} from './decorators';
import { ListLookupTypeResponse } from './';
/**
 * LookupType list request DTO with pagination and filtering
 * Combines standardized pagination with lookup-type-specific filters
 * Now extends shared base classes for consistency across domains
 *
 * Available sort fields:
 * - code: Sort by code (default: asc)
 * - name: Sort by name (default: asc)
 *
 * Example sortBy usage: {"code": "asc", "name": "desc"}
 */
export class ListLookupTypeFilterRequest extends BaseSortableListFilterRequest {
  @ApiLookupTypeCode({ required: false })
  code?: string;

  @ApiLookupTypeName({ required: false })
  name?: string;

  /**
   * Override sortBy with lookup-type-specific schema for better Swagger documentation
   */
  @ApiPropertyOptional({
    type: 'object',
    description: 'Sort criteria with field names and directions',
    example: { code: 'asc', name: 'asc' },
    default: { code: 'asc', name: 'asc' },
    properties: {
      Code: {
        type: 'string',
        enum: ['asc', 'desc'],
        description: 'Sort by code',
        default: 'asc',
      },
      Name: {
        type: 'string',
        enum: ['asc', 'desc'],
        description: 'Sort by name',
        default: 'asc',
      },
    },
    additionalProperties: false,
  })
  sortBy?: Record<string, 'asc' | 'desc'> = undefined;
  /**
   * Define available sort fields for lookupTypes
   * This method ensures type-safe sorting validation
   *
   * Available sort fields:
   * - code: LookupType code (sortable, default: asc)
   * - name: LookupType name (sortable, default: asc)
   *
   * @returns Configuration object defining sortable fields and their properties
   */
  getSortFieldConfig(): SortFieldConfig {
    return {
      code: {
        description: 'LookupType code',
        sortable: true,
        defaultDirection: 'asc' as const,
      },
      name: {
        description: 'LookupType name',
        sortable: true,
        defaultDirection: 'asc' as const,
      },
    };
  }
}

/**
 * LookupType page response DTO with metadata for pagination
 * Extends the generic PagedResponse pattern while maintaining API compatibility
 */
export class LookupTypePageResponse {
  @ApiListOf(ListLookupTypeResponse)
  readonly data: ListLookupTypeResponse[];

  @ApiLookupTypeListMeta()
  readonly meta: PaginationMetaResponse;

  constructor(data: ListLookupTypeResponse[], meta: PaginationMetaResponse) {
    this.data = data;
    this.meta = meta;
  }

  /**
   * Static factory method for creating response instances
   */
  static create(
    data: ListLookupTypeResponse[],
    meta: PaginationMetaResponse,
  ): LookupTypePageResponse {
    return new LookupTypePageResponse(data, meta);
  }

  /**
   * Check if response has data
   */
  get isEmpty(): boolean {
    return this.data.length === 0;
  }

  /**
   * Get total count if available
   */
  get totalCount(): number | undefined {
    return this.meta.totalItems;
  }
}
