// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Result, DomainError } from 'src/shared/errors';
import { RepositoryOptions } from 'src/shared/infrastructure/repositories';
import { ActorContext } from 'src/shared/application/context';
import { Option } from 'src/shared/domain/types';
import { LookupTypeSnapshotProps } from '../../domain/props';
import { LookupTypeCode } from '../../domain/value-objects';

/**
 * Token for injecting ILookupTypeReader port implementation
 */
export const LOOKUP_TYPE_READER_TOKEN = 'ILookupTypeReader' as const;

/**
 * LookupType Reader Port - Interface Segregation Principle Implementation
 *
 * Handles basic read operations for LookupType aggregates.
 * This interface focuses solely on simple retrieval operations.
 *
 * ISP Benefits:
 * - Clients only depend on methods they actually use
 * - Easier to mock and test specific read scenarios
 * - Can be implemented independently from write operations
 * - Supports read-only implementations (e.g., reporting services)
 *
 * @domain Core Context - LookupType Reader Port
 * @layer Application Ports
 * @pattern Interface Segregation Principle
 */
export interface ILookupTypeReader {
  /**
   * Find a LookupType by its unique identifier
   * @param actor - The authenticated user context
   * @param code - The unique identifier of the LookupType
   * @param options - Optional repository options
   * @returns Result containing the LookupType snapshot or null if not found
   */
  findById(
    actor: ActorContext,
    code: LookupTypeCode,
    options?: RepositoryOptions,
  ): Promise<Result<Option<LookupTypeSnapshotProps>, DomainError>>;

  /**
   * Check if a lookupType exists by ID (for write-path validation)
   * @param actor - The authenticated user context
   * @param code - The unique identifier of the LookupType
   * @param options - Optional repository options
   * @returns Result containing boolean indicating existence
   */
  exists(
    actor: ActorContext,
    code: LookupTypeCode,
    options?: RepositoryOptions,
  ): Promise<Result<boolean, DomainError>>;

  /**
   * Get lookupType version for optimistic concurrency control
   * @param actor - The authenticated user context
   * @param code - The unique identifier of the LookupType
   * @param options - Optional repository options
   * @returns Result containing version number or null if not found
   */
  getVersion(
    actor: ActorContext,
    code: LookupTypeCode,
    options?: RepositoryOptions,
  ): Promise<Result<Option<number>, DomainError>>;

  /**
   * Get minimal lookupType data for write-path operations
   * @param actor - The authenticated user context
   * @param code - The unique identifier of the LookupType
   * @param options - Optional repository options
   * @returns Result containing minimal lookupType data or null if not found
   */
  getMinimal(
    actor: ActorContext,
    code: LookupTypeCode,
    options?: RepositoryOptions,
  ): Promise<Result<Option<{ code: string; version: number }>, DomainError>>;
}
