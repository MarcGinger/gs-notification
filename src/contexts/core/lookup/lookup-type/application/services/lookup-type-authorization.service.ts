// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

// Framework imports
import { Injectable, Logger, Inject } from '@nestjs/common';

// Shared utilities and infrastructure
import { Result, DomainError, ok, err, withContext } from 'src/shared/errors';
import {
  AuthorizationPort,
  buildAuthorizationContext,
} from 'src/shared/security/authorization.port';
import type { AuthorizationRequest } from 'src/shared/security/authorization.port';

// Domain types and errors
import { LookupTypePermission } from '../../domain/permissions/lookup-type.permissions';
import { LookupTypeErrors } from '../../domain/errors/lookup-type.errors';
import { LOOKUP_DI_TOKENS } from '../../../lookup.constants';

// Application layer
import { LookupTypeFieldPermissionMatrix } from '../security/lookup-type-field-permission-matrix';
import {
  LookupTypeAuthContext,
  CrudOperation,
  BatchOperation,
} from '../types/lookup-type-auth-context';

/**
 * Domain-specific authorization service for LookupType operations.
 * Provides LookupType-aware convenience methods and field-level permission checking.
 *
 * Improvements:
 * - Uses typed LookupTypeAuthContext for better ABAC support
 * - Includes tenant/role information in actor context
 * - Optimized field-level checks with anyOf pattern
 * - Proper error handling with domain error factories
 * - Chunked batch processing for performance
 */
@Injectable()
export class LookupTypeAuthorizationService {
  private readonly logger = new Logger(LookupTypeAuthorizationService.name);

  constructor(
    @Inject(LOOKUP_DI_TOKENS.AUTHORIZATION_SERVICE)
    private readonly authorizationPort: AuthorizationPort<LookupTypePermission>,
  ) {}

  /**
   * Helper to create consistent logging context
   */
  private createLogContext(
    operation: string,
    correlationId: string,
    userId?: string,
    additionalContext?: Record<string, unknown>,
  ): Record<string, unknown> {
    return {
      serviceName: 'LookupTypeAuthorizationService',
      component: 'LookupTypeAuthorizationService',
      operation,
      correlationId,
      userId,
      ...additionalContext,
    };
  }

  /**
   * Check if user can read a specific lookupType
   */
  async canReadLookupType(
    userId: string,
    lookupTypes: string,
    correlationId: string,
    context?: LookupTypeAuthContext,
  ): Promise<Result<boolean, DomainError>> {
    if (!lookupTypes) {
      return err(
        withContext(LookupTypeErrors.PERMISSION_DENIED, {
          lookupTypes: lookupTypes || '',
          userId,
          correlationId,
          operation: 'read',
        }),
      );
    }

    const actor = {
      userId,
      tenant: context?.tenant,
      roles: context?.roles,
    };

    const request: AuthorizationRequest<LookupTypePermission> = {
      domain: 'lookup_type',
      permissions: [LookupTypePermission.DOMAIN_LOOKUP_TYPE_READ],
      actor,
      resource: { type: 'lookup_type', id: lookupTypes },
      context: buildAuthorizationContext(correlationId, {
        metadata: context?.metadata,
      }),
    };

    const result = await this.authorizationPort.check(request);
    return result.ok ? ok(result.value.allowed) : err(result.error);
  }

  /**
   * Check if user can create lookupTypes
   */
  async canCreateLookupType(
    userId: string,
    correlationId: string,
    context?: LookupTypeAuthContext,
  ): Promise<Result<boolean, DomainError>> {
    const actor = {
      userId,
      tenant: context?.tenant,
      roles: context?.roles,
    };

    const request: AuthorizationRequest<LookupTypePermission> = {
      domain: 'lookup_type',
      permissions: [LookupTypePermission.DOMAIN_LOOKUP_TYPE_CREATE],
      actor,
      resource: { type: 'lookup_type' },
      context: buildAuthorizationContext(correlationId, {
        metadata: context?.metadata,
      }),
    };

    const result = await this.authorizationPort.check(request);
    return result.ok ? ok(result.value.allowed) : err(result.error);
  }

  /**
   * Check if user can update a specific lookupType
   */
  async canUpdateLookupType(
    userId: string,
    lookupTypes: string,
    correlationId: string,
    context?: LookupTypeAuthContext,
  ): Promise<Result<boolean, DomainError>> {
    if (!lookupTypes) {
      return err(
        withContext(LookupTypeErrors.PERMISSION_DENIED, {
          lookupTypes: lookupTypes || '',
          userId,
          correlationId,
          operation: 'update',
        }),
      );
    }

    const actor = {
      userId,
      tenant: context?.tenant,
      roles: context?.roles,
    };

    const request: AuthorizationRequest<LookupTypePermission> = {
      domain: 'lookup_type',
      permissions: [LookupTypePermission.DOMAIN_LOOKUP_TYPE_UPDATE],
      actor,
      resource: { type: 'lookup_type', id: lookupTypes },
      context: buildAuthorizationContext(correlationId, {
        metadata: context?.metadata,
      }),
    };

    const result = await this.authorizationPort.check(request);
    return result.ok ? ok(result.value.allowed) : err(result.error);
  }

  /**
   * Check if user can delete a specific lookupType
   */
  async canDeleteLookupType(
    userId: string,
    lookupTypes: string,
    correlationId: string,
    context?: LookupTypeAuthContext,
  ): Promise<Result<boolean, DomainError>> {
    if (!lookupTypes) {
      return err(
        withContext(LookupTypeErrors.PERMISSION_DENIED, {
          lookupTypes: lookupTypes || '',
          userId,
          correlationId,
          operation: 'delete',
        }),
      );
    }

    const actor = {
      userId,
      tenant: context?.tenant,
      roles: context?.roles,
    };

    const request: AuthorizationRequest<LookupTypePermission> = {
      domain: 'lookup_type',
      permissions: [LookupTypePermission.DOMAIN_LOOKUP_TYPE_DELETE],
      actor,
      resource: { type: 'lookup_type', id: lookupTypes },
      context: buildAuthorizationContext(correlationId, {
        metadata: context?.metadata,
      }),
    };

    const result = await this.authorizationPort.check(request);
    return result.ok ? ok(result.value.allowed) : err(result.error);
  }

  /**
   * Check if user can perform admin operations on lookupTypes
   */
  async canAdministerLookupType(
    userId: string,
    correlationId: string,
    lookupTypes?: string,
    context?: LookupTypeAuthContext,
  ): Promise<Result<boolean, DomainError>> {
    const actor = {
      userId,
      tenant: context?.tenant,
      roles: context?.roles,
    };

    const request: AuthorizationRequest<LookupTypePermission> = {
      domain: 'lookup_type',
      permissions: [LookupTypePermission.DOMAIN_LOOKUP_TYPE_ADMIN],
      actor,
      resource: { type: 'lookup_type', id: lookupTypes },
      context: buildAuthorizationContext(correlationId, {
        metadata: context?.metadata,
      }),
    };

    const result = await this.authorizationPort.check(request);
    return result.ok ? ok(result.value.allowed) : err(result.error);
  }

  /**
   * Check field-level permissions for lookupType updates.
   * Returns which fields the user can modify based on the permission matrix.
   *
   * Optimizations:
   * - Uses anyOf pattern to reduce OPA calls
   * - Parallel processing of field checks
   * - Proper actor context with tenant/role information
   */
  async checkFieldPermissions(
    userId: string,
    lookupTypes: string,
    requestedFields: string[],
    correlationId: string,
    context?: LookupTypeAuthContext,
  ): Promise<
    Result<{ allowedFields: string[]; deniedFields: string[] }, DomainError>
  > {
    if (!lookupTypes) {
      return err(
        withContext(LookupTypeErrors.PERMISSION_DENIED, {
          lookupTypes: lookupTypes || '',
          userId,
          correlationId,
          operation: 'checkFieldPermissions',
        }),
      );
    }

    try {
      const actor = {
        userId,
        tenant: context?.tenant,
        roles: context?.roles,
      };

      // Process fields in parallel for better performance
      const results = await Promise.all(
        requestedFields.map(async (field) => {
          const requiredPermissions =
            LookupTypeFieldPermissionMatrix.getRequiredPermissions(field);

          // Use basic UPDATE permission if no special permissions required
          const permissions = requiredPermissions.length
            ? requiredPermissions
            : [LookupTypePermission.DOMAIN_LOOKUP_TYPE_UPDATE];

          const request: AuthorizationRequest<LookupTypePermission> = {
            domain: 'lookup_type',
            permissions,
            anyOf: true, // User needs ANY of the required permissions
            actor,
            resource: { type: 'lookup_type', id: lookupTypes },
            context: buildAuthorizationContext(correlationId, {
              metadata: { ...context?.metadata, field },
            }),
          };

          const result = await this.authorizationPort.check(request);
          return { field, allowed: result.ok && result.value.allowed };
        }),
      );

      const allowedFields = results
        .filter((r) => r.allowed)
        .map((r) => r.field);
      const deniedFields = results
        .filter((r) => !r.allowed)
        .map((r) => r.field);

      return ok({ allowedFields, deniedFields });
    } catch (error: unknown) {
      this.logger.error(
        'Error checking field permissions',
        this.createLogContext('checkFieldPermissions', correlationId, userId, {
          lookupTypes,
          requestedFields,
          error: error instanceof Error ? error.message : String(error),
        }),
      );

      return err(
        withContext(LookupTypeErrors.AUTHORIZATION_FAILED, {
          lookupTypes: lookupTypes || '',
          userId,
          correlationId,
          operation: 'checkFieldPermissions',
          reason: error instanceof Error ? error.message : String(error),
        }),
      );
    }
  }

  /**
   * Comprehensive authorization check for lookupType operations.
   * Validates both operation-level and field-level permissions.
   *
   * Improvements:
   * - Proper type safety with CrudOperation
   * - Explicit guards for required lookupType IDs
   * - Uses typed LookupTypeAuthContext
   */
  async authorizeLookupTypeOperation(
    userId: string,
    operation: CrudOperation,
    correlationId: string,
    lookupTypes?: string,
    fields?: string[],
    context?: LookupTypeAuthContext,
  ): Promise<
    Result<
      {
        authorized: boolean;
        allowedFields?: string[];
        deniedFields?: string[];
      },
      DomainError
    >
  > {
    try {
      // Validate required lookupTypes for operations that need it
      if (['read', 'update', 'delete'].includes(operation) && !lookupTypes) {
        return err(
          withContext(LookupTypeErrors.PERMISSION_DENIED, {
            lookupTypes: lookupTypes || '',
            userId,
            correlationId,
            operation: String(operation),
          }),
        );
      }

      // First check operation-level permission
      let operationResult: Result<boolean, DomainError>;

      switch (operation) {
        case 'read':
          operationResult = await this.canReadLookupType(
            userId,
            lookupTypes!,
            correlationId,
            context,
          );
          break;
        case 'create':
          operationResult = await this.canCreateLookupType(
            userId,
            correlationId,
            context,
          );
          break;
        case 'update':
          operationResult = await this.canUpdateLookupType(
            userId,
            lookupTypes!,
            correlationId,
            context,
          );
          break;
        case 'delete':
          operationResult = await this.canDeleteLookupType(
            userId,
            lookupTypes!,
            correlationId,
            context,
          );
          break;
        default:
          return err(
            withContext(LookupTypeErrors.NOT_IMPLEMENTED, {
              lookupTypes: lookupTypes || '',
              userId,
              correlationId,
              operation: String(operation),
            }),
          );
      }

      if (!operationResult.ok) {
        return err(operationResult.error);
      }

      if (!operationResult.value) {
        return ok({ authorized: false });
      }

      // For update operations with field specifications, check field-level permissions
      if (
        operation === 'update' &&
        fields &&
        fields.length > 0 &&
        lookupTypes
      ) {
        const fieldResult = await this.checkFieldPermissions(
          userId,
          lookupTypes,
          fields,
          correlationId,
          context,
        );

        if (!fieldResult.ok) {
          return err(fieldResult.error);
        }

        const { allowedFields, deniedFields } = fieldResult.value;

        // Operation is authorized, but some fields might be denied
        return ok({
          authorized: allowedFields.length > 0,
          allowedFields,
          deniedFields,
        });
      }

      // Simple operation authorization without field-level checks
      return ok({ authorized: true });
    } catch (error: unknown) {
      this.logger.error(
        'Error in comprehensive lookupType authorization',
        this.createLogContext('comprehensiveCheck', correlationId, userId, {
          operation,
          lookupTypes,
          fields,
          error: error instanceof Error ? error.message : String(error),
        }),
      );

      return err(
        withContext(LookupTypeErrors.AUTHORIZATION_FAILED, {
          lookupTypes: lookupTypes || '',
          userId,
          correlationId,
          operation: String(operation),
          reason: error instanceof Error ? error.message : String(error),
        }),
      );
    }
  }

  /**
   * Batch authorization check for multiple lookupTypes.
   * Useful for list operations where you need to filter results.
   *
   * Optimizations:
   * - Chunked processing to avoid overwhelming OPA
   * - Typed LookupTypeAuthContext and BatchOperation
   * - Proper actor context with tenant/role information
   */
  async authorizeLookupTypeList(
    userId: string,
    lookupTypess: string[],
    correlationId: string,
    operation: BatchOperation = 'read',
    context?: LookupTypeAuthContext,
  ): Promise<Result<{ authorized: string[]; denied: string[] }, DomainError>> {
    try {
      const authorized: string[] = [];
      const denied: string[] = [];

      // Chunk processing to avoid stampeding OPA with large lists
      const chunk = <T>(arr: T[], size: number): T[][] =>
        Array.from({ length: Math.ceil(arr.length / size) }, (_, i) =>
          arr.slice(i * size, (i + 1) * size),
        );

      // Process in chunks of 50 to balance performance and resource usage
      for (const batch of chunk(lookupTypess, 50)) {
        const results = await Promise.allSettled(
          batch.map(async (lookupTypes) => {
            let result: Result<boolean, DomainError>;

            switch (operation) {
              case 'read':
                result = await this.canReadLookupType(
                  userId,
                  lookupTypes,
                  correlationId,
                  context,
                );
                break;
              case 'update':
                result = await this.canUpdateLookupType(
                  userId,
                  lookupTypes,
                  correlationId,
                  context,
                );
                break;
              case 'delete':
                result = await this.canDeleteLookupType(
                  userId,
                  lookupTypes,
                  correlationId,
                  context,
                );
                break;
              default:
                result = err(
                  withContext(LookupTypeErrors.NOT_IMPLEMENTED, {
                    lookupTypes,
                    userId,
                    correlationId,
                    operation: String(operation),
                  }),
                );
                break;
            }

            return { lookupTypes, result };
          }),
        );

        // Process results from this chunk
        results.forEach((promiseResult, index) => {
          const lookupTypes = batch[index];

          if (promiseResult.status === 'fulfilled') {
            const { result } = promiseResult.value;
            if (result.ok && result.value) {
              authorized.push(lookupTypes);
            } else {
              denied.push(lookupTypes);
            }
          } else {
            // Promise rejected, treat as denied
            denied.push(lookupTypes);
            this.logger.warn(
              'Authorization check failed for lookupType',
              this.createLogContext(
                'batchAuthorization',
                correlationId,
                userId,
                {
                  lookupTypes,
                  error: String(promiseResult.reason),
                },
              ),
            );
          }
        });
      }

      return ok({ authorized, denied });
    } catch (error: unknown) {
      this.logger.error(
        'Error in batch lookupType authorization',
        this.createLogContext('batchAuthorization', correlationId, userId, {
          lookupTypess: lookupTypess.slice(0, 10), // Log only first 10 for brevity
          operation,
          error: error instanceof Error ? error.message : String(error),
        }),
      );

      return err(
        withContext(LookupTypeErrors.AUTHORIZATION_FAILED, {
          lookupTypes: '',
          userId,
          correlationId,
          operation: String(operation),
          reason: error instanceof Error ? error.message : String(error),
        }),
      );
    }
  }
}
