// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

// Framework imports
import { Injectable, Inject } from '@nestjs/common';
import { Result, ok, err, DomainError, withContext } from 'src/shared/errors';

// Shared utilities and infrastructure
import { APP_LOGGER, componentLogger, Logger } from 'src/shared/logging';
import { Clock, CLOCK } from 'src/shared/infrastructure/time';
import { IUserToken } from 'src/shared/security';
import { ActorContextAdapter } from 'src/shared/application/actor-context.adapter';
import { validateSortFields, toTakeSkip } from 'src/shared/domain/properties';
import {
  DEFAULT_PAGINATION_CONFIG,
  DEFAULT_LIST_CACHE_CONFIG,
} from 'src/shared/domain/pagination.config';
import {
  UseCaseLoggingUtil,
  UseCaseLoggingConfig,
} from 'src/shared/application/utils/use-case-logging.util';

import { LookupServiceConstants } from '../../../service-constants';

// Service configuration
// Using string literals for service constants

// Application layer
import { ListLookupTypeQuery } from '../queries';
import { ILookupTypeQuery, LOOKUP_TYPE_QUERY_TOKEN } from '../ports';
import { LookupTypeAuthorizationAdapter } from '../services';
import { ListLookupTypeFilterRequest, LookupTypePageResponse } from '../dtos';
import { IListLookupTypeUseCase } from './contracts';

/**
 * List LookupType Use Case
 *
 * Features:
 * - Authorization-based filtering with batch processing
 * - Pagination validation and security controls
 * - Essential audit trails and error handling
 * - Query validation and execution
 */
@Injectable()
export class ListLookupTypeUseCase implements IListLookupTypeUseCase {
  private readonly logger: Logger;
  private readonly loggingConfig: UseCaseLoggingConfig;

  constructor(
    @Inject(LOOKUP_TYPE_QUERY_TOKEN)
    private readonly query: ILookupTypeQuery,
    private readonly authorizationService: LookupTypeAuthorizationAdapter,
    @Inject(APP_LOGGER) moduleLogger: Logger,
    @Inject(CLOCK) private readonly clock: Clock,
  ) {
    this.loggingConfig = {
      serviceName: LookupServiceConstants.SERVICE_NAME,
      component: 'ListLookupTypeUseCase',
      domain: 'lookup',
      entityType: 'lookup_type',
    };
    this.logger = componentLogger(moduleLogger, this.loggingConfig.component);
  }

  /**
   * Execute the list lookupType query with enhanced pagination, validation, and compliance
   */
  async execute(params: {
    user: IUserToken;
    filter?: ListLookupTypeFilterRequest;
    correlationId: string;
  }): Promise<Result<LookupTypePageResponse, DomainError>> {
    const operation = 'list_lookup_type';
    const startTime = this.clock.nowMs();

    // Create query object from params for internal use
    // Ensure we always have a proper ListLookupTypeFilterRequest instance with prototype methods
    const filter = params.filter
      ? Object.assign(new ListLookupTypeFilterRequest(), params.filter)
      : new ListLookupTypeFilterRequest();
    const query = new ListLookupTypeQuery(
      params.user,
      filter,
      params.correlationId,
    );

    // Step 1: Create safe logging context (no PII, deferred retention metadata)
    const safeLogContext = UseCaseLoggingUtil.createLogContext(
      this.loggingConfig,
      this.clock,
      operation,
      query as any, // Query adapted for use case logging
      {
        operationRisk: UseCaseLoggingUtil.assessOperationRisk(operation),
        listOperation: true,
        page: query.filter.page ?? DEFAULT_PAGINATION_CONFIG.defaultPage,
        size: query.filter.size ?? DEFAULT_PAGINATION_CONFIG.defaultPageSize,
        hasFilters: Object.keys(query.filter.sortBy ?? {}).length > 0,
      },
    );

    // Step 2: Validate query with enhanced logging
    const validation = UseCaseLoggingUtil.validateCommand(
      this.logger,
      query as any,
      safeLogContext,
    );
    if (!validation.ok) {
      return err(validation.error);
    }

    try {
      // Step 1: Validate sort fields for security using inherited configuration
      const sortFieldConfig = query.filter.getSortFieldConfig();
      const allowedFields = Object.keys(sortFieldConfig).filter(
        (field) => sortFieldConfig[field].sortable,
      );
      const sortValidation = validateSortFields(
        query.filter.sortBy ?? {},
        allowedFields,
      );
      if (!sortValidation.ok) {
        UseCaseLoggingUtil.logOperationError(
          this.logger,
          operation,
          safeLogContext,
          sortValidation.error,
          'MEDIUM',
        );
        return err(sortValidation.error);
      }

      // Step 2: Convert pagination to repository parameters
      const takeSkipResult = toTakeSkip(
        query.filter.page ?? DEFAULT_PAGINATION_CONFIG.defaultPage,
        query.filter.size ?? DEFAULT_PAGINATION_CONFIG.defaultPageSize,
        DEFAULT_PAGINATION_CONFIG.maxPageSize,
      );
      if (!takeSkipResult.ok) {
        UseCaseLoggingUtil.logOperationError(
          this.logger,
          operation,
          safeLogContext,
          takeSkipResult.error,
          'MEDIUM',
        );
        return err(takeSkipResult.error);
      }

      // Step 3: Execute the repository query
      const lookupTypeResult = await this.fetchLookupType(query);
      if (!lookupTypeResult.ok) {
        UseCaseLoggingUtil.logOperationError(
          this.logger,
          operation,
          safeLogContext,
          lookupTypeResult.error,
          'HIGH',
        );
        return err(lookupTypeResult.error);
      }

      // Step 4: Filter LookupType based on authorization
      const correlationId = query.correlationId ?? 'unknown';
      const authorizedLookupTypeResult = await this.filterAuthorizedLookupTypes(
        lookupTypeResult.value,
        query.user.sub,
        correlationId,
        {
          tenant: query.user.tenant,
          roles: query.user.roles,
          operationType: 'list',
        },
      );

      if (!authorizedLookupTypeResult.ok) {
        UseCaseLoggingUtil.logOperationError(
          this.logger,
          operation,
          safeLogContext,
          authorizedLookupTypeResult.error,
          'HIGH',
        );
        return err(authorizedLookupTypeResult.error);
      }

      // Step 5: Log successful data access for audit trail
      const finalResult = authorizedLookupTypeResult.value;

      // Step 6: Log operation success with comprehensive metrics
      const executionTime = this.clock.nowMs() - startTime;
      UseCaseLoggingUtil.logOperationSuccess(
        this.logger,
        operation,
        safeLogContext,
        {
          executionTimeMs: executionTime,
          businessData: {
            totalItems: finalResult.meta?.totalItems ?? 0,
            returnedItems: finalResult.data?.length ?? 0,
            page: query.filter.page ?? DEFAULT_PAGINATION_CONFIG.defaultPage,
            pageSize:
              query.filter.size ?? DEFAULT_PAGINATION_CONFIG.defaultPageSize,
            hasFilters: Object.keys(query.filter.sortBy ?? {}).length > 0,
            cacheEnabled: true,
            cacheTtl: DEFAULT_LIST_CACHE_CONFIG.defaultTtl,
          },
        },
      );

      return authorizedLookupTypeResult;
    } catch (error) {
      const domainError = withContext(
        {
          code: 'CORE_LOOKUP.LIST_LOOKUP_TYPES_FAILED',
          title: 'Failed to list lookupType',
          category: 'infrastructure' as const,
          retryable: true,
        },
        {
          ...safeLogContext,
          error: error instanceof Error ? error.message : String(error),
        },
      );

      UseCaseLoggingUtil.logOperationError(
        this.logger,
        operation,
        safeLogContext,
        domainError,
        'CRITICAL',
      );

      return err(domainError);
    }
  }

  /**
   * ✅ Fetch lookupType from repository with error handling
   */
  private async fetchLookupType(
    query: ListLookupTypeQuery,
  ): Promise<Result<LookupTypePageResponse, DomainError>> {
    try {
      // ✅ Use enhanced repository with proper Result handling
      const repositoryOptions = {
        correlationId: query.correlationId,
        source: 'list-lookup-type-use-case',
        cache: {
          ttl: DEFAULT_LIST_CACHE_CONFIG.defaultTtl, // Use configurable cache TTL
          skipCache: false,
        },
      };

      // ✅ Convert IUserToken to ActorContext for enhanced repository methods
      const actor = ActorContextAdapter.fromUserToken(query.user);

      // ✅ Use findPaginated directly since we have ILookupTypeQuery
      const lookupTypeResult = await this.query.findPaginated(
        actor,
        query.filter,
        repositoryOptions,
      );

      if (!lookupTypeResult.ok) {
        return err(lookupTypeResult.error);
      }

      return ok(lookupTypeResult.value);
    } catch (error) {
      return err(
        withContext(
          {
            code: 'CORE_LOOKUP.REPOSITORY_FETCH_FAILED',
            title: 'Failed to fetch lookupType from repository',
            category: 'infrastructure',
            retryable: true,
          },
          {
            operation: 'fetch_lookupType',
            error: error instanceof Error ? error.message : String(error),
          },
        ),
      );
    }
  }

  /**
   * Filter lookupTypes based on user authorization
   */
  private async filterAuthorizedLookupTypes(
    lookupTypePage: LookupTypePageResponse,
    userId: string,
    correlationId: string,
    context: Record<string, any>,
  ): Promise<Result<LookupTypePageResponse, DomainError>> {
    try {
      if (!lookupTypePage.data || lookupTypePage.data.length === 0) {
        return ok(lookupTypePage); // Empty page, nothing to filter
      }

      // Extract lookupType codes for batch authorization check
      const codes = lookupTypePage.data
        .map((item) => item.code)
        .filter((code): code is string => code !== undefined && code !== null);
      if (codes.length === 0) {
        return ok(lookupTypePage); // No valid lookupType codes
      }

      // Perform batch authorization check
      const authResult =
        await this.authorizationService.authorizeLookupTypeList(
          userId,
          codes,
          correlationId,
          'read',
          context,
        );

      if (!authResult.ok) {
        return err(authResult.error);
      }

      const { authorized } = authResult.value;

      // Filter items to only include authorized lookupTypes
      const authorizedItems = lookupTypePage.data.filter((item) =>
        authorized.includes(item.code),
      );

      // Update the page response with filtered results
      const filteredPage = LookupTypePageResponse.create(
        authorizedItems,
        lookupTypePage.meta, // Keep original metadata for now
      );

      // Log filtering results
      if (authorizedItems.length < lookupTypePage.data.length) {
        this.logger.info(
          `Filtered lookupType list: originalCount=${lookupTypePage.data.length} authorizedCount=${authorizedItems.length} userId=${userId} correlationId=${correlationId}`,
        );
      }

      return ok(filteredPage);
    } catch (error) {
      return err({
        code: 'LOOKUP_TYPE.LIST_AUTHORIZATION_FAILED',
        title: 'Failed to authorize lookupType list',
        category: 'security',
        context: {
          userId,
          correlationId,
          error: error instanceof Error ? error.message : String(error),
        },
      });
    }
  }
}

/**
 * ✅ Type-safe use case result
 */
export type ListLookupTypeUseCaseResult = LookupTypePageResponse;

/**
 * ✅ Use case execution context for dependency injection
 */
export interface ListLookupTypeUseCaseContext {
  correlationId?: string;
  userId?: string;
  tenant?: string;
}

/**
 * ✅ Factory function for creating queries with context
 */
export function createListLookupTypeQuery(
  user: IUserToken,
  filter: ListLookupTypeFilterRequest,
  context: ListLookupTypeUseCaseContext,
): ListLookupTypeQuery {
  return new ListLookupTypeQuery(user, filter, context.correlationId);
}
