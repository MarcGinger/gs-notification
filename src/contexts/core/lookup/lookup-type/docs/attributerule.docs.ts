// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DocumentBuilder, SwaggerModule } from '@nestjs/swagger';
import { INestApplication } from '@nestjs/common';
import { SwaggerConfigUtil } from 'src/docs/swagger-config.util';
import { LookupTypeModule } from '../lookup-type.module';

/**
 * Attributerule Documentation
 * This module handles the Swagger documentation for attributerules
 * including Attributerule attributes and configurations.
 */
export class AttributeruleDocumentation {
  static setup(app: INestApplication, port: string | number): void {
    const config = new DocumentBuilder()
      .addBearerAuth(
        { type: 'http', scheme: 'bearer', bearerFormat: 'JWT' },
        'bearer',
      )
      .setTitle('📱 Attributerule Management API')
      .setDescription(
        `
## bounded context: core
[← Back to documentation](/api/docs/core)

### application: lookup
[← Back to documentation](/api/docs/core/lookup)
  | Parent |
  |--------|
  | [**← LookupType**](/api/docs/core/lookup/lookup-type) |

`,
      )
      .setVersion('1.0.0')
      .addTag('Attributerule', `Configuration for attributerule table`);

    // Add dynamic server configuration
    SwaggerConfigUtil.addServers(config, port);

    const document = SwaggerModule.createDocument(app, config.build(), {
      include: [LookupTypeModule],
      extraModels: [],
    });

    SwaggerModule.setup('api/docs/core/lookup/attributerule', app, document);
  }

  static getEndpoint(port: string | number): string {
    return `${SwaggerConfigUtil.getServerUrl(port)}/api/docs/core/lookup/attributerule`;
  }
}
