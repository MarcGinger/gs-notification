// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DocumentBuilder, SwaggerModule } from '@nestjs/swagger';
import { INestApplication } from '@nestjs/common';
import { SwaggerConfigUtil } from 'src/docs/swagger-config.util';
import { LookupTypeModule } from '../lookup-type.module';
import {
  DetailAttributeruleResponse,
  CreateAttributeruleRequest,
  UpdateAttributeruleRequest,
} from '../application/dtos';
/**
 * LookupType documentation endpoints interface
 */
export interface LookupTypeDocumentationUrls {
  lookupType: string;
}

/**
 * LookupType Documentation
 * This module handles the Swagger documentation for lookup_types
 *
 */
export class LookupTypeDocumentation {
  static setup(app: INestApplication, port: string | number): void {
    const config = new DocumentBuilder()
      .addBearerAuth(
        { type: 'http', scheme: 'bearer', bearerFormat: 'JWT' },
        'bearer',
      )
      .setTitle('📱 LookupType Management API')
      .setDescription(
        `
## bounded context: core
[← Back to documentation](/api/docs/core)

### application: lookup
[← Back to documentation](/api/docs/core/lookup)
`,
      )
      .setVersion('1.0.0')
      .addTag('LookupTypes', `Configuration for lookup_type table`);

    // Add dynamic server configuration
    SwaggerConfigUtil.addServers(config, port);

    const document = SwaggerModule.createDocument(app, config.build(), {
      include: [LookupTypeModule],
      extraModels: [
        DetailAttributeruleResponse,
        CreateAttributeruleRequest,
        UpdateAttributeruleRequest,
      ],
    });

    SwaggerModule.setup('api/docs/core/lookup/lookup-type', app, document);
  }

  /**
   * Setup all lookupType-related documentation endpoints
   * This method centralizes the setup of all lookupType module documentation
   */
  static setupAll(
    app: INestApplication,
    port: string | number,
  ): LookupTypeDocumentationUrls {
    // Setup main lookupType documentation
    LookupTypeDocumentation.setup(app, port);

    // Setup sub-module documentation

    // Return all endpoint URLs
    return {
      lookupType: LookupTypeDocumentation.getEndpoint(port),
    };
  }

  static getEndpoint(port: string | number): string {
    return `${SwaggerConfigUtil.getServerUrl(port)}/api/docs/core/lookup/lookup-type`;
  }
}
