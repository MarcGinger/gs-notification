// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { LookupTypeErrors } from '../errors/lookup-type.errors';

/**
 * Description Value Object
 * Represents a validated description with business rules
 */
export const LookupTypeDescription = createStringVO({
  name: 'Description',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    LookupTypeErrors.INVALID_DESCRIPTION,
    'Description',
  ),
});

/**
 * Public instance type for Description
 */
export type LookupTypeDescription = StringVOInstance;

// Convenience creators
export const createLookupTypeDescription = (s: string) =>
  LookupTypeDescription.create(s);
export const lookupTypeDescriptionFrom = (v: unknown) =>
  LookupTypeDescription.from(v);
