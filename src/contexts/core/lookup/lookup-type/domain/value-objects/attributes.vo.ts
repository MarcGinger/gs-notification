// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  RecordVOInstance,
  createRecordVO,
  createRecordVOErrors,
} from 'src/shared/domain/value-objects';
import { LookupTypeErrors } from '../errors/lookup-type.errors';

/**
 * Attributes Record Value Object
 * Validates a plain object (Record<string, unknown>); deepFreeze.
 */
export const LookupTypeAttributes = createRecordVO({
  name: 'Attributes',
  // JSON key validation - must start with letter/underscore, contain only alphanumeric, underscore
  keyPattern: /^[a-zA-Z_][a-zA-Z0-9_]*$/,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createRecordVOErrors(
    LookupTypeErrors.INVALID_ATTRIBUTES,
    'Attributes',
  ),
});

/** Public instance type for Attributes */
export type LookupTypeAttributes = RecordVOInstance;

// Convenience creators
export const createLookupTypeAttributes = (v: Record<string, unknown>) =>
  LookupTypeAttributes.create(v);

export const lookupTypeAttributesFrom = (v: unknown) =>
  LookupTypeAttributes.from(v);
