// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  BooleanVOInstance,
  createBooleanVO,
  createBooleanVOErrors,
} from 'src/shared/domain/value-objects';
import { LookupTypeErrors } from '..//errors/lookup-type.errors';

/**
 * Enabled Boolean Value Object
 * Represents a validated enabled with business rules
 */
export const LookupTypeEnabled = createBooleanVO({
  name: 'Enabled',
  treatNullAsFalse: true,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createBooleanVOErrors(LookupTypeErrors.INVALID_ENABLED, 'Enabled'),
});

/** Public instance type for Enabled */
export type LookupTypeEnabled = BooleanVOInstance;

// Convenience creators
export const createLookupTypeEnabled = (v: boolean) =>
  LookupTypeEnabled.create(v);
export const lookupTypeEnabledFrom = (v: unknown) => LookupTypeEnabled.from(v);
