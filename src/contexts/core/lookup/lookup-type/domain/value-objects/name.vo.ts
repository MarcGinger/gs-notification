// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { LookupTypeErrors } from '../errors/lookup-type.errors';

/**
 * Name Value Object
 * Represents a validated name with business rules
 */
export const LookupTypeName = createStringVO({
  name: 'Name',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  maxLength: 128,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(LookupTypeErrors.INVALID_NAME, 'Name'),
});

/**
 * Public instance type for Name
 */
export type LookupTypeName = StringVOInstance;

// Convenience creators
export const createLookupTypeName = (s: string) => LookupTypeName.create(s);
export const lookupTypeNameFrom = (v: unknown) => LookupTypeName.from(v);
