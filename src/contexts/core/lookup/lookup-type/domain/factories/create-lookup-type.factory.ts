// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DomainError, Result, err, withContext } from 'src/shared/errors';
import { Clock } from 'src/shared/domain/clock';
import { EventMetadata } from 'src/shared/domain/events';
import { LookupTypeAggregate } from '../aggregates';
import { CreateLookupTypeProps } from '../props';
import { LookupTypeDomainState } from '../state';
import {
  LookupTypeCreatedAt,
  LookupTypeUpdatedAt,
  LookupTypeVersion,
  LookupTypeCode,
  LookupTypeName,
  LookupTypeDescription,
  LookupTypeEnabled,
  LookupTypeAttributeruleId,
} from '../value-objects';

/**
 * Enhanced lookupType entity factory with comprehensive validation and security context
 * Moved from application layer to properly separate business concerns
 */
export function createLookupTypeAggregateFromProps(
  props: CreateLookupTypeProps,
  metadata: EventMetadata,
  clock: Clock,
  // validatedEntities?: {
  //  channels?: ChannelReference[];
  // },
): Result<LookupTypeAggregate, DomainError> {
  // Validate each property by creating value objects
  const codeResult = LookupTypeCode.from(props.code);
  if (!codeResult.ok) {
    return err(
      withContext(codeResult.error, {
        ...codeResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_lookup_type',
        code: props.code,
      }),
    );
  }

  const nameResult = LookupTypeName.from(props.name);
  if (!nameResult.ok) {
    return err(
      withContext(nameResult.error, {
        ...nameResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_lookup_type',
        name: props.name,
      }),
    );
  }

  const descriptionResult = LookupTypeDescription.from(props.description);
  if (!descriptionResult.ok) {
    return err(
      withContext(descriptionResult.error, {
        ...descriptionResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_lookup_type',
        description: props.description,
      }),
    );
  }

  const enabledResult = LookupTypeEnabled.from(props.enabled);
  if (!enabledResult.ok) {
    return err(
      withContext(enabledResult.error, {
        ...enabledResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_lookup_type',
        enabled: props.enabled,
      }),
    );
  }

  // Create attributeruleId configuration using specialized factory
  const attributeruleIdResult = LookupTypeAttributeruleId.from(
    props.attributeruleId,
  );
  if (!attributeruleIdResult.ok) {
    return err(attributeruleIdResult.error);
  }

  const createdAtResult = LookupTypeCreatedAt.create(clock.now());
  if (!createdAtResult.ok) {
    return err(createdAtResult.error);
  }

  const updatedAtResult = LookupTypeUpdatedAt.create(clock.now());
  if (!updatedAtResult.ok) {
    return err(updatedAtResult.error);
  }

  const versionResult = LookupTypeVersion.create(1); // Initial version is 1
  if (!versionResult.ok) {
    return err(versionResult.error);
  }

  // Create the entity properties with validated value objects
  const entityProps: LookupTypeDomainState = {
    code: codeResult.value,
    name: nameResult.value,
    description: descriptionResult.value,
    enabled: enabledResult.value,
    attributeruleId: attributeruleIdResult.value,
    createdAt: createdAtResult.value,
    updatedAt: updatedAtResult.value,
    version: versionResult.value,
  };

  // Create the entity using the constructor
  return LookupTypeAggregate.create(entityProps, clock, metadata);
}
