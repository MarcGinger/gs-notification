// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  LookupCreatedAt,
  LookupUpdatedAt,
  LookupVersion,
  LookupAttributes,
  LookupCode,
  LookupDescription,
  LookupEnabled,
  LookupLookupType,
  LookupName,
} from '../value-objects';

/**
 * Lookup Domain State
 *
 * Domain state interface that uses value objects to model business concepts
 * while maintaining compatibility with entity operations. Business fields are
 * modeled as value objects with proper validation and domain rules.
 *
 * Domain-First State Interface
 *
 * This interface represents the domain model where business concepts
 * are properly encapsulated as value objects when appropriate.
 */

export interface LookupDomainState {
  lookupType: LookupLookupType;
  code: LookupCode;
  name: LookupName;
  description?: LookupDescription;
  enabled?: LookupEnabled;
  attributes?: LookupAttributes;
  version: LookupVersion;
  createdAt: LookupCreatedAt;
  updatedAt: LookupUpdatedAt;
}

export type LookupDomainStateOptional = Partial<LookupDomainState>;
