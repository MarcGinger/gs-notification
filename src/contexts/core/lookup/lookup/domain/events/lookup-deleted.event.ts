// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { EventMetadata } from 'src/shared/domain/events';

/**
 * Lookup Deleted Event Payload
 * Contains identifying information about the deleted lookup
 */
export interface LookupDeletedEventPayload {
  code: string;
  deletedAt: Date;
  version: number;
  metadata: EventMetadata;
}

/**
 * Lookup Deleted Domain Event
 *
 * Emitted when a lookup is successfully deleted.
 * Contains identifying information and deletion metadata.
 */
export class LookupDeletedEvent {
  public readonly eventType = 'CoreLookupLookupDeleted.v1';
  public readonly eventVersion = 'v1';

  constructor(public readonly payload: LookupDeletedEventPayload) {}

  // Factory method with security context
  static create(
    data: Omit<LookupDeletedEventPayload, 'metadata'>,
    baseMetadata: EventMetadata,
  ): LookupDeletedEvent {
    const metadata: EventMetadata = {
      ...baseMetadata,
      eventVersion: '1.0',
      source: 'core.domain.lookup',
    };

    return new LookupDeletedEvent({
      ...data,
      metadata,
    });
  }

  get code(): string {
    return this.payload.code;
  }

  get deletedAt(): Date {
    return this.payload.deletedAt;
  }

  get version(): number {
    return this.payload.version;
  }

  get metadata(): EventMetadata {
    return this.payload.metadata;
  }
}
