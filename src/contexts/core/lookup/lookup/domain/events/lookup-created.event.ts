// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

/**
 * Lookup Created Event Payload
 * Contains only business facts - no envelope metadata, timestamps, or versioning
 */
export interface LookupCreatedEventPayload {
  lookupType: string;
  code: string;
  name: string;
  description?: string;
  enabled?: boolean;
  attributes?: Record<string, unknown>;
}

/**
 * Lookup Created Domain Event
 *
 * Emitted when a new lookup is successfully created.
 * Contains complete lookup information and creation metadata.
 * Enhanced with security metadata and PII protection capabilities.
 */
export class LookupCreatedEvent {
  public readonly eventType = 'CoreLookupLookupCreated.v1';
  public readonly eventVersion = 'v1';

  constructor(public readonly payload: LookupCreatedEventPayload) {}

  // Factory method - no metadata needed, just business data
  static create(data: LookupCreatedEventPayload): LookupCreatedEvent {
    return new LookupCreatedEvent(data);
  }

  get lookupType(): string {
    return this.payload.lookupType;
  }

  get code(): string {
    return this.payload.code;
  }

  get name(): string {
    return this.payload.name;
  }

  get description(): string | undefined {
    return this.payload.description;
  }

  get enabled(): boolean | undefined {
    return this.payload.enabled;
  }

  get attributes(): Record<string, unknown> | undefined {
    return this.payload.attributes;
  }
}
