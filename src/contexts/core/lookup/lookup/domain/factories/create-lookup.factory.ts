// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DomainError, Result, err, withContext, ok } from 'src/shared/errors';
import { Clock } from 'src/shared/domain/clock';
import { EventMetadata } from 'src/shared/domain/events';
import { LookupAggregate } from '../aggregates';
import { CreateLookupProps } from '../props';
import { LookupDomainState } from '../state';
import {
  LookupCreatedAt,
  LookupUpdatedAt,
  LookupVersion,
  LookupLookupType,
  LookupCode,
  LookupName,
  LookupDescription,
  LookupEnabled,
  LookupAttributes,
} from '../value-objects';

/**
 * Enhanced lookup entity factory with comprehensive validation and security context
 * Moved from application layer to properly separate business concerns
 */
export function createLookupAggregateFromProps(
  props: CreateLookupProps,
  metadata: EventMetadata,
  clock: Clock,
  // validatedEntities?: {
  //  channels?: ChannelReference[];
  // },
): Result<LookupAggregate, DomainError> {
  // Validate each property by creating value objects
  const lookupTypeResult = LookupLookupType.from(props.lookupType);
  if (!lookupTypeResult.ok) {
    return err(
      withContext(lookupTypeResult.error, {
        ...lookupTypeResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_lookup',
        lookupType: props.lookupType,
      }),
    );
  }

  const codeResult = LookupCode.from(props.code);
  if (!codeResult.ok) {
    return err(
      withContext(codeResult.error, {
        ...codeResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_lookup',
        code: props.code,
      }),
    );
  }

  const nameResult = LookupName.from(props.name);
  if (!nameResult.ok) {
    return err(
      withContext(nameResult.error, {
        ...nameResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_lookup',
        name: props.name,
      }),
    );
  }

  const descriptionResult = LookupDescription.from(props.description);
  if (!descriptionResult.ok) {
    return err(
      withContext(descriptionResult.error, {
        ...descriptionResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_lookup',
        description: props.description,
      }),
    );
  }

  const enabledResult = LookupEnabled.from(props.enabled);
  if (!enabledResult.ok) {
    return err(
      withContext(enabledResult.error, {
        ...enabledResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_lookup',
        enabled: props.enabled,
      }),
    );
  }

  const attributesResult = LookupAttributes.from(props.attributes);
  if (!attributesResult.ok) {
    return err(
      withContext(attributesResult.error, {
        ...attributesResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_lookup',
        attributes: props.attributes,
      }),
    );
  }

  const createdAtResult = LookupCreatedAt.create(clock.now());
  if (!createdAtResult.ok) {
    return err(createdAtResult.error);
  }

  const updatedAtResult = LookupUpdatedAt.create(clock.now());
  if (!updatedAtResult.ok) {
    return err(updatedAtResult.error);
  }

  const versionResult = LookupVersion.create(1); // Initial version is 1
  if (!versionResult.ok) {
    return err(versionResult.error);
  }

  // Create the entity properties with validated value objects
  const entityProps: LookupDomainState = {
    lookupType: lookupTypeResult.value,
    code: codeResult.value,
    name: nameResult.value,
    description: descriptionResult.value,
    enabled: enabledResult.value,
    attributes: attributesResult.value,
    createdAt: createdAtResult.value,
    updatedAt: updatedAtResult.value,
    version: versionResult.value,
  };

  // Create the entity using the constructor
  return LookupAggregate.create(entityProps, clock, metadata);
}
