// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { LookupErrors } from '../errors/lookup.errors';

/**
 * LookupType Value Object
 * Represents a validated lookupType with business rules
 */
export const LookupLookupType = createStringVO({
  name: 'LookupType',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  maxLength: 64,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(LookupErrors.INVALID_LOOKUP_TYPE, 'LookupType'),
});

/**
 * Public instance type for LookupType
 */
export type LookupLookupType = StringVOInstance;

// Convenience creators
export const createLookupLookupType = (s: string) => LookupLookupType.create(s);
export const lookupLookupTypeFrom = (v: unknown) => LookupLookupType.from(v);
