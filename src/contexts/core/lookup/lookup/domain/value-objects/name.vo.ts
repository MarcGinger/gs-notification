// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { LookupErrors } from '../errors/lookup.errors';

/**
 * Name Value Object
 * Represents a validated name with business rules
 */
export const LookupName = createStringVO({
  name: 'Name',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  maxLength: 128,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(LookupErrors.INVALID_NAME, 'Name'),
});

/**
 * Public instance type for Name
 */
export type LookupName = StringVOInstance;

// Convenience creators
export const createLookupName = (s: string) => LookupName.create(s);
export const lookupNameFrom = (v: unknown) => LookupName.from(v);
