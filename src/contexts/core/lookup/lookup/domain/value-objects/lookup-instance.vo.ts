// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DomainError, Result, ok, err } from 'src/shared/errors';
import {
  createIntegerVO,
  createIntegerVOErrors,
  IntegerVOInstance,
  createDateTimeVO,
  createDateTimeVOErrors,
  DateTimeVOInstance,
} from 'src/shared/domain/value-objects';
import { Clock } from 'src/shared/domain/clock';
import { LookupErrors } from '../errors/lookup.errors';

/**
 * Lookup Instance Value Objects
 *
 * Instance VOs for Lookup aggregate lifecycle management.
 * Includes creation timestamps, update timestamps, and version tracking.
 * Follows the Clock injection pattern for deterministic testing.
 */

// ===========================================
// Lookup Created At Instance VO
// ===========================================

const LookupCreatedAtErrors = createDateTimeVOErrors(
  LookupErrors.CREATED_AT_REQUIRED,
  'Lookup Created At',
);

export const LookupCreatedAt = createDateTimeVO({
  name: 'LookupCreatedAt',
  allowFuture: false, // Cannot create lookup in the future
  allowPast: true, // Can create with past timestamps (data migration)
  customValidation: (date: Date): Result<void, DomainError> => {
    // Optional: Add domain-specific creation time validation
    // For example, cannot be more than 1 year in the past
    const oneYearAgo = new Date();
    oneYearAgo.setFullYear(oneYearAgo.getFullYear() - 1);

    if (date < oneYearAgo) {
      return err({
        ...LookupErrors.CREATED_AT_REQUIRED,
        detail: 'Lookup creation date cannot be more than 1 year in the past',
        context: { value: date, maxPastDate: oneYearAgo },
      } as DomainError);
    }

    return ok(undefined);
  },
  errors: LookupCreatedAtErrors,
});

export type LookupCreatedAt = DateTimeVOInstance;

// ===========================================
// Lookup Updated At Instance VO
// ===========================================

const LookupUpdatedAtErrors = createDateTimeVOErrors(
  LookupErrors.UPDATED_AT_REQUIRED,
  'Lookup Updated At',
);

export const LookupUpdatedAt = createDateTimeVO({
  name: 'LookupUpdatedAt',
  allowFuture: false, // Cannot update lookup in the future
  allowPast: true, // Can update with past timestamps (data correction)
  errors: LookupUpdatedAtErrors,
});

export type LookupUpdatedAt = DateTimeVOInstance;

// ===========================================
// Lookup Version Instance VO
// ===========================================

const LookupVersionErrors = createIntegerVOErrors(
  LookupErrors.INVALID_CODE_DATA, // Reusing existing error until specific version error is added
  'Lookup Version',
);

export const LookupVersion = createIntegerVO({
  name: 'LookupVersion',
  min: 0, // Version starts at 0
  allowNegative: false,
  errors: LookupVersionErrors,
});

export type LookupVersion = IntegerVOInstance;

// ===========================================
// Clock-Based Helper Functions (Explicit Dependencies)
// ===========================================

/**
 * Create LookupCreatedAt from Clock (explicit dependency)
 * Deterministic and testable - pass a fake clock in tests
 */
export const createdAtNow = (
  clock: Clock,
): Result<LookupCreatedAt, DomainError> => LookupCreatedAt.create(clock.now());

/**
 * Create LookupUpdatedAt from Clock (explicit dependency)
 * Deterministic and testable - pass a fake clock in tests
 */
export const updatedAtNow = (
  clock: Clock,
): Result<LookupUpdatedAt, DomainError> => LookupUpdatedAt.create(clock.now());

/**
 * Bump version by 1 (domain business rule)
 */
export const bumpVersion = (
  currentVersion: LookupVersion,
): Result<LookupVersion, DomainError> =>
  LookupVersion.create(currentVersion.value + 1);

/**
 * Create initial version (starts at 1 for new aggregates)
 */
export const initialVersion = (): Result<LookupVersion, DomainError> =>
  LookupVersion.create(1);

// ===========================================
// Direct Creation Functions (Non-Clock Based)
// ===========================================

/**
 * Create LookupCreatedAt from Date
 */
export const createLookupCreatedAt = (
  date: Date,
): Result<LookupCreatedAt, DomainError> => LookupCreatedAt.create(date);

/**
 * Create LookupUpdatedAt from Date
 */
export const createLookupUpdatedAt = (
  date: Date,
): Result<LookupUpdatedAt, DomainError> => LookupUpdatedAt.create(date);

/**
 * Create LookupVersion from number
 */
export const createLookupVersion = (
  version: number,
): Result<LookupVersion, DomainError> => LookupVersion.create(version);

// ===========================================
// Domain Invariants & Validation
// ===========================================

/**
 * Validate that updatedAt is not before createdAt (monotonic time)
 */
export const validateMonotonicTime = (
  createdAt: LookupCreatedAt,
  updatedAt: LookupUpdatedAt,
): Result<void, DomainError> => {
  if (updatedAt.value < createdAt.value) {
    return err({
      ...LookupErrors.UPDATED_AT_REQUIRED,
      detail: 'Updated at cannot be before created at',
      context: {
        createdAt: createdAt.value.toISOString(),
        updatedAt: updatedAt.value.toISOString(),
      },
    } as DomainError);
  }
  return ok(undefined);
};

// ===========================================
// Legacy Compatibility (DEPRECATED)
// ===========================================

/**
 * @deprecated Use createdAtNow(clock) instead for better testability
 * This function hides the Date dependency and makes testing harder
 */
export function createLookupCreatedAtNow(): Result<
  LookupCreatedAt,
  DomainError
> {
  return LookupCreatedAt.create(new Date());
}

/**
 * @deprecated Use updatedAtNow(clock) instead for better testability
 * This function hides the Date dependency and makes testing harder
 */
export function createLookupUpdatedAtNow(): Result<
  LookupUpdatedAt,
  DomainError
> {
  return LookupUpdatedAt.create(new Date());
}
