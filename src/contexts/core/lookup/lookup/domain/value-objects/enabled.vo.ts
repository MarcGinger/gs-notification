// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  BooleanVOInstance,
  createBooleanVO,
  createBooleanVOErrors,
} from 'src/shared/domain/value-objects';
import { LookupErrors } from '..//errors/lookup.errors';

/**
 * Enabled Boolean Value Object
 * Represents a validated enabled with business rules
 */
export const LookupEnabled = createBooleanVO({
  name: 'Enabled',
  treatNullAsFalse: true,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createBooleanVOErrors(LookupErrors.INVALID_ENABLED, 'Enabled'),
});

/** Public instance type for Enabled */
export type LookupEnabled = BooleanVOInstance;

// Convenience creators
export const createLookupEnabled = (v: boolean) => LookupEnabled.create(v);
export const lookupEnabledFrom = (v: unknown) => LookupEnabled.from(v);
