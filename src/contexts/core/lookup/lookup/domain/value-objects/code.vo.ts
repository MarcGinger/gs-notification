// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { LookupErrors } from '../errors/lookup.errors';

/**
 * Code Value Object
 * Represents a validated code with business rules
 */
export const LookupCode = createStringVO({
  name: 'Code',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  maxLength: 64,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(LookupErrors.INVALID_CODE, 'Code'),
});

/**
 * Public instance type for Code
 */
export type LookupCode = StringVOInstance;

// Convenience creators
export const createLookupCode = (s: string) => LookupCode.create(s);
export const lookupCodeFrom = (v: unknown) => LookupCode.from(v);
