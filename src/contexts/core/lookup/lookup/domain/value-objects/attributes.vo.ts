// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  RecordVOInstance,
  createRecordVO,
  createRecordVOErrors,
} from 'src/shared/domain/value-objects';
import { LookupErrors } from '../errors/lookup.errors';

/**
 * Attributes Record Value Object
 * Validates a plain object (Record<string, unknown>); deepFreeze.
 */
export const LookupAttributes = createRecordVO({
  name: 'Attributes',
  // JSON key validation - must start with letter/underscore, contain only alphanumeric, underscore
  keyPattern: /^[a-zA-Z_][a-zA-Z0-9_]*$/,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createRecordVOErrors(LookupErrors.INVALID_ATTRIBUTES, 'Attributes'),
});

/** Public instance type for Attributes */
export type LookupAttributes = RecordVOInstance;

// Convenience creators
export const createLookupAttributes = (v: Record<string, unknown>) =>
  LookupAttributes.create(v);

export const lookupAttributesFrom = (v: unknown) => LookupAttributes.from(v);
