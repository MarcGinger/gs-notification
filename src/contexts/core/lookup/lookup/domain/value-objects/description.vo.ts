// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { LookupErrors } from '../errors/lookup.errors';

/**
 * Description Value Object
 * Represents a validated description with business rules
 */
export const LookupDescription = createStringVO({
  name: 'Description',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(LookupErrors.INVALID_DESCRIPTION, 'Description'),
});

/**
 * Public instance type for Description
 */
export type LookupDescription = StringVOInstance;

// Convenience creators
export const createLookupDescription = (s: string) =>
  LookupDescription.create(s);
export const lookupDescriptionFrom = (v: unknown) => LookupDescription.from(v);
