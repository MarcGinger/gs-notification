// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { EntityIdBase } from 'src/shared/domain/entities';
import { Result, ok, err, DomainError } from 'src/shared/errors';
import { LookupSnapshotProps } from '../props';
import { LookupDomainState } from '../state';
import { LookupErrors } from '../errors/lookup.errors';
import {
  LookupAttributes,
  LookupCode,
  LookupCreatedAt,
  LookupUpdatedAt,
  LookupVersion,
  LookupDescription,
  LookupEnabled,
  LookupLookupType,
  LookupName,
} from '../value-objects';

/**
 * Domain Entity: Lookup
 *
 * Represents the core Lookup entity in the core domain.
 * Encapsulates lookup data, identity, and basic entity behavior.
 *
 * This entity follows DDD principles:
 * - Identity: Code as unique identifier
 * - Immutability: Changes create new instances
 * - Encapsulation: Private state with controlled access
 * - Business validation: Domain rules enforced
 *
 * @domain Core Context - Lookup Entity
 * @layer Domain Entities
 */

/**
 * Lookup Entity
 *
 * Core domain entity representing a lookup in the core.
 * Handles lookup identity, validation, and state management.
 */
export class LookupEntity extends EntityIdBase<LookupDomainState, LookupCode> {
  private static clock: { now: () => Date } = { now: () => new Date() };

  public static setClock(c: { now: () => Date }) {
    this.clock = c;
  }

  /**
   * Helper method to create updatedAt VO from date
   */
  private static createUpdatedAt(
    date?: Date,
  ): Result<LookupUpdatedAt, DomainError> {
    const targetDate = date || LookupEntity.clock.now();
    const result = LookupUpdatedAt.from(targetDate);
    if (!result.ok) {
      return err(result.error);
    }
    return ok(result.value);
  }

  /**
   * Helper method to create version VO from number
   */
  private static createVersion(
    versionNumber?: number,
    currentVersion?: LookupVersion,
  ): Result<LookupVersion, DomainError> {
    const targetVersion = versionNumber ?? currentVersion?.value ?? 1;
    const result = LookupVersion.from(targetVersion);
    if (!result.ok) {
      return err(result.error);
    }
    return ok(result.value);
  }

  /**
   * Helper method to create updated entity with common logic
   */
  private createUpdatedEntity(
    fieldUpdates: Partial<LookupDomainState>,
    updatedAt?: Date,
    version?: number,
  ): Result<LookupEntity, DomainError> {
    const updatedAtResult = LookupEntity.createUpdatedAt(updatedAt);
    if (!updatedAtResult.ok) {
      return err(updatedAtResult.error);
    }

    const versionResult = LookupEntity.createVersion(
      version,
      this.props.version,
    );
    if (!versionResult.ok) {
      return err(versionResult.error);
    }

    const updatedProps: LookupDomainState = {
      ...this.props,
      ...fieldUpdates,
      updatedAt: updatedAtResult.value,
      version: versionResult.value,
    };

    return LookupEntity.create(updatedProps);
  }

  private constructor(props: LookupDomainState) {
    super(props, props.code);
  }

  /**
   * Factory method to create a new lookup entity
   *
   * @param props - Lookup properties
   * @returns Result containing LookupEntity or DomainError
   */
  public static create(
    props: LookupDomainState,
  ): Result<LookupEntity, DomainError> {
    // Validate required properties
    const validationResult = LookupEntity.validate(props);
    if (validationResult.ok === false) {
      return err(validationResult.error);
    }

    return ok(new LookupEntity(props));
  }

  /**
   * Factory method to reconstitute a lookup entity from persistence
   *
   * @param props - Lookup properties from database
   * @returns LookupEntity instance
   */
  public static reconstitute(props: LookupDomainState): LookupEntity {
    return new LookupEntity(props);
  }

  /**
   * Factory method to reconstitute entity from snapshot
   *
   * @param snapshot - Lookup snapshot properties
   * @returns Result containing LookupEntity or DomainError
   */
  public static fromSnapshot(
    snapshot: LookupSnapshotProps,
  ): Result<LookupEntity, DomainError> {
    const lookupTypeResult = LookupLookupType.from(snapshot.lookupType);
    if (!lookupTypeResult.ok) {
      return err(lookupTypeResult.error);
    }
    const codeResult = LookupCode.from(snapshot.code);
    if (!codeResult.ok) {
      return err(codeResult.error);
    }
    const nameResult = LookupName.from(snapshot.name);
    if (!nameResult.ok) {
      return err(nameResult.error);
    }
    const descriptionResult = LookupDescription.from(snapshot.description);
    if (!descriptionResult.ok) {
      return err(descriptionResult.error);
    }
    const enabledResult = LookupEnabled.from(snapshot.enabled);
    if (!enabledResult.ok) {
      return err(enabledResult.error);
    }
    const attributesResult = LookupAttributes.from(snapshot.attributes);
    if (!attributesResult.ok) {
      return err(attributesResult.error);
    }
    const createdAtResult = LookupCreatedAt.from(snapshot.createdAt);
    if (!createdAtResult.ok) {
      return err(createdAtResult.error);
    }

    const updatedAtResult = LookupUpdatedAt.from(snapshot.updatedAt);
    if (!updatedAtResult.ok) {
      return err(updatedAtResult.error);
    }

    const versionResult = LookupVersion.from(snapshot.version);
    if (!versionResult.ok) {
      return err(versionResult.error);
    }

    const props: LookupDomainState = {
      lookupType: lookupTypeResult.value,
      code: codeResult.value,
      name: nameResult.value,
      description: descriptionResult.value,
      enabled: enabledResult.value,
      attributes: attributesResult.value,
      createdAt: createdAtResult.value,
      updatedAt: updatedAtResult.value,
      version: versionResult.value,
    };

    return LookupEntity.create(props);
  }

  /**
   * Validates lookup entity properties
   *
   * @param props - Properties to validate
   * @returns Validation result
   */
  private static validate(props: LookupDomainState): Result<void, DomainError> {
    // Basic validation
    if (!props.lookupType) {
      return err(LookupErrors.INVALID_LOOKUP_TYPE_DATA);
    }
    if (!props.code) {
      return err(LookupErrors.INVALID_CODE_DATA);
    }
    if (!props.name) {
      return err(LookupErrors.INVALID_NAME_DATA);
    }

    return ok(undefined);
  }

  // ======================
  // Getters (Public API)
  // ======================

  public get lookupType(): LookupLookupType {
    return this.props.lookupType;
  }

  public get code(): LookupCode {
    return this.props.code;
  }

  public get name(): LookupName {
    return this.props.name;
  }

  public get description(): LookupDescription | undefined {
    return this.props.description;
  }

  public get enabled(): LookupEnabled | undefined {
    return this.props.enabled;
  }

  public get attributes(): LookupAttributes | undefined {
    return this.props.attributes;
  }

  public get createdAt(): LookupCreatedAt {
    return this.props.createdAt;
  }

  public get updatedAt(): LookupUpdatedAt {
    return this.props.updatedAt;
  }

  public get version(): LookupVersion {
    return this.props.version;
  }

  // ======================
  // Simple Update Methods (No Business Logic)
  // ======================

  /**
   * Creates a new entity with updated lookupType (pure state transition)
   *
   * @param lookupType - New lookup_type value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<LookupEntity, DomainError>
   */
  public withLookupType(
    lookupType: LookupLookupType,
    updatedAt?: Date,
    version?: number,
  ): Result<LookupEntity, DomainError> {
    return this.createUpdatedEntity({ lookupType }, updatedAt, version);
  }

  /**
   * Creates a new entity with updated name (pure state transition)
   *
   * @param name - New name value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<LookupEntity, DomainError>
   */
  public withName(
    name: LookupName,
    updatedAt?: Date,
    version?: number,
  ): Result<LookupEntity, DomainError> {
    return this.createUpdatedEntity({ name }, updatedAt, version);
  }

  /**
   * Creates a new entity with updated description (pure state transition)
   *
   * @param description - New description value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<LookupEntity, DomainError>
   */
  public withDescription(
    description: LookupDescription,
    updatedAt?: Date,
    version?: number,
  ): Result<LookupEntity, DomainError> {
    return this.createUpdatedEntity({ description }, updatedAt, version);
  }

  /**
   * Creates a new entity with updated enabled (pure state transition)
   *
   * @param enabled - New enabled value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<LookupEntity, DomainError>
   */
  public withEnabled(
    enabled: LookupEnabled,
    updatedAt?: Date,
    version?: number,
  ): Result<LookupEntity, DomainError> {
    return this.createUpdatedEntity({ enabled }, updatedAt, version);
  }

  /**
   * Creates a new entity with updated attributes (pure state transition)
   *
   * @param attributes - New attributes value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<LookupEntity, DomainError>
   */
  public withAttributes(
    attributes: LookupAttributes,
    updatedAt?: Date,
    version?: number,
  ): Result<LookupEntity, DomainError> {
    return this.createUpdatedEntity({ attributes }, updatedAt, version);
  }

  // ======================
  // Query Methods
  // ======================

  /**
   * Checks if two lookups are the same entity
   *
   * @param other - Other lookup to compare
   */
  public sameAs(other: LookupEntity): boolean {
    return this.props.code.equals(other.props.code);
  }

  /**
   * Gets a snapshot of current entity state for serialization
   */
  public toSnapshot(): LookupSnapshotProps {
    return {
      lookupType: this.props.lookupType.value,
      code: this.props.code.value,
      name: this.props.name.value,
      description: this.props.description?.value,
      enabled: this.props.enabled?.value,
      attributes: this.props.attributes?.value,
      createdAt: this.props.createdAt.value,
      updatedAt: this.props.updatedAt.value,
      version: this.props.version.value,
    };
  }

  /**
   * Gets the current domain state with value objects
   *
   * Provides access to the rich domain state for application layer
   * without requiring infrastructure mappers. Maintains clean architecture.
   *
   * @returns Current domain state with value objects
   */
  public getDomainState(): LookupDomainState {
    return this.props;
  }
}
