// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { QueryHandler, IQueryHandler } from '@nestjs/cqrs';
import { DetailLookupResponse } from '../dtos';
import { GetLookupUseCase } from '../use-cases';
import { GetLookupQuery } from '../queries';
import { Result, DomainError } from 'src/shared/errors';

@QueryHandler(GetLookupQuery)
export class GetLookupHandler implements IQueryHandler<GetLookupQuery> {
  constructor(private readonly getLookupUseCase: GetLookupUseCase) {}

  async execute(
    query: GetLookupQuery,
  ): Promise<Result<DetailLookupResponse | null, DomainError>> {
    return this.getLookupUseCase.execute({
      user: query.user,
      code: query.code,
      correlationId: query.correlationId || 'query-handler',
    });
  }
}
