// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { CommandHandler, ICommandHandler } from '@nestjs/cqrs';
import { Inject } from '@nestjs/common';
import { Result, DomainError } from 'src/shared/errors';
import { DeleteLookupUseCase } from '../use-cases';
import { DeleteLookupCommand } from '../commands';

@CommandHandler(DeleteLookupCommand)
export class DeleteLookupHandler
  implements ICommandHandler<DeleteLookupCommand>
{
  constructor(
    @Inject(DeleteLookupUseCase)
    private readonly deleteLookupUseCase: DeleteLookupUseCase,
  ) {}

  async execute(
    command: DeleteLookupCommand,
  ): Promise<Result<void, DomainError>> {
    return await this.deleteLookupUseCase.execute({
      user: command.user,
      code: command.code,
      correlationId: command.correlationId,
      authorizationReason: 'CQRS Command Handler',
    });
  }
}
