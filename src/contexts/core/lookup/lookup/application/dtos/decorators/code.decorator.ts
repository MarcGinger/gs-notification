// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { applyDecorators } from '@nestjs/common';
import { ApiProperty } from '@nestjs/swagger';
import { IsOptional, IsNotEmpty, IsString, MaxLength } from 'class-validator';

/**
 * Options for property decorators
 */
interface PropOptions {
  required?: boolean;
}

/**
 * Property decorator for Lookup Code
 * @param {Object} options - Options for the decorator
 * @returns {PropertyDecorator}
 */
export function ApiLookupCode(options: PropOptions = {}) {
  const { required = true } = options;

  return applyDecorators(
    ApiProperty({
      description: `Primary key and unique identifier for the lookup entry. Used as the canonical reference value when this lookup is selected or referenced by other entities.`,
      example: `USA`,
      type: String,
      required,
    }),
    IsString(),
    MaxLength(64),
    required ? IsNotEmpty() : IsOptional(),
  );
}
