// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { LookupSnapshotProps } from '../../domain/props';
import { DetailLookupResponse } from '../dtos';

/**
 * Lookup Snapshot to DTO Mappers
 * Reusable mapping functions for converting domain snapshots to DTOs
 */

/**
 * Map lookup snapshot to DTO response
 */
export const mapLookupSnapshotToDto = (
  snapshot: LookupSnapshotProps,
): DetailLookupResponse => {
  return {
    lookupType: snapshot.lookupType,
    code: snapshot.code,
    name: snapshot.name,
    description: snapshot.description,
    enabled: snapshot.enabled,
    attributes: snapshot.attributes,
  };
};
