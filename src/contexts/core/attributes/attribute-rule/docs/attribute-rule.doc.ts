// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DocumentBuilder, SwaggerModule } from '@nestjs/swagger';
import { INestApplication } from '@nestjs/common';
import { SwaggerConfigUtil } from 'src/docs/swagger-config.util';
import { AttributeRuleModule } from '../attribute-rule.module';
import {
  DetailRuleResponse,
  CreateRuleRequest,
  UpdateRuleRequest,
} from '../application/dtos';
/**
 * AttributeRule documentation endpoints interface
 */
export interface AttributeRuleDocumentationUrls {
  attributeRule: string;
}

/**
 * AttributeRule Documentation
 * This module handles the Swagger documentation for attribute_rules
 *
 */
export class AttributeRuleDocumentation {
  static setup(app: INestApplication, port: string | number): void {
    const config = new DocumentBuilder()
      .addBearerAuth(
        { type: 'http', scheme: 'bearer', bearerFormat: 'JWT' },
        'bearer',
      )
      .setTitle('📱 AttributeRule Management API')
      .setDescription(
        `
## bounded context: core
[← Back to documentation](/api/docs/core)

### application: attributes
[← Back to documentation](/api/docs/core/attributes)
`,
      )
      .setVersion('1.0.0')
      .addTag(
        'AttributeRules',
        `Configuration and metadata definitions for attributes, including their rules and validation behavior`,
      );

    // Add dynamic server configuration
    SwaggerConfigUtil.addServers(config, port);

    const document = SwaggerModule.createDocument(app, config.build(), {
      include: [AttributeRuleModule],
      extraModels: [DetailRuleResponse, CreateRuleRequest, UpdateRuleRequest],
    });

    SwaggerModule.setup(
      'api/docs/core/attributes/attribute-rule',
      app,
      document,
    );
  }

  /**
   * Setup all attributeRule-related documentation endpoints
   * This method centralizes the setup of all attributeRule module documentation
   */
  static setupAll(
    app: INestApplication,
    port: string | number,
  ): AttributeRuleDocumentationUrls {
    // Setup main attributeRule documentation
    AttributeRuleDocumentation.setup(app, port);

    // Setup sub-module documentation

    // Return all endpoint URLs
    return {
      attributeRule: AttributeRuleDocumentation.getEndpoint(port),
    };
  }

  static getEndpoint(port: string | number): string {
    return `${SwaggerConfigUtil.getServerUrl(port)}/api/docs/core/attributes/attribute-rule`;
  }
}
