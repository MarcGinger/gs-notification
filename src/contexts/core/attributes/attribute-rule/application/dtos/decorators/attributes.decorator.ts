// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { applyDecorators } from '@nestjs/common';
import { ApiProperty, getSchemaPath } from '@nestjs/swagger';
import { transformAndValidateRecord } from 'src/shared/application/decorators';
import { Transform } from 'class-transformer';
import {
  IsOptional,
  IsNotEmpty,
  ValidateNested,
  IsObject,
} from 'class-validator';
import { DetailRuleResponse } from '../../dtos/rule';

/**
 * Options for property decorators
 */
interface PropOptions {
  required?: boolean;
}

/**
 * Property decorator for AttributeRule Attributes with required option
 * @param {Object} options - Options for the decorator
 * @returns {PropertyDecorator}
 */
export function ApiAttributeRuleAttributes(options: PropOptions = {}) {
  const { required = true } = options;
  return applyDecorators(
    ApiProperty({
      description: `Map of attribute rule definitions keyed by attribute code. Each entry defines the validation and behavior for that attribute.`,
      type: 'object',
      additionalProperties: { $ref: getSchemaPath(DetailRuleResponse) },
      required: [],
    }),
    Transform(({ value }) =>
      transformAndValidateRecord(value, DetailRuleResponse),
    ),
    ValidateNested({ each: true }),
    IsObject(),
    required ? IsNotEmpty() : IsOptional(),
  );
}
