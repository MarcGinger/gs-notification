// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { applyDecorators } from '@nestjs/common';
import { ApiProperty } from '@nestjs/swagger';
import { IsOptional, IsNotEmpty, IsString, MaxLength } from 'class-validator';

/**
 * Options for property decorators
 */
interface PropOptions {
  required?: boolean;
}

/**
 * Property decorator for Rule Code
 * @param {Object} options - Options for the decorator
 * @returns {PropertyDecorator}
 */
export function ApiRuleCode(options: PropOptions = {}) {
  const { required = true } = options;

  return applyDecorators(
    ApiProperty({
      description: `Unique technical identifier for the attribute rule within its scope. Used as a stable reference that doesn't change even if the display name is updated.`,
      example: `email_validation`,
      type: String,
      required,
    }),
    IsString(),
    MaxLength(64),
    required ? IsNotEmpty() : IsOptional(),
  );
}
