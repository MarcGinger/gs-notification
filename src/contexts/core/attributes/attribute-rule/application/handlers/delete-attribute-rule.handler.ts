// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { CommandHandler, ICommandHandler } from '@nestjs/cqrs';
import { Inject } from '@nestjs/common';
import { Result, DomainError } from 'src/shared/errors';
import { DeleteAttributeRuleUseCase } from '../use-cases';
import { DeleteAttributeRuleCommand } from '../commands';

@CommandHandler(DeleteAttributeRuleCommand)
export class DeleteAttributeRuleHandler
  implements ICommandHandler<DeleteAttributeRuleCommand>
{
  constructor(
    @Inject(DeleteAttributeRuleUseCase)
    private readonly deleteAttributeRuleUseCase: DeleteAttributeRuleUseCase,
  ) {}

  async execute(
    command: DeleteAttributeRuleCommand,
  ): Promise<Result<void, DomainError>> {
    return await this.deleteAttributeRuleUseCase.execute({
      user: command.user,
      code: command.code,
      correlationId: command.correlationId,
      authorizationReason: 'CQRS Command Handler',
    });
  }
}
