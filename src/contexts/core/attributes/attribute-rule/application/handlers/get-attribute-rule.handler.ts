// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { QueryHandler, IQueryHandler } from '@nestjs/cqrs';
import { DetailAttributeRuleResponse } from '../dtos';
import { GetAttributeRuleUseCase } from '../use-cases';
import { GetAttributeRuleQuery } from '../queries';
import { Result, DomainError } from 'src/shared/errors';

@QueryHandler(GetAttributeRuleQuery)
export class GetAttributeRuleHandler
  implements IQueryHandler<GetAttributeRuleQuery>
{
  constructor(
    private readonly getAttributeRuleUseCase: GetAttributeRuleUseCase,
  ) {}

  async execute(
    query: GetAttributeRuleQuery,
  ): Promise<Result<DetailAttributeRuleResponse | null, DomainError>> {
    return this.getAttributeRuleUseCase.execute({
      user: query.user,
      code: query.code,
      correlationId: query.correlationId || 'query-handler',
    });
  }
}
