// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { QueryHandler, IQueryHandler } from '@nestjs/cqrs';
import { Injectable } from '@nestjs/common';
import { DomainError, Result } from 'src/shared/errors';
import { ListAttributeRuleQuery } from '../queries';
import { ListAttributeRuleUseCase } from '../use-cases';
import { AttributeRulePageResponse } from '../dtos';

/**
 * ✅ Enhanced CQRS Query Handler with use case delegation
 * Follows the pattern: Handler → Use Case → Repository
 */
@Injectable()
@QueryHandler(ListAttributeRuleQuery)
export class ListAttributeRuleHandler
  implements IQueryHandler<ListAttributeRuleQuery>
{
  constructor(
    private readonly listAttributeRuleUseCase: ListAttributeRuleUseCase,
  ) {}

  /**
   * Handle the list attributeRule query by delegating to the use case
   */
  async execute(
    query: ListAttributeRuleQuery,
  ): Promise<Result<AttributeRulePageResponse, DomainError>> {
    // ✅ Delegate to use case for business logic
    return await this.listAttributeRuleUseCase.execute({
      user: query.user,
      filter: query.filter,
      correlationId: query.correlationId || 'unknown',
    });
  }
}
