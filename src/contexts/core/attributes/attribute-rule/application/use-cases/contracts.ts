// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

/**
 * Abstract Use Case Contracts for AttributeRule Operations
 *
 * These abstract classes are used instead of interfaces to work with NestJS DI.
 * TypeScript interfaces are erased at runtime and cannot be injected.
 */

import { Result, DomainError } from 'src/shared/errors';
import type { IUserToken } from 'src/shared/security';
import type { UpsertAttributeRuleProps } from '../../domain/props';
import type {
  DetailAttributeRuleResponse,
  AttributeRulePageResponse,
  ListAttributeRuleFilterRequest,
} from '../dtos';

export abstract class IUpsertAttributeRuleUseCase {
  abstract execute(params: {
    user: IUserToken;
    code: string;
    props: UpsertAttributeRuleProps;
    correlationId: string;
    authorizationReason: string;
  }): Promise<Result<DetailAttributeRuleResponse, DomainError>>;
}

export abstract class IDeleteAttributeRuleUseCase {
  abstract execute(params: {
    user: IUserToken;
    code: string;
    correlationId: string;
    authorizationReason: string;
  }): Promise<Result<void, DomainError>>;
}

export abstract class IGetAttributeRuleUseCase {
  abstract execute(params: {
    user: IUserToken;
    code: string;
    correlationId: string;
  }): Promise<Result<DetailAttributeRuleResponse, DomainError>>;
}
export abstract class IListAttributeRuleUseCase {
  abstract execute(params: {
    user: IUserToken;
    filter?: ListAttributeRuleFilterRequest;
    correlationId: string;
  }): Promise<Result<AttributeRulePageResponse, DomainError>>;
}
