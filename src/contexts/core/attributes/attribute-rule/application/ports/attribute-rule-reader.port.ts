// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Result, DomainError } from 'src/shared/errors';
import { RepositoryOptions } from 'src/shared/infrastructure/repositories';
import { ActorContext } from 'src/shared/application/context';
import { Option } from 'src/shared/domain/types';
import { AttributeRuleSnapshotProps } from '../../domain/props';
import { AttributeRuleCode } from '../../domain/value-objects';

/**
 * Token for injecting IAttributeRuleReader port implementation
 */
export const ATTRIBUTE_RULE_READER_TOKEN = 'IAttributeRuleReader' as const;

/**
 * AttributeRule Reader Port - Interface Segregation Principle Implementation
 *
 * Handles basic read operations for AttributeRule aggregates.
 * This interface focuses solely on simple retrieval operations.
 *
 * ISP Benefits:
 * - Clients only depend on methods they actually use
 * - Easier to mock and test specific read scenarios
 * - Can be implemented independently from write operations
 * - Supports read-only implementations (e.g., reporting services)
 *
 * @domain Core Context - AttributeRule Reader Port
 * @layer Application Ports
 * @pattern Interface Segregation Principle
 */
export interface IAttributeRuleReader {
  /**
   * Find a AttributeRule by its unique identifier
   * @param actor - The authenticated user context
   * @param code - The unique identifier of the AttributeRule
   * @param options - Optional repository options
   * @returns Result containing the AttributeRule snapshot or null if not found
   */
  findById(
    actor: ActorContext,
    code: AttributeRuleCode,
    options?: RepositoryOptions,
  ): Promise<Result<Option<AttributeRuleSnapshotProps>, DomainError>>;

  /**
   * Check if a attributeRule exists by code (for write-path validation)
   * @param actor - The authenticated user context
   * @param code - The unique identifier of the AttributeRule
   * @param options - Optional repository options
   * @returns Result containing boolean indicating existence
   */
  exists(
    actor: ActorContext,
    code: AttributeRuleCode,
    options?: RepositoryOptions,
  ): Promise<Result<boolean, DomainError>>;

  /**
   * Get attributeRule version for optimistic concurrency control
   * @param actor - The authenticated user context
   * @param code - The unique identifier of the AttributeRule
   * @param options - Optional repository options
   * @returns Result containing version number or null if not found
   */
  getVersion(
    actor: ActorContext,
    code: AttributeRuleCode,
    options?: RepositoryOptions,
  ): Promise<Result<Option<number>, DomainError>>;

  /**
   * Get minimal attributeRule data for write-path operations
   * @param actor - The authenticated user context
   * @param code - The unique identifier of the AttributeRule
   * @param options - Optional repository options
   * @returns Result containing minimal attributeRule data or null if not found
   */
  getMinimal(
    actor: ActorContext,
    code: AttributeRuleCode,
    options?: RepositoryOptions,
  ): Promise<Result<Option<{ code: string; version: number }>, DomainError>>;
}
