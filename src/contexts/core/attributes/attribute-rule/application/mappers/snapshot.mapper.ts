// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { AttributeRuleSnapshotProps } from '../../domain/props';
import { DetailAttributeRuleResponse } from '../dtos';
import { AttributeRuleRuleConfiguration } from '../../domain/value-objects';
import { extractRuleConfigurationData } from '../../domain/utilities';

/**
 * AttributeRule Snapshot to DTO Mappers
 * Reusable mapping functions for converting domain snapshots to DTOs
 */

/**
 * Map attributeRule snapshot to DTO response
 */
export const mapAttributeRuleSnapshotToDto = (
  snapshot: AttributeRuleSnapshotProps,
): DetailAttributeRuleResponse => {
  return {
    code: snapshot.code,
    name: snapshot.name,
    description: snapshot.description,
    enabled: snapshot.enabled,
    attributes: snapshot.attributes,
  };
};

/**
 * Map attributes configuration to DTO
 *
 * This application-layer function uses domain extraction and transforms
 * the data to match the required DTO structure without business defaults.
 *
 * @param ruleConfiguration - Domain value object containing rule configurations
 * @returns Record<string, T> where T is typically DetailRuleResponse
 *
 * @example
 * ```typescript
 * // Typical usage:
 * const ruleDto = mapRuleConfigurationToDto<DetailRuleResponse>(rulesConfig);
 * // Result: Record<string, DetailRuleResponse>
 * ```
 */
export const mapRuleConfigurationToDto = <T>(
  ruleConfiguration: AttributeRuleRuleConfiguration,
): Record<string, T> => extractRuleConfigurationData<T>(ruleConfiguration);
