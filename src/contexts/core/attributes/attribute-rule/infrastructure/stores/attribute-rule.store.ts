// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

/**
 * Simple Generic Store
 *
 * Direct, no-nonsense storage for any projections.
 * Used by both projectors (write) and repositories (read).
 */

import { BaseProjection, SimpleStore } from 'src/shared/infrastructure/stores';
import { DetailAttributeRuleResponse } from '../../application/dtos';

/**
 * AttributeRule projection data
 */
export interface AttributeRuleProjection
  extends DetailAttributeRuleResponse,
    BaseProjection {
  lastStreamRevision: string;
}

/**
 * AttributeRule-specific store instance - shared across projectors and repositories
 */
export const attributeRuleStore = new SimpleStore<AttributeRuleProjection>(
  5000,
  'attributeRule',
);
