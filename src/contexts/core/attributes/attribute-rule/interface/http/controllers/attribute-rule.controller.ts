// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  Controller,
  Get,
  Query,
  Post,
  Body,
  Put,
  Delete,
  HttpStatus,
  HttpCode,
  Param,
  UseInterceptors,
  UseGuards,
} from '@nestjs/common';
import {
  ApiTags,
  ApiOperation,
  ApiResponse,
  ApiNoContentResponse,
  ApiCreatedResponse,
  ApiBody,
  ApiHeader,
  ApiParam,
  ApiOkResponse,
  ApiBearerAuth,
} from '@nestjs/swagger';
import { IdempotencyKey } from 'src/shared/interfaces/decorators';
import { IdempotencyKeyPipe } from 'src/shared/interfaces/pipes';
import { AttributeRuleApplicationService } from '../../../application/services';
import { CurrentUser, IUserToken, JwtAuthGuard } from 'src/shared/security';
import {
  DetailAttributeRuleResponse,
  CreateAttributeRuleRequest,
  UpdateAttributeRuleRequest,
  AttributeRulePageResponse,
  ListAttributeRuleFilterRequest,
  ListAttributeRuleResponse,
} from '../../../application/dtos';
import { Result, ResultInterceptor, DomainError } from 'src/shared/errors';
import { PaginatedResponse } from 'src/shared/application/dtos';
import {
  AttributeRuleReadResource,
  AttributeRuleCreateResource,
  AttributeRuleUpdateResource,
  AttributeRuleDeleteResource,
} from '../../attribute-rule.resource';
import { ApiCommonErrors } from 'src/shared/interfaces/http';

@Controller()
@ApiTags('AttributeRules')
@ApiBearerAuth()
@UseGuards(JwtAuthGuard)
@UseInterceptors(ResultInterceptor)
export class AttributeRuleController {
  constructor(
    private readonly attributeRuleApplicationService: AttributeRuleApplicationService,
  ) {}

  @Get()
  @AttributeRuleReadResource()
  @ApiOperation({
    summary: 'List Attribute rules',
    description:
      'Retrieves a list of Attribute rules with optional filtering. Requires READ permission (LOW risk).',
  })
  @ApiOkResponse({
    description: 'List of Attribute rules retrieved successfully',
    type: AttributeRulePageResponse,
  })
  @ApiCommonErrors()
  async list(
    @CurrentUser() user: IUserToken,
    @Query() pageRequest?: ListAttributeRuleFilterRequest,
  ): Promise<
    Result<PaginatedResponse<ListAttributeRuleResponse>, DomainError>
  > {
    const result =
      await this.attributeRuleApplicationService.listAttributeRules(
        user,
        pageRequest,
      );
    return result;
  }

  @Get(':code')
  @AttributeRuleReadResource()
  @ApiOperation({
    summary: 'Get Attribute rule by code',
    description:
      'Retrieves a single Attribute rule by its unique identifier. Requires READ permission (LOW risk).',
  })
  @ApiParam({
    name: 'code',
    type: 'string',
    description: 'AttributeRule unique identifier',
    example: 'COUNTRY',
  })
  @ApiOkResponse({
    description: 'AttributeRule details retrieved successfully',
    type: DetailAttributeRuleResponse,
  })
  @ApiCommonErrors()
  async get(
    @CurrentUser() user: IUserToken,
    @Param('code') code: string,
  ): Promise<Result<DetailAttributeRuleResponse, DomainError>> {
    const result =
      await this.attributeRuleApplicationService.getAttributeRuleById(
        user,
        code,
      );

    return result;
  }
  // ========================================
  // Core CRUD Operations
  // ========================================

  @Post()
  @AttributeRuleCreateResource()
  @HttpCode(HttpStatus.CREATED)
  @ApiOperation({
    summary: 'Create a new Attribute rule',
    description:
      'Creates a new Attribute rule in the catalog. Requires CREATE permission (MEDIUM risk).',
  })
  @ApiHeader({
    name: 'Idempotency-Key',
    required: false,
    description:
      'Ensures the request is processed only once. Accepts both Idempotency-Key and x-idempotency-key headers.',
  })
  @ApiCreatedResponse({
    type: DetailAttributeRuleResponse,
    description: 'AttributeRule successfully created',
    headers: {
      Location: {
        description: 'URI of the newly created resource',
        schema: {
          type: 'string',
          example: '/attribute-rules/123e4567-e89b-12d3-a456-426614174000',
        },
      },
    },
  })
  @ApiCommonErrors()
  @ApiBody({ type: CreateAttributeRuleRequest })
  async create(
    @CurrentUser() user: IUserToken,
    @Body() createAttributeRuleRequest: CreateAttributeRuleRequest,
    @IdempotencyKey(new IdempotencyKeyPipe())
    idempotencyKey: string | undefined,
  ): Promise<Result<DetailAttributeRuleResponse, DomainError>> {
    // idempotencyKey is validated by the IdempotencyKeyPipe

    // Call service with options including idempotency key
    const result =
      await this.attributeRuleApplicationService.createAttributeRule(
        user,
        createAttributeRuleRequest,
        {
          idempotencyKey,
        },
      );

    return result;
  }

  @Put(':code')
  @AttributeRuleUpdateResource()
  @ApiOperation({
    summary: 'Update a Attribute rule',
    description:
      'Updates an existing Attribute rule with new data. Supports partial updates. Requires UPDATE permission (MEDIUM risk).',
  })
  @ApiParam({
    name: 'code',
    type: 'string',
    description: 'AttributeRule unique identifier',
    example: 'COUNTRY',
  })
  @ApiResponse({
    status: HttpStatus.OK,
    description: 'AttributeRule updated successfully',
    type: DetailAttributeRuleResponse,
  })
  @ApiCommonErrors({
    include422: true,
    extra: [
      { status: 429, description: 'Too Many Requests' },
      { status: 503, description: 'Upstream dependency unavailable' },
    ],
  })
  @ApiBody({
    type: UpdateAttributeRuleRequest,
    description:
      'AttributeRule update data. Only provided fields will be updated.',
  })
  async update(
    @CurrentUser() user: IUserToken,
    @Param('code') code: string,
    @Body() updateAttributeRuleRequest: UpdateAttributeRuleRequest,
  ): Promise<Result<DetailAttributeRuleResponse, DomainError>> {
    const result =
      await this.attributeRuleApplicationService.updateAttributeRule(
        user,
        code,
        updateAttributeRuleRequest,
      );

    return result;
  }

  @Delete(':code')
  @AttributeRuleDeleteResource()
  @HttpCode(HttpStatus.NO_CONTENT)
  @ApiOperation({
    summary: 'Delete a Attribute rule',
    description:
      'Soft-deletes a Attribute rule (marks as deleted). Requires DELETE permission (HIGH risk, justification required).',
  })
  @ApiParam({
    name: 'code',
    type: 'string',
    description: 'AttributeRule unique identifier',
    example: 'COUNTRY',
  })
  @ApiCommonErrors()
  @ApiNoContentResponse({
    description: 'AttributeRule deleted successfully. No content returned.',
  })
  async remove(
    @CurrentUser() user: IUserToken,
    @Param('code') code: string,
  ): Promise<Result<void, DomainError>> {
    const result =
      await this.attributeRuleApplicationService.deleteAttributeRule(
        user,
        code,
      );
    return result;
  }
}
