// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Module } from '@nestjs/common';
import { RouterModule, Routes } from '@nestjs/core';
import { AttributeRuleModule } from '../../attribute-rule.module';
import { AttributeRuleProjectorModule } from '../../attribute-rule-projector.module';
/**
 * Attribute rule Router Configuration
 *
 * Configures nested routing for attribute-rule endpoints to follow
 * the module monolith structure: /core/attributes/api/v1/attribute-rules
 *
 * This replaces flat routing (/api/v1/attribute-rules) with contextual routing
 * that reflects the bounded context hierarchy.
 */

// Define the routing structure for the attribute-rule bounded context
const attributeRuleRoutes: Routes = [
  {
    path: 'core',
    children: [
      {
        path: 'attributes',
        children: [
          {
            path: 'attribute-rules',
            module: AttributeRuleModule,
          },
          {
            path: 'health',
            module: AttributeRuleProjectorModule,
          },
        ],
      },
    ],
  },
];

/**
 * Attribute rule Router Module
 *
 * Provides nested routing configuration for attribute-rule endpoints.
 * This module should be imported in the main app module to enable
 * the nested route structure.
 *
 * Routes configured:
 * - /core/attributes/api/v1/attribute-rules (GET, POST)
 * - /core/attributes/api/v1/attribute-rules/:code (GET, PUT, DELETE)
 * - /core/attributes/health/projectors/attributeRule (GET)
 * - /core/attributes/health/projectors/attribute-rule/metrics (GET)
 */
@Module({
  imports: [
    AttributeRuleModule,
    AttributeRuleProjectorModule,
    RouterModule.register(attributeRuleRoutes),
  ],
  exports: [AttributeRuleModule, AttributeRuleProjectorModule],
})
export class AttributeRuleRouterModule {}
