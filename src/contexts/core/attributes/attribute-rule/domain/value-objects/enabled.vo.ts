// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  BooleanVOInstance,
  createBooleanVO,
  createBooleanVOErrors,
} from 'src/shared/domain/value-objects';
import { AttributeRuleErrors } from '..//errors/attribute-rule.errors';

/**
 * Enabled Boolean Value Object
 * Represents a validated enabled with business rules
 */
export const AttributeRuleEnabled = createBooleanVO({
  name: 'Enabled',
  treatNullAsFalse: true,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createBooleanVOErrors(AttributeRuleErrors.INVALID_ENABLED, 'Enabled'),
});

/** Public instance type for Enabled */
export type AttributeRuleEnabled = BooleanVOInstance;

// Convenience creators
export const createAttributeRuleEnabled = (v: boolean) =>
  AttributeRuleEnabled.create(v);
export const attributeRuleEnabledFrom = (v: unknown) =>
  AttributeRuleEnabled.from(v);
