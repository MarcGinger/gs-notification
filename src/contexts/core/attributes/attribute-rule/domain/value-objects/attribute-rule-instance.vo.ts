// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DomainError, Result, ok, err } from 'src/shared/errors';
import {
  createIntegerVO,
  createIntegerVOErrors,
  IntegerVOInstance,
  createDateTimeVO,
  createDateTimeVOErrors,
  DateTimeVOInstance,
} from 'src/shared/domain/value-objects';
import { Clock } from 'src/shared/domain/clock';
import { AttributeRuleErrors } from '../errors/attribute-rule.errors';

/**
 * AttributeRule Instance Value Objects
 *
 * Instance VOs for AttributeRule aggregate lifecycle management.
 * Includes creation timestamps, update timestamps, and version tracking.
 * Follows the Clock injection pattern for deterministic testing.
 */

// ===========================================
// AttributeRule Created At Instance VO
// ===========================================

const AttributeRuleCreatedAtErrors = createDateTimeVOErrors(
  AttributeRuleErrors.CREATED_AT_REQUIRED,
  'AttributeRule Created At',
);

export const AttributeRuleCreatedAt = createDateTimeVO({
  name: 'AttributeRuleCreatedAt',
  allowFuture: false, // Cannot create attribute_rule in the future
  allowPast: true, // Can create with past timestamps (data migration)
  customValidation: (date: Date): Result<void, DomainError> => {
    // Optional: Add domain-specific creation time validation
    // For example, cannot be more than 1 year in the past
    const oneYearAgo = new Date();
    oneYearAgo.setFullYear(oneYearAgo.getFullYear() - 1);

    if (date < oneYearAgo) {
      return err({
        ...AttributeRuleErrors.CREATED_AT_REQUIRED,
        detail:
          'AttributeRule creation date cannot be more than 1 year in the past',
        context: { value: date, maxPastDate: oneYearAgo },
      } as DomainError);
    }

    return ok(undefined);
  },
  errors: AttributeRuleCreatedAtErrors,
});

export type AttributeRuleCreatedAt = DateTimeVOInstance;

// ===========================================
// AttributeRule Updated At Instance VO
// ===========================================

const AttributeRuleUpdatedAtErrors = createDateTimeVOErrors(
  AttributeRuleErrors.UPDATED_AT_REQUIRED,
  'AttributeRule Updated At',
);

export const AttributeRuleUpdatedAt = createDateTimeVO({
  name: 'AttributeRuleUpdatedAt',
  allowFuture: false, // Cannot update attribute_rule in the future
  allowPast: true, // Can update with past timestamps (data correction)
  errors: AttributeRuleUpdatedAtErrors,
});

export type AttributeRuleUpdatedAt = DateTimeVOInstance;

// ===========================================
// AttributeRule Version Instance VO
// ===========================================

const AttributeRuleVersionErrors = createIntegerVOErrors(
  AttributeRuleErrors.INVALID_CODE_DATA, // Reusing existing error until specific version error is added
  'AttributeRule Version',
);

export const AttributeRuleVersion = createIntegerVO({
  name: 'AttributeRuleVersion',
  min: 0, // Version starts at 0
  allowNegative: false,
  errors: AttributeRuleVersionErrors,
});

export type AttributeRuleVersion = IntegerVOInstance;

// ===========================================
// Clock-Based Helper Functions (Explicit Dependencies)
// ===========================================

/**
 * Create AttributeRuleCreatedAt from Clock (explicit dependency)
 * Deterministic and testable - pass a fake clock in tests
 */
export const createdAtNow = (
  clock: Clock,
): Result<AttributeRuleCreatedAt, DomainError> =>
  AttributeRuleCreatedAt.create(clock.now());

/**
 * Create AttributeRuleUpdatedAt from Clock (explicit dependency)
 * Deterministic and testable - pass a fake clock in tests
 */
export const updatedAtNow = (
  clock: Clock,
): Result<AttributeRuleUpdatedAt, DomainError> =>
  AttributeRuleUpdatedAt.create(clock.now());

/**
 * Bump version by 1 (domain business rule)
 */
export const bumpVersion = (
  currentVersion: AttributeRuleVersion,
): Result<AttributeRuleVersion, DomainError> =>
  AttributeRuleVersion.create(currentVersion.value + 1);

/**
 * Create initial version (starts at 1 for new aggregates)
 */
export const initialVersion = (): Result<AttributeRuleVersion, DomainError> =>
  AttributeRuleVersion.create(1);

// ===========================================
// Direct Creation Functions (Non-Clock Based)
// ===========================================

/**
 * Create AttributeRuleCreatedAt from Date
 */
export const createAttributeRuleCreatedAt = (
  date: Date,
): Result<AttributeRuleCreatedAt, DomainError> =>
  AttributeRuleCreatedAt.create(date);

/**
 * Create AttributeRuleUpdatedAt from Date
 */
export const createAttributeRuleUpdatedAt = (
  date: Date,
): Result<AttributeRuleUpdatedAt, DomainError> =>
  AttributeRuleUpdatedAt.create(date);

/**
 * Create AttributeRuleVersion from number
 */
export const createAttributeRuleVersion = (
  version: number,
): Result<AttributeRuleVersion, DomainError> =>
  AttributeRuleVersion.create(version);

// ===========================================
// Domain Invariants & Validation
// ===========================================

/**
 * Validate that updatedAt is not before createdAt (monotonic time)
 */
export const validateMonotonicTime = (
  createdAt: AttributeRuleCreatedAt,
  updatedAt: AttributeRuleUpdatedAt,
): Result<void, DomainError> => {
  if (updatedAt.value < createdAt.value) {
    return err({
      ...AttributeRuleErrors.UPDATED_AT_REQUIRED,
      detail: 'Updated at cannot be before created at',
      context: {
        createdAt: createdAt.value.toISOString(),
        updatedAt: updatedAt.value.toISOString(),
      },
    } as DomainError);
  }
  return ok(undefined);
};

// ===========================================
// Legacy Compatibility (DEPRECATED)
// ===========================================

/**
 * @deprecated Use createdAtNow(clock) instead for better testability
 * This function hides the Date dependency and makes testing harder
 */
export function createAttributeRuleCreatedAtNow(): Result<
  AttributeRuleCreatedAt,
  DomainError
> {
  return AttributeRuleCreatedAt.create(new Date());
}

/**
 * @deprecated Use updatedAtNow(clock) instead for better testability
 * This function hides the Date dependency and makes testing harder
 */
export function createAttributeRuleUpdatedAtNow(): Result<
  AttributeRuleUpdatedAt,
  DomainError
> {
  return AttributeRuleUpdatedAt.create(new Date());
}
