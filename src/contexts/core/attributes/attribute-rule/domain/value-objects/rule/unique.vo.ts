// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  BooleanVOInstance,
  createBooleanVO,
  createBooleanVOErrors,
} from 'src/shared/domain/value-objects';
import { RuleErrors } from '../..//errors/rule.errors';

/**
 * Unique Boolean Value Object
 * Represents a validated unique with business rules
 */
export const RuleUnique = createBooleanVO({
  name: 'Unique',
  treatNullAsFalse: true,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createBooleanVOErrors(RuleErrors.INVALID_UNIQUE, 'Unique'),
});

/** Public instance type for Unique */
export type RuleUnique = BooleanVOInstance;

// Convenience creators
export const createRuleUnique = (v: boolean) => RuleUnique.create(v);
export const ruleUniqueFrom = (v: unknown) => RuleUnique.from(v);
