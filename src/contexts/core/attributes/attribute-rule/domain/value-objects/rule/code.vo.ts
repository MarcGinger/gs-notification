// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { RuleErrors } from '../../errors/rule.errors';

/**
 * Code Value Object
 * Represents a validated code with business rules
 */
export const RuleCode = createStringVO({
  name: 'Code',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  maxLength: 64,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(RuleErrors.INVALID_CODE, 'Code'),
});

/**
 * Public instance type for Code
 */
export type RuleCode = StringVOInstance;

// Convenience creators
export const createRuleCode = (s: string) => RuleCode.create(s);
export const ruleCodeFrom = (v: unknown) => RuleCode.from(v);
