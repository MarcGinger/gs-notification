// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { RuleErrors } from '../../errors/rule.errors';

/**
 * Regex Value Object
 * Represents a validated regex with business rules
 */
export const RuleRegex = createStringVO({
  name: 'Regex',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 120,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(RuleErrors.INVALID_REGEX, 'Regex'),
});

/**
 * Public instance type for Regex
 */
export type RuleRegex = StringVOInstance;

// Convenience creators
export const createRuleRegex = (s: string) => RuleRegex.create(s);
export const ruleRegexFrom = (v: unknown) => RuleRegex.from(v);
