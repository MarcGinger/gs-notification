// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { EntityIdBase } from 'src/shared/domain/entities';
import { Result, ok, err, DomainError } from 'src/shared/errors';
import { AttributeRuleSnapshotProps } from '../props';
import { AttributeRuleDomainState } from '../state';
import { AttributeRuleErrors } from '../errors/attribute-rule.errors';
import {
  AttributeRuleCode,
  AttributeRuleCreatedAt,
  AttributeRuleUpdatedAt,
  AttributeRuleVersion,
  AttributeRuleDescription,
  AttributeRuleEnabled,
  AttributeRuleName,
  AttributeRuleRuleConfiguration,
  ruleConfigurationToSnapshotProps,
} from '../value-objects';

/**
 * Domain Entity: AttributeRule
 *
 * Represents the core AttributeRule entity in the core domain.
 * Encapsulates attributeRule data, identity, and basic entity behavior.
 *
 * This entity follows DDD principles:
 * - Identity: Code as unique identifier
 * - Immutability: Changes create new instances
 * - Encapsulation: Private state with controlled access
 * - Business validation: Domain rules enforced
 *
 * @domain Core Context - AttributeRule Entity
 * @layer Domain Entities
 */

/**
 * AttributeRule Entity
 *
 * Core domain entity representing a attributeRule in the core.
 * Handles attributeRule identity, validation, and state management.
 */
export class AttributeRuleEntity extends EntityIdBase<
  AttributeRuleDomainState,
  AttributeRuleCode
> {
  private static clock: { now: () => Date } = { now: () => new Date() };

  public static setClock(c: { now: () => Date }) {
    this.clock = c;
  }

  /**
   * Helper method to create updatedAt VO from date
   */
  private static createUpdatedAt(
    date?: Date,
  ): Result<AttributeRuleUpdatedAt, DomainError> {
    const targetDate = date || AttributeRuleEntity.clock.now();
    const result = AttributeRuleUpdatedAt.from(targetDate);
    if (!result.ok) {
      return err(result.error);
    }
    return ok(result.value);
  }

  /**
   * Helper method to create version VO from number
   */
  private static createVersion(
    versionNumber?: number,
    currentVersion?: AttributeRuleVersion,
  ): Result<AttributeRuleVersion, DomainError> {
    const targetVersion = versionNumber ?? currentVersion?.value ?? 1;
    const result = AttributeRuleVersion.from(targetVersion);
    if (!result.ok) {
      return err(result.error);
    }
    return ok(result.value);
  }

  /**
   * Helper method to create updated entity with common logic
   */
  private createUpdatedEntity(
    fieldUpdates: Partial<AttributeRuleDomainState>,
    updatedAt?: Date,
    version?: number,
  ): Result<AttributeRuleEntity, DomainError> {
    const updatedAtResult = AttributeRuleEntity.createUpdatedAt(updatedAt);
    if (!updatedAtResult.ok) {
      return err(updatedAtResult.error);
    }

    const versionResult = AttributeRuleEntity.createVersion(
      version,
      this.props.version,
    );
    if (!versionResult.ok) {
      return err(versionResult.error);
    }

    const updatedProps: AttributeRuleDomainState = {
      ...this.props,
      ...fieldUpdates,
      updatedAt: updatedAtResult.value,
      version: versionResult.value,
    };

    return AttributeRuleEntity.create(updatedProps);
  }

  private constructor(props: AttributeRuleDomainState) {
    super(props, props.code);
  }

  /**
   * Factory method to create a new attributeRule entity
   *
   * @param props - AttributeRule properties
   * @returns Result containing AttributeRuleEntity or DomainError
   */
  public static create(
    props: AttributeRuleDomainState,
  ): Result<AttributeRuleEntity, DomainError> {
    // Validate required properties
    const validationResult = AttributeRuleEntity.validate(props);
    if (validationResult.ok === false) {
      return err(validationResult.error);
    }

    return ok(new AttributeRuleEntity(props));
  }

  /**
   * Factory method to reconstitute a attributeRule entity from persistence
   *
   * @param props - AttributeRule properties from database
   * @returns AttributeRuleEntity instance
   */
  public static reconstitute(
    props: AttributeRuleDomainState,
  ): AttributeRuleEntity {
    return new AttributeRuleEntity(props);
  }

  /**
   * Factory method to reconstitute entity from snapshot
   *
   * @param snapshot - AttributeRule snapshot properties
   * @returns Result containing AttributeRuleEntity or DomainError
   */
  public static fromSnapshot(
    snapshot: AttributeRuleSnapshotProps,
  ): Result<AttributeRuleEntity, DomainError> {
    const codeResult = AttributeRuleCode.from(snapshot.code);
    if (!codeResult.ok) {
      return err(codeResult.error);
    }
    const nameResult = AttributeRuleName.from(snapshot.name);
    if (!nameResult.ok) {
      return err(nameResult.error);
    }
    const descriptionResult = AttributeRuleDescription.from(
      snapshot.description,
    );
    if (!descriptionResult.ok) {
      return err(descriptionResult.error);
    }
    const enabledResult = AttributeRuleEnabled.from(snapshot.enabled);
    if (!enabledResult.ok) {
      return err(enabledResult.error);
    }
    const attributesResult = AttributeRuleRuleConfiguration.from(
      snapshot.attributes,
    );
    if (!attributesResult.ok) {
      return err(attributesResult.error);
    }
    const createdAtResult = AttributeRuleCreatedAt.from(snapshot.createdAt);
    if (!createdAtResult.ok) {
      return err(createdAtResult.error);
    }

    const updatedAtResult = AttributeRuleUpdatedAt.from(snapshot.updatedAt);
    if (!updatedAtResult.ok) {
      return err(updatedAtResult.error);
    }

    const versionResult = AttributeRuleVersion.from(snapshot.version);
    if (!versionResult.ok) {
      return err(versionResult.error);
    }

    const props: AttributeRuleDomainState = {
      code: codeResult.value,
      name: nameResult.value,
      description: descriptionResult.value,
      enabled: enabledResult.value,
      attributes: attributesResult.value,
      createdAt: createdAtResult.value,
      updatedAt: updatedAtResult.value,
      version: versionResult.value,
    };

    return AttributeRuleEntity.create(props);
  }

  /**
   * Validates attributeRule entity properties
   *
   * @param props - Properties to validate
   * @returns Validation result
   */
  private static validate(
    props: AttributeRuleDomainState,
  ): Result<void, DomainError> {
    // Basic validation
    if (!props.code) {
      return err(AttributeRuleErrors.INVALID_CODE_DATA);
    }
    if (!props.name) {
      return err(AttributeRuleErrors.INVALID_NAME_DATA);
    }
    if (!props.attributes) {
      return err(AttributeRuleErrors.INVALID_ATTRIBUTES_DATA);
    }

    return ok(undefined);
  }

  // ======================
  // Getters (Public API)
  // ======================

  public get code(): AttributeRuleCode {
    return this.props.code;
  }

  public get name(): AttributeRuleName {
    return this.props.name;
  }

  public get description(): AttributeRuleDescription | undefined {
    return this.props.description;
  }

  public get enabled(): AttributeRuleEnabled | undefined {
    return this.props.enabled;
  }

  public get attributes(): AttributeRuleRuleConfiguration {
    return this.props.attributes;
  }

  public get createdAt(): AttributeRuleCreatedAt {
    return this.props.createdAt;
  }

  public get updatedAt(): AttributeRuleUpdatedAt {
    return this.props.updatedAt;
  }

  public get version(): AttributeRuleVersion {
    return this.props.version;
  }

  // ======================
  // Simple Update Methods (No Business Logic)
  // ======================

  /**
   * Creates a new entity with updated name (pure state transition)
   *
   * @param name - New name value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<AttributeRuleEntity, DomainError>
   */
  public withName(
    name: AttributeRuleName,
    updatedAt?: Date,
    version?: number,
  ): Result<AttributeRuleEntity, DomainError> {
    return this.createUpdatedEntity({ name }, updatedAt, version);
  }

  /**
   * Creates a new entity with updated description (pure state transition)
   *
   * @param description - New description value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<AttributeRuleEntity, DomainError>
   */
  public withDescription(
    description: AttributeRuleDescription,
    updatedAt?: Date,
    version?: number,
  ): Result<AttributeRuleEntity, DomainError> {
    return this.createUpdatedEntity({ description }, updatedAt, version);
  }

  /**
   * Creates a new entity with updated enabled (pure state transition)
   *
   * @param enabled - New enabled value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<AttributeRuleEntity, DomainError>
   */
  public withEnabled(
    enabled: AttributeRuleEnabled,
    updatedAt?: Date,
    version?: number,
  ): Result<AttributeRuleEntity, DomainError> {
    return this.createUpdatedEntity({ enabled }, updatedAt, version);
  }

  /**
   * Creates a new entity with updated attributes (pure state transition)
   *
   * @param attributes - New attributes value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<AttributeRuleEntity, DomainError>
   */
  public withAttributes(
    attributes: AttributeRuleRuleConfiguration,
    updatedAt?: Date,
    version?: number,
  ): Result<AttributeRuleEntity, DomainError> {
    return this.createUpdatedEntity({ attributes }, updatedAt, version);
  }

  // ======================
  // Query Methods
  // ======================

  /**
   * Checks if two attributeRules are the same entity
   *
   * @param other - Other attributeRule to compare
   */
  public sameAs(other: AttributeRuleEntity): boolean {
    return this.props.code.equals(other.props.code);
  }

  /**
   * Gets a snapshot of current entity state for serialization
   */
  public toSnapshot(): AttributeRuleSnapshotProps {
    return {
      code: this.props.code.value,
      name: this.props.name.value,
      description: this.props.description?.value,
      enabled: this.props.enabled?.value,
      attributes: ruleConfigurationToSnapshotProps(this.props.attributes),
      createdAt: this.props.createdAt.value,
      updatedAt: this.props.updatedAt.value,
      version: this.props.version.value,
    };
  }

  /**
   * Gets the current domain state with value objects
   *
   * Provides access to the rich domain state for application layer
   * without requiring infrastructure mappers. Maintains clean architecture.
   *
   * @returns Current domain state with value objects
   */
  public getDomainState(): AttributeRuleDomainState {
    return this.props;
  }
}
