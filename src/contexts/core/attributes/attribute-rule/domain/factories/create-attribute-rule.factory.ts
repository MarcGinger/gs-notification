// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DomainError, Result, err, withContext } from 'src/shared/errors';
import { Clock } from 'src/shared/domain/clock';
import { EventMetadata } from 'src/shared/domain/events';
import { AttributeRuleAggregate } from '../aggregates';
import { CreateAttributeRuleProps } from '../props';
import { AttributeRuleDomainState } from '../state';
import {
  AttributeRuleCreatedAt,
  AttributeRuleUpdatedAt,
  AttributeRuleVersion,
  AttributeRuleCode,
  AttributeRuleName,
  AttributeRuleDescription,
  AttributeRuleEnabled,
  AttributeRuleAttributes,
} from '../value-objects';

/**
 * Enhanced attributeRule entity factory with comprehensive validation and security context
 * Moved from application layer to properly separate business concerns
 */
export function createAttributeRuleAggregateFromProps(
  props: CreateAttributeRuleProps,
  metadata: EventMetadata,
  clock: Clock,
  // validatedEntities?: {
  //  channels?: ChannelReference[];
  // },
): Result<AttributeRuleAggregate, DomainError> {
  // Validate each property by creating value objects
  const codeResult = AttributeRuleCode.from(props.code);
  if (!codeResult.ok) {
    return err(
      withContext(codeResult.error, {
        ...codeResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_attribute_rule',
        code: props.code,
      }),
    );
  }

  const nameResult = AttributeRuleName.from(props.name);
  if (!nameResult.ok) {
    return err(
      withContext(nameResult.error, {
        ...nameResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_attribute_rule',
        name: props.name,
      }),
    );
  }

  const descriptionResult = AttributeRuleDescription.from(props.description);
  if (!descriptionResult.ok) {
    return err(
      withContext(descriptionResult.error, {
        ...descriptionResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_attribute_rule',
        description: props.description,
      }),
    );
  }

  const enabledResult = AttributeRuleEnabled.from(props.enabled);
  if (!enabledResult.ok) {
    return err(
      withContext(enabledResult.error, {
        ...enabledResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_attribute_rule',
        enabled: props.enabled,
      }),
    );
  }

  // Create attributes configuration using specialized factory
  const attributesResult = AttributeRuleAttributes.from(props.attributes);
  if (!attributesResult.ok) {
    return err(attributesResult.error);
  }

  const createdAtResult = AttributeRuleCreatedAt.create(clock.now());
  if (!createdAtResult.ok) {
    return err(createdAtResult.error);
  }

  const updatedAtResult = AttributeRuleUpdatedAt.create(clock.now());
  if (!updatedAtResult.ok) {
    return err(updatedAtResult.error);
  }

  const versionResult = AttributeRuleVersion.create(1); // Initial version is 1
  if (!versionResult.ok) {
    return err(versionResult.error);
  }

  // Create the entity properties with validated value objects
  const entityProps: AttributeRuleDomainState = {
    code: codeResult.value,
    name: nameResult.value,
    description: descriptionResult.value,
    enabled: enabledResult.value,
    attributes: attributesResult.value,
    createdAt: createdAtResult.value,
    updatedAt: updatedAtResult.value,
    version: versionResult.value,
  };

  // Create the entity using the constructor
  return AttributeRuleAggregate.create(entityProps, clock, metadata);
}
