// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DocumentBuilder, SwaggerModule } from '@nestjs/swagger';
import { INestApplication } from '@nestjs/common';
import { SwaggerConfigUtil } from 'src/docs/swagger-config.util';
import { LookupModule } from '../lookup.module';
/**
 * Lookup documentation endpoints interface
 */
export interface LookupDocumentationUrls {
  lookup: string;
}

/**
 * Lookup Documentation
 * This module handles the Swagger documentation for lookups
 *
 */
export class LookupDocumentation {
  static setup(app: INestApplication, port: string | number): void {
    const config = new DocumentBuilder()
      .addBearerAuth(
        { type: 'http', scheme: 'bearer', bearerFormat: 'JWT' },
        'bearer',
      )
      .setTitle('📱 Lookup Management API')
      .setDescription(
        `
## bounded context: core
[← Back to documentation](/api/docs/core)

### application: attributes
[← Back to documentation](/api/docs/core/attributes)
`,
      )
      .setVersion('1.0.0')
      .addTag('Lookups', `Configuration for lookup table`);

    // Add dynamic server configuration
    SwaggerConfigUtil.addServers(config, port);

    const document = SwaggerModule.createDocument(app, config.build(), {
      include: [LookupModule],
      extraModels: [],
    });

    SwaggerModule.setup('api/docs/core/attributes/lookup', app, document);
  }

  /**
   * Setup all lookup-related documentation endpoints
   * This method centralizes the setup of all lookup module documentation
   */
  static setupAll(
    app: INestApplication,
    port: string | number,
  ): LookupDocumentationUrls {
    // Setup main lookup documentation
    LookupDocumentation.setup(app, port);

    // Setup sub-module documentation

    // Return all endpoint URLs
    return {
      lookup: LookupDocumentation.getEndpoint(port),
    };
  }

  static getEndpoint(port: string | number): string {
    return `${SwaggerConfigUtil.getServerUrl(port)}/api/docs/core/attributes/lookup`;
  }
}
