// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { LookupPermission } from '../../domain/permissions/lookup.permissions';

/**
 * Field-level permission matrix for Lookup updates.
 * Maps lookup fields to the required permissions for modification.
 */
export class LookupFieldPermissionMatrix {
  private static readonly fieldPermissions: Record<string, LookupPermission[]> =
    {
      // Basic lookup information fields - require standard update permission
      name: [LookupPermission.DOMAIN_LOOKUP_UPDATE],
      description: [LookupPermission.DOMAIN_LOOKUP_UPDATE],
      category: [LookupPermission.DOMAIN_LOOKUP_UPDATE],

      // Status fields - may require special permissions or admin rights
      active: [
        LookupPermission.DOMAIN_LOOKUP_UPDATE,
        LookupPermission.DOMAIN_LOOKUP_ADMIN,
      ],
      status: [
        LookupPermission.DOMAIN_LOOKUP_UPDATE,
        LookupPermission.DOMAIN_LOOKUP_ADMIN,
      ],

      // Financial fields - typically require higher permissions
      fees: [LookupPermission.DOMAIN_LOOKUP_UPDATE],
      rates: [LookupPermission.DOMAIN_LOOKUP_UPDATE],
      pricing: [LookupPermission.DOMAIN_LOOKUP_UPDATE],

      // Configuration fields
      channelCodes: [LookupPermission.DOMAIN_LOOKUP_UPDATE],
      railCodes: [LookupPermission.DOMAIN_LOOKUP_UPDATE],
      configuration: [LookupPermission.DOMAIN_LOOKUP_UPDATE],

      // Administrative fields - require admin permissions
      tenant: [LookupPermission.DOMAIN_LOOKUP_ADMIN],
      auditInfo: [LookupPermission.DOMAIN_LOOKUP_ADMIN],
      metadata: [LookupPermission.DOMAIN_LOOKUP_UPDATE],
    };

  /**
   * Get required permissions for a specific field.
   * Returns empty array if field has no special permission requirements.
   */
  static getRequiredPermissions(fieldName: string): LookupPermission[] {
    return this.fieldPermissions[fieldName] || [];
  }

  /**
   * Check if a field has specific permission requirements.
   */
  static hasSpecialPermissions(fieldName: string): boolean {
    return fieldName in this.fieldPermissions;
  }

  /**
   * Get all fields that require a specific permission.
   */
  static getFieldsForPermission(permission: LookupPermission): string[] {
    return Object.entries(this.fieldPermissions)
      .filter(([, perms]) => perms.includes(permission))
      .map(([field]) => field);
  }

  /**
   * Get all defined field permissions.
   */
  static getAllFieldPermissions(): Record<string, LookupPermission[]> {
    return { ...this.fieldPermissions };
  }

  /**
   * Add or update permission requirements for a field.
   */
  static setFieldPermissions(
    fieldName: string,
    permissions: LookupPermission[],
  ): void {
    this.fieldPermissions[fieldName] = [...permissions];
  }
}
