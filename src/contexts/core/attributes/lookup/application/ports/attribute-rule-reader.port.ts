// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Result, DomainError } from 'src/shared/errors';
import { ActorContext } from 'src/shared/application/context';
import { RepositoryOptions } from 'src/shared/infrastructure/repositories';
import { Option } from 'src/shared/domain/types';
import { AttributeRuleReference } from './references';

export const ATTRIBUTE_RULE_REFERENCE_READER_TOKEN =
  'IAttributeRuleReader' as const;

/**
 * AttributeRule Reader Port (Bounded Context: Core)
 *
 * Handles attribute-rule reference data lookups for FK validation.
 * Clean ownership of attribute-rule-related operations with swappable implementations.
 */
export interface IAttributeRuleReader {
  /**
   * Find valid AttributeRule Codes for AttributeRule configuration
   * @param actor - The authenticated user context
   * @param options - Optional repository options
   * @returns Result containing available AttributeRule Codes
   */
  findValidAttributeRuleCodes(
    actor: ActorContext,
    options?: RepositoryOptions,
  ): Promise<Result<string[], DomainError>>;

  /**
   * Find AttributeRule configuration by AttributeRule code
   * @param actor - The authenticated user context
   * @param  - The   to lookup
   * @param options - Optional repository options
   * @returns Result containing AttributeRule configuration or null if not found
   */
  findAttributeRuleByCode(
    actor: ActorContext,
    code: string,
    options?: RepositoryOptions,
  ): Promise<Result<Option<AttributeRuleReference>, DomainError>>;
}
