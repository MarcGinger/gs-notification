// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Result, DomainError } from 'src/shared/errors';
import { ActorContext } from 'src/shared/application/context';
import { RepositoryOptions } from 'src/shared/infrastructure/repositories';
import { Option } from 'src/shared/domain/types';
import { AttributeRuleSetReference } from './references';

export const ATTRIBUTE_RULE_SET_REFERENCE_READER_TOKEN =
  'IAttributeRuleSetReader' as const;

/**
 * AttributeRuleSet Reader Port (Bounded Context: Core)
 *
 * Handles attribute-rule-set reference data lookups for FK validation.
 * Clean ownership of attribute-rule-set-related operations with swappable implementations.
 */
export interface IAttributeRuleSetReader {
  /**
   * Find valid AttributeRuleSet Codes for AttributeRuleSet configuration
   * @param actor - The authenticated user context
   * @param options - Optional repository options
   * @returns Result containing available AttributeRuleSet Codes
   */
  findValidAttributeRuleSetCodes(
    actor: ActorContext,
    options?: RepositoryOptions,
  ): Promise<Result<string[], DomainError>>;

  /**
   * Find AttributeRuleSet configuration by AttributeRuleSet code
   * @param actor - The authenticated user context
   * @param  - The   to lookup
   * @param options - Optional repository options
   * @returns Result containing AttributeRuleSet configuration or null if not found
   */
  findAttributeRuleSetByCode(
    actor: ActorContext,
    code: string,
    options?: RepositoryOptions,
  ): Promise<Result<Option<AttributeRuleSetReference>, DomainError>>;
}
