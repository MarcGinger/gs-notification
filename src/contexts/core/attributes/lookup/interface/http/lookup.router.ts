// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Module } from '@nestjs/common';
import { RouterModule, Routes } from '@nestjs/core';
import { LookupModule } from '../../lookup.module';
import { LookupProjectorModule } from '../../lookup-projector.module';
/**
 * Lookup Router Configuration
 *
 * Configures nested routing for lookup endpoints to follow
 * the module monolith structure: /core/attributes/api/v1/lookups
 *
 * This replaces flat routing (/api/v1/lookups) with contextual routing
 * that reflects the bounded context hierarchy.
 */

// Define the routing structure for the lookup bounded context
const lookupRoutes: Routes = [
  {
    path: 'core',
    children: [
      {
        path: 'attributes',
        children: [
          {
            path: 'lookups',
            module: LookupModule,
          },
          {
            path: 'health',
            module: LookupProjectorModule,
          },
        ],
      },
    ],
  },
];

/**
 * Lookup Router Module
 *
 * Provides nested routing configuration for lookup endpoints.
 * This module should be imported in the main app module to enable
 * the nested route structure.
 *
 * Routes configured:
 * - /core/attributes/api/v1/lookups/:code (GET, PUT, DELETE)
 * - /core/attributes/health/projectors/lookup (GET)
 * - /core/attributes/health/projectors/lookup/metrics (GET)
 */
@Module({
  imports: [
    LookupModule,
    LookupProjectorModule,
    RouterModule.register(lookupRoutes),
  ],
  exports: [LookupModule, LookupProjectorModule],
})
export class LookupRouterModule {}
