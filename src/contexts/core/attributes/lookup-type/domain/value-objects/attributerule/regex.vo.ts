// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { AttributeruleErrors } from '../../errors/attributerule.errors';

/**
 * Regex Value Object
 * Represents a validated regex with business rules
 */
export const AttributeruleRegex = createStringVO({
  name: 'Regex',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 120,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(AttributeruleErrors.INVALID_REGEX, 'Regex'),
});

/**
 * Public instance type for Regex
 */
export type AttributeruleRegex = StringVOInstance;

// Convenience creators
export const createAttributeruleRegex = (s: string) =>
  AttributeruleRegex.create(s);
export const attributeruleRegexFrom = (v: unknown) =>
  AttributeruleRegex.from(v);
