// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { QueryHandler, IQueryHandler } from '@nestjs/cqrs';
import { Injectable } from '@nestjs/common';
import { DomainError, Result } from 'src/shared/errors';
import { ListLookupTypeQuery } from '../queries';
import { ListLookupTypeUseCase } from '../use-cases';
import { LookupTypePageResponse } from '../dtos';

/**
 * ✅ Enhanced CQRS Query Handler with use case delegation
 * Follows the pattern: Handler → Use Case → Repository
 */
@Injectable()
@QueryHandler(ListLookupTypeQuery)
export class ListLookupTypeHandler
  implements IQueryHandler<ListLookupTypeQuery>
{
  constructor(private readonly listLookupTypeUseCase: ListLookupTypeUseCase) {}

  /**
   * Handle the list lookupType query by delegating to the use case
   */
  async execute(
    query: ListLookupTypeQuery,
  ): Promise<Result<LookupTypePageResponse, DomainError>> {
    // ✅ Delegate to use case for business logic
    return await this.listLookupTypeUseCase.execute({
      user: query.user,
      filter: query.filter,
      correlationId: query.correlationId || 'unknown',
    });
  }
}
