// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { CommandHandler, ICommandHandler } from '@nestjs/cqrs';
import { Inject } from '@nestjs/common';
import { Result, DomainError } from 'src/shared/errors';
import { DeleteLookupTypeUseCase } from '../use-cases';
import { DeleteLookupTypeCommand } from '../commands';

@CommandHandler(DeleteLookupTypeCommand)
export class DeleteLookupTypeHandler
  implements ICommandHandler<DeleteLookupTypeCommand>
{
  constructor(
    @Inject(DeleteLookupTypeUseCase)
    private readonly deleteLookupTypeUseCase: DeleteLookupTypeUseCase,
  ) {}

  async execute(
    command: DeleteLookupTypeCommand,
  ): Promise<Result<void, DomainError>> {
    return await this.deleteLookupTypeUseCase.execute({
      user: command.user,
      code: command.code,
      correlationId: command.correlationId,
      authorizationReason: 'CQRS Command Handler',
    });
  }
}
