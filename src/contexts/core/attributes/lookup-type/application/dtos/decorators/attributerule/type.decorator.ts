// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { applyDecorators } from '@nestjs/common';
import { ApiProperty } from '@nestjs/swagger';
import { IsEnum, IsOptional, IsNotEmpty } from 'class-validator';
import {
  AttributeruleTypeValues,
  AttributeruleTypeValue,
} from '../../../../domain/value-objects';

export { AttributeruleTypeValues, AttributeruleTypeValue };
/**
 * Options for property decorators (enum)
 */
interface PropOptions {
  required?: boolean;
}

/**
 * Property decorator for Attributerule Type with required option
 * @param {Object} options - Options for the decorator
 * @returns {PropertyDecorator}
 */
export function ApiAttributeruleType(options: PropOptions = {}) {
  const { required = true } = options;
  return applyDecorators(
    ApiProperty({
      description: `Data type classification for the attribute field. Determines validation logic, storage format, and UI input controls to be used for this attribute. Values include string, boolean, integer, currency, date, datetime, other.`,
      example: `string`,
      type: String,
      enum: AttributeruleTypeValues,
      required,
    }),
    IsEnum(AttributeruleTypeValues),
    required ? IsNotEmpty() : IsOptional(),
  );
}
