// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { LookupTypePermission } from '../../domain/permissions/lookup-type.permissions';

/**
 * Field-level permission matrix for LookupType updates.
 * Maps lookupType fields to the required permissions for modification.
 */
export class LookupTypeFieldPermissionMatrix {
  private static readonly fieldPermissions: Record<
    string,
    LookupTypePermission[]
  > = {
    // Basic lookupType information fields - require standard update permission
    name: [LookupTypePermission.DOMAIN_LOOKUP_TYPE_UPDATE],
    description: [LookupTypePermission.DOMAIN_LOOKUP_TYPE_UPDATE],
    category: [LookupTypePermission.DOMAIN_LOOKUP_TYPE_UPDATE],

    // Status fields - may require special permissions or admin rights
    active: [
      LookupTypePermission.DOMAIN_LOOKUP_TYPE_UPDATE,
      LookupTypePermission.DOMAIN_LOOKUP_TYPE_ADMIN,
    ],
    status: [
      LookupTypePermission.DOMAIN_LOOKUP_TYPE_UPDATE,
      LookupTypePermission.DOMAIN_LOOKUP_TYPE_ADMIN,
    ],

    // Financial fields - typically require higher permissions
    fees: [LookupTypePermission.DOMAIN_LOOKUP_TYPE_UPDATE],
    rates: [LookupTypePermission.DOMAIN_LOOKUP_TYPE_UPDATE],
    pricing: [LookupTypePermission.DOMAIN_LOOKUP_TYPE_UPDATE],

    // Configuration fields
    channelCodes: [LookupTypePermission.DOMAIN_LOOKUP_TYPE_UPDATE],
    railCodes: [LookupTypePermission.DOMAIN_LOOKUP_TYPE_UPDATE],
    configuration: [LookupTypePermission.DOMAIN_LOOKUP_TYPE_UPDATE],

    // Administrative fields - require admin permissions
    tenant: [LookupTypePermission.DOMAIN_LOOKUP_TYPE_ADMIN],
    auditInfo: [LookupTypePermission.DOMAIN_LOOKUP_TYPE_ADMIN],
    metadata: [LookupTypePermission.DOMAIN_LOOKUP_TYPE_UPDATE],
  };

  /**
   * Get required permissions for a specific field.
   * Returns empty array if field has no special permission requirements.
   */
  static getRequiredPermissions(fieldName: string): LookupTypePermission[] {
    return this.fieldPermissions[fieldName] || [];
  }

  /**
   * Check if a field has specific permission requirements.
   */
  static hasSpecialPermissions(fieldName: string): boolean {
    return fieldName in this.fieldPermissions;
  }

  /**
   * Get all fields that require a specific permission.
   */
  static getFieldsForPermission(permission: LookupTypePermission): string[] {
    return Object.entries(this.fieldPermissions)
      .filter(([, perms]) => perms.includes(permission))
      .map(([field]) => field);
  }

  /**
   * Get all defined field permissions.
   */
  static getAllFieldPermissions(): Record<string, LookupTypePermission[]> {
    return { ...this.fieldPermissions };
  }

  /**
   * Add or update permission requirements for a field.
   */
  static setFieldPermissions(
    fieldName: string,
    permissions: LookupTypePermission[],
  ): void {
    this.fieldPermissions[fieldName] = [...permissions];
  }
}
