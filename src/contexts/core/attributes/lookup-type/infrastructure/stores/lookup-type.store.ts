// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

/**
 * Simple Generic Store
 *
 * Direct, no-nonsense storage for any projections.
 * Used by both projectors (write) and repositories (read).
 */

import { BaseProjection, SimpleStore } from 'src/shared/infrastructure/stores';
import { DetailLookupTypeResponse } from '../../application/dtos';

/**
 * LookupType projection data
 */
export interface LookupTypeProjection
  extends DetailLookupTypeResponse,
    BaseProjection {
  lastStreamRevision: string;
}

/**
 * LookupType-specific store instance - shared across projectors and repositories
 */
export const lookupTypeStore = new SimpleStore<LookupTypeProjection>(
  5000,
  'lookupType',
);
