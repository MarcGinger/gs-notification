// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DocumentBuilder, SwaggerModule } from '@nestjs/swagger';
import { INestApplication } from '@nestjs/common';
import { SwaggerConfigUtil } from 'src/docs/swagger-config.util';
import { AttributeRuleSetModule } from '../attribute-rule-set.module';
import {
  DetailAttributeRuleResponse,
  CreateAttributeRuleRequest,
  UpdateAttributeRuleRequest,
} from '../application/dtos';
/**
 * AttributeRuleSet documentation endpoints interface
 */
export interface AttributeRuleSetDocumentationUrls {
  attributeRuleSet: string;
}

/**
 * AttributeRuleSet Documentation
 * This module handles the Swagger documentation for attribute_rule_sets
 *
 */
export class AttributeRuleSetDocumentation {
  static setup(app: INestApplication, port: string | number): void {
    const config = new DocumentBuilder()
      .addBearerAuth(
        { type: 'http', scheme: 'bearer', bearerFormat: 'JWT' },
        'bearer',
      )
      .setTitle('📱 AttributeRuleSet Management API')
      .setDescription(
        `
## bounded context: core
[← Back to documentation](/api/docs/core)

### application: attributes
[← Back to documentation](/api/docs/core/attributes)
`,
      )
      .setVersion('1.0.0')
      .addTag(
        'AttributeRuleSets',
        `Configuration for attribute_rule_set table`,
      );

    // Add dynamic server configuration
    SwaggerConfigUtil.addServers(config, port);

    const document = SwaggerModule.createDocument(app, config.build(), {
      include: [AttributeRuleSetModule],
      extraModels: [
        DetailAttributeRuleResponse,
        CreateAttributeRuleRequest,
        UpdateAttributeRuleRequest,
      ],
    });

    SwaggerModule.setup(
      'api/docs/core/attributes/attribute-rule-set',
      app,
      document,
    );
  }

  /**
   * Setup all attributeRuleSet-related documentation endpoints
   * This method centralizes the setup of all attributeRuleSet module documentation
   */
  static setupAll(
    app: INestApplication,
    port: string | number,
  ): AttributeRuleSetDocumentationUrls {
    // Setup main attributeRuleSet documentation
    AttributeRuleSetDocumentation.setup(app, port);

    // Setup sub-module documentation

    // Return all endpoint URLs
    return {
      attributeRuleSet: AttributeRuleSetDocumentation.getEndpoint(port),
    };
  }

  static getEndpoint(port: string | number): string {
    return `${SwaggerConfigUtil.getServerUrl(port)}/api/docs/core/attributes/attribute-rule-set`;
  }
}
