// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Module } from '@nestjs/common';
import { RouterModule, Routes } from '@nestjs/core';
import { AttributeRuleSetModule } from '../../attribute-rule-set.module';
import { AttributeRuleSetProjectorModule } from '../../attribute-rule-set-projector.module';
/**
 * Attribute rule set Router Configuration
 *
 * Configures nested routing for attribute-rule-set endpoints to follow
 * the module monolith structure: /core/attributes/api/v1/attribute-rule-sets
 *
 * This replaces flat routing (/api/v1/attribute-rule-sets) with contextual routing
 * that reflects the bounded context hierarchy.
 */

// Define the routing structure for the attribute-rule-set bounded context
const attributeRuleSetRoutes: Routes = [
  {
    path: 'core',
    children: [
      {
        path: 'attributes',
        children: [
          {
            path: 'attribute-rule-sets',
            module: AttributeRuleSetModule,
          },
          {
            path: 'health',
            module: AttributeRuleSetProjectorModule,
          },
        ],
      },
    ],
  },
];

/**
 * Attribute rule set Router Module
 *
 * Provides nested routing configuration for attribute-rule-set endpoints.
 * This module should be imported in the main app module to enable
 * the nested route structure.
 *
 * Routes configured:
 * - /core/attributes/api/v1/attribute-rule-sets (GET, POST)
 * - /core/attributes/api/v1/attribute-rule-sets/:code (GET, PUT, DELETE)
 * - /core/attributes/health/projectors/attributeRuleSet (GET)
 * - /core/attributes/health/projectors/attribute-rule-set/metrics (GET)
 */
@Module({
  imports: [
    AttributeRuleSetModule,
    AttributeRuleSetProjectorModule,
    RouterModule.register(attributeRuleSetRoutes),
  ],
  exports: [AttributeRuleSetModule, AttributeRuleSetProjectorModule],
})
export class AttributeRuleSetRouterModule {}
