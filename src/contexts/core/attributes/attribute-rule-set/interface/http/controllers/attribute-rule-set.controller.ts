// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  Controller,
  Get,
  Query,
  Post,
  Body,
  Put,
  HttpStatus,
  HttpCode,
  Param,
  UseInterceptors,
  UseGuards,
} from '@nestjs/common';
import {
  ApiTags,
  ApiOperation,
  ApiResponse,
  ApiCreatedResponse,
  ApiBody,
  ApiHeader,
  ApiParam,
  ApiOkResponse,
  ApiBearerAuth,
} from '@nestjs/swagger';
import { IdempotencyKey } from 'src/shared/interfaces/decorators';
import { IdempotencyKeyPipe } from 'src/shared/interfaces/pipes';
import { AttributeRuleSetApplicationService } from '../../../application/services';
import { CurrentUser, IUserToken, JwtAuthGuard } from 'src/shared/security';
import {
  DetailAttributeRuleSetResponse,
  CreateAttributeRuleSetRequest,
  UpdateAttributeRuleSetRequest,
  AttributeRuleSetPageResponse,
  ListAttributeRuleSetFilterRequest,
  ListAttributeRuleSetResponse,
} from '../../../application/dtos';
import { Result, ResultInterceptor, DomainError } from 'src/shared/errors';
import { PaginatedResponse } from 'src/shared/application/dtos';
import {
  AttributeRuleSetReadResource,
  AttributeRuleSetCreateResource,
  AttributeRuleSetUpdateResource,
} from '../../attribute-rule-set.resource';
import { ApiCommonErrors } from 'src/shared/interfaces/http';

@Controller()
@ApiTags('AttributeRuleSets')
@ApiBearerAuth()
@UseGuards(JwtAuthGuard)
@UseInterceptors(ResultInterceptor)
export class AttributeRuleSetController {
  constructor(
    private readonly attributeRuleSetApplicationService: AttributeRuleSetApplicationService,
  ) {}

  @Get()
  @AttributeRuleSetReadResource()
  @ApiOperation({
    summary: 'List Attribute rule sets',
    description:
      'Retrieves a list of Attribute rule sets with optional filtering. Requires READ permission (LOW risk).',
  })
  @ApiOkResponse({
    description: 'List of Attribute rule sets retrieved successfully',
    type: AttributeRuleSetPageResponse,
  })
  @ApiCommonErrors()
  async list(
    @CurrentUser() user: IUserToken,
    @Query() pageRequest?: ListAttributeRuleSetFilterRequest,
  ): Promise<
    Result<PaginatedResponse<ListAttributeRuleSetResponse>, DomainError>
  > {
    const result =
      await this.attributeRuleSetApplicationService.listAttributeRuleSets(
        user,
        pageRequest,
      );
    return result;
  }

  @Get(':code')
  @AttributeRuleSetReadResource()
  @ApiOperation({
    summary: 'Get Attribute rule set by code',
    description:
      'Retrieves a single Attribute rule set by its unique identifier. Requires READ permission (LOW risk).',
  })
  @ApiParam({
    name: 'code',
    type: 'string',
    description: 'AttributeRuleSet unique identifier',
    example: 'COUNTRY',
  })
  @ApiOkResponse({
    description: 'AttributeRuleSet details retrieved successfully',
    type: DetailAttributeRuleSetResponse,
  })
  @ApiCommonErrors()
  async get(
    @CurrentUser() user: IUserToken,
    @Param('code') code: string,
  ): Promise<Result<DetailAttributeRuleSetResponse, DomainError>> {
    const result =
      await this.attributeRuleSetApplicationService.getAttributeRuleSetById(
        user,
        code,
      );

    return result;
  }
  // ========================================
  // Core CRUD Operations
  // ========================================

  @Post()
  @AttributeRuleSetCreateResource()
  @HttpCode(HttpStatus.CREATED)
  @ApiOperation({
    summary: 'Create a new Attribute rule set',
    description:
      'Creates a new Attribute rule set in the catalog. Requires CREATE permission (MEDIUM risk).',
  })
  @ApiHeader({
    name: 'Idempotency-Key',
    required: false,
    description:
      'Ensures the request is processed only once. Accepts both Idempotency-Key and x-idempotency-key headers.',
  })
  @ApiCreatedResponse({
    type: DetailAttributeRuleSetResponse,
    description: 'AttributeRuleSet successfully created',
    headers: {
      Location: {
        description: 'URI of the newly created resource',
        schema: {
          type: 'string',
          example: '/attribute-rule-sets/123e4567-e89b-12d3-a456-426614174000',
        },
      },
    },
  })
  @ApiCommonErrors()
  @ApiBody({ type: CreateAttributeRuleSetRequest })
  async create(
    @CurrentUser() user: IUserToken,
    @Body() createAttributeRuleSetRequest: CreateAttributeRuleSetRequest,
    @IdempotencyKey(new IdempotencyKeyPipe())
    idempotencyKey: string | undefined,
  ): Promise<Result<DetailAttributeRuleSetResponse, DomainError>> {
    // idempotencyKey is validated by the IdempotencyKeyPipe

    // Call service with options including idempotency key
    const result =
      await this.attributeRuleSetApplicationService.createAttributeRuleSet(
        user,
        createAttributeRuleSetRequest,
        {
          idempotencyKey,
        },
      );

    return result;
  }

  @Put(':code')
  @AttributeRuleSetUpdateResource()
  @ApiOperation({
    summary: 'Update an Attribute rule set',
    description:
      'Updates an existing Attribute rule set with new data. Supports partial updates. Requires UPDATE permission (MEDIUM risk).',
  })
  @ApiParam({
    name: 'code',
    type: 'string',
    description: 'AttributeRuleSet unique identifier',
    example: 'COUNTRY',
  })
  @ApiResponse({
    status: HttpStatus.OK,
    description: 'AttributeRuleSet updated successfully',
    type: DetailAttributeRuleSetResponse,
  })
  @ApiCommonErrors({
    include422: true,
    extra: [
      { status: 429, description: 'Too Many Requests' },
      { status: 503, description: 'Upstream dependency unavailable' },
    ],
  })
  @ApiBody({
    type: UpdateAttributeRuleSetRequest,
    description:
      'AttributeRuleSet update data. Only provided fields will be updated.',
  })
  async update(
    @CurrentUser() user: IUserToken,
    @Param('code') code: string,
    @Body() updateAttributeRuleSetRequest: UpdateAttributeRuleSetRequest,
  ): Promise<Result<DetailAttributeRuleSetResponse, DomainError>> {
    const result =
      await this.attributeRuleSetApplicationService.updateAttributeRuleSet(
        user,
        code,
        updateAttributeRuleSetRequest,
      );

    return result;
  }
}
