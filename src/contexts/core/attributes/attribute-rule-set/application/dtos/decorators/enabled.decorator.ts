// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { applyDecorators } from '@nestjs/common';
import { ApiProperty } from '@nestjs/swagger';
import { IsBoolean, IsOptional, IsNotEmpty } from 'class-validator';

/**
 * Options for property decorators (boolean)
 */
interface PropOptions {
  required?: boolean;
}

/**
 * Property decorator for AttributeRuleSet Enabled
 * @param {Object} options - Options for the decorator
 * @returns {PropertyDecorator}
 */
export function ApiAttributeRuleSetEnabled(options: PropOptions = {}) {
  const { required = false } = options;

  return applyDecorators(
    ApiProperty({
      description: `Controls whether this attribute rule set is active and available for use. When disabled, the rule set and its associated attribute rules are hidden from validation interfaces.`,
      example: true,
      type: Boolean,
      required,
    }),
    IsBoolean(),
    required ? IsNotEmpty() : IsOptional(),
  );
}
