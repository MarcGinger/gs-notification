// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

/**
 * Abstract Use Case Contracts for AttributeRuleSet Operations
 *
 * These abstract classes are used instead of interfaces to work with NestJS DI.
 * TypeScript interfaces are erased at runtime and cannot be injected.
 */

import { Result, DomainError } from 'src/shared/errors';
import type { IUserToken } from 'src/shared/security';
import type { UpsertAttributeRuleSetProps } from '../../domain/props';
import type {
  DetailAttributeRuleSetResponse,
  AttributeRuleSetPageResponse,
  ListAttributeRuleSetFilterRequest,
} from '../dtos';

export abstract class IUpsertAttributeRuleSetUseCase {
  abstract execute(params: {
    user: IUserToken;
    code: string;
    props: UpsertAttributeRuleSetProps;
    correlationId: string;
    authorizationReason: string;
  }): Promise<Result<DetailAttributeRuleSetResponse, DomainError>>;
}

export abstract class IDeleteAttributeRuleSetUseCase {
  abstract execute(params: {
    user: IUserToken;
    code: string;
    correlationId: string;
    authorizationReason: string;
  }): Promise<Result<void, DomainError>>;
}

export abstract class IGetAttributeRuleSetUseCase {
  abstract execute(params: {
    user: IUserToken;
    code: string;
    correlationId: string;
  }): Promise<Result<DetailAttributeRuleSetResponse, DomainError>>;
}
export abstract class IListAttributeRuleSetUseCase {
  abstract execute(params: {
    user: IUserToken;
    filter?: ListAttributeRuleSetFilterRequest;
    correlationId: string;
  }): Promise<Result<AttributeRuleSetPageResponse, DomainError>>;
}
