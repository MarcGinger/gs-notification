// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { AttributeRuleSetSnapshotProps } from '../../domain/props';
import { DetailAttributeRuleSetResponse } from '../dtos';
import { AttributeRuleSetAttributeRuleConfiguration } from '../../domain/value-objects';
import { extractAttributeRuleConfigurationData } from '../../domain/utilities';

/**
 * AttributeRuleSet Snapshot to DTO Mappers
 * Reusable mapping functions for converting domain snapshots to DTOs
 */

/**
 * Map attributeRuleSet snapshot to DTO response
 */
export const mapAttributeRuleSetSnapshotToDto = (
  snapshot: AttributeRuleSetSnapshotProps,
): DetailAttributeRuleSetResponse => {
  return {
    code: snapshot.code,
    name: snapshot.name,
    description: snapshot.description,
    enabled: snapshot.enabled,
    attributes: snapshot.attributes,
  };
};

/**
 * Map attributes configuration to DTO
 *
 * This application-layer function uses domain extraction and transforms
 * the data to match the required DTO structure without business defaults.
 */

export const mapAttributeRuleConfigurationToDto = <T>(
  attributeRuleConfiguration: AttributeRuleSetAttributeRuleConfiguration,
): T => extractAttributeRuleConfigurationData<T>(attributeRuleConfiguration);
