// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { CommandHandler, ICommandHandler } from '@nestjs/cqrs';
import { Inject } from '@nestjs/common';
import { Result, DomainError } from 'src/shared/errors';
import { DeleteAttributeRuleSetUseCase } from '../use-cases';
import { DeleteAttributeRuleSetCommand } from '../commands';

@CommandHandler(DeleteAttributeRuleSetCommand)
export class DeleteAttributeRuleSetHandler
  implements ICommandHandler<DeleteAttributeRuleSetCommand>
{
  constructor(
    @Inject(DeleteAttributeRuleSetUseCase)
    private readonly deleteAttributeRuleSetUseCase: DeleteAttributeRuleSetUseCase,
  ) {}

  async execute(
    command: DeleteAttributeRuleSetCommand,
  ): Promise<Result<void, DomainError>> {
    return await this.deleteAttributeRuleSetUseCase.execute({
      user: command.user,
      code: command.code,
      correlationId: command.correlationId,
      authorizationReason: 'CQRS Command Handler',
    });
  }
}
