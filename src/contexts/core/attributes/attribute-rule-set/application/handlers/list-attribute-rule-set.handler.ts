// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { QueryHandler, IQueryHandler } from '@nestjs/cqrs';
import { Injectable } from '@nestjs/common';
import { DomainError, Result } from 'src/shared/errors';
import { ListAttributeRuleSetQuery } from '../queries';
import { ListAttributeRuleSetUseCase } from '../use-cases';
import { AttributeRuleSetPageResponse } from '../dtos';

/**
 * ✅ Enhanced CQRS Query Handler with use case delegation
 * Follows the pattern: Handler → Use Case → Repository
 */
@Injectable()
@QueryHandler(ListAttributeRuleSetQuery)
export class ListAttributeRuleSetHandler
  implements IQueryHandler<ListAttributeRuleSetQuery>
{
  constructor(
    private readonly listAttributeRuleSetUseCase: ListAttributeRuleSetUseCase,
  ) {}

  /**
   * Handle the list attributeRuleSet query by delegating to the use case
   */
  async execute(
    query: ListAttributeRuleSetQuery,
  ): Promise<Result<AttributeRuleSetPageResponse, DomainError>> {
    // ✅ Delegate to use case for business logic
    return await this.listAttributeRuleSetUseCase.execute({
      user: query.user,
      filter: query.filter,
      correlationId: query.correlationId || 'unknown',
    });
  }
}
