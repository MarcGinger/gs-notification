// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Result, DomainError } from 'src/shared/errors';
import { RepositoryOptions } from 'src/shared/infrastructure/repositories';
import { ActorContext } from 'src/shared/application/context';
import { Option } from 'src/shared/domain/types';
import { AttributeRuleSetSnapshotProps } from '../../domain/props';
import { AttributeRuleSetCode } from '../../domain/value-objects';

/**
 * Token for injecting IAttributeRuleSetReader port implementation
 */
export const ATTRIBUTE_RULE_SET_READER_TOKEN =
  'IAttributeRuleSetReader' as const;

/**
 * AttributeRuleSet Reader Port - Interface Segregation Principle Implementation
 *
 * Handles basic read operations for AttributeRuleSet aggregates.
 * This interface focuses solely on simple retrieval operations.
 *
 * ISP Benefits:
 * - Clients only depend on methods they actually use
 * - Easier to mock and test specific read scenarios
 * - Can be implemented independently from write operations
 * - Supports read-only implementations (e.g., reporting services)
 *
 * @domain Core Context - AttributeRuleSet Reader Port
 * @layer Application Ports
 * @pattern Interface Segregation Principle
 */
export interface IAttributeRuleSetReader {
  /**
   * Find an AttributeRuleSet by its unique identifier
   * @param actor - The authenticated user context
   * @param code - The unique identifier of the AttributeRuleSet
   * @param options - Optional repository options
   * @returns Result containing the AttributeRuleSet snapshot or null if not found
   */
  findById(
    actor: ActorContext,
    code: AttributeRuleSetCode,
    options?: RepositoryOptions,
  ): Promise<Result<Option<AttributeRuleSetSnapshotProps>, DomainError>>;

  /**
   * Check if an attributeRuleSet exists by code (for write-path validation)
   * @param actor - The authenticated user context
   * @param code - The unique identifier of the AttributeRuleSet
   * @param options - Optional repository options
   * @returns Result containing boolean indicating existence
   */
  exists(
    actor: ActorContext,
    code: AttributeRuleSetCode,
    options?: RepositoryOptions,
  ): Promise<Result<boolean, DomainError>>;

  /**
   * Get attributeRuleSet version for optimistic concurrency control
   * @param actor - The authenticated user context
   * @param code - The unique identifier of the AttributeRuleSet
   * @param options - Optional repository options
   * @returns Result containing version number or null if not found
   */
  getVersion(
    actor: ActorContext,
    code: AttributeRuleSetCode,
    options?: RepositoryOptions,
  ): Promise<Result<Option<number>, DomainError>>;

  /**
   * Get minimal attributeRuleSet data for write-path operations
   * @param actor - The authenticated user context
   * @param code - The unique identifier of the AttributeRuleSet
   * @param options - Optional repository options
   * @returns Result containing minimal attributeRuleSet data or null if not found
   */
  getMinimal(
    actor: ActorContext,
    code: AttributeRuleSetCode,
    options?: RepositoryOptions,
  ): Promise<Result<Option<{ code: string; version: number }>, DomainError>>;
}
