// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Result, DomainError } from 'src/shared/errors';
import { ActorContext } from 'src/shared/application/context';
import { SaveReceipt } from 'src/shared/infrastructure/repositories';
import { AttributeRuleSetAggregate } from '../../domain/aggregates';
import { AttributeRuleSetCode } from '../../domain/value-objects';

/**
 * Token for injecting IAttributeRuleSetWriter port implementation
 */
export const ATTRIBUTE_RULE_SET_WRITER_TOKEN =
  'IAttributeRuleSetWriter' as const;

/**
 * AttributeRuleSet Writer Port - Interface Segregation Principle Implementation
 *
 * Handles write operations (create, update, delete) for AttributeRuleSet aggregates.
 * This interface focuses solely on data modification operations.
 *
 * ISP Benefits:
 * - Clients that only need to write data don't depend on read methods
 * - Easier to implement write-only scenarios (e.g., event sourcing)
 * - Can be secured separately from read operations
 * - Supports command-only patterns in CQRS architectures
 *
 * @domain Core Context - AttributeRuleSet Writer Port
 * @layer Application Ports
 * @pattern Interface Segregation Principle
 */
export interface IAttributeRuleSetWriter {
  /**
   * Save a AttributeRuleSet aggregate (create or update)
   * @param user - The authenticated user context
   * @param attributeRuleSet - The AttributeRuleSet aggregate to save
   * @param expectedVersion - Optional version for optimistic locking
   * @returns Result indicating success or domain error
   */
  save(
    actor: ActorContext,
    attributeRuleSet: AttributeRuleSetAggregate,
    expectedVersion?: number,
  ): Promise<Result<SaveReceipt, DomainError>>;

  /**
   * Delete a AttributeRuleSet by its unique identifier
   * @param user - The authenticated user context
   * @param code - The unique identifier of the AttributeRuleSet to delete
   * @returns Result indicating success or domain error
   */
  delete(
    actor: ActorContext,
    code: AttributeRuleSetCode,
  ): Promise<Result<SaveReceipt, DomainError>>;
}
