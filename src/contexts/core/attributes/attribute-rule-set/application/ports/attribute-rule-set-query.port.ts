// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Result, DomainError } from 'src/shared/errors';
import { RepositoryOptions } from 'src/shared/infrastructure/repositories';
import { ActorContext } from 'src/shared/application/context';
import { Option } from 'src/shared/domain/types';
import {
  AttributeRuleSetPageResponse,
  ListAttributeRuleSetFilterRequest,
  DetailAttributeRuleSetResponse,
} from '../dtos';

/**
 * Token for injecting IAttributeRuleSetQuery port implementation
 */
export const ATTRIBUTE_RULE_SET_QUERY_TOKEN = 'IAttributeRuleSetQuery' as const;

/**
 * AttributeRuleSet Query Port - Interface Segregation Principle Implementation
 *
 * Handles complex query operations including pagination, filtering, and search.
 * This interface focuses solely on advanced data retrieval operations.
 *
 * ISP Benefits:
 * - Clients that need simple reads don't depend on complex query methods
 * - Can be optimized independently (e.g., using read replicas)
 * - Easier to implement caching strategies for query operations
 * - Supports analytical and reporting use cases
 *
 * @domain Core Context - AttributeRuleSet Query Port
 * @layer Application Ports
 * @pattern Interface Segregation Principle
 */
export interface IAttributeRuleSetQuery {
  /**
   * Find a single AttributeRuleSet by its unique identifier (for CQRS query operations)
   * @param actor - The actor context containing authentication and request metadata
   * @param code - The unique identifier of the AttributeRuleSet
   * @param options - Optional repository options
   * @returns A promise resolving to a Result containing the AttributeRuleSet or null if not found
   */
  findById(
    actor: ActorContext,
    code: string,
    options?: RepositoryOptions,
  ): Promise<Result<Option<DetailAttributeRuleSetResponse>, DomainError>>;

  /**
   * Find AttributeRuleSet records with pagination and filtering.
   * @param actor - The actor context containing authentication and request metadata.
   * @param filter - Optional filter criteria for the product search.
   * @param options - Optional repository options (e.g., pagination, sorting).
   * @returns A promise resolving to a Result containing paginated AttributeRuleSet responses or a DomainError.
   */
  findPaginated(
    actor: ActorContext,
    filter?: ListAttributeRuleSetFilterRequest,
    options?: RepositoryOptions,
  ): Promise<Result<AttributeRuleSetPageResponse, DomainError>>;
}
