// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { AttributeRuleSetDomainState } from '../../domain/state';
import {
  DetailAttributeRuleResponse,
  DetailAttributeRuleSetResponse,
  ListAttributeRuleSetResponse,
} from '../dtos';
import { mapAttributeRuleConfigurationToDto } from '../mappers';
import { AttributeRuleSetAttributeRuleConfiguration } from '../../domain/value-objects';

/**
 * AttributeRuleSet DTO Assembler
 *
 * Converts AttributeRuleSetDomainState (pure VOs) to transport DTOs.
 * Bridges the gap between rich domain objects and API response format.
 * Uses existing generated DTOs and follows established patterns.
 */

/**
 * Assembles DTOs from AttributeRuleSetDomainState for API transport
 */
export class AttributeRuleSetDtoAssembler {
  /**
   * Convert AttributeRuleSetDomainState to detailed response DTO
   *
   * @param domainState - Rich domain state with VOs
   * @returns Clean DTO for API response
   */
  static toDetailResponse(
    domainState: AttributeRuleSetDomainState,
  ): DetailAttributeRuleSetResponse {
    const dto = new DetailAttributeRuleSetResponse();

    // Extract primitive values from VOs
    dto.code = domainState.code.value;
    dto.name = domainState.name.value;
    dto.description = domainState.description?.value;
    dto.enabled = domainState.enabled?.value;

    // Use existing mappers for complex configurations
    dto.attributes = domainState.attributes
      .toArray()
      .map((item) =>
        mapAttributeRuleConfigurationToDto<DetailAttributeRuleResponse>(
          item as AttributeRuleSetAttributeRuleConfiguration,
        ),
      );

    return dto;
  }

  /**
   * Convert AttributeRuleSetDomainState to list response DTO
   *
   * @param domainState - Rich domain state with VOs
   * @returns Clean DTO for list API response
   */
  static toListResponse(
    domainState: AttributeRuleSetDomainState,
  ): ListAttributeRuleSetResponse {
    const dto = new ListAttributeRuleSetResponse();

    // Extract primitive values from VOs
    dto.code = domainState.code.value;
    dto.name = domainState.name.value;
    dto.description = domainState.description?.value;
    dto.enabled = domainState.enabled?.value;

    // Use existing mappers for complex configurations
    dto.attributes = domainState.attributes
      .toArray()
      .map((item) =>
        mapAttributeRuleConfigurationToDto<DetailAttributeRuleResponse>(
          item as AttributeRuleSetAttributeRuleConfiguration,
        ),
      );

    return dto;
  }

  /**
   * Convert array of AttributeRuleSetDomainState to array of list DTOs
   *
   * @param domainStates - Array of rich domain states
   * @returns Array of clean DTOs for list API response
   */
  static toListResponseArray(
    domainStates: AttributeRuleSetDomainState[],
  ): ListAttributeRuleSetResponse[] {
    return domainStates.map((domainState) => this.toListResponse(domainState));
  }

  /**
   * Convert array of AttributeRuleSetDomainState to array of detail DTOs
   *
   * @param domainStates - Array of rich domain states
   * @returns Array of clean DTOs for detail API response
   */
  static toDetailResponseArray(
    domainStates: AttributeRuleSetDomainState[],
  ): DetailAttributeRuleSetResponse[] {
    return domainStates.map((domainState) =>
      this.toDetailResponse(domainState),
    );
  }
}
