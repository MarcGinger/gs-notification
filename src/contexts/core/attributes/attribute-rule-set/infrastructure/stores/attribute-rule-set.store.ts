// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

/**
 * Simple Generic Store
 *
 * Direct, no-nonsense storage for any projections.
 * Used by both projectors (write) and repositories (read).
 */

import { BaseProjection, SimpleStore } from 'src/shared/infrastructure/stores';
import { DetailAttributeRuleSetResponse } from '../../application/dtos';

/**
 * AttributeRuleSet projection data
 */
export interface AttributeRuleSetProjection
  extends DetailAttributeRuleSetResponse,
    BaseProjection {
  lastStreamRevision: string;
}

/**
 * AttributeRuleSet-specific store instance - shared across projectors and repositories
 */
export const attributeRuleSetStore =
  new SimpleStore<AttributeRuleSetProjection>(5000, 'attributeRuleSet');
