// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { AttributesServiceConstants } from '../service-constants';
/**
 * Domain value object for AttributeRuleSet projection keys
 * Centralizes all key patterns to prevent duplication and ensure consistency
 * Follows DDD principle of explicit domain concepts
 */
export class AttributeRuleSetProjectionKeys {
  // EventStore DB patterns
  static readonly ESDB_BOUNDED_CONTEXT =
    AttributesServiceConstants.MODULE_NAMESPACE;
  static readonly ESDB_AGGREGATE_NAME = 'attribute-rule-set';
  static readonly ESDB_VERSION = 'v1';

  /**
   * Get EventStore stream prefix for individual streams
   * Format: <serviceName>.<aggregate>.<version>
   * Example: attributes.attribute-rule-set.v1
   */
  static getEventStoreStreamPrefix(): string {
    return `${this.ESDB_BOUNDED_CONTEXT}.${this.ESDB_AGGREGATE_NAME}.${this.ESDB_VERSION}`;
  }

  /**
   * Get EventStore category projection pattern for catchup
   * Format: $ce-<boundedContext>.<aggregate>.<version>
   * Example: $ce-attributes.attribute-rule-set.v1
   */
  static getEventStoreCategoryPattern(): string {
    return `$ce-${this.getEventStoreStreamPrefix()}`;
  }

  /**
   * Get individual EventStore stream name for specific tenant and code
   * Format: <boundedContext>.<aggregate>.<version>-{tenant}-{code}
   * Example: attributes.attribute-rule-set.v1-tenant-12345
   */
  static getEventStoreStreamName(tenant: string, code: string): string {
    return `${this.getEventStoreStreamPrefix()}-${tenant}-${code}`;
  }

  /**
   * Extract tenant and code from EventStore stream name
   * Validates format: <boundedContext>.<aggregate>.<version>-{tenant}-{code}
   */
  static extractFromStreamName(
    streamName: string,
  ): { tenant: string; code: string } | null {
    const prefix = this.getEventStoreStreamPrefix();
    const pattern = new RegExp(
      `^${prefix.replace(/\./g, '\\.')}-([^-]+)-(.+)$`,
    );
    const match = streamName.match(pattern);
    if (!match) return null;

    const [, tenant, code] = match;
    return { tenant, code };
  }

  /**
   * Validate if stream name matches attribute-rule-set pattern
   */
  static isAttributeRuleSetStream(streamName: string): boolean {
    return this.extractFromStreamName(streamName) !== null;
  }

  /**
   * Get stream pattern for tenant-specific catchup
   * Format: <boundedContext>.<aggregate>.<version>-{tenant}-*
   */
  static getTenantStreamPattern(tenant: string): string {
    return `${this.getEventStoreStreamPrefix()}-${tenant}-*`;
  }

  /**
   * Get global stream pattern for all tenants
   * Format: <boundedContext>.<aggregate>.<version>-*
   */
  static getGlobalStreamPattern(): string {
    return `${this.getEventStoreStreamPrefix()}-*`;
  }

  // Redis projection key patterns
  static readonly REDIS_KEY_PREFIX =
    AttributesServiceConstants.MODULE_NAMESPACE;
  static readonly REDIS_VERSION = 'v1';
  static readonly PROJECTOR_NAME = 'attribute-rule-set-projector';
  static readonly SUBSCRIPTION_GROUP = 'attribute-rule-set-projection';

  // ⚠️ IMPORTANT: Cache optimization keys use different format
  // The CacheOptimizationUtils class uses core:pp:ver:{tenant}:attribute-rule-set:{attributeRuleSetId}
  // for version hints. These are NOT migrated and serve different purposes:
  // - Business keys (this class): Human-readable, descriptive, versioned
  // - Cache keys (CacheOptimizationUtils): Compact, performance-optimized, module-namespaced
  // See docs/REDIS_KEY_ARCHITECTURE.md for complete explanation

  /**
   * Get Redis key for attribute-rule-set projection with cluster-safe hash tags
   * Format: core:v1:{tenant}:attribute-rule-set:{attributeRuleSetId}
   */
  static getRedisAttributeRuleSetKey(
    tenant: string,
    attributeRuleSetId: string,
  ): string {
    return `${this.REDIS_KEY_PREFIX}:${this.REDIS_VERSION}:{${tenant}}:attribute-rule-set:${attributeRuleSetId}`;
  }

  /**
   * Get Redis key for attribute-rule-set index with cluster-safe hash tags
   * Format: core:v1:{tenant}:idx:attribute-rule-set:list
   */
  static getRedisAttributeRuleSetIndexKey(tenant: string): string {
    return `${this.REDIS_KEY_PREFIX}:${this.REDIS_VERSION}:{${tenant}}:idx:attribute-rule-set:list`;
  }

  /**
   * Get Redis key for category-based attribute-rule-set index with cluster-safe hash tags
   * Format: core:v1:{tenant}:idx:attribute-rule-set:by-category:{category}
   */
  static getRedisCategoryIndexKey(tenant: string, category: string): string {
    return `${this.REDIS_KEY_PREFIX}:${this.REDIS_VERSION}:{${tenant}}:idx:attribute-rule-set:by-category:${category}`;
  }

  /**
   * Get Redis key pattern for all attribute-rule-sets in a tenant
   * Format: core:v1:{tenant}:attribute-rule-set:*
   */
  static getRedisTenantAttributeRuleSetPattern(tenant: string): string {
    return `${this.REDIS_KEY_PREFIX}:${this.REDIS_VERSION}:{${tenant}}:attribute-rule-set:*`;
  }

  /**
   * Get Redis key pattern for all attribute-rule-set projections
   * Format: core:v1:*:attribute-rule-set:*
   */
  static getRedisAllAttributeRuleSetsPattern(): string {
    return `${this.REDIS_KEY_PREFIX}:${this.REDIS_VERSION}:*:attribute-rule-set:*`;
  }

  /**
   * Get Redis key pattern for all projections in a specific tenant
   * Format: core:v1:{tenant}:*
   */
  static getRedisTenantPattern(tenant: string): string {
    return `${this.REDIS_KEY_PREFIX}:${this.REDIS_VERSION}:{${tenant}}:*`;
  }

  /**
   * Get Redis key pattern for all attributes projections
   * Format: core:*
   */
  static getRedisAttributesPattern(): string {
    return `${this.REDIS_KEY_PREFIX}:*`;
  }

  /**
   * Get new namespaced cache key format (current format)
   * Format: core:pp:ver:{tenant}:attribute-rule-set:{attributeRuleSetId}
   */
  static getNamespacedVersionHintKey(
    tenant: string,
    attributeRuleSetId: string,
  ): string {
    return `core:pp:ver:{${tenant}}:attribute-rule-set:${attributeRuleSetId}`;
  }

  /**
   * Get current namespaced key patterns (should not be cleaned up)
   */
  static getCurrentKeyPatterns(): string[] {
    return [
      'core:v1:*:attribute-rule-set:*', // Business data keys
      'core:pp:ver:*:attribute-rule-set:*', // Namespaced version hints
      'core:pd:dup:*', // Namespaced dedup keys
    ];
  }
}
