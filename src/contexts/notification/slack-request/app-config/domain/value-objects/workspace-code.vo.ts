// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { AppConfigErrors } from '../errors/app-config.errors';

/**
 * WorkspaceCode Value Object
 * Represents a validated workspaceCode with business rules
 */
export const AppConfigWorkspaceCode = createStringVO({
  name: 'WorkspaceCode',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  maxLength: 32,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    AppConfigErrors.INVALID_WORKSPACE_CODE,
    'WorkspaceCode',
  ),
});

/**
 * Public instance type for WorkspaceCode
 */
export type AppConfigWorkspaceCode = StringVOInstance;

// Convenience creators
export const createAppConfigWorkspaceCode = (s: string) =>
  AppConfigWorkspaceCode.create(s);
export const appConfigWorkspaceCodeFrom = (v: unknown) =>
  AppConfigWorkspaceCode.from(v);
