// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { WorkspaceErrors } from '../errors/workspace.errors';

/**
 * BotToken Value Object
 * Represents a validated botToken with business rules
 */
export const WorkspaceBotToken = createStringVO({
  name: 'BotToken',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(WorkspaceErrors.INVALID_BOT_TOKEN, 'BotToken'),
});

/**
 * Public instance type for BotToken
 */
export type WorkspaceBotToken = StringVOInstance;

// Convenience creators
export const createWorkspaceBotToken = (s: string) =>
  WorkspaceBotToken.create(s);
export const workspaceBotTokenFrom = (v: unknown) => WorkspaceBotToken.from(v);
