// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { WorkspaceErrors } from '../errors/workspace.errors';

/**
 * Name Value Object
 * Represents a validated name with business rules
 */
export const WorkspaceName = createStringVO({
  name: 'Name',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  maxLength: 255,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(WorkspaceErrors.INVALID_NAME, 'Name'),
});

/**
 * Public instance type for Name
 */
export type WorkspaceName = StringVOInstance;

// Convenience creators
export const createWorkspaceName = (s: string) => WorkspaceName.create(s);
export const workspaceNameFrom = (v: unknown) => WorkspaceName.from(v);
