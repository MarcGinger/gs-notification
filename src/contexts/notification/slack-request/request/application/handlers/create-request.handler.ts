// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { CommandHandler, ICommandHandler } from '@nestjs/cqrs';
import { Inject } from '@nestjs/common';
import { Result, DomainError } from 'src/shared/errors';
import { APP_LOGGER, componentLogger, Logger } from 'src/shared/logging';
import { CommandHandlerUtil } from 'src/shared/application';
import { DetailRequestResponse } from '../dtos';
import { CreateRequestUseCase } from '../use-cases';
import { CreateRequestCommand } from '../commands';

/**
 * Enhanced Create Request Handler with security context support
 */
@CommandHandler(CreateRequestCommand)
export class CreateRequestHandler
  implements ICommandHandler<CreateRequestCommand>
{
  private readonly logger: Logger;

  constructor(
    @Inject(CreateRequestUseCase)
    private readonly createRequestUseCase: CreateRequestUseCase,
    @Inject(APP_LOGGER) moduleLogger: Logger,
  ) {
    this.logger = componentLogger(moduleLogger, 'CreateRequestHandler');
  }

  /**
   * Executes the create request command with enhanced security and validation
   */
  async execute(
    command: CreateRequestCommand,
  ): Promise<Result<DetailRequestResponse, DomainError>> {
    const commandName = 'CreateRequestCommand';

    // Log command execution start
    CommandHandlerUtil.logCommandStart(this.logger, commandName, command, {
      application: 'slack-request',
      component: 'CreateRequestHandler',
      extractCommandData: (cmd: CreateRequestCommand) => ({
        propsKeys: Object.keys(cmd.props || {}),
      }),
    });

    // Delegate to use case with full command context
    const result = await this.createRequestUseCase.execute({
      user: command.user,
      props: command.props,
      correlationId: command.correlationId,
      authorizationReason: 'CQRS Command Handler',
    });

    // Log result with enhanced context
    if (result.ok) {
      CommandHandlerUtil.logCommandSuccess(
        this.logger,
        commandName,
        command,
        result.value,
        {
          application: 'slack-request',
          component: 'CreateRequestHandler',
        },
      );
    } else {
      // Use shared error handling with custom context
      CommandHandlerUtil.logValidationError(
        this.logger,
        commandName,
        result.error,
        command,
        {
          application: 'slack-request',
          component: 'CreateRequestHandler',
          extractErrorContext: (cmd: CreateRequestCommand) => ({
            inputStructure: {
              hasProps: !!cmd.props,
              propsKeys: cmd.props ? Object.keys(cmd.props) : [],
            },
          }),
        },
      );
    }

    return result;
  }
}
