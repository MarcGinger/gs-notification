// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { QueryHandler, IQueryHandler } from '@nestjs/cqrs';
import { Injectable } from '@nestjs/common';
import { DomainError, Result } from 'src/shared/errors';
import { ListRequestQuery } from '../queries';
import { ListRequestUseCase } from '../use-cases';
import { RequestPageResponse } from '../dtos';

/**
 * ✅ Enhanced CQRS Query Handler with use case delegation
 * Follows the pattern: Handler → Use Case → Repository
 */
@Injectable()
@QueryHandler(ListRequestQuery)
export class ListRequestHandler implements IQueryHandler<ListRequestQuery> {
  constructor(private readonly listRequestUseCase: ListRequestUseCase) {}

  /**
   * Handle the list request query by delegating to the use case
   */
  async execute(
    query: ListRequestQuery,
  ): Promise<Result<RequestPageResponse, DomainError>> {
    // ✅ Delegate to use case for business logic
    return await this.listRequestUseCase.execute({
      user: query.user,
      filter: query.filter,
      correlationId: query.correlationId || 'unknown',
    });
  }
}
