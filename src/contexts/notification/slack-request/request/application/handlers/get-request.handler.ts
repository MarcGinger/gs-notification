// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { QueryHandler, IQueryHandler } from '@nestjs/cqrs';
import { DetailRequestResponse } from '../dtos';
import { GetRequestUseCase } from '../use-cases';
import { GetRequestQuery } from '../queries';
import { Result, DomainError } from 'src/shared/errors';

@QueryHandler(GetRequestQuery)
export class GetRequestHandler implements IQueryHandler<GetRequestQuery> {
  constructor(private readonly getRequestUseCase: GetRequestUseCase) {}

  async execute(
    query: GetRequestQuery,
  ): Promise<Result<DetailRequestResponse | null, DomainError>> {
    return this.getRequestUseCase.execute({
      user: query.user,
      code: query.code,
      correlationId: query.correlationId || 'query-handler',
    });
  }
}
