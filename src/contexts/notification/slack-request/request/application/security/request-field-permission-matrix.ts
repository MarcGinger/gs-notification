// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { RequestPermission } from '../../domain/permissions/request.permissions';

/**
 * Field-level permission matrix for Request updates.
 * Maps request fields to the required permissions for modification.
 */
export class RequestFieldPermissionMatrix {
  private static readonly fieldPermissions: Record<
    string,
    RequestPermission[]
  > = {
    // Basic request information fields - require standard update permission
    name: [RequestPermission.DOMAIN_REQUEST_UPDATE],
    description: [RequestPermission.DOMAIN_REQUEST_UPDATE],
    category: [RequestPermission.DOMAIN_REQUEST_UPDATE],

    // Status fields - may require special permissions or admin rights
    active: [
      RequestPermission.DOMAIN_REQUEST_UPDATE,
      RequestPermission.DOMAIN_REQUEST_ADMIN,
    ],
    status: [
      RequestPermission.DOMAIN_REQUEST_UPDATE,
      RequestPermission.DOMAIN_REQUEST_ADMIN,
    ],

    // Financial fields - typically require higher permissions
    fees: [RequestPermission.DOMAIN_REQUEST_UPDATE],
    rates: [RequestPermission.DOMAIN_REQUEST_UPDATE],
    pricing: [RequestPermission.DOMAIN_REQUEST_UPDATE],

    // Configuration fields
    channelCodes: [RequestPermission.DOMAIN_REQUEST_UPDATE],
    railCodes: [RequestPermission.DOMAIN_REQUEST_UPDATE],
    configuration: [RequestPermission.DOMAIN_REQUEST_UPDATE],

    // Administrative fields - require admin permissions
    tenant: [RequestPermission.DOMAIN_REQUEST_ADMIN],
    auditInfo: [RequestPermission.DOMAIN_REQUEST_ADMIN],
    metadata: [RequestPermission.DOMAIN_REQUEST_UPDATE],
  };

  /**
   * Get required permissions for a specific field.
   * Returns empty array if field has no special permission requirements.
   */
  static getRequiredPermissions(fieldName: string): RequestPermission[] {
    return this.fieldPermissions[fieldName] || [];
  }

  /**
   * Check if a field has specific permission requirements.
   */
  static hasSpecialPermissions(fieldName: string): boolean {
    return fieldName in this.fieldPermissions;
  }

  /**
   * Get all fields that require a specific permission.
   */
  static getFieldsForPermission(permission: RequestPermission): string[] {
    return Object.entries(this.fieldPermissions)
      .filter(([, perms]) => perms.includes(permission))
      .map(([field]) => field);
  }

  /**
   * Get all defined field permissions.
   */
  static getAllFieldPermissions(): Record<string, RequestPermission[]> {
    return { ...this.fieldPermissions };
  }

  /**
   * Add or update permission requirements for a field.
   */
  static setFieldPermissions(
    fieldName: string,
    permissions: RequestPermission[],
  ): void {
    this.fieldPermissions[fieldName] = [...permissions];
  }
}
