// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { applyDecorators } from '@nestjs/common';
import { ApiProperty } from '@nestjs/swagger';
import { IsEnum, IsOptional, IsNotEmpty } from 'class-validator';
import {
  RequestStatusValues,
  RequestStatusValue,
} from '../../../domain/value-objects';

export { RequestStatusValues, RequestStatusValue };
/**
 * Options for property decorators (enum)
 */
interface PropOptions {
  required?: boolean;
}

/**
 * Property decorator for Request Status with required option
 * @param {Object} options - Options for the decorator
 * @returns {PropertyDecorator}
 */
export function ApiRequestStatus(options: PropOptions = {}) {
  const { required = false } = options;
  return applyDecorators(
    ApiProperty({
      description: `Current message lifecycle status. Values include requested, validated, queued, failed, sent.`,
      type: String,
      enum: RequestStatusValues,
      required,
    }),
    IsEnum(RequestStatusValues),
    required ? IsNotEmpty() : IsOptional(),
  );
}
