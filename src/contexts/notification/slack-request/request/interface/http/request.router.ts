// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Module } from '@nestjs/common';
import { RouterModule, Routes } from '@nestjs/core';
import { RequestModule } from '../../request.module';
import { RequestProjectorModule } from '../../request-projector.module';
/**
 * Request Router Configuration
 *
 * Configures nested routing for request endpoints to follow
 * the module monolith structure: /notification/slack-request/api/v1/requests
 *
 * This replaces flat routing (/api/v1/requests) with contextual routing
 * that reflects the bounded context hierarchy.
 */

// Define the routing structure for the request bounded context
const requestRoutes: Routes = [
  {
    path: 'notification',
    children: [
      {
        path: 'slack-request',
        children: [
          {
            path: 'requests',
            module: RequestModule,
          },
          {
            path: 'health',
            module: RequestProjectorModule,
          },
        ],
      },
    ],
  },
];

/**
 * Request Router Module
 *
 * Provides nested routing configuration for request endpoints.
 * This module should be imported in the main app module to enable
 * the nested route structure.
 *
 * Routes configured:
 * - /notification/slack-request/api/v1/requests (GET, POST)
 * - /notification/slack-request/api/v1/requests/:code (GET)
 * - /notification/slack-request/health/projectors/request (GET)
 * - /notification/slack-request/health/projectors/request/metrics (GET)
 */
@Module({
  imports: [
    RequestModule,
    RequestProjectorModule,
    RouterModule.register(requestRoutes),
  ],
  exports: [RequestModule, RequestProjectorModule],
})
export class RequestRouterModule {}
