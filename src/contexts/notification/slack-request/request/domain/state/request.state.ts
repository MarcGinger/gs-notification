// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  RequestCreatedAt,
  RequestUpdatedAt,
  RequestVersion,
  RequestChannelCode,
  RequestCode,
  RequestData,
  RequestRecipient,
  RequestStatus,
  RequestTemplateCode,
  RequestWorkspaceCode,
} from '../value-objects';

/**
 * Request Domain State
 *
 * Domain state interface that uses value objects to model business concepts
 * while maintaining compatibility with entity operations. Business fields are
 * modeled as value objects with proper validation and domain rules.
 *
 * Domain-First State Interface
 *
 * This interface represents the domain model where business concepts
 * are properly encapsulated as value objects when appropriate.
 */

export interface RequestDomainState {
  code: RequestCode;
  recipient?: RequestRecipient;
  data?: RequestData;
  status?: RequestStatus;
  workspaceCode: RequestWorkspaceCode;
  templateCode: RequestTemplateCode;
  channelCode?: RequestChannelCode;
  version: RequestVersion;
  createdAt: RequestCreatedAt;
  updatedAt: RequestUpdatedAt;
}

export type RequestDomainStateOptional = Partial<RequestDomainState>;
