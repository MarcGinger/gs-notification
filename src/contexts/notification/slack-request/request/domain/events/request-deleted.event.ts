// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { EventMetadata } from 'src/shared/domain/events';

/**
 * Request Deleted Event Payload
 * Contains identifying information about the deleted request
 */
export interface RequestDeletedEventPayload {
  code: string;
  deletedAt: Date;
  version: number;
  metadata: EventMetadata;
}

/**
 * Request Deleted Domain Event
 *
 * Emitted when a request is successfully deleted.
 * Contains identifying information and deletion metadata.
 */
export class RequestDeletedEvent {
  public readonly eventType = 'NotificationSlackRequestRequestDeleted.v1';
  public readonly eventVersion = 'v1';

  constructor(public readonly payload: RequestDeletedEventPayload) {}

  // Factory method with security context
  static create(
    data: Omit<RequestDeletedEventPayload, 'metadata'>,
    baseMetadata: EventMetadata,
  ): RequestDeletedEvent {
    const metadata: EventMetadata = {
      ...baseMetadata,
      eventVersion: '1.0',
      source: 'notification.domain.request',
    };

    return new RequestDeletedEvent({
      ...data,
      metadata,
    });
  }

  get code(): string {
    return this.payload.code;
  }

  get deletedAt(): Date {
    return this.payload.deletedAt;
  }

  get version(): number {
    return this.payload.version;
  }

  get metadata(): EventMetadata {
    return this.payload.metadata;
  }
}
