// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { EntityIdBase } from 'src/shared/domain/entities';
import { Result, ok, err, DomainError } from 'src/shared/errors';
import { RequestSnapshotProps } from '../props';
import { RequestDomainState } from '../state';
import { RequestErrors } from '../errors/request.errors';
import {
  RequestChannelCode,
  RequestCode,
  RequestCreatedAt,
  RequestUpdatedAt,
  RequestVersion,
  RequestData,
  RequestRecipient,
  RequestStatus,
  RequestStatusLogic,
  RequestStatusValue,
  RequestTemplateCode,
  RequestWorkspaceCode,
  createRequestStatus,
} from '../value-objects';

/**
 * Domain Entity: Request
 *
 * Represents the core Request entity in the notification domain.
 * Encapsulates request data, identity, and basic entity behavior.
 *
 * This entity follows DDD principles:
 * - Identity: Code as unique identifier
 * - Immutability: Changes create new instances
 * - Encapsulation: Private state with controlled access
 * - Business validation: Domain rules enforced
 *
 * @domain Notification Context - Request Entity
 * @layer Domain Entities
 */

/**
 * Request Entity
 *
 * Core domain entity representing a request in the notification.
 * Handles request identity, validation, and state management.
 */
export class RequestEntity extends EntityIdBase<
  RequestDomainState,
  RequestCode
> {
  private static clock: { now: () => Date } = { now: () => new Date() };

  public static setClock(c: { now: () => Date }) {
    this.clock = c;
  }

  /**
   * Helper method to create updatedAt VO from date
   */
  private static createUpdatedAt(
    date?: Date,
  ): Result<RequestUpdatedAt, DomainError> {
    const targetDate = date || RequestEntity.clock.now();
    const result = RequestUpdatedAt.from(targetDate);
    if (!result.ok) {
      return err(result.error);
    }
    return ok(result.value);
  }

  /**
   * Helper method to create version VO from number
   */
  private static createVersion(
    versionNumber?: number,
    currentVersion?: RequestVersion,
  ): Result<RequestVersion, DomainError> {
    const targetVersion = versionNumber ?? currentVersion?.value ?? 1;
    const result = RequestVersion.from(targetVersion);
    if (!result.ok) {
      return err(result.error);
    }
    return ok(result.value);
  }

  /**
   * Helper method to create updated entity with common logic
   */
  private createUpdatedEntity(
    fieldUpdates: Partial<RequestDomainState>,
    updatedAt?: Date,
    version?: number,
  ): Result<RequestEntity, DomainError> {
    const updatedAtResult = RequestEntity.createUpdatedAt(updatedAt);
    if (!updatedAtResult.ok) {
      return err(updatedAtResult.error);
    }

    const versionResult = RequestEntity.createVersion(
      version,
      this.props.version,
    );
    if (!versionResult.ok) {
      return err(versionResult.error);
    }

    const updatedProps: RequestDomainState = {
      ...this.props,
      ...fieldUpdates,
      updatedAt: updatedAtResult.value,
      version: versionResult.value,
    };

    return RequestEntity.create(updatedProps);
  }

  private constructor(props: RequestDomainState) {
    super(props, props.code);
  }

  /**
   * Factory method to create a new request entity
   *
   * @param props - Request properties
   * @returns Result containing RequestEntity or DomainError
   */
  public static create(
    props: RequestDomainState,
  ): Result<RequestEntity, DomainError> {
    // Validate required properties
    const validationResult = RequestEntity.validate(props);
    if (validationResult.ok === false) {
      return err(validationResult.error);
    }

    return ok(new RequestEntity(props));
  }

  /**
   * Factory method to reconstitute a request entity from persistence
   *
   * @param props - Request properties from database
   * @returns RequestEntity instance
   */
  public static reconstitute(props: RequestDomainState): RequestEntity {
    return new RequestEntity(props);
  }

  /**
   * Factory method to reconstitute entity from snapshot
   *
   * @param snapshot - Request snapshot properties
   * @returns Result containing RequestEntity or DomainError
   */
  public static fromSnapshot(
    snapshot: RequestSnapshotProps,
  ): Result<RequestEntity, DomainError> {
    const codeResult = RequestCode.from(snapshot.code);
    if (!codeResult.ok) {
      return err(codeResult.error);
    }
    const recipientResult = RequestRecipient.from(snapshot.recipient);
    if (!recipientResult.ok) {
      return err(recipientResult.error);
    }
    const dataResult = RequestData.from(snapshot.data);
    if (!dataResult.ok) {
      return err(dataResult.error);
    }
    const statusResult = createRequestStatus(snapshot.status);
    if (!statusResult.ok) {
      return err(statusResult.error);
    }
    const workspaceCodeResult = RequestWorkspaceCode.from(
      snapshot.workspaceCode,
    );
    if (!workspaceCodeResult.ok) {
      return err(workspaceCodeResult.error);
    }
    const templateCodeResult = RequestTemplateCode.from(snapshot.templateCode);
    if (!templateCodeResult.ok) {
      return err(templateCodeResult.error);
    }
    const channelCodeResult = RequestChannelCode.from(snapshot.channelCode);
    if (!channelCodeResult.ok) {
      return err(channelCodeResult.error);
    }
    const createdAtResult = RequestCreatedAt.from(snapshot.createdAt);
    if (!createdAtResult.ok) {
      return err(createdAtResult.error);
    }

    const updatedAtResult = RequestUpdatedAt.from(snapshot.updatedAt);
    if (!updatedAtResult.ok) {
      return err(updatedAtResult.error);
    }

    const versionResult = RequestVersion.from(snapshot.version);
    if (!versionResult.ok) {
      return err(versionResult.error);
    }

    const props: RequestDomainState = {
      code: codeResult.value,
      recipient: recipientResult.value,
      data: dataResult.value,
      status: statusResult.value,
      workspaceCode: workspaceCodeResult.value,
      templateCode: templateCodeResult.value,
      channelCode: channelCodeResult.value,
      createdAt: createdAtResult.value,
      updatedAt: updatedAtResult.value,
      version: versionResult.value,
    };

    return RequestEntity.create(props);
  }

  /**
   * Validates request entity properties
   *
   * @param props - Properties to validate
   * @returns Validation result
   */
  private static validate(
    props: RequestDomainState,
  ): Result<void, DomainError> {
    // Basic validation
    if (!props.code) {
      return err(RequestErrors.INVALID_CODE_DATA);
    }
    if (!props.workspaceCode) {
      return err(RequestErrors.INVALID_WORKSPACE_CODE_DATA);
    }
    if (!props.templateCode) {
      return err(RequestErrors.INVALID_TEMPLATE_CODE_DATA);
    }

    return ok(undefined);
  }

  // ======================
  // Getters (Public API)
  // ======================

  public get code(): RequestCode {
    return this.props.code;
  }

  public get recipient(): RequestRecipient | undefined {
    return this.props.recipient;
  }

  public get data(): RequestData | undefined {
    return this.props.data;
  }

  public get status(): RequestStatus | undefined {
    return this.props.status;
  }

  public get workspaceCode(): RequestWorkspaceCode {
    return this.props.workspaceCode;
  }

  public get templateCode(): RequestTemplateCode {
    return this.props.templateCode;
  }

  public get channelCode(): RequestChannelCode | undefined {
    return this.props.channelCode;
  }

  public get createdAt(): RequestCreatedAt {
    return this.props.createdAt;
  }

  public get updatedAt(): RequestUpdatedAt {
    return this.props.updatedAt;
  }

  public get version(): RequestVersion {
    return this.props.version;
  }

  // ======================
  // Simple Update Methods (No Business Logic)
  // ======================

  /**
   * Creates a new entity with updated recipient (pure state transition)
   *
   * @param recipient - New recipient value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<RequestEntity, DomainError>
   */
  public withRecipient(
    recipient: RequestRecipient,
    updatedAt?: Date,
    version?: number,
  ): Result<RequestEntity, DomainError> {
    return this.createUpdatedEntity({ recipient }, updatedAt, version);
  }

  /**
   * Creates a new entity with updated data (pure state transition)
   *
   * @param data - New data value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<RequestEntity, DomainError>
   */
  public withData(
    data: RequestData,
    updatedAt?: Date,
    version?: number,
  ): Result<RequestEntity, DomainError> {
    return this.createUpdatedEntity({ data }, updatedAt, version);
  }

  /**
   * Creates a new entity with updated status (with basic transition validation)
   *
   * @param status - New status value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<RequestEntity, DomainError>
   */
  public withStatus(
    status: RequestStatus,
    updatedAt?: Date,
    version?: number,
  ): Result<RequestEntity, DomainError> {
    // Basic validation - business rules handled by aggregate
    if (this.props.status) {
      // If current status exists, validate transition
      const currentStatus = this.props.status.value;
      const targetStatus = status.value;

      if (!RequestStatusLogic.canTransition(currentStatus, targetStatus)) {
        return err({
          ...RequestErrors.INVALID_STATUS_TRANSITION,
          context: {
            currentStatus,
            targetStatus,
            validTransitions:
              RequestStatusLogic.getValidTransitions(currentStatus),
          },
        });
      }
    }

    return this.createUpdatedEntity({ status }, updatedAt, version);
  }

  /**
   * Creates a new entity with updated workspaceCode (pure state transition)
   *
   * @param workspaceCode - New workspace_code value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<RequestEntity, DomainError>
   */
  public withWorkspaceCode(
    workspaceCode: RequestWorkspaceCode,
    updatedAt?: Date,
    version?: number,
  ): Result<RequestEntity, DomainError> {
    return this.createUpdatedEntity({ workspaceCode }, updatedAt, version);
  }

  /**
   * Creates a new entity with updated templateCode (pure state transition)
   *
   * @param templateCode - New template_code value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<RequestEntity, DomainError>
   */
  public withTemplateCode(
    templateCode: RequestTemplateCode,
    updatedAt?: Date,
    version?: number,
  ): Result<RequestEntity, DomainError> {
    return this.createUpdatedEntity({ templateCode }, updatedAt, version);
  }

  /**
   * Creates a new entity with updated channelCode (pure state transition)
   *
   * @param channelCode - New channel_code value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<RequestEntity, DomainError>
   */
  public withChannelCode(
    channelCode: RequestChannelCode,
    updatedAt?: Date,
    version?: number,
  ): Result<RequestEntity, DomainError> {
    return this.createUpdatedEntity({ channelCode }, updatedAt, version);
  }

  /**
   * Get valid next statuses from current state
   * @returns readonly RequestStatusValue[]
   */
  public getValidStatusTransitions(): readonly RequestStatusValue[] {
    // Get valid next statuses from current state
    if (!this.props.status) {
      return []; // No transitions possible if no current status
    }
    return RequestStatusLogic.getValidTransitions(this.props.status.value);
  }

  // ======================
  // Query Methods
  // ======================

  /**
   * Checks if two requests are the same entity
   *
   * @param other - Other request to compare
   */
  public sameAs(other: RequestEntity): boolean {
    return this.props.code.equals(other.props.code);
  }

  /**
   * Gets a snapshot of current entity state for serialization
   */
  public toSnapshot(): RequestSnapshotProps {
    return {
      code: this.props.code.value,
      recipient: this.props.recipient?.value,
      data: this.props.data?.value,
      status: this.props.status?.value,
      workspaceCode: this.props.workspaceCode.value,
      templateCode: this.props.templateCode.value,
      channelCode: this.props.channelCode?.value,
      createdAt: this.props.createdAt.value,
      updatedAt: this.props.updatedAt.value,
      version: this.props.version.value,
    };
  }

  /**
   * Gets the current domain state with value objects
   *
   * Provides access to the rich domain state for application layer
   * without requiring infrastructure mappers. Maintains clean architecture.
   *
   * @returns Current domain state with value objects
   */
  public getDomainState(): RequestDomainState {
    return this.props;
  }
}
