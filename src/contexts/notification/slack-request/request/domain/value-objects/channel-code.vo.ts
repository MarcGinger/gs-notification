// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { RequestErrors } from '../errors/request.errors';

/**
 * ChannelCode Value Object
 * Represents a validated channelCode with business rules
 */
export const RequestChannelCode = createStringVO({
  name: 'ChannelCode',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,
  minLength: 10,
  maxLength: 32,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    RequestErrors.INVALID_CHANNEL_CODE,
    'ChannelCode',
  ),
});

/**
 * Public instance type for ChannelCode
 */
export type RequestChannelCode = StringVOInstance;

// Convenience creators
export const createRequestChannelCode = (s: string) =>
  RequestChannelCode.create(s);
export const requestChannelCodeFrom = (v: unknown) =>
  RequestChannelCode.from(v);
