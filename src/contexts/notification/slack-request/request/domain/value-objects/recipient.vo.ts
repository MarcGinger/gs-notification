// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { RequestErrors } from '../errors/request.errors';

/**
 * Recipient Value Object
 * Represents a validated recipient with business rules
 */
export const RequestRecipient = createStringVO({
  name: 'Recipient',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 32,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(RequestErrors.INVALID_RECIPIENT, 'Recipient'),
});

/**
 * Public instance type for Recipient
 */
export type RequestRecipient = StringVOInstance;

// Convenience creators
export const createRequestRecipient = (s: string) => RequestRecipient.create(s);
export const requestRecipientFrom = (v: unknown) => RequestRecipient.from(v);
