// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { RequestErrors } from '../errors/request.errors';

/**
 * TemplateCode Value Object
 * Represents a validated templateCode with business rules
 */
export const RequestTemplateCode = createStringVO({
  name: 'TemplateCode',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  maxLength: 64,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    RequestErrors.INVALID_TEMPLATE_CODE,
    'TemplateCode',
  ),
});

/**
 * Public instance type for TemplateCode
 */
export type RequestTemplateCode = StringVOInstance;

// Convenience creators
export const createRequestTemplateCode = (s: string) =>
  RequestTemplateCode.create(s);
export const requestTemplateCodeFrom = (v: unknown) =>
  RequestTemplateCode.from(v);
