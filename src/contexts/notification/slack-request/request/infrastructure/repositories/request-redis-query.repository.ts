// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Injectable, Inject } from '@nestjs/common';
import { Redis } from 'ioredis';
import { APP_LOGGER, Log, componentLogger, Logger } from 'src/shared/logging';
import { CorrelationUtil } from 'src/shared/utilities/correlation.util';
import { Clock, CLOCK } from 'src/shared/infrastructure/time';
import {
  RepositoryLoggingUtil,
  RepositoryLoggingConfig,
  handleRepositoryError,
  safeParseJSON,
  safeParseJSONArray,
  RepositoryOptions,
} from 'src/shared/infrastructure/repositories';
import { Result, DomainError, err, ok } from 'src/shared/errors';
import { Option } from 'src/shared/domain/types';
import { ActorContext } from 'src/shared/application/context';
import { RepositoryErrorFactory } from 'src/shared/domain/errors/repository.error';
import { CacheMetricsCollector } from 'src/shared/infrastructure/projections/cache-optimization';
import { SLACK_REQUEST_DI_TOKENS } from '../../../slack-request.constants';
import { RequestProjectionKeys } from '../../request-projection-keys';
import {
  DetailRequestResponse,
  RequestPageResponse,
  ListRequestFilterRequest,
  ListRequestResponse,
} from '../../application/dtos';
import { PaginationMetaResponse } from 'src/shared/application/dtos';
import { IRequestQuery } from '../../application/ports';

/**
 * Internal request data structure for Redis operations
 * Extends DetailRequestResponse with additional fields needed for caching and versioning
 */
interface RequestCacheData extends DetailRequestResponse {
  version: number;
  createdAt: Date;
  updatedAt: Date;
}

/**
 * Request Query Repository - Interface Segregation Principle Implementation
 *
 * Bounded Context: Notification/Request
 * Handles complex query operations including pagination, filtering, and search.
 * This repository focuses solely on advanced data retrieval operations using Redis projections.
 *
 * ISP Benefits:
 * - Clients that need simple reads don't depend on complex query methods
 * - Can be optimized independently using Redis indexing strategies
 * - Easier to implement caching strategies for query operations
 * - Supports analytical and reporting use cases with Redis sorted sets
 *
 * Redis Features Used:
 * - Hash-based request storage with cluster-safe keys
 * - Sorted set indexing for efficient pagination and sorting
 * - Pattern matching for code and name filtering
 * - SCAN operations for tenant isolation
 * - Production-ready caching with metrics collection
 *
 * @domain Notification Context - Request Query Repository (Redis)
 * @layer Infrastructure
 * @pattern Repository Pattern + Interface Segregation Principle + Redis Projector
 */
@Injectable()
export class RequestQueryRepository implements IRequestQuery {
  private readonly logger: Logger;
  private readonly loggingConfig: RepositoryLoggingConfig;
  private readonly metricsCollector = new CacheMetricsCollector();

  constructor(
    @Inject(APP_LOGGER) baseLogger: Logger,
    @Inject(CLOCK) private readonly clock: Clock,
    @Inject(SLACK_REQUEST_DI_TOKENS.IO_REDIS)
    private readonly redis: Redis,
  ) {
    this.loggingConfig = {
      serviceName: 'RequestConfigService',
      component: 'RequestQueryRepository',
    };
    this.logger = componentLogger(baseLogger, this.loggingConfig.component);

    Log.info(
      this.logger,
      'RequestQueryRepository initialized with Redis backend',
      {
        component: this.loggingConfig.component,
        redisStatus: this.redis.status,
        clusterSafe: true,
        cacheOptimized: true,
      },
    );
  }

  /**
   * Find a single request by code using Redis hash lookup
   *
   * Leverages the established Redis patterns from RequestProjector
   * with cluster-safe keys and production-ready caching.
   *
   * Features:
   * - Cluster-safe Redis keys with hash tags for co-location
   * - Direct hash lookup for optimal performance (O(1))
   * - Tenant isolation using Redis key patterns
   * - Comprehensive logging and error handling
   * - Production-ready metrics collection
   * - Soft delete awareness
   *
   * @param actor - The actor context containing authentication and request metadata.
   * @param code - The request code to search for.
   * @param options - Optional repository options (e.g., timeout, correlation).
   * @returns A promise resolving to a Result containing the Request response or a DomainError.
   */
  async findById(
    actor: ActorContext,
    code: string,
    options?: RepositoryOptions,
  ): Promise<Result<Option<DetailRequestResponse>, DomainError>> {
    const operation = 'findById';
    const correlationId =
      options?.correlationId ??
      CorrelationUtil.generateForOperation('request-query-findById');

    const logContext = this.createLogContext(operation, correlationId, actor, {
      requestCode: code,
      dataSource: 'redis-projector',
    });

    // Validate actor context with enhanced security logging
    const validation = RepositoryLoggingUtil.validateActorContext(
      this.logger,
      actor,
      logContext,
    );
    if (!validation.ok) return err(validation.error);

    // Guard tenant explicitly
    if (!actor.tenant) {
      return err(
        RepositoryErrorFactory.validationError('tenant', 'Missing tenant id'),
      );
    }

    // Log successful authorization
    RepositoryLoggingUtil.logAuthorizationSuccess(
      this.logger,
      operation,
      logContext,
      {
        operationType: 'request_query_findById',
        scope: 'redis_single_lookup',
        tenant: actor.tenant,
      },
    );

    try {
      // Generate cluster-safe Redis key
      const requestKey = this.generateRequestKey(actor.tenant, code);

      Log.debug(this.logger, 'Executing Redis hash lookup', {
        ...logContext,
        queryDetails: {
          scope: 'redis_hgetall',
          method: 'redis.hgetall',
          key: requestKey,
          clusterSafe: true,
        },
      });

      // Fetch request hash from Redis
      const hashData = await this.redis.hgetall(requestKey);

      if (!hashData || Object.keys(hashData).length === 0) {
        Log.debug(this.logger, 'Request not found in Redis', logContext);
        return ok(Option.none());
      }

      // Parse Redis hash to request data
      const request = this.parseRedisHashToRequest(hashData);

      if (!request) {
        // Request exists but is soft deleted or malformed
        Log.debug(
          this.logger,
          'Request exists but is soft deleted or malformed',
          {
            ...logContext,
            softDeleted: !!hashData.deletedAt,
          },
        );
        return ok(Option.none());
      }

      // Transform to DetailRequestResponse DTO (excluding internal fields)
      const detailResponse: DetailRequestResponse = {
        code: request.code,
        recipient: request.recipient,
        data: request.data,
        status: request.status,
        workspaceCode: request.workspaceCode,
        templateCode: request.templateCode,
        channelCode: request.channelCode,
      };

      Log.debug(this.logger, 'Request found successfully in Redis', {
        ...logContext,
        resultData: {
          requestCode: detailResponse.code,
          cacheHit: true,
        },
      });

      return ok(Option.some(detailResponse));
    } catch (error) {
      // Log operation error using shared utility
      RepositoryLoggingUtil.logOperationError(
        this.logger,
        operation,
        logContext,
        error as Error,
        'MEDIUM',
      );

      // Handle and return the classified error using shared utility
      return handleRepositoryError(error);
    }
  }

  /**
   * Generate cluster-safe Redis keys using centralized RequestProjectionKeys
   * Ensures consistency with projector key patterns
   */
  private generateRequestKey(tenant: string, id: string): string {
    // ✅ Use centralized key generation for consistency
    return RequestProjectionKeys.getRedisRequestKey(tenant, id);
  }

  /**
   * Generate tenant index key for pagination and sorting using centralized keys
   */
  private generateTenantIndexKey(tenant: string): string {
    return RequestProjectionKeys.getRedisRequestIndexKey(tenant);
  }

  /**
   * Parse Redis hash data into DetailRequestResponse DTO
   * Uses DTO instead of domain props for CQRS compliance
   */
  private parseRedisHashToRequest(
    hashData: Record<string, string>,
  ): RequestCacheData | null {
    try {
      if (!hashData || Object.keys(hashData).length === 0) {
        return null;
      }

      // Check for soft deletion
      if (hashData.deletedAt) {
        return null;
      }

      // Parse array fields using safeParseJSONArray utility

      // Parse object fields using safeParseJSON utility
      const data = safeParseJSON<Record<string, unknown>>(
        hashData.data,
        'data',
      );
      // Extract basic fields directly from hash data

      // Type assertion for status - cached data should already be validated
      const status = hashData.status as DetailRequestResponse['status'];

      return {
        code: hashData.code,
        recipient: hashData.recipient || undefined,
        data,
        status,
        workspaceCode: hashData.workspaceCode,
        templateCode: hashData.templateCode,
        channelCode: hashData.channelCode || undefined,
        version: parseInt(hashData.version, 10),
        createdAt: new Date(hashData.createdAt),
        updatedAt: new Date(hashData.updatedAt),
      };
    } catch (error) {
      Log.error(
        this.logger,
        'Failed to parse Redis hash data to RequestSnapshot',
        {
          method: 'parseRedisHashToRequest',
          error: (error as Error).message,
          code: hashData?.code,
        },
      );
      return null;
    }
  }

  /**
   * Transform Request CacheData to ListRequestResponse DTO
   */
  private toListResponse(request: RequestCacheData): ListRequestResponse {
    return {
      code: request.code,
      recipient: request.recipient,
      data: request.data,
      status: request.status,
      workspaceCode: request.workspaceCode,
      templateCode: request.templateCode,
      channelCode: request.channelCode,
    } as ListRequestResponse;
  }

  /**
   * Apply filters to request data using Redis pattern matching and client-side filtering
   */
  private matchesFilter(
    request: RequestCacheData,
    filter?: ListRequestFilterRequest,
  ): boolean {
    if (!filter) return true;

    // Filter by recipient (partial match)
    if (filter.recipient) {
      if (
        request.recipient
          ?.toLowerCase()
          .includes(filter.recipient.toLowerCase())
      ) {
        return false;
      }
    }
    // Filter by status (exact match)
    if (filter.status !== undefined) {
      if (request.status !== filter.status) {
        return false;
      }
    }

    return true;
  }

  /**
   * Sort requests based on sort criteria
   * Handles undefined/null values properly and provides stable sorting
   */
  private sortRequests(
    requests: RequestCacheData[],
    sortBy?: Record<string, string>,
  ): RequestCacheData[] {
    if (!sortBy || Object.keys(sortBy).length === 0) {
      // No sorting criteria provided, return as-is
      return requests;
    }

    return requests.sort((a, b) => {
      for (const [field, direction] of Object.entries(sortBy)) {
        let aVal: number | Date | string | undefined;
        let bVal: number | Date | string | undefined;

        switch (field) {
          case 'recipient':
            aVal = a.recipient;
            bVal = b.recipient;
            break;
          case 'status':
            aVal = a.status;
            bVal = b.status;
            break;
          case 'createdAt':
            aVal = a.createdAt;
            bVal = b.createdAt;
            break;
          case 'updatedAt':
            aVal = a.updatedAt;
            bVal = b.updatedAt;
            break;
          default:
            continue;
        }

        // Handle undefined/null values - put them at the end
        if (aVal == null && bVal == null) continue;
        if (aVal == null) return 1; // null values go to the end
        if (bVal == null) return -1; // null values go to the end

        if (aVal === bVal) continue;

        let comparison: number;

        // Handle different types properly
        if (aVal instanceof Date && bVal instanceof Date) {
          comparison = aVal.getTime() - bVal.getTime() < 0 ? -1 : 1;
        } else if (typeof aVal === 'number' && typeof bVal === 'number') {
          comparison = aVal - bVal < 0 ? -1 : 1;
        } else {
          // String comparison (convert to string if needed)
          const aStr = String(aVal).toLowerCase();
          const bStr = String(bVal).toLowerCase();
          comparison = aStr < bStr ? -1 : 1;
        }

        return direction?.toLowerCase() === 'desc' ? -comparison : comparison;
      }
      return 0;
    });
  }
  /**
   * Helper to create consistent logging context using shared utilities
   */
  private createLogContext(
    operation: string,
    correlationId: string,
    actor: ActorContext,
    additionalContext?: Record<string, unknown>,
  ): Record<string, unknown> {
    return RepositoryLoggingUtil.createLogContext(
      this.loggingConfig,
      this.clock,
      operation,
      correlationId,
      actor,
      additionalContext,
    );
  }

  /**
   * Find Request records with pagination and filtering using Redis projections.
   *
   * Leverages the established Redis patterns from RequestProjector and RequestReaderRepository
   * with cluster-safe keys and production-ready caching.
   *
   * Features:
   * - Cluster-safe Redis keys with hash tags for co-location
   * - Client-side filtering for code patterns and name search
   * - Efficient in-memory sorting with configurable directions
   * - Pagination with total count calculation
   * - Tenant isolation using Redis key patterns
   * - Comprehensive logging and error handling
   * - Production-ready metrics collection
   *
   * @param actor - The actor context containing authentication and request metadata.
   * @param filter - Optional filter criteria for the request search.
   * @param options - Optional repository options (e.g., pagination, sorting).
   * @returns A promise resolving to a Result containing paginated Request responses or a DomainError.
   */
  async findPaginated(
    actor: ActorContext,
    filter?: ListRequestFilterRequest,
    options?: RepositoryOptions,
  ): Promise<Result<RequestPageResponse, DomainError>> {
    const operation = 'findPaginated';
    const correlationId =
      options?.correlationId ??
      CorrelationUtil.generateForOperation('request-query-paginated');

    const logContext = this.createLogContext(operation, correlationId, actor, {
      filterRecipient: filter?.recipient,
      filterStatus: filter?.status,
      page: filter?.page,
      size: filter?.size,
      sortBy: filter?.sortBy,
      dataSource: 'redis-projector',
    });

    // Validate actor context with enhanced security logging
    const validation = RepositoryLoggingUtil.validateActorContext(
      this.logger,
      actor,
      logContext,
    );
    if (!validation.ok) return err(validation.error);

    // Guard tenant explicitly
    if (!actor.tenant) {
      return err(
        RepositoryErrorFactory.validationError('tenant', 'Missing tenant id'),
      );
    }

    // Log successful authorization
    RepositoryLoggingUtil.logAuthorizationSuccess(
      this.logger,
      operation,
      logContext,
      {
        operationType: 'request_query_paginated',
        scope: 'redis_projection_search',
        tenant: actor.tenant,
      },
    );

    try {
      const page = filter?.page ?? 1;
      const size = Math.min(filter?.size ?? 20, 100); // Cap at 100 items per page

      // Generate the proper Redis key pattern using centralized RequestProjectionKeys
      const pattern = RequestProjectionKeys.getRedisTenantRequestPattern(
        actor.tenant,
      );

      Log.debug(this.logger, 'Scanning Redis for tenant requests', {
        ...logContext,
        queryDetails: {
          scope: 'redis_scan',
          method: 'redis.scan',
          pattern,
          clusterSafe: true,
        },
      });

      // Use Redis SCAN to find all request keys for the tenant
      const requestKeys: string[] = [];
      const scanIterator = this.redis.scanStream({
        match: pattern,
        count: 1000, // Batch size for scanning
      });

      for await (const keys of scanIterator) {
        requestKeys.push(...(keys as string[]));
      }

      if (requestKeys.length === 0) {
        // No requests found for tenant
        const meta = new PaginationMetaResponse({
          page,
          size,
          totalItems: 0,
          totalPages: 0,
          hasNextPage: false,
          hasPreviousPage: false,
        });

        const response = RequestPageResponse.create([], meta);

        Log.debug(this.logger, 'No requests found for tenant', {
          ...logContext,
          totalItems: 0,
        });

        return ok(response);
      }

      // Batch fetch all request hashes using pipeline for efficiency
      const pipeline = this.redis.pipeline();
      requestKeys.forEach((key) => {
        pipeline.hgetall(key);
      });

      const results = await pipeline.exec();

      if (!results) {
        throw new Error('Redis pipeline execution failed');
      }

      // Parse all requests from Redis hashes
      const allRequests: RequestCacheData[] = [];
      for (let i = 0; i < results.length; i++) {
        const [error, hashData] = results[i];
        if (!error && hashData) {
          const request = this.parseRedisHashToRequest(
            hashData as Record<string, string>,
          );
          if (request) {
            allRequests.push(request);
          }
        }
      }

      // Apply client-side filtering
      const filteredRequests = allRequests.filter((request) =>
        this.matchesFilter(request, filter),
      );

      // Apply client-side sorting
      const sortedRequests = this.sortRequests(
        filteredRequests,
        filter?.sortBy,
      );

      // Calculate pagination metadata
      const totalItems = sortedRequests.length;
      const totalPages = Math.ceil(totalItems / size);
      const hasNextPage = page < totalPages;
      const hasPreviousPage = page > 1;

      // Apply pagination
      const startIndex = (page - 1) * size;
      const endIndex = startIndex + size;
      const paginatedRequests = sortedRequests.slice(startIndex, endIndex);

      // Transform to DTOs
      const requestResponses = paginatedRequests.map((request) =>
        this.toListResponse(request),
      );

      const meta = new PaginationMetaResponse({
        page,
        size,
        totalItems,
        totalPages,
        hasNextPage,
        hasPreviousPage,
      });

      const response = RequestPageResponse.create(requestResponses, meta);

      // Log successful query metrics
      RepositoryLoggingUtil.logQueryMetrics(
        this.logger,
        operation,
        logContext,
        {
          resultCount: paginatedRequests.length,
          dataQuality: paginatedRequests.length > 0 ? 'good' : 'empty',
          sampleData: {
            totalItems,
            totalKeysScanned: requestKeys.length,
            filteredCount: filteredRequests.length,
            page,
            size,
            hasFilters: !!(filter?.recipient || filter?.status !== undefined),
            sortFields: Object.keys(filter?.sortBy ?? {}),
          },
        },
      );

      return ok(response);
    } catch (error) {
      // Log operation error using shared utility
      RepositoryLoggingUtil.logOperationError(
        this.logger,
        operation,
        logContext,
        error as Error,
        'HIGH',
      );

      // Handle and return the classified error using shared utility
      return handleRepositoryError(error);
    }
  }
}
