// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { ChannelSnapshotProps } from '../../domain/props';
import { DetailChannelResponse } from '../dtos';

/**
 * Channel Snapshot to DTO Mappers
 * Reusable mapping functions for converting domain snapshots to DTOs
 */

/**
 * Map channel snapshot to DTO response
 */
export const mapChannelSnapshotToDto = (
  snapshot: ChannelSnapshotProps,
): DetailChannelResponse => {
  return {
    code: snapshot.code,
    name: snapshot.name,
    workspaceCode: snapshot.workspaceCode,
    isPrivate: snapshot.isPrivate,
    isDm: snapshot.isDm,
    topic: snapshot.topic,
    purpose: snapshot.purpose,
    subscribedEvents: snapshot.subscribedEvents,
    enabled: snapshot.enabled,
  };
};
