// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Module } from '@nestjs/common';
import { RouterModule, Routes } from '@nestjs/core';
import { ChannelModule } from '../../channel.module';
/**
 * Channel Router Configuration
 *
 * Configures nested routing for channel endpoints to follow
 * the module monolith structure: /notification/slack-request/api/v1/channels
 *
 * This replaces flat routing (/api/v1/channels) with contextual routing
 * that reflects the bounded context hierarchy.
 */

// Define the routing structure for the channel bounded context
const channelRoutes: Routes = [
  {
    path: 'notification',
    children: [
      {
        path: 'slack-request',
        children: [
          {
            path: 'api/v1',
            module: ChannelModule,
          },
        ],
      },
    ],
  },
];

/**
 * Channel Router Module
 *
 * Provides nested routing configuration for channel endpoints.
 * This module should be imported in the main app module to enable
 * the nested route structure.
 *
 * Routes configured:
 * - /notification/slack-request/api/v1/channels (GET)
 * - /notification/slack-request/api/v1/channels/:code (GET)
 * - /notification/slack-request/health/projectors/channel (GET)
 * - /notification/slack-request/health/projectors/channel/metrics (GET)
 */
@Module({
  imports: [ChannelModule, RouterModule.register(channelRoutes)],
  exports: [ChannelModule],
})
export class ChannelRouterModule {}
