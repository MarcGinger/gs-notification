// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { ChannelErrors } from '../errors/channel.errors';

/**
 * WorkspaceCode Value Object
 * Represents a validated workspaceCode with business rules
 */
export const ChannelWorkspaceCode = createStringVO({
  name: 'WorkspaceCode',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  maxLength: 32,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    ChannelErrors.INVALID_WORKSPACE_CODE,
    'WorkspaceCode',
  ),
});

/**
 * Public instance type for WorkspaceCode
 */
export type ChannelWorkspaceCode = StringVOInstance;

// Convenience creators
export const createChannelWorkspaceCode = (s: string) =>
  ChannelWorkspaceCode.create(s);
export const channelWorkspaceCodeFrom = (v: unknown) =>
  ChannelWorkspaceCode.from(v);
