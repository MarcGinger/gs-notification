// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { EventMetadata } from 'src/shared/domain/events';

/**
 * Template Deleted Event Payload
 * Contains identifying information about the deleted template
 */
export interface TemplateDeletedEventPayload {
  code: string;
  deletedAt: Date;
  version: number;
  metadata: EventMetadata;
}

/**
 * Template Deleted Domain Event
 *
 * Emitted when a template is successfully deleted.
 * Contains identifying information and deletion metadata.
 */
export class TemplateDeletedEvent {
  public readonly eventType = 'NotificationSlackRequestTemplateDeleted.v1';
  public readonly eventVersion = 'v1';

  constructor(public readonly payload: TemplateDeletedEventPayload) {}

  // Factory method with security context
  static create(
    data: Omit<TemplateDeletedEventPayload, 'metadata'>,
    baseMetadata: EventMetadata,
  ): TemplateDeletedEvent {
    const metadata: EventMetadata = {
      ...baseMetadata,
      eventVersion: '1.0',
      source: 'notification.domain.template',
    };

    return new TemplateDeletedEvent({
      ...data,
      metadata,
    });
  }

  get code(): string {
    return this.payload.code;
  }

  get deletedAt(): Date {
    return this.payload.deletedAt;
  }

  get version(): number {
    return this.payload.version;
  }

  get metadata(): EventMetadata {
    return this.payload.metadata;
  }
}
