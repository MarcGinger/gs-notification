// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  RecordVOInstance,
  createRecordVO,
  createRecordVOErrors,
} from 'src/shared/domain/value-objects';
import { TemplateErrors } from '../errors/template.errors';

/**
 * SamplePayload Record Value Object
 * Validates a plain object (Record<string, unknown>); deepFreeze.
 */
export const TemplateSamplePayload = createRecordVO({
  name: 'SamplePayload',
  // JSON key validation - must start with letter/underscore, contain only alphanumeric, underscore
  keyPattern: /^[a-zA-Z_][a-zA-Z0-9_]*$/,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createRecordVOErrors(
    TemplateErrors.INVALID_SAMPLE_PAYLOAD,
    'SamplePayload',
  ),
});

/** Public instance type for SamplePayload */
export type TemplateSamplePayload = RecordVOInstance;

// Convenience creators
export const createTemplateSamplePayload = (v: Record<string, unknown>) =>
  TemplateSamplePayload.create(v);

export const templateSamplePayloadFrom = (v: unknown) =>
  TemplateSamplePayload.from(v);
