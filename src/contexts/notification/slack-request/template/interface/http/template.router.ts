// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Module } from '@nestjs/common';
import { RouterModule, Routes } from '@nestjs/core';
import { TemplateModule } from '../../template.module';
/**
 * Template Router Configuration
 *
 * Configures nested routing for template endpoints to follow
 * the module monolith structure: /notification/slack-request/api/v1/templates
 *
 * This replaces flat routing (/api/v1/templates) with contextual routing
 * that reflects the bounded context hierarchy.
 */

// Define the routing structure for the template bounded context
const templateRoutes: Routes = [
  {
    path: 'notification',
    children: [
      {
        path: 'slack-request',
        children: [
          {
            path: 'api/v1',
            module: TemplateModule,
          },
        ],
      },
    ],
  },
];

/**
 * Template Router Module
 *
 * Provides nested routing configuration for template endpoints.
 * This module should be imported in the main app module to enable
 * the nested route structure.
 *
 * Routes configured:
 * - /notification/slack-request/api/v1/templates (GET)
 * - /notification/slack-request/api/v1/templates/:code (GET)
 * - /notification/slack-request/health/projectors/template (GET)
 * - /notification/slack-request/health/projectors/template/metrics (GET)
 */
@Module({
  imports: [TemplateModule, RouterModule.register(templateRoutes)],
  exports: [TemplateModule],
})
export class TemplateRouterModule {}
