// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { QueryHandler, IQueryHandler } from '@nestjs/cqrs';
import { DetailTemplateResponse } from '../dtos';
import { GetTemplateUseCase } from '../use-cases';
import { GetTemplateQuery } from '../queries';
import { Result, DomainError } from 'src/shared/errors';

@QueryHandler(GetTemplateQuery)
export class GetTemplateHandler implements IQueryHandler<GetTemplateQuery> {
  constructor(private readonly getTemplateUseCase: GetTemplateUseCase) {}

  async execute(
    query: GetTemplateQuery,
  ): Promise<Result<DetailTemplateResponse | null, DomainError>> {
    return this.getTemplateUseCase.execute({
      user: query.user,
      code: query.code,
      correlationId: query.correlationId || 'query-handler',
    });
  }
}
