// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Module } from '@nestjs/common';
import { RouterModule, Routes } from '@nestjs/core';
import { FullMessageModule } from '../../full-message.module';
import { FullMessageProjectorModule } from '../../full-message-projector.module';
/**
 * Full message Router Configuration
 *
 * Configures nested routing for full-message endpoints to follow
 * the module monolith structure: /notification/slack-request/api/v1/full-messages
 *
 * This replaces flat routing (/api/v1/full-messages) with contextual routing
 * that reflects the bounded context hierarchy.
 */

// Define the routing structure for the full-message bounded context
const fullMessageRoutes: Routes = [
  {
    path: 'notification',
    children: [
      {
        path: 'slack-request',
        children: [
          {
            path: 'full-messages',
            module: FullMessageModule,
          },
          {
            path: 'health',
            module: FullMessageProjectorModule,
          },
        ],
      },
    ],
  },
];

/**
 * Full message Router Module
 *
 * Provides nested routing configuration for full-message endpoints.
 * This module should be imported in the main app module to enable
 * the nested route structure.
 *
 * Routes configured:
 * - /notification/slack-request/api/v1/full-messages (GET, POST)
 * - /notification/slack-request/api/v1/full-messages/:code (GET)
 * - /notification/slack-request/health/projectors/fullMessage (GET)
 * - /notification/slack-request/health/projectors/full-message/metrics (GET)
 */
@Module({
  imports: [
    FullMessageModule,
    FullMessageProjectorModule,
    RouterModule.register(fullMessageRoutes),
  ],
  exports: [FullMessageModule, FullMessageProjectorModule],
})
export class FullMessageRouterModule {}
