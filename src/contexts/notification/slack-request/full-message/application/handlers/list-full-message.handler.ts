// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { QueryHandler, IQueryHandler } from '@nestjs/cqrs';
import { Injectable } from '@nestjs/common';
import { DomainError, Result } from 'src/shared/errors';
import { ListFullMessageQuery } from '../queries';
import { ListFullMessageUseCase } from '../use-cases';
import { FullMessagePageResponse } from '../dtos';

/**
 * ✅ Enhanced CQRS Query Handler with use case delegation
 * Follows the pattern: Handler → Use Case → Repository
 */
@Injectable()
@QueryHandler(ListFullMessageQuery)
export class ListFullMessageHandler
  implements IQueryHandler<ListFullMessageQuery>
{
  constructor(
    private readonly listFullMessageUseCase: ListFullMessageUseCase,
  ) {}

  /**
   * Handle the list fullMessage query by delegating to the use case
   */
  async execute(
    query: ListFullMessageQuery,
  ): Promise<Result<FullMessagePageResponse, DomainError>> {
    // ✅ Delegate to use case for business logic
    return await this.listFullMessageUseCase.execute({
      user: query.user,
      filter: query.filter,
      correlationId: query.correlationId || 'unknown',
    });
  }
}
