// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { QueryHandler, IQueryHandler } from '@nestjs/cqrs';
import { DetailFullMessageResponse } from '../dtos';
import { GetFullMessageUseCase } from '../use-cases';
import { GetFullMessageQuery } from '../queries';
import { Result, DomainError } from 'src/shared/errors';

@QueryHandler(GetFullMessageQuery)
export class GetFullMessageHandler
  implements IQueryHandler<GetFullMessageQuery>
{
  constructor(private readonly getFullMessageUseCase: GetFullMessageUseCase) {}

  async execute(
    query: GetFullMessageQuery,
  ): Promise<Result<DetailFullMessageResponse | null, DomainError>> {
    return this.getFullMessageUseCase.execute({
      user: query.user,
      code: query.code,
      correlationId: query.correlationId || 'query-handler',
    });
  }
}
