// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

/**
 * Abstract Use Case Contracts for FullMessage Operations
 *
 * These abstract classes are used instead of interfaces to work with NestJS DI.
 * TypeScript interfaces are erased at runtime and cannot be injected.
 */

import { Result, DomainError } from 'src/shared/errors';
import type { IUserToken } from 'src/shared/security';
import type {
  CreateFullMessageProps,
  FailedFullMessageProps,
  SentFullMessageProps,
} from '../../domain/props';
import type {
  DetailFullMessageResponse,
  FullMessagePageResponse,
  ListFullMessageFilterRequest,
} from '../dtos';

export abstract class ICreateFullMessageUseCase {
  abstract execute(params: {
    user: IUserToken;
    props: CreateFullMessageProps;
    correlationId: string;
    authorizationReason: string;
  }): Promise<Result<DetailFullMessageResponse, DomainError>>;
}

export abstract class ISentFullMessageUseCase {
  abstract execute(params: {
    user: IUserToken;
    props: SentFullMessageProps;
    correlationId: string;
    authorizationReason: string;
  }): Promise<Result<DetailFullMessageResponse, DomainError>>;
}

export abstract class IFailedFullMessageUseCase {
  abstract execute(params: {
    user: IUserToken;
    props: FailedFullMessageProps;
    correlationId: string;
    authorizationReason: string;
  }): Promise<Result<DetailFullMessageResponse, DomainError>>;
}

export abstract class IGetFullMessageUseCase {
  abstract execute(params: {
    user: IUserToken;
    id: string;
    correlationId: string;
  }): Promise<Result<DetailFullMessageResponse, DomainError>>;
}
export abstract class IListFullMessageUseCase {
  abstract execute(params: {
    user: IUserToken;
    filter?: ListFullMessageFilterRequest;
    correlationId: string;
  }): Promise<Result<FullMessagePageResponse, DomainError>>;
}
