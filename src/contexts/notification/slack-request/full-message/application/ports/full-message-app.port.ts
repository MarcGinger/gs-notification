// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

/**
 * Application Port for FullMessage outcome reporting
 *
 * Allows the executor (worker) to report delivery outcomes back to the
 * FullMessage bounded context via clean application layer interfaces.
 */
export interface IFullMessageAppPort {
  /**
   * Record successful message delivery
   */
  recordSent(input: {
    id: string; // fullMessageId
    attempts: number;
    tenant?: string;
    correlationId?: string;
    causationId?: string;
  }): Promise<void>;

  /**
   * Record failed message delivery
   */
  recordFailed(input: {
    id: string;
    reason: string; // normalized error code (e.g., 'invalid_auth')
    attempts: number;
    retryable?: boolean;
    lastError?: string;
    tenant?: string;
    correlationId?: string;
    causationId?: string;
  }): Promise<void>;
}

/**
 * DI Token for the FullMessage Application Port
 */
export const FULL_MESSAGE_APP_PORT = Symbol('FULL_MESSAGE_APP_PORT');
