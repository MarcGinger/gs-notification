// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Injectable, Inject } from '@nestjs/common';
import { Redis } from 'ioredis';
import { CacheMetricsCollector } from 'src/shared/infrastructure/projections/cache-optimization';
import { APP_LOGGER, Log, componentLogger, Logger } from 'src/shared/logging';
import { CorrelationUtil } from 'src/shared/utilities/correlation.util';
import { Clock, CLOCK } from 'src/shared/infrastructure/time';
import {
  RepositoryLoggingUtil,
  RepositoryLoggingConfig,
  handleRepositoryError,
  RepositoryOptions,
  isString,
  safeParseJSONArray,
} from 'src/shared/infrastructure/repositories';
import { Result, DomainError, err, ok } from 'src/shared/errors';
import { Option } from 'src/shared/domain/types';
import { ActorContext } from 'src/shared/application/context';
import { RepositoryErrorFactory } from 'src/shared/domain/errors/repository.error';
import { SLACK_REQUEST_DI_TOKENS } from '../../../slack-request.constants';
import { IChannelReader, ChannelReference } from '../../application/ports';

/**
 * Channel Reader Repository - Redis Implementation
 *
 * Bounded Context: Notification/FullMessage
 * Handles channel reference data lookups for FK validation in the FullMessage context using Redis projections
 * as the data source with cluster-safe operations.
 *
 * Benefits:
 * - High-performance Redis backend
 * - Cluster-safe operations with hash tags
 * - Version hint optimization for cache efficiency
 * - Maintains same interface as SQL-based implementation
 * - Comprehensive logging and error handling
 *
 * @domain Notification Context - FullMessage Channel Reader Repository (Redis)
 * @layer Infrastructure
 * @pattern Cross-Context Reference Reader + Redis Projector
 */
@Injectable()
export class ChannelReaderRepository implements IChannelReader {
  private readonly logger: Logger;
  private readonly loggingConfig: RepositoryLoggingConfig;
  private readonly metricsCollector = new CacheMetricsCollector();

  constructor(
    @Inject(APP_LOGGER) baseLogger: Logger,
    @Inject(CLOCK) private readonly clock: Clock,
    @Inject(SLACK_REQUEST_DI_TOKENS.IO_REDIS)
    private readonly redis: Redis,
  ) {
    this.loggingConfig = {
      serviceName: 'FullMessageConfigService',
      component: 'ChannelReaderRepository',
    };
    this.logger = componentLogger(baseLogger, this.loggingConfig.component);

    Log.info(
      this.logger,
      'ChannelReaderRepository initialized with Redis backend',
      {
        component: this.loggingConfig.component,
        redisStatus: this.redis.status,
        clusterSafe: true,
        cacheOptimized: true,
      },
    );
  }

  /**
   * Generate cluster-safe Redis keys with hash tags for locality
   * Uses same pattern as ChannelProjector for consistency
   */
  private generateChannelKey(tenant: string, code: string): string {
    // ✅ Hash-tags ensure key routes to same Redis Cluster slot as projector
    return `notification.slack:v1:{${tenant}}:channel:${code}`;
  }

  /**
   * Parse Redis hash data into ChannelReference
   */
  private parseRedisHashToChannel(
    hashData: Record<string, string>,
  ): ChannelReference | null {
    try {
      if (!hashData || Object.keys(hashData).length === 0) {
        return null;
      }

      // Check for soft deletion
      if (hashData.deletedAt) {
        return null;
      }

      // Parse JSON fields using safeParseJSON utility
      // Extract fields directly from hash data
      return {
        code: hashData.code,
        name: hashData.name,
        workspaceCode: hashData.workspaceCode,
        isPrivate: hashData.isPrivate === 'true',
        isDm: hashData.isDm === 'true',
        topic: hashData.topic || undefined,
        purpose: hashData.purpose || undefined,
        subscribedEvents: safeParseJSONArray(
          hashData.subscribedEvents,
          'subscribedEvents',
          isString,
        ),
        enabled: hashData.enabled === 'true',
      };
    } catch (error) {
      Log.error(
        this.logger,
        'Failed to parse Redis hash data to ChannelReference',
        {
          method: 'parseRedisHashToChannel',
          error: (error as Error).message,
          code: hashData?.code,
        },
      );
      return null;
    }
  }

  /**
   * Helper to create consistent logging context using shared utilities
   */
  private createLogContext(
    operation: string,
    correlationId: string,
    actor: ActorContext,
    additionalContext?: Record<string, unknown>,
  ): Record<string, unknown> {
    return RepositoryLoggingUtil.createLogContext(
      this.loggingConfig,
      this.clock,
      operation,
      correlationId,
      actor,
      additionalContext,
    );
  }

  /**
   * Helper to assess operation risk level using shared utilities
   */
  private assessOperationRisk(operation: string): 'LOW' | 'MEDIUM' | 'HIGH' {
    return RepositoryLoggingUtil.assessOperationRisk(operation);
  }

  /**
   * Find valid Channel Codes for Channel configuration
   * @param actor - The authenticated user context
   * @param options - Optional repository options (correlation ID, timeout, etc.)
   * @returns Result containing available Channel Codes
   */
  async findValidChannelCodes(
    actor: ActorContext,
    options?: RepositoryOptions,
  ): Promise<Result<string[], DomainError>> {
    const operation = 'findValidChannelCodes';
    const riskLevel = this.assessOperationRisk(operation);
    const correlationId =
      options?.correlationId ??
      CorrelationUtil.generateForOperation('channel-list-codes');

    const logContext = this.createLogContext(operation, correlationId, actor, {
      riskLevel,
      customCorrelationId: !!options?.correlationId,
      source: options?.source,
      requestId: options?.requestId,
      dataSource: 'redis-projector',
    });

    // Validate actor context with enhanced security logging
    const validation = RepositoryLoggingUtil.validateActorContext(
      this.logger,
      actor,
      logContext,
    );
    if (!validation.ok) return err(validation.error);

    // Guard tenant explicitly
    if (!actor.tenant) {
      return err(
        RepositoryErrorFactory.validationError('tenant', 'Missing tenant id'),
      );
    }

    // Log successful authorization
    RepositoryLoggingUtil.logAuthorizationSuccess(
      this.logger,
      operation,
      logContext,
      { queryType: 'reference_lookup', scope: 'redis_projection' },
    );

    try {
      Log.debug(this.logger, 'Finding valid channel codes in Redis', {
        ...logContext,
        queryDetails: {
          scope: 'redis_keys',
          method: 'redis.scan',
          pattern: `channel-projector:{${actor.tenant}}:channel:*`,
          clusterSafe: true,
        },
      });

      // Use Redis SCAN to find all channel keys for the tenant
      const pattern = `channel-projector:{${actor.tenant}}:channel:*`;
      const codes: string[] = [];
      const scanIterator = this.redis.scanStream({
        match: pattern,
        count: 100,
      });

      for await (const keys of scanIterator) {
        for (const key of keys as string[]) {
          // Extract code from key pattern: channel-projector:{tenant}:channel:{ code }
          const keyParts = key.split(':');
          if (keyParts.length === 4) {
            const code = keyParts[3];
            // Check if channel is not soft-deleted
            const deletedAt = await this.redis.hget(key, 'deletedAt');
            if (!deletedAt) {
              codes.push(code);
            }
          }
        }
      }

      // Sort the codes for consistent ordering
      codes.sort((a, b) => a.localeCompare(b));
      // Log query metrics using shared utility
      RepositoryLoggingUtil.logQueryMetrics(
        this.logger,
        operation,
        logContext,
        {
          resultCount: codes.length,
          dataQuality: codes.length > 0 ? 'good' : 'empty',
          sampleData: codes.slice(0, 5),
        },
      );

      return ok(codes);
    } catch (error) {
      // Log operation error using shared utility
      RepositoryLoggingUtil.logOperationError(
        this.logger,
        operation,
        logContext,
        error as Error,
        'HIGH',
      );

      // Handle and return the classified error using shared utility
      return handleRepositoryError(error);
    }
  }

  /**
   * Find Channel configuration by Channel code
   * @param actor - The authenticated user context
   * @param code - The Channel code to lookup
   * @param options - Optional repository options (correlation ID, timeout, caching, etc.)
   * @returns Result containing Channel configuration or null if not found
   */
  async findChannelByCode(
    actor: ActorContext,
    code: string,
    options?: RepositoryOptions,
  ): Promise<Result<Option<ChannelReference>, DomainError>> {
    const operation = 'findChannelByCode';
    const riskLevel = this.assessOperationRisk(operation);
    const correlationId =
      options?.correlationId ??
      CorrelationUtil.generateForOperation('channel-find-by-code');

    const logContext = this.createLogContext(operation, correlationId, actor, {
      riskLevel,
      targetCode: code,
      customCorrelationId: !!options?.correlationId,
      source: options?.source,
      requestId: options?.requestId,
      dataSource: 'redis-projector',
    });

    try {
      // Enhanced validation with security logging using shared utilities
      const actorValidation = RepositoryLoggingUtil.validateActorContext(
        this.logger,
        actor,
        logContext,
      );

      if (!actorValidation.ok) return err(actorValidation.error);

      // Guard tenant explicitly
      if (!actor.tenant) {
        return err(
          RepositoryErrorFactory.validationError('tenant', 'Missing tenant id'),
        );
      }

      // Enhanced input validation
      if (!code) {
        Log.warn(this.logger, 'Invalid channel code provided', {
          ...logContext,
          validationError: 'INVALID_CHANNEL_CODE',
          inputValidation: 'failed',
        });
        return err(
          RepositoryErrorFactory.validationError(
            'Channel code is required',
            'INVALID_CHANNEL_CODE',
          ),
        );
      }

      RepositoryLoggingUtil.logAuthorizationSuccess(
        this.logger,
        'Channel lookup',
        logContext,
        {
          dataAccess: 'read',
          targetCode: code,
        },
      );

      // Fetch from Redis using cluster-safe key
      const redisKey = this.generateChannelKey(actor.tenant, code);
      const hashData = await this.redis.hgetall(redisKey);

      if (!hashData || Object.keys(hashData).length === 0) {
        RepositoryLoggingUtil.logQueryMetrics(
          this.logger,
          'Channel lookup',
          logContext,
          {
            resultCount: 0,
            dataQuality: 'empty',
          },
        );
        return ok(Option.none());
      }

      const channel = this.parseRedisHashToChannel(hashData);
      if (!channel) {
        return ok(Option.none());
      }

      RepositoryLoggingUtil.logQueryMetrics(
        this.logger,
        'Channel lookup',
        logContext,
        {
          resultCount: 1,
          dataQuality: 'good',
          sampleData: {
            code: channel.code,
            name: channel.name,
            enabled: channel.enabled,
          },
        },
      );

      return ok(Option.some(channel));
    } catch (error) {
      RepositoryLoggingUtil.logOperationError(
        this.logger,
        'Channel lookup',
        logContext,
        error as Error,
        'HIGH',
      );

      // Handle and return the classified error using shared utility
      return handleRepositoryError(error);
    }
  }
}
