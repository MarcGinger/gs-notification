// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Injectable, Inject } from '@nestjs/common';
import { Queue } from 'bullmq';
import { APP_LOGGER, Log, Logger, componentLogger } from 'src/shared/logging';
import { CreateFullMessageRequest } from '../../application/dtos';
import { IUserToken } from 'src/shared/security';
import { FullMessageJobs } from '../processors';
import { Result, DomainError, err, fromError } from 'src/shared/errors';
import { ActorContext } from 'src/shared/application/context';
import { Option } from 'src/shared/domain/types';
import { ConfigErrors } from 'src/shared/config/errors/config.errors';
import { SendFullMessageJob } from './full-message-queue.types';
import {
  IWorkspaceQuery,
  WORKSPACE_QUERY_TOKEN,
} from 'src/contexts/notification/slack-config/workspace/application/ports';
import {
  ITemplateQuery,
  TEMPLATE_QUERY_TOKEN,
} from 'src/contexts/notification/slack-config/template/application/ports';
import {
  IChannelQuery,
  CHANNEL_QUERY_TOKEN,
} from 'src/contexts/notification/slack-config/channel/application/ports';
import {
  IAppConfigQuery,
  APP_CONFIG_QUERY_TOKEN,
} from 'src/contexts/notification/slack-config/app-config/application/ports';
import { DetailWorkspaceResponse } from 'src/contexts/notification/slack-config/workspace/application/dtos';
import { DetailTemplateResponse } from 'src/contexts/notification/slack-config/template/application/dtos';
import { DetailChannelResponse } from 'src/contexts/notification/slack-config/channel/application/dtos';
import { DetailAppConfigResponse } from 'src/contexts/notification/slack-config/app-config/application/dtos';

/**
 * FullMessage Queue Service
 *
 * Enhanced service for adding jobs to the FullMessageQueue with tenant configuration support.
 * Integrates with Redis repositories to enrich job data with workspace, template, channel,
 * and app configuration information needed for message processing.
 *
 * Features:
 * - Job queuing with tenant-aware configuration
 * - Redis-based tenant data enrichment
 * - Comprehensive job validation and error handling
 * - Production-ready logging and monitoring
 */
@Injectable()
export class FullMessageQueueService {
  private readonly logger: Logger;

  constructor(
    @Inject(APP_LOGGER) baseLogger: Logger,
    @Inject('Queue:FullMessageQueue') private readonly queue: Queue,
    @Inject(WORKSPACE_QUERY_TOKEN)
    private readonly workspaceQuery: IWorkspaceQuery,
    @Inject(TEMPLATE_QUERY_TOKEN)
    private readonly templateQuery: ITemplateQuery,
    @Inject(CHANNEL_QUERY_TOKEN)
    private readonly channelQuery: IChannelQuery,
    @Inject(APP_CONFIG_QUERY_TOKEN)
    private readonly appConfigQuery: IAppConfigQuery,
  ) {
    this.logger = componentLogger(baseLogger, 'FullMessageQueueService');

    Log.info(
      this.logger,
      'FullMessageQueueService initialized with Redis repositories',
      {
        component: 'FullMessageQueueService',
        repositoriesWired: {
          workspace: !!this.workspaceQuery,
          template: !!this.templateQuery,
          channel: !!this.channelQuery,
          appConfig: !!this.appConfigQuery,
        },
      },
    );
  }

  /**
   * Enhanced method to queue a send message job with tenant configuration data
   *
   * Fetches workspace, template, channel, and app config from Redis repositories
   * to provide all necessary context for message processing.
   */
  async queueEnrichedSendFullMessage(
    fullMessageCode: string,
    tenant: string,
    workspaceCode: string,
    templateCode?: string,
    channelCode?: string,
    options?: {
      delay?: number;
      priority?: number;
    },
  ): Promise<Result<string, DomainError>> {
    const actor: ActorContext = {
      tenant,
      userId: 'system',
      tenant_userId: 'system',
    };

    try {
      // Fetch tenant configuration data in parallel
      const [workspaceResult, templateResult, channelResult, appConfigResult] =
        await Promise.all([
          this.workspaceQuery.findById(actor, workspaceCode),
          templateCode
            ? this.templateQuery.findById(actor, templateCode)
            : Promise.resolve({
                ok: true,
                value: Option.none(),
              } as Result<Option<DetailTemplateResponse>, DomainError>),
          channelCode
            ? this.channelQuery.findById(actor, channelCode)
            : Promise.resolve({
                ok: true,
                value: Option.none(),
              } as Result<Option<DetailChannelResponse>, DomainError>),
          this.appConfigQuery.findById(actor, workspaceCode),
        ]);

      // Check for fetch errors
      if (!workspaceResult.ok) {
        Log.error(this.logger, 'Failed to fetch workspace configuration', {
          method: 'queueEnrichedSendFullMessage',
          fullMessageCode,
          tenant,
          workspaceCode,
          error: workspaceResult.error.detail,
        });
        return workspaceResult;
      }

      if (templateCode && templateResult && !templateResult.ok) {
        Log.error(this.logger, 'Failed to fetch template configuration', {
          method: 'queueEnrichedSendFullMessage',
          fullMessageCode,
          tenant,
          templateCode,
          error: templateResult.error.detail,
        });
        return templateResult;
      }

      if (channelCode && channelResult && !channelResult.ok) {
        Log.error(this.logger, 'Failed to fetch channel configuration', {
          method: 'queueEnrichedSendFullMessage',
          fullMessageCode,
          tenant,
          channelCode,
          error: channelResult.error.detail,
        });
        return channelResult;
      }

      if (!appConfigResult.ok) {
        Log.error(this.logger, 'Failed to fetch app configuration', {
          method: 'queueEnrichedSendFullMessage',
          fullMessageCode,
          tenant,
          workspaceCode,
          error: appConfigResult.error.detail,
        });
        return appConfigResult;
      }

      // Extract configuration data using proper Option handling
      const workspace = Option.isSome(workspaceResult.value)
        ? workspaceResult.value.value
        : null;
      const template =
        templateResult &&
        templateResult.ok &&
        Option.isSome(templateResult.value)
          ? templateResult.value.value
          : null;
      const channel =
        channelResult && channelResult.ok && Option.isSome(channelResult.value)
          ? channelResult.value.value
          : null;
      const appConfig = Option.isSome(appConfigResult.value)
        ? appConfigResult.value.value
        : null;

      if (!workspace) {
        const error = `Workspace not found: ${workspaceCode}`;
        Log.error(this.logger, error, {
          method: 'queueEnrichedSendFullMessage',
          fullMessageCode,
          tenant,
          workspaceCode,
        });
        return err(
          fromError(ConfigErrors.VALIDATION_FAILED, error, {
            configKey: 'workspace',
            fullMessageCode,
            tenant,
            workspaceCode,
          }),
        );
      }

      // Create enriched job data
      const enrichedJobData: FullMessageJobs['send-full-message'] & {
        tenantConfig?: {
          workspace: DetailWorkspaceResponse;
          template?: DetailTemplateResponse;
          channel?: DetailChannelResponse;
          appConfig?: DetailAppConfigResponse;
        };
      } = {
        fullMessageCode,
        tenant,
        tenantConfig: {
          workspace,
          template: template || undefined,
          channel: channel || undefined,
          appConfig: appConfig || undefined,
        },
      };

      const job = await this.queue.add('send-full-message', enrichedJobData, {
        delay: options?.delay || 0,
        priority: options?.priority || 0,
        removeOnComplete: 100,
        removeOnFail: 50,
        attempts: 3,
        backoff: {
          type: 'exponential',
          delay: 2000,
        },
      });

      Log.info(this.logger, 'Queued enriched send fullMessage request job', {
        method: 'queueEnrichedSendFullMessage',
        jobId: job.id,
        fullMessageCode,
        tenant,
        configurationLoaded: {
          workspace: !!workspace,
          template: !!template,
          channel: !!channel,
          appConfig: !!appConfig,
        },
        delay: options?.delay || 0,
        priority: options?.priority || 0,
      });

      return { ok: true, value: job.id?.toString() || 'unknown' };
    } catch (error) {
      const e = error as Error;
      Log.error(
        this.logger,
        'Error queuing enriched send fullMessage request',
        {
          method: 'queueEnrichedSendFullMessage',
          fullMessageCode,
          tenant,
          error: e.message,
          stack: e.stack,
        },
      );
      return err(
        fromError(ConfigErrors.VALIDATION_FAILED, e, {
          configKey: 'queue',
          fullMessageCode,
          tenant,
        }),
      );
    }
  }

  /**
   * Queue a fullMessage request creation job
   */
  async queueCreateFullMessage(
    user: IUserToken,
    request: CreateFullMessageRequest,
    options?: {
      idempotencyKey?: string;
      delay?: number;
      priority?: number;
    },
  ): Promise<string> {
    const jobData: FullMessageJobs['create-full-message'] = {
      user,
      request,
      options: {
        idempotencyKey: options?.idempotencyKey,
      },
    };

    const job = await this.queue.add('create-full-message', jobData, {
      delay: options?.delay || 0,
      priority: options?.priority || 0,
      removeOnComplete: 100,
      removeOnFail: 50,
      attempts: 3,
      backoff: {
        type: 'exponential',
        delay: 2000,
      },
    });

    Log.info(this.logger, 'Queued create fullMessage request job', {
      method: 'queueCreateFullMessage',
      jobId: job.id,
      tenant: user.tenant,
      workspaceCode: request.workspaceCode,
      templateCode: request.templateCode,
      channelCode: request.channelCode,
      delay: options?.delay || 0,
      priority: options?.priority || 0,
    });

    return job.id?.toString() || 'unknown';
  }

  /**
   * Queue a message sending job
   */
  async queueSendFullMessage(
    fullMessageCode: string,
    tenant: string,
    options?: {
      delay?: number;
      priority?: number;
    },
  ): Promise<string> {
    const jobData: FullMessageJobs['send-full-message'] = {
      fullMessageCode,
      tenant,
    };

    const job = await this.queue.add('send-full-message', jobData, {
      delay: options?.delay || 0,
      priority: options?.priority || 0,
      removeOnComplete: 100,
      removeOnFail: 50,
      attempts: 3,
      backoff: {
        type: 'exponential',
        delay: 2000,
      },
    });

    Log.info(this.logger, 'Queued send fullMessage request job', {
      method: 'queueSendFullMessage',
      jobId: job.id,
      fullMessageCode,
      tenant,
      delay: options?.delay || 0,
      priority: options?.priority || 0,
    });

    return job.id?.toString() || 'unknown';
  }

  /**
   * Queue a retry job for failed fullMessage requests
   */
  async queueRetryFailedFullMessage(
    fullMessageCode: string,
    tenant: string,
    retryReason: string,
    options?: {
      delay?: number;
      priority?: number;
    },
  ): Promise<string> {
    const jobData: FullMessageJobs['retry-failed-full-message'] = {
      fullMessageCode,
      tenant,
      retryReason,
    };

    const job = await this.queue.add('retry-failed-full-message', jobData, {
      delay: options?.delay || 30000, // Default 30s delay for retries
      priority: options?.priority || 0,
      removeOnComplete: 100,
      removeOnFail: 50,
      attempts: 2, // Fewer attempts for retries
      backoff: {
        type: 'exponential',
        delay: 5000,
      },
    });

    Log.info(this.logger, 'Queued retry failed fullMessage request job', {
      method: 'queueRetryFailedFullMessage',
      jobId: job.id,
      fullMessageCode,
      tenant,
      retryReason,
      delay: options?.delay || 30000,
      priority: options?.priority || 0,
    });

    return job.id?.toString() || 'unknown';
  }

  /**
   * Simple BullMQ job enqueue with minimal payload (following refinement.md)
   *
   * Enqueues a SendFullMessageJob with minimal payload following the reference-only,
   * secret-free principles. The worker will resolve configuration by codes.
   */
  async enqueueSimpleSendFullMessageJob(
    job: SendFullMessageJob,
    options?: {
      priority?: number;
      delay?: number;
      attempts?: number;
    },
  ): Promise<{
    success: boolean;
    jobId?: string;
    error?: string;
  }> {
    const { fullMessageCode, tenant } = job;

    try {
      Log.debug(this.logger, 'Enqueuing simple SendFullMessageJob', {
        method: 'enqueueSimpleSendFullMessageJob',
        fullMessageCode,
        tenant,
        threadTs: job.threadTs,
        options,
      });

      const bullmqJob = await this.queue.add('SendFullMessageJob', job, {
        priority: options?.priority || 0,
        delay: options?.delay || 0,
        attempts: options?.attempts || 1,
        // Minimal job options - no complex retry logic here
        // Worker handles retries via send-lock SETNX patterns
      });

      Log.info(this.logger, 'Successfully enqueued simple SendFullMessageJob', {
        method: 'enqueueSimpleSendFullMessageJob',
        fullMessageCode,
        tenant,
        jobId: bullmqJob.id,
        priority: options?.priority || 0,
        delay: options?.delay || 0,
        attempts: options?.attempts || 1,
      });

      return {
        success: true,
        jobId: bullmqJob.id?.toString(),
      };
    } catch (error) {
      const errorMessage =
        error instanceof Error ? error.message : 'Unknown error';

      Log.error(this.logger, 'Failed to enqueue simple SendFullMessageJob', {
        method: 'enqueueSimpleSendFullMessageJob',
        fullMessageCode,
        tenant,
        error: errorMessage,
      });

      return {
        success: false,
        error: errorMessage,
      };
    }
  }

  /**
   * Get queue statistics and health information
   */
  async getQueueInfo(): Promise<{
    waiting: number;
    active: number;
    completed: number;
    failed: number;
    delayed: number;
  }> {
    const [waiting, active, completed, failed, delayed] = await Promise.all([
      this.queue.getWaiting(),
      this.queue.getActive(),
      this.queue.getCompleted(),
      this.queue.getFailed(),
      this.queue.getDelayed(),
    ]);

    return {
      waiting: waiting.length,
      active: active.length,
      completed: completed.length,
      failed: failed.length,
      delayed: delayed.length,
    };
  }
}
