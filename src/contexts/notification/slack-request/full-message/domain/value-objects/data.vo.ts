// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  RecordVOInstance,
  createRecordVO,
  createRecordVOErrors,
} from 'src/shared/domain/value-objects';
import { FullMessageErrors } from '../errors/full-message.errors';

/**
 * Data Record Value Object
 * Validates a plain object (Record<string, unknown>); deepFreeze.
 */
export const FullMessageData = createRecordVO({
  name: 'Data',
  // JSON key validation - must start with letter/underscore, contain only alphanumeric, underscore
  keyPattern: /^[a-zA-Z_][a-zA-Z0-9_]*$/,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createRecordVOErrors(FullMessageErrors.INVALID_DATA, 'Data'),
});

/** Public instance type for Data */
export type FullMessageData = RecordVOInstance;

// Convenience creators
export const createFullMessageData = (v: Record<string, unknown>) =>
  FullMessageData.create(v);

export const fullMessageDataFrom = (v: unknown) => FullMessageData.from(v);
