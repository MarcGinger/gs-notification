// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DomainError, Result, ok, err } from 'src/shared/errors';
import {
  createIntegerVO,
  createIntegerVOErrors,
  IntegerVOInstance,
  createDateTimeVO,
  createDateTimeVOErrors,
  DateTimeVOInstance,
} from 'src/shared/domain/value-objects';
import { Clock } from 'src/shared/domain/clock';
import { FullMessageErrors } from '../errors/full-message.errors';

/**
 * FullMessage Instance Value Objects
 *
 * Instance VOs for FullMessage aggregate lifecycle management.
 * Includes creation timestamps, update timestamps, and version tracking.
 * Follows the Clock injection pattern for deterministic testing.
 */

// ===========================================
// FullMessage Created At Instance VO
// ===========================================

const FullMessageCreatedAtErrors = createDateTimeVOErrors(
  FullMessageErrors.CREATED_AT_REQUIRED,
  'FullMessage Created At',
);

export const FullMessageCreatedAt = createDateTimeVO({
  name: 'FullMessageCreatedAt',
  allowFuture: false, // Cannot create full_message in the future
  allowPast: true, // Can create with past timestamps (data migration)
  customValidation: (date: Date): Result<void, DomainError> => {
    // Optional: Add domain-specific creation time validation
    // For example, cannot be more than 1 year in the past
    const oneYearAgo = new Date();
    oneYearAgo.setFullYear(oneYearAgo.getFullYear() - 1);

    if (date < oneYearAgo) {
      return err({
        ...FullMessageErrors.CREATED_AT_REQUIRED,
        detail:
          'FullMessage creation date cannot be more than 1 year in the past',
        context: { value: date, maxPastDate: oneYearAgo },
      } as DomainError);
    }

    return ok(undefined);
  },
  errors: FullMessageCreatedAtErrors,
});

export type FullMessageCreatedAt = DateTimeVOInstance;

// ===========================================
// FullMessage Updated At Instance VO
// ===========================================

const FullMessageUpdatedAtErrors = createDateTimeVOErrors(
  FullMessageErrors.UPDATED_AT_REQUIRED,
  'FullMessage Updated At',
);

export const FullMessageUpdatedAt = createDateTimeVO({
  name: 'FullMessageUpdatedAt',
  allowFuture: false, // Cannot update full_message in the future
  allowPast: true, // Can update with past timestamps (data correction)
  errors: FullMessageUpdatedAtErrors,
});

export type FullMessageUpdatedAt = DateTimeVOInstance;

// ===========================================
// FullMessage Version Instance VO
// ===========================================

const FullMessageVersionErrors = createIntegerVOErrors(
  FullMessageErrors.INVALID_CODE_DATA, // Reusing existing error until specific version error is added
  'FullMessage Version',
);

export const FullMessageVersion = createIntegerVO({
  name: 'FullMessageVersion',
  min: 0, // Version starts at 0
  allowNegative: false,
  errors: FullMessageVersionErrors,
});

export type FullMessageVersion = IntegerVOInstance;

// ===========================================
// Clock-Based Helper Functions (Explicit Dependencies)
// ===========================================

/**
 * Create FullMessageCreatedAt from Clock (explicit dependency)
 * Deterministic and testable - pass a fake clock in tests
 */
export const createdAtNow = (
  clock: Clock,
): Result<FullMessageCreatedAt, DomainError> =>
  FullMessageCreatedAt.create(clock.now());

/**
 * Create FullMessageUpdatedAt from Clock (explicit dependency)
 * Deterministic and testable - pass a fake clock in tests
 */
export const updatedAtNow = (
  clock: Clock,
): Result<FullMessageUpdatedAt, DomainError> =>
  FullMessageUpdatedAt.create(clock.now());

/**
 * Bump version by 1 (domain business rule)
 */
export const bumpVersion = (
  currentVersion: FullMessageVersion,
): Result<FullMessageVersion, DomainError> =>
  FullMessageVersion.create(currentVersion.value + 1);

/**
 * Create initial version (starts at 1 for new aggregates)
 */
export const initialVersion = (): Result<FullMessageVersion, DomainError> =>
  FullMessageVersion.create(1);

// ===========================================
// Direct Creation Functions (Non-Clock Based)
// ===========================================

/**
 * Create FullMessageCreatedAt from Date
 */
export const createFullMessageCreatedAt = (
  date: Date,
): Result<FullMessageCreatedAt, DomainError> =>
  FullMessageCreatedAt.create(date);

/**
 * Create FullMessageUpdatedAt from Date
 */
export const createFullMessageUpdatedAt = (
  date: Date,
): Result<FullMessageUpdatedAt, DomainError> =>
  FullMessageUpdatedAt.create(date);

/**
 * Create FullMessageVersion from number
 */
export const createFullMessageVersion = (
  version: number,
): Result<FullMessageVersion, DomainError> =>
  FullMessageVersion.create(version);

// ===========================================
// Domain Invariants & Validation
// ===========================================

/**
 * Validate that updatedAt is not before createdAt (monotonic time)
 */
export const validateMonotonicTime = (
  createdAt: FullMessageCreatedAt,
  updatedAt: FullMessageUpdatedAt,
): Result<void, DomainError> => {
  if (updatedAt.value < createdAt.value) {
    return err({
      ...FullMessageErrors.UPDATED_AT_REQUIRED,
      detail: 'Updated at cannot be before created at',
      context: {
        createdAt: createdAt.value.toISOString(),
        updatedAt: updatedAt.value.toISOString(),
      },
    } as DomainError);
  }
  return ok(undefined);
};

// ===========================================
// Legacy Compatibility (DEPRECATED)
// ===========================================

/**
 * @deprecated Use createdAtNow(clock) instead for better testability
 * This function hides the Date dependency and makes testing harder
 */
export function createFullMessageCreatedAtNow(): Result<
  FullMessageCreatedAt,
  DomainError
> {
  return FullMessageCreatedAt.create(new Date());
}

/**
 * @deprecated Use updatedAtNow(clock) instead for better testability
 * This function hides the Date dependency and makes testing harder
 */
export function createFullMessageUpdatedAtNow(): Result<
  FullMessageUpdatedAt,
  DomainError
> {
  return FullMessageUpdatedAt.create(new Date());
}
