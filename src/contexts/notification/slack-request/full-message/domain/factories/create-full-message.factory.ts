// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DomainError, Result, err, withContext, ok } from 'src/shared/errors';
import { Clock } from 'src/shared/domain/clock';
import { EventMetadata } from 'src/shared/domain/events';
import { FullMessageAggregate } from '../aggregates';
import { CreateFullMessageProps } from '../props';
import { FullMessageDomainState } from '../state';
import {
  FullMessageCreatedAt,
  FullMessageUpdatedAt,
  FullMessageVersion,
  FullMessageId,
  FullMessageRecipient,
  FullMessageData,
  createFullMessageStatus,
  FullMessageWorkspaceCode,
  FullMessageTemplateCode,
  FullMessageChannelCode,
} from '../value-objects';

/**
 * Enhanced fullMessage entity factory with comprehensive validation and security context
 * Moved from application layer to properly separate business concerns
 */
export function createFullMessageAggregateFromProps(
  props: CreateFullMessageProps,
  metadata: EventMetadata,
  clock: Clock,
  // validatedEntities?: {
  //  channels?: ChannelReference[];
  // },
): Result<FullMessageAggregate, DomainError> {
  // Validate each property by creating value objects
  // Generate a validate identity column UUID
  const idResult = FullMessageId.generate();
  if (!idResult.ok) {
    return err(
      withContext(idResult.error, {
        ...idResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_full_message',
      }),
    );
  }

  const recipientResult = FullMessageRecipient.from(props.recipient);
  if (!recipientResult.ok) {
    return err(
      withContext(recipientResult.error, {
        ...recipientResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_full_message',
        recipient: props.recipient,
      }),
    );
  }

  const dataResult = FullMessageData.from(props.data);
  if (!dataResult.ok) {
    return err(
      withContext(dataResult.error, {
        ...dataResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_full_message',
        data: props.data,
      }),
    );
  }

  const statusResult = createFullMessageStatus('requested');
  if (!statusResult.ok) {
    return err(
      withContext(statusResult.error, {
        ...statusResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_full_message',
        status: 'requested',
      }),
    );
  }

  const workspaceCodeResult = FullMessageWorkspaceCode.from(
    props.workspaceCode,
  );
  if (!workspaceCodeResult.ok) {
    return err(
      withContext(workspaceCodeResult.error, {
        ...workspaceCodeResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_full_message',
        workspaceCode: props.workspaceCode,
      }),
    );
  }

  const templateCodeResult = FullMessageTemplateCode.from(props.templateCode);
  if (!templateCodeResult.ok) {
    return err(
      withContext(templateCodeResult.error, {
        ...templateCodeResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_full_message',
        templateCode: props.templateCode,
      }),
    );
  }

  const channelCodeResult = FullMessageChannelCode.from(props.channelCode);
  if (!channelCodeResult.ok) {
    return err(
      withContext(channelCodeResult.error, {
        ...channelCodeResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_full_message',
        channelCode: props.channelCode,
      }),
    );
  }

  const createdAtResult = FullMessageCreatedAt.create(clock.now());
  if (!createdAtResult.ok) {
    return err(createdAtResult.error);
  }

  const updatedAtResult = FullMessageUpdatedAt.create(clock.now());
  if (!updatedAtResult.ok) {
    return err(updatedAtResult.error);
  }

  const versionResult = FullMessageVersion.create(1); // Initial version is 1
  if (!versionResult.ok) {
    return err(versionResult.error);
  }

  // Create the entity properties with validated value objects
  const entityProps: FullMessageDomainState = {
    id: idResult.value,
    recipient: recipientResult.value,
    data: dataResult.value,
    status: statusResult.value,
    workspaceCode: workspaceCodeResult.value,
    templateCode: templateCodeResult.value,
    channelCode: channelCodeResult.value,
    createdAt: createdAtResult.value,
    updatedAt: updatedAtResult.value,
    version: versionResult.value,
  };

  // Create the entity using the constructor
  return FullMessageAggregate.create(entityProps, clock, metadata);
}
