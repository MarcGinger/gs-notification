// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { EntityIdBase } from 'src/shared/domain/entities';
import { Result, ok, err, DomainError } from 'src/shared/errors';
import { FullMessageSnapshotProps } from '../props';
import { FullMessageDomainState } from '../state';
import { FullMessageErrors } from '../errors/full-message.errors';
import {
  FullMessageChannelCode,
  FullMessageCreatedAt,
  FullMessageUpdatedAt,
  FullMessageVersion,
  FullMessageData,
  FullMessageId,
  FullMessageRecipient,
  FullMessageStatus,
  FullMessageStatusLogic,
  FullMessageStatusValue,
  FullMessageTemplateCode,
  FullMessageWorkspaceCode,
  createFullMessageStatus,
} from '../value-objects';

/**
 * Domain Entity: FullMessage
 *
 * Represents the core FullMessage entity in the notification domain.
 * Encapsulates fullMessage data, identity, and basic entity behavior.
 *
 * This entity follows DDD principles:
 * - Identity: Id as unique identifier
 * - Immutability: Changes create new instances
 * - Encapsulation: Private state with controlled access
 * - Business validation: Domain rules enforced
 *
 * @domain Notification Context - FullMessage Entity
 * @layer Domain Entities
 */

/**
 * FullMessage Entity
 *
 * Core domain entity representing a fullMessage in the notification.
 * Handles fullMessage identity, validation, and state management.
 */
export class FullMessageEntity extends EntityIdBase<
  FullMessageDomainState,
  FullMessageId
> {
  private static clock: { now: () => Date } = { now: () => new Date() };

  public static setClock(c: { now: () => Date }) {
    this.clock = c;
  }

  /**
   * Helper method to create updatedAt VO from date
   */
  private static createUpdatedAt(
    date?: Date,
  ): Result<FullMessageUpdatedAt, DomainError> {
    const targetDate = date || FullMessageEntity.clock.now();
    const result = FullMessageUpdatedAt.from(targetDate);
    if (!result.ok) {
      return err(result.error);
    }
    return ok(result.value);
  }

  /**
   * Helper method to create version VO from number
   */
  private static createVersion(
    versionNumber?: number,
    currentVersion?: FullMessageVersion,
  ): Result<FullMessageVersion, DomainError> {
    const targetVersion = versionNumber ?? currentVersion?.value ?? 1;
    const result = FullMessageVersion.from(targetVersion);
    if (!result.ok) {
      return err(result.error);
    }
    return ok(result.value);
  }

  /**
   * Helper method to create updated entity with common logic
   */
  private createUpdatedEntity(
    fieldUpdates: Partial<FullMessageDomainState>,
    updatedAt?: Date,
    version?: number,
  ): Result<FullMessageEntity, DomainError> {
    const updatedAtResult = FullMessageEntity.createUpdatedAt(updatedAt);
    if (!updatedAtResult.ok) {
      return err(updatedAtResult.error);
    }

    const versionResult = FullMessageEntity.createVersion(
      version,
      this.props.version,
    );
    if (!versionResult.ok) {
      return err(versionResult.error);
    }

    const updatedProps: FullMessageDomainState = {
      ...this.props,
      ...fieldUpdates,
      updatedAt: updatedAtResult.value,
      version: versionResult.value,
    };

    return FullMessageEntity.create(updatedProps);
  }

  private constructor(props: FullMessageDomainState) {
    super(props, props.id);
  }

  /**
   * Factory method to create a new fullMessage entity
   *
   * @param props - FullMessage properties
   * @returns Result containing FullMessageEntity or DomainError
   */
  public static create(
    props: FullMessageDomainState,
  ): Result<FullMessageEntity, DomainError> {
    // Validate required properties
    const validationResult = FullMessageEntity.validate(props);
    if (validationResult.ok === false) {
      return err(validationResult.error);
    }

    return ok(new FullMessageEntity(props));
  }

  /**
   * Factory method to reconstitute a fullMessage entity from persistence
   *
   * @param props - FullMessage properties from database
   * @returns FullMessageEntity instance
   */
  public static reconstitute(props: FullMessageDomainState): FullMessageEntity {
    return new FullMessageEntity(props);
  }

  /**
   * Factory method to reconstitute entity from snapshot
   *
   * @param snapshot - FullMessage snapshot properties
   * @returns Result containing FullMessageEntity or DomainError
   */
  public static fromSnapshot(
    snapshot: FullMessageSnapshotProps,
  ): Result<FullMessageEntity, DomainError> {
    const idResult = FullMessageId.from(snapshot.id);
    if (!idResult.ok) {
      return err(idResult.error);
    }
    const recipientResult = FullMessageRecipient.from(snapshot.recipient);
    if (!recipientResult.ok) {
      return err(recipientResult.error);
    }
    const dataResult = FullMessageData.from(snapshot.data);
    if (!dataResult.ok) {
      return err(dataResult.error);
    }
    const statusResult = createFullMessageStatus(snapshot.status);
    if (!statusResult.ok) {
      return err(statusResult.error);
    }
    const workspaceCodeResult = FullMessageWorkspaceCode.from(
      snapshot.workspaceCode,
    );
    if (!workspaceCodeResult.ok) {
      return err(workspaceCodeResult.error);
    }
    const templateCodeResult = FullMessageTemplateCode.from(
      snapshot.templateCode,
    );
    if (!templateCodeResult.ok) {
      return err(templateCodeResult.error);
    }
    const channelCodeResult = FullMessageChannelCode.from(snapshot.channelCode);
    if (!channelCodeResult.ok) {
      return err(channelCodeResult.error);
    }
    const createdAtResult = FullMessageCreatedAt.from(snapshot.createdAt);
    if (!createdAtResult.ok) {
      return err(createdAtResult.error);
    }

    const updatedAtResult = FullMessageUpdatedAt.from(snapshot.updatedAt);
    if (!updatedAtResult.ok) {
      return err(updatedAtResult.error);
    }

    const versionResult = FullMessageVersion.from(snapshot.version);
    if (!versionResult.ok) {
      return err(versionResult.error);
    }

    const props: FullMessageDomainState = {
      id: idResult.value,
      recipient: recipientResult.value,
      data: dataResult.value,
      status: statusResult.value,
      workspaceCode: workspaceCodeResult.value,
      templateCode: templateCodeResult.value,
      channelCode: channelCodeResult.value,
      createdAt: createdAtResult.value,
      updatedAt: updatedAtResult.value,
      version: versionResult.value,
    };

    return FullMessageEntity.create(props);
  }

  /**
   * Validates fullMessage entity properties
   *
   * @param props - Properties to validate
   * @returns Validation result
   */
  private static validate(
    props: FullMessageDomainState,
  ): Result<void, DomainError> {
    // Basic validation
    if (!props.id) {
      return err(FullMessageErrors.INVALID_ID_DATA);
    }
    if (!props.workspaceCode) {
      return err(FullMessageErrors.INVALID_WORKSPACE_CODE_DATA);
    }
    if (!props.templateCode) {
      return err(FullMessageErrors.INVALID_TEMPLATE_CODE_DATA);
    }

    return ok(undefined);
  }

  // ======================
  // Getters (Public API)
  // ======================

  public get id(): FullMessageId {
    return this.props.id;
  }

  public get recipient(): FullMessageRecipient | undefined {
    return this.props.recipient;
  }

  public get data(): FullMessageData | undefined {
    return this.props.data;
  }

  public get status(): FullMessageStatus | undefined {
    return this.props.status;
  }

  public get workspaceCode(): FullMessageWorkspaceCode {
    return this.props.workspaceCode;
  }

  public get templateCode(): FullMessageTemplateCode {
    return this.props.templateCode;
  }

  public get channelCode(): FullMessageChannelCode | undefined {
    return this.props.channelCode;
  }

  public get createdAt(): FullMessageCreatedAt {
    return this.props.createdAt;
  }

  public get updatedAt(): FullMessageUpdatedAt {
    return this.props.updatedAt;
  }

  public get version(): FullMessageVersion {
    return this.props.version;
  }

  // ======================
  // Simple Update Methods (No Business Logic)
  // ======================

  /**
   * Creates a new entity with updated recipient (pure state transition)
   *
   * @param recipient - New recipient value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<FullMessageEntity, DomainError>
   */
  public withRecipient(
    recipient: FullMessageRecipient,
    updatedAt?: Date,
    version?: number,
  ): Result<FullMessageEntity, DomainError> {
    return this.createUpdatedEntity({ recipient }, updatedAt, version);
  }

  /**
   * Creates a new entity with updated data (pure state transition)
   *
   * @param data - New data value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<FullMessageEntity, DomainError>
   */
  public withData(
    data: FullMessageData,
    updatedAt?: Date,
    version?: number,
  ): Result<FullMessageEntity, DomainError> {
    return this.createUpdatedEntity({ data }, updatedAt, version);
  }

  /**
   * Creates a new entity with updated status (with basic transition validation)
   *
   * @param status - New status value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<FullMessageEntity, DomainError>
   */
  public withStatus(
    status: FullMessageStatus,
    updatedAt?: Date,
    version?: number,
  ): Result<FullMessageEntity, DomainError> {
    // Basic validation - business rules handled by aggregate
    if (this.props.status) {
      // If current status exists, validate transition
      const currentStatus = this.props.status.value;
      const targetStatus = status.value;

      if (!FullMessageStatusLogic.canTransition(currentStatus, targetStatus)) {
        return err({
          ...FullMessageErrors.INVALID_STATUS_TRANSITION,
          context: {
            currentStatus,
            targetStatus,
            validTransitions:
              FullMessageStatusLogic.getValidTransitions(currentStatus),
          },
        });
      }
    }

    return this.createUpdatedEntity({ status }, updatedAt, version);
  }

  /**
   * Creates a new entity with updated workspaceCode (pure state transition)
   *
   * @param workspaceCode - New workspace_code value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<FullMessageEntity, DomainError>
   */
  public withWorkspaceCode(
    workspaceCode: FullMessageWorkspaceCode,
    updatedAt?: Date,
    version?: number,
  ): Result<FullMessageEntity, DomainError> {
    return this.createUpdatedEntity({ workspaceCode }, updatedAt, version);
  }

  /**
   * Creates a new entity with updated templateCode (pure state transition)
   *
   * @param templateCode - New template_code value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<FullMessageEntity, DomainError>
   */
  public withTemplateCode(
    templateCode: FullMessageTemplateCode,
    updatedAt?: Date,
    version?: number,
  ): Result<FullMessageEntity, DomainError> {
    return this.createUpdatedEntity({ templateCode }, updatedAt, version);
  }

  /**
   * Creates a new entity with updated channelCode (pure state transition)
   *
   * @param channelCode - New channel_code value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<FullMessageEntity, DomainError>
   */
  public withChannelCode(
    channelCode: FullMessageChannelCode,
    updatedAt?: Date,
    version?: number,
  ): Result<FullMessageEntity, DomainError> {
    return this.createUpdatedEntity({ channelCode }, updatedAt, version);
  }

  /**
   * Get valid next statuses from current state
   * @returns readonly FullMessageStatusValue[]
   */
  public getValidStatusTransitions(): readonly FullMessageStatusValue[] {
    // Get valid next statuses from current state
    if (!this.props.status) {
      return []; // No transitions possible if no current status
    }
    return FullMessageStatusLogic.getValidTransitions(this.props.status.value);
  }

  // ======================
  // Query Methods
  // ======================

  /**
   * Checks if two fullMessages are the same entity
   *
   * @param other - Other fullMessage to compare
   */
  public sameAs(other: FullMessageEntity): boolean {
    return this.props.id.equals(other.props.id);
  }

  /**
   * Gets a snapshot of current entity state for serialization
   */
  public toSnapshot(): FullMessageSnapshotProps {
    return {
      id: this.props.id.value,
      recipient: this.props.recipient?.value,
      data: this.props.data?.value,
      status: this.props.status?.value,
      workspaceCode: this.props.workspaceCode.value,
      templateCode: this.props.templateCode.value,
      channelCode: this.props.channelCode?.value,
      createdAt: this.props.createdAt.value,
      updatedAt: this.props.updatedAt.value,
      version: this.props.version.value,
    };
  }

  /**
   * Gets the current domain state with value objects
   *
   * Provides access to the rich domain state for application layer
   * without requiring infrastructure mappers. Maintains clean architecture.
   *
   * @returns Current domain state with value objects
   */
  public getDomainState(): FullMessageDomainState {
    return this.props;
  }
}
