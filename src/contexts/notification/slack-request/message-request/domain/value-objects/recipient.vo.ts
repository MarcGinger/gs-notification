// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { MessageRequestErrors } from '../errors/message-request.errors';

/**
 * Recipient Value Object
 * Represents a validated recipient with business rules
 */
export const MessageRequestRecipient = createStringVO({
  name: 'Recipient',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 32,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    MessageRequestErrors.INVALID_RECIPIENT,
    'Recipient',
  ),
});

/**
 * Public instance type for Recipient
 */
export type MessageRequestRecipient = StringVOInstance;

// Convenience creators
export const createMessageRequestRecipient = (s: string) =>
  MessageRequestRecipient.create(s);
export const messageRequestRecipientFrom = (v: unknown) =>
  MessageRequestRecipient.from(v);
