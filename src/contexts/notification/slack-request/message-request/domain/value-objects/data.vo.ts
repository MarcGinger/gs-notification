// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  RecordVOInstance,
  createRecordVO,
  createRecordVOErrors,
} from 'src/shared/domain/value-objects';
import { MessageRequestErrors } from '../errors/message-request.errors';

/**
 * Data Record Value Object
 * Validates a plain object (Record<string, unknown>); deepFreeze.
 */
export const MessageRequestData = createRecordVO({
  name: 'Data',
  // JSON key validation - must start with letter/underscore, contain only alphanumeric, underscore
  keyPattern: /^[a-zA-Z_][a-zA-Z0-9_]*$/,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createRecordVOErrors(MessageRequestErrors.INVALID_DATA, 'Data'),
});

/** Public instance type for Data */
export type MessageRequestData = RecordVOInstance;

// Convenience creators
export const createMessageRequestData = (v: Record<string, unknown>) =>
  MessageRequestData.create(v);

export const messageRequestDataFrom = (v: unknown) =>
  MessageRequestData.from(v);
