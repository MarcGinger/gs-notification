// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { QueryHandler, IQueryHandler } from '@nestjs/cqrs';
import { DetailMessageRequestResponse } from '../dtos';
import { GetMessageRequestUseCase } from '../use-cases';
import { GetMessageRequestQuery } from '../queries';
import { Result, DomainError } from 'src/shared/errors';

@QueryHandler(GetMessageRequestQuery)
export class GetMessageRequestHandler
  implements IQueryHandler<GetMessageRequestQuery>
{
  constructor(
    private readonly getMessageRequestUseCase: GetMessageRequestUseCase,
  ) {}

  async execute(
    query: GetMessageRequestQuery,
  ): Promise<Result<DetailMessageRequestResponse | null, DomainError>> {
    return this.getMessageRequestUseCase.execute({
      user: query.user,
      id: query.id,
      correlationId: query.correlationId || 'query-handler',
    });
  }
}
