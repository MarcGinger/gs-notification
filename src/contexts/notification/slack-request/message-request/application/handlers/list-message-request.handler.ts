// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { QueryHandler, IQueryHandler } from '@nestjs/cqrs';
import { Injectable } from '@nestjs/common';
import { DomainError, Result } from 'src/shared/errors';
import { ListMessageRequestQuery } from '../queries';
import { ListMessageRequestUseCase } from '../use-cases';
import { MessageRequestPageResponse } from '../dtos';

/**
 * ✅ Enhanced CQRS Query Handler with use case delegation
 * Follows the pattern: Handler → Use Case → Repository
 */
@Injectable()
@QueryHandler(ListMessageRequestQuery)
export class ListMessageRequestHandler
  implements IQueryHandler<ListMessageRequestQuery>
{
  constructor(
    private readonly listMessageRequestUseCase: ListMessageRequestUseCase,
  ) {}

  /**
   * Handle the list messageRequest query by delegating to the use case
   */
  async execute(
    query: ListMessageRequestQuery,
  ): Promise<Result<MessageRequestPageResponse, DomainError>> {
    // ✅ Delegate to use case for business logic
    return await this.listMessageRequestUseCase.execute({
      user: query.user,
      filter: query.filter,
      correlationId: query.correlationId || 'unknown',
    });
  }
}
