// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { MessageRequestSnapshotProps } from '../../domain/props';
import { DetailMessageRequestResponse } from '../dtos';

/**
 * MessageRequest Snapshot to DTO Mappers
 * Reusable mapping functions for converting domain snapshots to DTOs
 */

/**
 * Map messageRequest snapshot to DTO response
 */
export const mapMessageRequestSnapshotToDto = (
  snapshot: MessageRequestSnapshotProps,
): DetailMessageRequestResponse => {
  return {
    id: snapshot.id,
    recipient: snapshot.recipient,
    data: snapshot.data,
    status: snapshot.status,
    workspaceCode: snapshot.workspaceCode,
    templateCode: snapshot.templateCode,
    channelCode: snapshot.channelCode,
  };
};
