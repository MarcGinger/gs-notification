// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { applyDecorators } from '@nestjs/common';
import { ApiProperty } from '@nestjs/swagger';
import { IsUUID, IsOptional, IsNotEmpty } from 'class-validator';

/**
 * Options for property decorators
 */
interface PropOptions {
  required?: boolean;
}

/**
 * Unique ID for the message request (UUID).
 *
 *
 */
export function ApiMessageRequestId(
  options: PropOptions = {},
): PropertyDecorator {
  const { required = false } = options;

  return applyDecorators(
    ApiProperty({
      type: String,
      description: 'Unique ID for the message request (UUID).',
      example: '',
      required,
    }),

    IsUUID(4),
    required ? IsNotEmpty() : IsOptional(),
  );
}
