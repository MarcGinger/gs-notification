// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { MessageRequestPermission } from '../../domain/permissions/message-request.permissions';

/**
 * Field-level permission matrix for MessageRequest updates.
 * Maps messageRequest fields to the required permissions for modification.
 */
export class MessageRequestFieldPermissionMatrix {
  private static readonly fieldPermissions: Record<
    string,
    MessageRequestPermission[]
  > = {
    // Basic messageRequest information fields - require standard update permission
    name: [MessageRequestPermission.DOMAIN_MESSAGE_REQUEST_UPDATE],
    description: [MessageRequestPermission.DOMAIN_MESSAGE_REQUEST_UPDATE],
    category: [MessageRequestPermission.DOMAIN_MESSAGE_REQUEST_UPDATE],

    // Status fields - may require special permissions or admin rights
    active: [
      MessageRequestPermission.DOMAIN_MESSAGE_REQUEST_UPDATE,
      MessageRequestPermission.DOMAIN_MESSAGE_REQUEST_ADMIN,
    ],
    status: [
      MessageRequestPermission.DOMAIN_MESSAGE_REQUEST_UPDATE,
      MessageRequestPermission.DOMAIN_MESSAGE_REQUEST_ADMIN,
    ],

    // Financial fields - typically require higher permissions
    fees: [MessageRequestPermission.DOMAIN_MESSAGE_REQUEST_UPDATE],
    rates: [MessageRequestPermission.DOMAIN_MESSAGE_REQUEST_UPDATE],
    pricing: [MessageRequestPermission.DOMAIN_MESSAGE_REQUEST_UPDATE],

    // Configuration fields
    channelCodes: [MessageRequestPermission.DOMAIN_MESSAGE_REQUEST_UPDATE],
    railCodes: [MessageRequestPermission.DOMAIN_MESSAGE_REQUEST_UPDATE],
    configuration: [MessageRequestPermission.DOMAIN_MESSAGE_REQUEST_UPDATE],

    // Administrative fields - require admin permissions
    tenantId: [MessageRequestPermission.DOMAIN_MESSAGE_REQUEST_ADMIN],
    auditInfo: [MessageRequestPermission.DOMAIN_MESSAGE_REQUEST_ADMIN],
    metadata: [MessageRequestPermission.DOMAIN_MESSAGE_REQUEST_UPDATE],
  };

  /**
   * Get required permissions for a specific field.
   * Returns empty array if field has no special permission requirements.
   */
  static getRequiredPermissions(fieldName: string): MessageRequestPermission[] {
    return this.fieldPermissions[fieldName] || [];
  }

  /**
   * Check if a field has specific permission requirements.
   */
  static hasSpecialPermissions(fieldName: string): boolean {
    return fieldName in this.fieldPermissions;
  }

  /**
   * Get all fields that require a specific permission.
   */
  static getFieldsForPermission(
    permission: MessageRequestPermission,
  ): string[] {
    return Object.entries(this.fieldPermissions)
      .filter(([, perms]) => perms.includes(permission))
      .map(([field]) => field);
  }

  /**
   * Get all defined field permissions.
   */
  static getAllFieldPermissions(): Record<string, MessageRequestPermission[]> {
    return { ...this.fieldPermissions };
  }

  /**
   * Add or update permission requirements for a field.
   */
  static setFieldPermissions(
    fieldName: string,
    permissions: MessageRequestPermission[],
  ): void {
    this.fieldPermissions[fieldName] = [...permissions];
  }
}
