// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Result, DomainError } from 'src/shared/errors';
import { RepositoryOptions } from 'src/shared/infrastructure/repositories';
import { ActorContext } from 'src/shared/application/context';
import { Option } from 'src/shared/domain/types';
import { MessageRequestSnapshotProps } from '../../domain/props';
import { MessageRequestId } from '../../domain/value-objects';

/**
 * Token for injecting IMessageRequestReader port implementation
 */
export const MESSAGE_REQUEST_READER_TOKEN = 'IMessageRequestReader' as const;

/**
 * MessageRequest Reader Port - Interface Segregation Principle Implementation
 *
 * Handles basic read operations for MessageRequest aggregates.
 * This interface focuses solely on simple retrieval operations.
 *
 * ISP Benefits:
 * - Clients only depend on methods they actually use
 * - Easier to mock and test specific read scenarios
 * - Can be implemented independently from write operations
 * - Supports read-only implementations (e.g., reporting services)
 *
 * @domain Notification Context - MessageRequest Reader Port
 * @layer Application Ports
 * @pattern Interface Segregation Principle
 */
export interface IMessageRequestReader {
  /**
   * Find a MessageRequest by its unique identifier
   * @param actor - The authenticated user context
   * @param id - The unique identifier of the MessageRequest
   * @param options - Optional repository options
   * @returns Result containing the MessageRequest snapshot or null if not found
   */
  findById(
    actor: ActorContext,
    id: MessageRequestId,
    options?: RepositoryOptions,
  ): Promise<Result<Option<MessageRequestSnapshotProps>, DomainError>>;

  /**
   * Check if a messageRequest exists by ID (for write-path validation)
   * @param actor - The authenticated user context
   * @param id - The unique identifier of the MessageRequest
   * @param options - Optional repository options
   * @returns Result containing boolean indicating existence
   */
  exists(
    actor: ActorContext,
    id: MessageRequestId,
    options?: RepositoryOptions,
  ): Promise<Result<boolean, DomainError>>;

  /**
   * Get messageRequest version for optimistic concurrency control
   * @param actor - The authenticated user context
   * @param id - The unique identifier of the MessageRequest
   * @param options - Optional repository options
   * @returns Result containing version number or null if not found
   */
  getVersion(
    actor: ActorContext,
    id: MessageRequestId,
    options?: RepositoryOptions,
  ): Promise<Result<Option<number>, DomainError>>;

  /**
   * Get minimal messageRequest data for write-path operations
   * @param actor - The authenticated user context
   * @param id - The unique identifier of the MessageRequest
   * @param options - Optional repository options
   * @returns Result containing minimal messageRequest data or null if not found
   */
  getMinimal(
    actor: ActorContext,
    id: MessageRequestId,
    options?: RepositoryOptions,
  ): Promise<Result<Option<{ id: string; version: number }>, DomainError>>;
}
