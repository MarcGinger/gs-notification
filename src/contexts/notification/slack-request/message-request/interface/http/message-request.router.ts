// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Module } from '@nestjs/common';
import { RouterModule, Routes } from '@nestjs/core';
import { MessageRequestModule } from '../../message-request.module';
import { MessageRequestProjectorModule } from '../../message-request-projector.module';

/**
 * Message Request Router Configuration
 *
 * Configures nested routing for message-request endpoints to follow
 * the module monolith structure: /notification/slack-request/api/v1/message-requests
 *
 * This replaces flat routing (/api/v1/message-requests) with contextual routing
 * that reflects the bounded context hierarchy.
 */

// Define the routing structure for the message-request bounded context
const messageRequestRoutes: Routes = [
  {
    path: 'notification',
    children: [
      {
        path: 'slack-request',
        children: [
          {
            path: 'api/v1',
            module: MessageRequestModule,
          },
          {
            path: 'health',
            module: MessageRequestProjectorModule,
          },
        ],
      },
    ],
  },
];

/**
 * Message Request Router Module
 *
 * Provides nested routing configuration for message-request endpoints.
 * This module should be imported in the main app module to enable
 * the nested route structure.
 *
 * Routes configured:
 * - /notification/slack-request/api/v1/message-requests (GET, POST)
 * - /notification/slack-request/api/v1/message-requests/:id (GET)
 * - /notification/slack-request/health/projectors/messageRequest (GET)
 * - /notification/slack-request/health/projectors/message-request/metrics (GET)
 */
@Module({
  imports: [
    MessageRequestModule,
    MessageRequestProjectorModule,
    RouterModule.register(messageRequestRoutes),
  ],
  exports: [MessageRequestModule, MessageRequestProjectorModule],
})
export class MessageRequestRouterModule {}
