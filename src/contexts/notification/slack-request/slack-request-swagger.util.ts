// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { INestApplication } from '@nestjs/common';
import { AppConfigUtil } from 'src/shared/config/app-config.util';

import { SlackRequestDocumentation } from './slack-request.doc';
import { MessageRequestDocumentation } from './message-request/docs/message-request.doc';
import { WorkspaceDocumentation } from './workspace/docs/workspace.doc';
import { TemplateDocumentation } from './template/docs/template.doc';

/**
 * Setup notification-slack-request application/service Swagger documentation
 * Configures documentation for all aggregates within the notification_slack_request domain
 */
export function setupNotificationSlackRequestDocs(
  app: INestApplication,
  port: string | number,
): Record<string, string> {
  if (AppConfigUtil.isProduction()) {
    return {};
  }

  const boundedContextUrls = SlackRequestDocumentation.setupAll(app, port);

  // Setup documentation for all notification_slack_request aggregates
  const messageRequestUrls = MessageRequestDocumentation.setupAll(app, port);
  const workspaceUrls = WorkspaceDocumentation.setupAll(app, port);
  const templateUrls = TemplateDocumentation.setupAll(app, port);

  return {
    ...boundedContextUrls,
    ...messageRequestUrls,
    ...workspaceUrls,
    ...templateUrls,
  };
}
