// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Module } from '@nestjs/common';
import { RouterModule, Routes } from '@nestjs/core';
import { WorkspaceModule } from '../../workspace.module';
import { WorkspaceProjectorModule } from '../../workspace-projector.module';
/**
 * Workspace Router Configuration
 *
 * Configures nested routing for workspace endpoints to follow
 * the module monolith structure: /notification/webhook-config/api/v1/workspaces
 *
 * This replaces flat routing (/api/v1/workspaces) with contextual routing
 * that reflects the bounded context hierarchy.
 */

// Define the routing structure for the workspace bounded context
const workspaceRoutes: Routes = [
  {
    path: 'notification',
    children: [
      {
        path: 'webhook-config',
        children: [
          {
            path: 'workspaces',
            module: WorkspaceModule,
          },
          {
            path: 'health',
            module: WorkspaceProjectorModule,
          },
        ],
      },
    ],
  },
];

/**
 * Workspace Router Module
 *
 * Provides nested routing configuration for workspace endpoints.
 * This module should be imported in the main app module to enable
 * the nested route structure.
 *
 * Routes configured:
 * - /notification/webhook-config/api/v1/workspaces (GET, POST)
 * - /notification/webhook-config/api/v1/workspaces/:id (GET, PUT)
 * - /notification/webhook-config/health/projectors/workspace (GET)
 * - /notification/webhook-config/health/projectors/workspace/metrics (GET)
 */
@Module({
  imports: [
    WorkspaceModule,
    WorkspaceProjectorModule,
    RouterModule.register(workspaceRoutes),
  ],
  exports: [WorkspaceModule, WorkspaceProjectorModule],
})
export class WorkspaceRouterModule {}
