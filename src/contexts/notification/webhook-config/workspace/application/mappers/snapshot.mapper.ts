// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { WorkspaceSnapshotProps } from '../../domain/props';
import { DetailWorkspaceResponse } from '../dtos';

/**
 * Workspace Snapshot to DTO Mappers
 * Reusable mapping functions for converting domain snapshots to DTOs
 */

/**
 * Map workspace snapshot to DTO response
 */
export const mapWorkspaceSnapshotToDto = (
  snapshot: WorkspaceSnapshotProps,
): DetailWorkspaceResponse => {
  return {
    id: snapshot.id,
    name: snapshot.name,
    description: snapshot.description,
    targetUrl: snapshot.targetUrl,
    webhookEventType: snapshot.webhookEventType,
    method: snapshot.method,
    headers: snapshot.headers,
    status: snapshot.status,
  };
};
