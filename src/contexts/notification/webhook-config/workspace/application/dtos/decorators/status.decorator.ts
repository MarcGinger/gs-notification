// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { applyDecorators } from '@nestjs/common';
import { ApiProperty } from '@nestjs/swagger';
import { IsEnum, IsOptional, IsNotEmpty } from 'class-validator';
import {
  WorkspaceStatusValues,
  WorkspaceStatusValue,
} from '../../../domain/value-objects';

export { WorkspaceStatusValues, WorkspaceStatusValue };
/**
 * Options for property decorators (enum)
 */
interface PropOptions {
  required?: boolean;
}

/**
 * Property decorator for Workspace Status with required option
 * @param {Object} options - Options for the decorator
 * @returns {PropertyDecorator}
 */
export function ApiWorkspaceStatus(options: PropOptions = {}) {
  const { required = true } = options;
  return applyDecorators(
    ApiProperty({
      description: `Current operational status of the webhook. &#x27;active&#x27; &#x3D; receiving events, &#x27;paused&#x27; &#x3D; temporarily disabled, &#x27;disabled&#x27; &#x3D; permanently inactive. Values include active, paused, disabled.`,
      type: String,
      enum: WorkspaceStatusValues,
      required,
    }),
    IsEnum(WorkspaceStatusValues),
    required ? IsNotEmpty() : IsOptional(),
  );
}
