// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { applyDecorators } from '@nestjs/common';
import { ApiProperty } from '@nestjs/swagger';
import { IsEnum, IsOptional, IsNotEmpty } from 'class-validator';
import {
  WorkspaceMethodValues,
  WorkspaceMethodValue,
} from '../../../domain/value-objects';

export { WorkspaceMethodValues, WorkspaceMethodValue };
/**
 * Options for property decorators (enum)
 */
interface PropOptions {
  required?: boolean;
}

/**
 * Property decorator for Workspace Method with required option
 * @param {Object} options - Options for the decorator
 * @returns {PropertyDecorator}
 */
export function ApiWorkspaceMethod(options: PropOptions = {}) {
  const { required = true } = options;
  return applyDecorators(
    ApiProperty({
      description: `HTTP method used for webhook delivery. POST is most common for event notifications, but GET/PUT/PATCH/DELETE are supported for specialized integrations. Values include GET, POST, PUT, PATCH.`,
      type: String,
      enum: WorkspaceMethodValues,
      required,
    }),
    IsEnum(WorkspaceMethodValues),
    required ? IsNotEmpty() : IsOptional(),
  );
}
