// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DomainError, Result, err, withContext, ok } from 'src/shared/errors';
import { Clock } from 'src/shared/domain/clock';
import { EventMetadata } from 'src/shared/domain/events';
import { WorkspaceAggregate } from '../aggregates';
import { CreateWorkspaceProps } from '../props';
import { WorkspaceDomainState } from '../state';
import {
  WorkspaceCreatedAt,
  WorkspaceUpdatedAt,
  WorkspaceVersion,
  WorkspaceId,
  WorkspaceName,
  WorkspaceDescription,
  WorkspaceTargetUrl,
  WorkspaceWebhookEventType,
  createWorkspaceMethod,
  WorkspaceHeaders,
  createWorkspaceStatus,
} from '../value-objects';

/**
 * Enhanced workspace entity factory with comprehensive validation and security context
 * Moved from application layer to properly separate business concerns
 */
export function createWorkspaceAggregateFromProps(
  props: CreateWorkspaceProps,
  metadata: EventMetadata,
  clock: Clock,
  // validatedEntities?: {
  //  channels?: ChannelReference[];
  // },
): Result<WorkspaceAggregate, DomainError> {
  // Validate each property by creating value objects
  const idResult = WorkspaceId.from(props.id);
  if (!idResult.ok) {
    return err(
      withContext(idResult.error, {
        ...idResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_workspace',
        id: props.id,
      }),
    );
  }

  const nameResult = WorkspaceName.from(props.name);
  if (!nameResult.ok) {
    return err(
      withContext(nameResult.error, {
        ...nameResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_workspace',
        name: props.name,
      }),
    );
  }

  const descriptionResult = WorkspaceDescription.from(props.description);
  if (!descriptionResult.ok) {
    return err(
      withContext(descriptionResult.error, {
        ...descriptionResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_workspace',
        description: props.description,
      }),
    );
  }

  const targetUrlResult = WorkspaceTargetUrl.from(props.targetUrl);
  if (!targetUrlResult.ok) {
    return err(
      withContext(targetUrlResult.error, {
        ...targetUrlResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_workspace',
        targetUrl: props.targetUrl,
      }),
    );
  }

  const webhookEventTypeResult = WorkspaceWebhookEventType.from(
    props.webhookEventType,
  );
  if (!webhookEventTypeResult.ok) {
    return err(
      withContext(webhookEventTypeResult.error, {
        ...webhookEventTypeResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_workspace',
        webhookEventType: props.webhookEventType,
      }),
    );
  }

  const methodResult = createWorkspaceMethod(props.method);
  if (!methodResult.ok) {
    return err(
      withContext(methodResult.error, {
        ...methodResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_workspace',
        method: props.method,
      }),
    );
  }

  const headersResult = WorkspaceHeaders.from(props.headers);
  if (!headersResult.ok) {
    return err(
      withContext(headersResult.error, {
        ...headersResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_workspace',
        headers: props.headers,
      }),
    );
  }

  const statusResult = createWorkspaceStatus(props.status);
  if (!statusResult.ok) {
    return err(
      withContext(statusResult.error, {
        ...statusResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_workspace',
        status: props.status,
      }),
    );
  }

  const createdAtResult = WorkspaceCreatedAt.create(clock.now());
  if (!createdAtResult.ok) {
    return err(createdAtResult.error);
  }

  const updatedAtResult = WorkspaceUpdatedAt.create(clock.now());
  if (!updatedAtResult.ok) {
    return err(updatedAtResult.error);
  }

  const versionResult = WorkspaceVersion.create(1); // Initial version is 1
  if (!versionResult.ok) {
    return err(versionResult.error);
  }

  // Create the entity properties with validated value objects
  const entityProps: WorkspaceDomainState = {
    id: idResult.value,
    name: nameResult.value,
    description: descriptionResult.value,
    targetUrl: targetUrlResult.value,
    webhookEventType: webhookEventTypeResult.value,
    method: methodResult.value,
    headers: headersResult.value,
    status: statusResult.value,
    createdAt: createdAtResult.value,
    updatedAt: updatedAtResult.value,
    version: versionResult.value,
  };

  // Create the entity using the constructor
  return WorkspaceAggregate.create(entityProps, clock, metadata);
}
