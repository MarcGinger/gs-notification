// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { EntityIdBase } from 'src/shared/domain/entities';
import { Result, ok, err, DomainError } from 'src/shared/errors';
import { WorkspaceSnapshotProps } from '../props';
import { WorkspaceDomainState } from '../state';
import { WorkspaceErrors } from '../errors/workspace.errors';
import {
  WorkspaceCreatedAt,
  WorkspaceUpdatedAt,
  WorkspaceVersion,
  WorkspaceDescription,
  WorkspaceHeaders,
  WorkspaceId,
  WorkspaceMethod,
  WorkspaceMethodLogic,
  WorkspaceName,
  WorkspaceStatus,
  WorkspaceStatusLogic,
  WorkspaceStatusValue,
  WorkspaceTargetUrl,
  WorkspaceWebhookEventType,
  createWorkspaceMethod,
  createWorkspaceStatus,
} from '../value-objects';

/**
 * Domain Entity: Workspace
 *
 * Represents the core Workspace entity in the notification domain.
 * Encapsulates workspace data, identity, and basic entity behavior.
 *
 * This entity follows DDD principles:
 * - Identity: Id as unique identifier
 * - Immutability: Changes create new instances
 * - Encapsulation: Private state with controlled access
 * - Business validation: Domain rules enforced
 *
 * @domain Notification Context - Workspace Entity
 * @layer Domain Entities
 */

/**
 * Workspace Entity
 *
 * Core domain entity representing a workspace in the notification.
 * Handles workspace identity, validation, and state management.
 */
export class WorkspaceEntity extends EntityIdBase<
  WorkspaceDomainState,
  WorkspaceId
> {
  private static clock: { now: () => Date } = { now: () => new Date() };

  public static setClock(c: { now: () => Date }) {
    this.clock = c;
  }

  /**
   * Helper method to create updatedAt VO from date
   */
  private static createUpdatedAt(
    date?: Date,
  ): Result<WorkspaceUpdatedAt, DomainError> {
    const targetDate = date || WorkspaceEntity.clock.now();
    const result = WorkspaceUpdatedAt.from(targetDate);
    if (!result.ok) {
      return err(result.error);
    }
    return ok(result.value);
  }

  /**
   * Helper method to create version VO from number
   */
  private static createVersion(
    versionNumber?: number,
    currentVersion?: WorkspaceVersion,
  ): Result<WorkspaceVersion, DomainError> {
    const targetVersion = versionNumber ?? currentVersion?.value ?? 1;
    const result = WorkspaceVersion.from(targetVersion);
    if (!result.ok) {
      return err(result.error);
    }
    return ok(result.value);
  }

  /**
   * Helper method to create updated entity with common logic
   */
  private createUpdatedEntity(
    fieldUpdates: Partial<WorkspaceDomainState>,
    updatedAt?: Date,
    version?: number,
  ): Result<WorkspaceEntity, DomainError> {
    const updatedAtResult = WorkspaceEntity.createUpdatedAt(updatedAt);
    if (!updatedAtResult.ok) {
      return err(updatedAtResult.error);
    }

    const versionResult = WorkspaceEntity.createVersion(
      version,
      this.props.version,
    );
    if (!versionResult.ok) {
      return err(versionResult.error);
    }

    const updatedProps: WorkspaceDomainState = {
      ...this.props,
      ...fieldUpdates,
      updatedAt: updatedAtResult.value,
      version: versionResult.value,
    };

    return WorkspaceEntity.create(updatedProps);
  }

  private constructor(props: WorkspaceDomainState) {
    super(props, props.id);
  }

  /**
   * Factory method to create a new workspace entity
   *
   * @param props - Workspace properties
   * @returns Result containing WorkspaceEntity or DomainError
   */
  public static create(
    props: WorkspaceDomainState,
  ): Result<WorkspaceEntity, DomainError> {
    // Validate required properties
    const validationResult = WorkspaceEntity.validate(props);
    if (validationResult.ok === false) {
      return err(validationResult.error);
    }

    return ok(new WorkspaceEntity(props));
  }

  /**
   * Factory method to reconstitute a workspace entity from persistence
   *
   * @param props - Workspace properties from database
   * @returns WorkspaceEntity instance
   */
  public static reconstitute(props: WorkspaceDomainState): WorkspaceEntity {
    return new WorkspaceEntity(props);
  }

  /**
   * Factory method to reconstitute entity from snapshot
   *
   * @param snapshot - Workspace snapshot properties
   * @returns Result containing WorkspaceEntity or DomainError
   */
  public static fromSnapshot(
    snapshot: WorkspaceSnapshotProps,
  ): Result<WorkspaceEntity, DomainError> {
    const idResult = WorkspaceId.from(snapshot.id);
    if (!idResult.ok) {
      return err(idResult.error);
    }
    const nameResult = WorkspaceName.from(snapshot.name);
    if (!nameResult.ok) {
      return err(nameResult.error);
    }
    const descriptionResult = WorkspaceDescription.from(snapshot.description);
    if (!descriptionResult.ok) {
      return err(descriptionResult.error);
    }
    const targetUrlResult = WorkspaceTargetUrl.from(snapshot.targetUrl);
    if (!targetUrlResult.ok) {
      return err(targetUrlResult.error);
    }
    const webhookEventTypeResult = WorkspaceWebhookEventType.from(
      snapshot.webhookEventType,
    );
    if (!webhookEventTypeResult.ok) {
      return err(webhookEventTypeResult.error);
    }
    const methodResult = createWorkspaceMethod(snapshot.method);
    if (!methodResult.ok) {
      return err(methodResult.error);
    }
    const headersResult = WorkspaceHeaders.from(snapshot.headers);
    if (!headersResult.ok) {
      return err(headersResult.error);
    }
    const statusResult = createWorkspaceStatus(snapshot.status);
    if (!statusResult.ok) {
      return err(statusResult.error);
    }
    const createdAtResult = WorkspaceCreatedAt.from(snapshot.createdAt);
    if (!createdAtResult.ok) {
      return err(createdAtResult.error);
    }

    const updatedAtResult = WorkspaceUpdatedAt.from(snapshot.updatedAt);
    if (!updatedAtResult.ok) {
      return err(updatedAtResult.error);
    }

    const versionResult = WorkspaceVersion.from(snapshot.version);
    if (!versionResult.ok) {
      return err(versionResult.error);
    }

    const props: WorkspaceDomainState = {
      id: idResult.value,
      name: nameResult.value,
      description: descriptionResult.value,
      targetUrl: targetUrlResult.value,
      webhookEventType: webhookEventTypeResult.value,
      method: methodResult.value,
      headers: headersResult.value,
      status: statusResult.value,
      createdAt: createdAtResult.value,
      updatedAt: updatedAtResult.value,
      version: versionResult.value,
    };

    return WorkspaceEntity.create(props);
  }

  /**
   * Validates workspace entity properties
   *
   * @param props - Properties to validate
   * @returns Validation result
   */
  private static validate(
    props: WorkspaceDomainState,
  ): Result<void, DomainError> {
    // Basic validation
    if (!props.id) {
      return err(WorkspaceErrors.INVALID_ID_DATA);
    }
    if (!props.name) {
      return err(WorkspaceErrors.INVALID_NAME_DATA);
    }
    if (!props.targetUrl) {
      return err(WorkspaceErrors.INVALID_TARGET_URL_DATA);
    }
    if (!props.webhookEventType) {
      return err(WorkspaceErrors.INVALID_WEBHOOK_EVENT_TYPE_DATA);
    }
    if (!props.method) {
      return err(WorkspaceErrors.INVALID_METHOD_DATA);
    }
    if (!props.status) {
      return err(WorkspaceErrors.INVALID_STATUS_DATA);
    }

    return ok(undefined);
  }

  // ======================
  // Getters (Public API)
  // ======================

  public get id(): WorkspaceId {
    return this.props.id;
  }

  public get name(): WorkspaceName {
    return this.props.name;
  }

  public get description(): WorkspaceDescription | undefined {
    return this.props.description;
  }

  public get targetUrl(): WorkspaceTargetUrl {
    return this.props.targetUrl;
  }

  public get webhookEventType(): WorkspaceWebhookEventType {
    return this.props.webhookEventType;
  }

  public get method(): WorkspaceMethod {
    return this.props.method;
  }

  public get headers(): WorkspaceHeaders | undefined {
    return this.props.headers;
  }

  public get status(): WorkspaceStatus {
    return this.props.status;
  }

  public get createdAt(): WorkspaceCreatedAt {
    return this.props.createdAt;
  }

  public get updatedAt(): WorkspaceUpdatedAt {
    return this.props.updatedAt;
  }

  public get version(): WorkspaceVersion {
    return this.props.version;
  }

  // ======================
  // Simple Update Methods (No Business Logic)
  // ======================

  /**
   * Creates a new entity with updated name (pure state transition)
   *
   * @param name - New name value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<WorkspaceEntity, DomainError>
   */
  public withName(
    name: WorkspaceName,
    updatedAt?: Date,
    version?: number,
  ): Result<WorkspaceEntity, DomainError> {
    return this.createUpdatedEntity({ name }, updatedAt, version);
  }

  /**
   * Creates a new entity with updated description (pure state transition)
   *
   * @param description - New description value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<WorkspaceEntity, DomainError>
   */
  public withDescription(
    description: WorkspaceDescription,
    updatedAt?: Date,
    version?: number,
  ): Result<WorkspaceEntity, DomainError> {
    return this.createUpdatedEntity({ description }, updatedAt, version);
  }

  /**
   * Creates a new entity with updated targetUrl (pure state transition)
   *
   * @param targetUrl - New targetUrl value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<WorkspaceEntity, DomainError>
   */
  public withTargetUrl(
    targetUrl: WorkspaceTargetUrl,
    updatedAt?: Date,
    version?: number,
  ): Result<WorkspaceEntity, DomainError> {
    return this.createUpdatedEntity({ targetUrl }, updatedAt, version);
  }

  /**
   * Creates a new entity with updated webhookEventType (pure state transition)
   *
   * @param webhookEventType - New webhookEventType value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<WorkspaceEntity, DomainError>
   */
  public withWebhookEventType(
    webhookEventType: WorkspaceWebhookEventType,
    updatedAt?: Date,
    version?: number,
  ): Result<WorkspaceEntity, DomainError> {
    return this.createUpdatedEntity({ webhookEventType }, updatedAt, version);
  }

  /**
   * Creates a new entity with updated method (with basic transition validation)
   *
   * @param method - New method value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<WorkspaceEntity, DomainError>
   */
  public withMethod(
    method: WorkspaceMethod,
    updatedAt?: Date,
    version?: number,
  ): Result<WorkspaceEntity, DomainError> {
    // Basic validation - business rules handled by aggregate
    const currentStatus = this.props.method.value;
    const targetStatus = method.value;

    if (!WorkspaceMethodLogic.canTransition(currentStatus, targetStatus)) {
      return err({
        ...WorkspaceErrors.INVALID_METHOD_TRANSITION,
        context: {
          currentStatus,
          targetStatus,
          validTransitions:
            WorkspaceMethodLogic.getValidTransitions(currentStatus),
        },
      });
    }

    return this.createUpdatedEntity({ method }, updatedAt, version);
  }

  /**
   * Creates a new entity with updated headers (pure state transition)
   *
   * @param headers - New headers value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<WorkspaceEntity, DomainError>
   */
  public withHeaders(
    headers: WorkspaceHeaders,
    updatedAt?: Date,
    version?: number,
  ): Result<WorkspaceEntity, DomainError> {
    return this.createUpdatedEntity({ headers }, updatedAt, version);
  }

  /**
   * Creates a new entity with updated status (with basic transition validation)
   *
   * @param status - New status value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<WorkspaceEntity, DomainError>
   */
  public withStatus(
    status: WorkspaceStatus,
    updatedAt?: Date,
    version?: number,
  ): Result<WorkspaceEntity, DomainError> {
    // Basic validation - business rules handled by aggregate
    const currentStatus = this.props.status.value;
    const targetStatus = status.value;

    if (!WorkspaceStatusLogic.canTransition(currentStatus, targetStatus)) {
      return err({
        ...WorkspaceErrors.INVALID_STATUS_TRANSITION,
        context: {
          currentStatus,
          targetStatus,
          validTransitions:
            WorkspaceStatusLogic.getValidTransitions(currentStatus),
        },
      });
    }

    return this.createUpdatedEntity({ status }, updatedAt, version);
  }

  /**
   * Get valid next statuses from current state
   * @returns readonly WorkspaceStatusValue[]
   */
  public getValidStatusTransitions(): readonly WorkspaceStatusValue[] {
    // Get valid next statuses from current state
    return WorkspaceStatusLogic.getValidTransitions(this.props.status.value);
  }

  // ======================
  // Query Methods
  // ======================

  /**
   * Checks if two workspaces are the same entity
   *
   * @param other - Other workspace to compare
   */
  public sameAs(other: WorkspaceEntity): boolean {
    return this.props.id.equals(other.props.id);
  }

  /**
   * Gets a snapshot of current entity state for serialization
   */
  public toSnapshot(): WorkspaceSnapshotProps {
    return {
      id: this.props.id.value,
      name: this.props.name.value,
      description: this.props.description?.value,
      targetUrl: this.props.targetUrl.value,
      webhookEventType: this.props.webhookEventType.value,
      method: this.props.method.value,
      headers: this.props.headers?.value,
      status: this.props.status.value,
      createdAt: this.props.createdAt.value,
      updatedAt: this.props.updatedAt.value,
      version: this.props.version.value,
    };
  }

  /**
   * Gets the current domain state with value objects
   *
   * Provides access to the rich domain state for application layer
   * without requiring infrastructure mappers. Maintains clean architecture.
   *
   * @returns Current domain state with value objects
   */
  public getDomainState(): WorkspaceDomainState {
    return this.props;
  }
}
