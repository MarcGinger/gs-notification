// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { WorkspaceErrors } from '../errors/workspace.errors';

/**
 * TargetUrl Value Object
 * Represents a validated targetUrl with business rules
 */
export const WorkspaceTargetUrl = createStringVO({
  name: 'TargetUrl',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(WorkspaceErrors.INVALID_TARGET_URL, 'TargetUrl'),
});

/**
 * Public instance type for TargetUrl
 */
export type WorkspaceTargetUrl = StringVOInstance;

// Convenience creators
export const createWorkspaceTargetUrl = (s: string) =>
  WorkspaceTargetUrl.create(s);
export const workspaceTargetUrlFrom = (v: unknown) =>
  WorkspaceTargetUrl.from(v);
