// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { WorkspaceErrors } from '../errors/workspace.errors';

/**
 * WebhookEventType Value Object
 * Represents a validated webhookEventType with business rules
 */
export const WorkspaceWebhookEventType = createStringVO({
  name: 'WebhookEventType',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  maxLength: 128,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    WorkspaceErrors.INVALID_WEBHOOK_EVENT_TYPE,
    'WebhookEventType',
  ),
});

/**
 * Public instance type for WebhookEventType
 */
export type WorkspaceWebhookEventType = StringVOInstance;

// Convenience creators
export const createWorkspaceWebhookEventType = (s: string) =>
  WorkspaceWebhookEventType.create(s);
export const workspaceWebhookEventTypeFrom = (v: unknown) =>
  WorkspaceWebhookEventType.from(v);
