// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  RecordVOInstance,
  createRecordVO,
  createRecordVOErrors,
} from 'src/shared/domain/value-objects';
import { WorkspaceErrors } from '../errors/workspace.errors';

/**
 * Headers Record Value Object
 * Validates a plain object (Record<string, unknown>); deepFreeze.
 */
export const WorkspaceHeaders = createRecordVO({
  name: 'Headers',
  // JSON key validation - must start with letter/underscore, contain only alphanumeric, underscore
  keyPattern: /^[a-zA-Z_][a-zA-Z0-9_]*$/,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createRecordVOErrors(WorkspaceErrors.INVALID_HEADERS, 'Headers'),
});

/** Public instance type for Headers */
export type WorkspaceHeaders = RecordVOInstance;

// Convenience creators
export const createWorkspaceHeaders = (v: Record<string, unknown>) =>
  WorkspaceHeaders.create(v);

export const workspaceHeadersFrom = (v: unknown) => WorkspaceHeaders.from(v);
