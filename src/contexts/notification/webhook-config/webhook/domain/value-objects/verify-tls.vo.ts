// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  BooleanVOInstance,
  createBooleanVO,
  createBooleanVOErrors,
} from 'src/shared/domain/value-objects';
import { WebhookErrors } from '..//errors/webhook.errors';

/**
 * VerifyTls Boolean Value Object
 * Represents a validated verifyTls with business rules
 */
export const WebhookVerifyTls = createBooleanVO({
  name: 'VerifyTls',
  treatNullAsFalse: true,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createBooleanVOErrors(WebhookErrors.INVALID_VERIFY_TLS, 'VerifyTls'),
});

/** Public instance type for VerifyTls */
export type WebhookVerifyTls = BooleanVOInstance;

// Convenience creators
export const createWebhookVerifyTls = (v: boolean) =>
  WebhookVerifyTls.create(v);
export const webhookVerifyTlsFrom = (v: unknown) => WebhookVerifyTls.from(v);
