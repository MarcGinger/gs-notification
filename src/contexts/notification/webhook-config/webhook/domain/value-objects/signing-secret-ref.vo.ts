// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { WebhookErrors } from '../errors/webhook.errors';

/**
 * SigningSecretRef Value Object
 * Represents a validated signingSecretRef with business rules
 */
export const WebhookSigningSecretRef = createStringVO({
  name: 'SigningSecretRef',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 128,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    WebhookErrors.INVALID_SIGNING_SECRET_REF,
    'SigningSecretRef',
  ),
});

/**
 * Public instance type for SigningSecretRef
 */
export type WebhookSigningSecretRef = StringVOInstance;

// Convenience creators
export const createWebhookSigningSecretRef = (s: string) =>
  WebhookSigningSecretRef.create(s);
export const webhookSigningSecretRefFrom = (v: unknown) =>
  WebhookSigningSecretRef.from(v);
