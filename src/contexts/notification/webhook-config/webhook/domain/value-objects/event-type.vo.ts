// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { WebhookErrors } from '../errors/webhook.errors';

/**
 * EventType Value Object
 * Represents a validated eventType with business rules
 */
export const WebhookEventType = createStringVO({
  name: 'EventType',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(WebhookErrors.INVALID_EVENT_TYPE, 'EventType'),
});

/**
 * Public instance type for EventType
 */
export type WebhookEventType = StringVOInstance;

// Convenience creators
export const createWebhookEventType = (s: string) => WebhookEventType.create(s);
export const webhookEventTypeFrom = (v: unknown) => WebhookEventType.from(v);
