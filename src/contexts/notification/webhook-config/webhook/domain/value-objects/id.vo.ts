// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { WebhookErrors } from '../errors/webhook.errors';

/**
 * Id Value Object
 * Represents a validated id with business rules
 */
export const WebhookId = createStringVO({
  name: 'Id',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,
  minLength: 10,
  maxLength: 32,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(WebhookErrors.INVALID_ID, 'Id'),
});

/**
 * Public instance type for Id
 */
export type WebhookId = StringVOInstance;

// Convenience creators
export const createWebhookId = (s: string) => WebhookId.create(s);
export const webhookIdFrom = (v: unknown) => WebhookId.from(v);
