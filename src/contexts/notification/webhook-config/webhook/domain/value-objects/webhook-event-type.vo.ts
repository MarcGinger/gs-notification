// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { WebhookErrors } from '../errors/webhook.errors';

/**
 * WebhookEventType Value Object
 * Represents a validated webhookEventType with business rules
 */
export const WebhookWebhookEventType = createStringVO({
  name: 'WebhookEventType',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  maxLength: 128,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    WebhookErrors.INVALID_WEBHOOK_EVENT_TYPE,
    'WebhookEventType',
  ),
});

/**
 * Public instance type for WebhookEventType
 */
export type WebhookWebhookEventType = StringVOInstance;

// Convenience creators
export const createWebhookWebhookEventType = (s: string) =>
  WebhookWebhookEventType.create(s);
export const webhookWebhookEventTypeFrom = (v: unknown) =>
  WebhookWebhookEventType.from(v);
