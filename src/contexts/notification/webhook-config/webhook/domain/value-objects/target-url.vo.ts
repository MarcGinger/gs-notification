// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { WebhookErrors } from '../errors/webhook.errors';

/**
 * TargetUrl Value Object
 * Represents a validated targetUrl with business rules
 */
export const WebhookTargetUrl = createStringVO({
  name: 'TargetUrl',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(WebhookErrors.INVALID_TARGET_URL, 'TargetUrl'),
});

/**
 * Public instance type for TargetUrl
 */
export type WebhookTargetUrl = StringVOInstance;

// Convenience creators
export const createWebhookTargetUrl = (s: string) => WebhookTargetUrl.create(s);
export const webhookTargetUrlFrom = (v: unknown) => WebhookTargetUrl.from(v);
