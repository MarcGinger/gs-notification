// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { WebhookErrors } from '../errors/webhook.errors';

/**
 * Name Value Object
 * Represents a validated name with business rules
 */
export const WebhookName = createStringVO({
  name: 'Name',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  maxLength: 255,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(WebhookErrors.INVALID_NAME, 'Name'),
});

/**
 * Public instance type for Name
 */
export type WebhookName = StringVOInstance;

// Convenience creators
export const createWebhookName = (s: string) => WebhookName.create(s);
export const webhookNameFrom = (v: unknown) => WebhookName.from(v);
