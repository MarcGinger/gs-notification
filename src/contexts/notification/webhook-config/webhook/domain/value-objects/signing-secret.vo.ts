// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { WebhookErrors } from '../errors/webhook.errors';

/**
 * SigningSecret Value Object
 * Represents a validated signingSecret with business rules
 */
export const WebhookSigningSecret = createStringVO({
  name: 'SigningSecret',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 128,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    WebhookErrors.INVALID_SIGNING_SECRET,
    'SigningSecret',
  ),
});

/**
 * Public instance type for SigningSecret
 */
export type WebhookSigningSecret = StringVOInstance;

// Convenience creators
export const createWebhookSigningSecret = (s: string) =>
  WebhookSigningSecret.create(s);
export const webhookSigningSecretFrom = (v: unknown) =>
  WebhookSigningSecret.from(v);
