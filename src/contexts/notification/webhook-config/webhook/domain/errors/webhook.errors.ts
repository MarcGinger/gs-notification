// @generated by gen v1.0.0 hash:regen
// DO NOT EDIT THIS BLOCK BY HAND. Your changes will be overwritten.
import { DomainError } from 'src/shared/errors';

export interface WebhookContext extends Record<string, unknown> {
  id: string;
  name: string;
  description?: string;
  targetUrl?: string;
  eventType?: string;
  method?: string;
  headers?: Record<string, unknown>;
  signingSecret?: string;
  status: string;
  userId?: string;
  correlationId?: string;
  operation?: string;
  reason?: string;
}

export const WebhookErrors = {
  AUTHORIZATION_FAILED: {
    code: 'WEBHOOK.AUTHORIZATION_FAILED',
    title: 'Authorization Failed',
    detail: 'Policy evaluation failed for webhook operation',
    category: 'domain',
    retryable: false,
  } as DomainError<'WEBHOOK.AUTHORIZATION_FAILED', WebhookContext>,
  CANNOT_DELETE_WEBHOOK_WITH_DEPENDENCIES: {
    code: 'WEBHOOK.CANNOT_DELETE_WEBHOOK_WITH_DEPENDENCIES',
    title: 'Cannot Delete',
    detail: 'Cannot delete Webhook with existing dependencies',
    category: 'validation',
    retryable: false,
  } as DomainError<
    'WEBHOOK.CANNOT_DELETE_WEBHOOK_WITH_DEPENDENCIES',
    WebhookContext
  >,
  CONCURRENCY_CONFLICT: {
    code: 'WEBHOOK.CONCURRENCY_CONFLICT',
    title: 'Concurrency Conflict',
    detail: 'Concurrency conflict for Webhook',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.CONCURRENCY_CONFLICT', WebhookContext>,
  CREATED_AT_REQUIRED: {
    code: 'WEBHOOK.CREATED_AT_REQUIRED',
    title: 'Created At Required',
    detail: 'The webhook creation timestamp is required.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.CREATED_AT_REQUIRED', WebhookContext>,
  ENUM_NOT_ALLOWED_METHOD: {
    code: 'WEBHOOK.ENUM_NOT_ALLOWED_METHOD',
    title: 'Invalid Option',
    detail: 'Method must be one of: post, put.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.ENUM_NOT_ALLOWED_METHOD', WebhookContext>,
  ENUM_NOT_ALLOWED_STATUS: {
    code: 'WEBHOOK.ENUM_NOT_ALLOWED_STATUS',
    title: 'Invalid Option',
    detail: 'Status must be one of: active, paused, disabled.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.ENUM_NOT_ALLOWED_STATUS', WebhookContext>,
  INVALID_CODE_DATA: {
    code: 'WEBHOOK.INVALID_CODE_DATA',
    title: 'Invalid Version Data',
    detail: 'The webhook version data is invalid.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.INVALID_CODE_DATA', WebhookContext>,
  INVALID_DESCRIPTION: {
    code: 'WEBHOOK.INVALID_DESCRIPTION',
    title: 'Value Required',
    detail: 'Description is required for Webhook.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.INVALID_DESCRIPTION', WebhookContext>,
  INVALID_DESCRIPTION_DATA: {
    code: 'WEBHOOK.INVALID_DESCRIPTION_DATA',
    title: 'Value Required',
    detail: 'Description is required for Webhook.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.INVALID_DESCRIPTION_DATA', WebhookContext>,
  INVALID_EVENT_TYPE: {
    code: 'WEBHOOK.INVALID_EVENT_TYPE',
    title: 'Value Required',
    detail: 'Event type is required for Webhook.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.INVALID_EVENT_TYPE', WebhookContext>,
  INVALID_EVENT_TYPE_DATA: {
    code: 'WEBHOOK.INVALID_EVENT_TYPE_DATA',
    title: 'Value Required',
    detail: 'Event type is required for Webhook.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.INVALID_EVENT_TYPE_DATA', WebhookContext>,
  INVALID_HEADERS: {
    code: 'WEBHOOK.INVALID_HEADERS',
    title: 'Value Required',
    detail: 'Headers is required for Webhook.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.INVALID_HEADERS', WebhookContext>,
  INVALID_HEADERS_DATA: {
    code: 'WEBHOOK.INVALID_HEADERS_DATA',
    title: 'Value Required',
    detail: 'Headers is required for Webhook.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.INVALID_HEADERS_DATA', WebhookContext>,
  INVALID_ID: {
    code: 'WEBHOOK.INVALID_ID',
    title: 'Value Required',
    detail: 'Id is required for Webhook.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.INVALID_ID', WebhookContext>,
  INVALID_ID_DATA: {
    code: 'WEBHOOK.INVALID_ID_DATA',
    title: 'Value Required',
    detail: 'Id is required for Webhook.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.INVALID_ID_DATA', WebhookContext>,
  INVALID_METHOD: {
    code: 'WEBHOOK.INVALID_METHOD',
    title: 'Value Required',
    detail: 'Method is required for Webhook.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.INVALID_METHOD', WebhookContext>,
  INVALID_METHOD_DATA: {
    code: 'WEBHOOK.INVALID_METHOD_DATA',
    title: 'Value Required',
    detail: 'Method is required for Webhook.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.INVALID_METHOD_DATA', WebhookContext>,
  INVALID_METHOD_TRANSITION: {
    code: 'WEBHOOK.INVALID_METHOD_TRANSITION',
    title: 'Invalid State Transition',
    detail: 'Method cannot transition to the requested state.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.INVALID_METHOD_TRANSITION', WebhookContext>,
  INVALID_NAME: {
    code: 'WEBHOOK.INVALID_NAME',
    title: 'Value Required',
    detail: 'Name is required for Webhook.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.INVALID_NAME', WebhookContext>,
  INVALID_NAME_DATA: {
    code: 'WEBHOOK.INVALID_NAME_DATA',
    title: 'Value Required',
    detail: 'Name is required for Webhook.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.INVALID_NAME_DATA', WebhookContext>,
  INVALID_SIGNING_SECRET: {
    code: 'WEBHOOK.INVALID_SIGNING_SECRET',
    title: 'Value Required',
    detail: 'Signing secret is required for Webhook.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.INVALID_SIGNING_SECRET', WebhookContext>,
  INVALID_SIGNING_SECRET_DATA: {
    code: 'WEBHOOK.INVALID_SIGNING_SECRET_DATA',
    title: 'Value Required',
    detail: 'Signing secret is required for Webhook.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.INVALID_SIGNING_SECRET_DATA', WebhookContext>,
  INVALID_STATUS: {
    code: 'WEBHOOK.INVALID_STATUS',
    title: 'Value Required',
    detail: 'Status is required for Webhook.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.INVALID_STATUS', WebhookContext>,
  INVALID_STATUS_DATA: {
    code: 'WEBHOOK.INVALID_STATUS_DATA',
    title: 'Value Required',
    detail: 'Status is required for Webhook.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.INVALID_STATUS_DATA', WebhookContext>,
  INVALID_STATUS_TRANSITION: {
    code: 'WEBHOOK.INVALID_STATUS_TRANSITION',
    title: 'Invalid State Transition',
    detail: 'Status cannot transition to the requested state.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.INVALID_STATUS_TRANSITION', WebhookContext>,
  INVALID_TARGET_URL: {
    code: 'WEBHOOK.INVALID_TARGET_URL',
    title: 'Value Required',
    detail: 'Target url is required for Webhook.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.INVALID_TARGET_URL', WebhookContext>,
  INVALID_TARGET_URL_DATA: {
    code: 'WEBHOOK.INVALID_TARGET_URL_DATA',
    title: 'Value Required',
    detail: 'Target url is required for Webhook.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.INVALID_TARGET_URL_DATA', WebhookContext>,
  INVALID_WEBHOOK_DATA: {
    code: 'WEBHOOK.INVALID_WEBHOOK_DATA',
    title: 'Invalid Data',
    detail: 'Invalid Webhook data provided',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.INVALID_WEBHOOK_DATA', WebhookContext>,
  NOT_IMPLEMENTED: {
    code: 'WEBHOOK.NOT_IMPLEMENTED',
    title: 'Not Implemented',
    detail: 'Method not implemented for Webhook.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.NOT_IMPLEMENTED', WebhookContext>,
  PERMISSION_DENIED: {
    code: 'WEBHOOK.PERMISSION_DENIED',
    title: 'Permission Denied',
    detail: 'Insufficient permissions to access webhook',
    category: 'domain',
    retryable: false,
  } as DomainError<'WEBHOOK.PERMISSION_DENIED', WebhookContext>,
  PERMISSION_INVALID: {
    code: 'WEBHOOK.PERMISSION_INVALID',
    title: 'Invalid Permission',
    detail: 'The provided permission for webhook is not valid',
    category: 'domain',
    retryable: false,
  } as DomainError<'WEBHOOK.PERMISSION_INVALID', WebhookContext>,
  PERMISSION_NOT_FOUND: {
    code: 'WEBHOOK.PERMISSION_NOT_FOUND',
    title: 'Permission Not Found',
    detail: 'The requested permission for webhook does not exist',
    category: 'domain',
    retryable: false,
  } as DomainError<'WEBHOOK.PERMISSION_NOT_FOUND', WebhookContext>,
  STATUS_INVALID_TRANSITION: {
    code: 'WEBHOOK.STATUS_INVALID_TRANSITION',
    title: 'Invalid Status Transition',
    detail: 'Cannot transition Status to the requested state.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.STATUS_INVALID_TRANSITION', WebhookContext>,
  UPDATED_AT_REQUIRED: {
    code: 'WEBHOOK.UPDATED_AT_REQUIRED',
    title: 'Updated At Required',
    detail: 'The webhook update timestamp is required.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.UPDATED_AT_REQUIRED', WebhookContext>,
  WEBHOOK_ALREADY_ACTIVE: {
    code: 'WEBHOOK.WEBHOOK_ALREADY_ACTIVE',
    title: 'Already Active',
    detail: 'Status is already active for Webhook.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.WEBHOOK_ALREADY_ACTIVE', WebhookContext>,
  WEBHOOK_ALREADY_EXISTS: {
    code: 'WEBHOOK.WEBHOOK_ALREADY_EXISTS',
    title: 'Already Exists',
    detail: 'Webhook already exists',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.WEBHOOK_ALREADY_EXISTS', WebhookContext>,
  WEBHOOK_ALREADY_INACTIVE: {
    code: 'WEBHOOK.WEBHOOK_ALREADY_INACTIVE',
    title: 'Already Inactive',
    detail: 'Status is already inactive for Webhook.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.WEBHOOK_ALREADY_INACTIVE', WebhookContext>,
  WEBHOOK_FACTORY_CREATION_FAILED: {
    code: 'WEBHOOK.WEBHOOK_FACTORY_CREATION_FAILED',
    title: 'Factory Creation Failed',
    detail: 'Failed to create Webhook using factory.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.WEBHOOK_FACTORY_CREATION_FAILED', WebhookContext>,
  WEBHOOK_INVARIANT_VIOLATION: {
    code: 'WEBHOOK.WEBHOOK_INVARIANT_VIOLATION',
    title: 'Invariant Violation',
    detail: 'Business invariant violation for Webhook',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.WEBHOOK_INVARIANT_VIOLATION', WebhookContext>,
  WEBHOOK_NOT_FOUND: {
    code: 'WEBHOOK.WEBHOOK_NOT_FOUND',
    title: 'Not Found',
    detail: 'Webhook not found',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.WEBHOOK_NOT_FOUND', WebhookContext>,
  WEBHOOK_UPDATE_FACTORY_FAILED: {
    code: 'WEBHOOK.WEBHOOK_UPDATE_FACTORY_FAILED',
    title: 'Update Factory Failed',
    detail: 'Failed to update Webhook using factory.',
    category: 'validation',
    retryable: false,
  } as DomainError<'WEBHOOK.WEBHOOK_UPDATE_FACTORY_FAILED', WebhookContext>,
  WEBHOOK_UPDATE_RECONSTITUTE_FAILED: {
    code: 'WEBHOOK.WEBHOOK_UPDATE_RECONSTITUTE_FAILED',
    title: 'Update Reconstitute Failed',
    detail: 'Failed to reconstitute Webhook from snapshot for update.',
    category: 'validation',
    retryable: false,
  } as DomainError<
    'WEBHOOK.WEBHOOK_UPDATE_RECONSTITUTE_FAILED',
    WebhookContext
  >,
} as const;
