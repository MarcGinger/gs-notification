// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  Controller,
  Get,
  Query,
  Post,
  Body,
  Put,
  HttpStatus,
  HttpCode,
  Param,
  UseInterceptors,
  UseGuards,
} from '@nestjs/common';
import {
  ApiTags,
  ApiOperation,
  ApiResponse,
  ApiCreatedResponse,
  ApiBody,
  ApiHeader,
  ApiParam,
  ApiOkResponse,
  ApiBearerAuth,
} from '@nestjs/swagger';
import { IdempotencyKey } from 'src/shared/interfaces/decorators';
import { IdempotencyKeyPipe } from 'src/shared/interfaces/pipes';
import { WebhookApplicationService } from '../../../application/services';
import { CurrentUser, IUserToken, JwtAuthGuard } from 'src/shared/security';
import {
  DetailWebhookResponse,
  CreateWebhookRequest,
  UpdateWebhookRequest,
  WebhookPageResponse,
  ListWebhookFilterRequest,
  ListWebhookResponse,
} from '../../../application/dtos';
import { Result, ResultInterceptor, DomainError } from 'src/shared/errors';
import { PaginatedResponse } from 'src/shared/application/dtos';
import {
  WebhookReadResource,
  WebhookCreateResource,
  WebhookUpdateResource,
} from '../../webhook.resource';
import { ApiCommonErrors } from 'src/shared/interfaces/http';

@Controller()
@ApiTags('Webhooks')
@ApiBearerAuth()
@UseGuards(JwtAuthGuard)
@UseInterceptors(ResultInterceptor)
export class WebhookController {
  constructor(
    private readonly webhookApplicationService: WebhookApplicationService,
  ) {}

  @Get()
  @WebhookReadResource()
  @ApiOperation({
    summary: 'List Webhooks',
    description:
      'Retrieves a list of Webhooks with optional filtering. Supports pagination and filtering by name or category. Requires READ permission (LOW risk).',
  })
  @ApiOkResponse({
    description: 'List of Webhooks retrieved successfully',
    type: WebhookPageResponse,
  })
  @ApiCommonErrors()
  async list(
    @CurrentUser() user: IUserToken,
    @Query() pageRequest?: ListWebhookFilterRequest,
  ): Promise<Result<PaginatedResponse<ListWebhookResponse>, DomainError>> {
    const result = await this.webhookApplicationService.listWebhooks(
      user,
      pageRequest,
    );
    return result;
  }

  @Get(':id')
  @WebhookReadResource()
  @ApiOperation({
    summary: 'Get Webhook by ID',
    description:
      'Retrieves a single Webhook by its unique identifier. Requires READ permission (LOW risk).',
  })
  @ApiParam({
    name: 'id',
    type: 'string',
    description: 'Webhook unique identifier',
    example: 'T01EXAMPLE123',
  })
  @ApiOkResponse({
    description: 'Webhook details retrieved successfully',
    type: DetailWebhookResponse,
  })
  @ApiCommonErrors()
  async get(
    @CurrentUser() user: IUserToken,
    @Param('id') id: string,
  ): Promise<Result<DetailWebhookResponse, DomainError>> {
    const result = await this.webhookApplicationService.getWebhookById(
      user,
      id,
    );

    return result;
  }
  // ========================================
  // Core CRUD Operations
  // ========================================

  @Post()
  @WebhookCreateResource()
  @HttpCode(HttpStatus.CREATED)
  @ApiOperation({
    summary: 'Create a new Webhook',
    description:
      'Creates a new Webhook in the catalog. Requires CREATE permission (MEDIUM risk).',
  })
  @ApiHeader({
    name: 'Idempotency-Key',
    required: false,
    description:
      'Ensures the request is processed only once. Accepts both Idempotency-Key and x-idempotency-key headers.',
  })
  @ApiCreatedResponse({
    type: DetailWebhookResponse,
    description: 'Webhook successfully created',
    headers: {
      Location: {
        description: 'URI of the newly created resource',
        schema: {
          type: 'string',
          example: '/webhooks/123e4567-e89b-12d3-a456-426614174000',
        },
      },
    },
  })
  @ApiCommonErrors()
  @ApiBody({ type: CreateWebhookRequest })
  async create(
    @CurrentUser() user: IUserToken,
    @Body() createWebhookRequest: CreateWebhookRequest,
    @IdempotencyKey(new IdempotencyKeyPipe())
    idempotencyKey: string | undefined,
  ): Promise<Result<DetailWebhookResponse, DomainError>> {
    // idempotencyKey is validated by the IdempotencyKeyPipe

    // Call service with options including idempotency key
    const result = await this.webhookApplicationService.createWebhook(
      user,
      createWebhookRequest,
      {
        idempotencyKey,
      },
    );

    return result;
  }

  @Put(':id')
  @WebhookUpdateResource()
  @ApiOperation({
    summary: 'Update a Webhook',
    description:
      'Updates an existing Webhook with new data. Supports partial updates. Requires UPDATE permission (MEDIUM risk).',
  })
  @ApiParam({
    name: 'id',
    type: 'string',
    description: 'Webhook unique identifier',
    example: 'T01EXAMPLE123',
  })
  @ApiResponse({
    status: HttpStatus.OK,
    description: 'Webhook updated successfully',
    type: DetailWebhookResponse,
  })
  @ApiCommonErrors({
    include422: true,
    extra: [
      { status: 429, description: 'Too Many Requests' },
      { status: 503, description: 'Upstream dependency unavailable' },
    ],
  })
  @ApiBody({
    type: UpdateWebhookRequest,
    description: 'Webhook update data. Only provided fields will be updated.',
  })
  async update(
    @CurrentUser() user: IUserToken,
    @Param('id') id: string,
    @Body() updateWebhookRequest: UpdateWebhookRequest,
  ): Promise<Result<DetailWebhookResponse, DomainError>> {
    const result = await this.webhookApplicationService.updateWebhook(
      user,
      id,
      updateWebhookRequest,
    );

    return result;
  }
}
