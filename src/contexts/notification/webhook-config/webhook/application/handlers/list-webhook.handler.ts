// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { QueryHandler, IQueryHandler } from '@nestjs/cqrs';
import { Injectable } from '@nestjs/common';
import { DomainError, Result } from 'src/shared/errors';
import { ListWebhookQuery } from '../queries';
import { ListWebhookUseCase } from '../use-cases';
import { WebhookPageResponse } from '../dtos';

/**
 * ✅ Enhanced CQRS Query Handler with use case delegation
 * Follows the pattern: Handler → Use Case → Repository
 */
@Injectable()
@QueryHandler(ListWebhookQuery)
export class ListWebhookHandler implements IQueryHandler<ListWebhookQuery> {
  constructor(private readonly listWebhookUseCase: ListWebhookUseCase) {}

  /**
   * Handle the list webhook query by delegating to the use case
   */
  async execute(
    query: ListWebhookQuery,
  ): Promise<Result<WebhookPageResponse, DomainError>> {
    // ✅ Delegate to use case for business logic
    return await this.listWebhookUseCase.execute({
      user: query.user,
      filter: query.filter,
      correlationId: query.correlationId || 'unknown',
    });
  }
}
