// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { QueryHandler, IQueryHandler } from '@nestjs/cqrs';
import { DetailWebhookResponse } from '../dtos';
import { GetWebhookUseCase } from '../use-cases';
import { GetWebhookQuery } from '../queries';
import { Result, DomainError } from 'src/shared/errors';

@QueryHandler(GetWebhookQuery)
export class GetWebhookHandler implements IQueryHandler<GetWebhookQuery> {
  constructor(private readonly getWebhookUseCase: GetWebhookUseCase) {}

  async execute(
    query: GetWebhookQuery,
  ): Promise<Result<DetailWebhookResponse | null, DomainError>> {
    return this.getWebhookUseCase.execute({
      user: query.user,
      id: query.id,
      correlationId: query.correlationId || 'query-handler',
    });
  }
}
