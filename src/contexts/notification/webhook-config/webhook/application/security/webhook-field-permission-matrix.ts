// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { WebhookPermission } from '../../domain/permissions/webhook.permissions';

/**
 * Field-level permission matrix for Webhook updates.
 * Maps webhook fields to the required permissions for modification.
 */
export class WebhookFieldPermissionMatrix {
  private static readonly fieldPermissions: Record<
    string,
    WebhookPermission[]
  > = {
    // Basic webhook information fields - require standard update permission
    name: [WebhookPermission.DOMAIN_WEBHOOK_UPDATE],
    description: [WebhookPermission.DOMAIN_WEBHOOK_UPDATE],
    category: [WebhookPermission.DOMAIN_WEBHOOK_UPDATE],

    // Status fields - may require special permissions or admin rights
    active: [
      WebhookPermission.DOMAIN_WEBHOOK_UPDATE,
      WebhookPermission.DOMAIN_WEBHOOK_ADMIN,
    ],
    status: [
      WebhookPermission.DOMAIN_WEBHOOK_UPDATE,
      WebhookPermission.DOMAIN_WEBHOOK_ADMIN,
    ],

    // Financial fields - typically require higher permissions
    fees: [WebhookPermission.DOMAIN_WEBHOOK_UPDATE],
    rates: [WebhookPermission.DOMAIN_WEBHOOK_UPDATE],
    pricing: [WebhookPermission.DOMAIN_WEBHOOK_UPDATE],

    // Configuration fields
    channelCodes: [WebhookPermission.DOMAIN_WEBHOOK_UPDATE],
    railCodes: [WebhookPermission.DOMAIN_WEBHOOK_UPDATE],
    configuration: [WebhookPermission.DOMAIN_WEBHOOK_UPDATE],

    // Administrative fields - require admin permissions
    tenant: [WebhookPermission.DOMAIN_WEBHOOK_ADMIN],
    auditInfo: [WebhookPermission.DOMAIN_WEBHOOK_ADMIN],
    metadata: [WebhookPermission.DOMAIN_WEBHOOK_UPDATE],
  };

  /**
   * Get required permissions for a specific field.
   * Returns empty array if field has no special permission requirements.
   */
  static getRequiredPermissions(fieldName: string): WebhookPermission[] {
    return this.fieldPermissions[fieldName] || [];
  }

  /**
   * Check if a field has specific permission requirements.
   */
  static hasSpecialPermissions(fieldName: string): boolean {
    return fieldName in this.fieldPermissions;
  }

  /**
   * Get all fields that require a specific permission.
   */
  static getFieldsForPermission(permission: WebhookPermission): string[] {
    return Object.entries(this.fieldPermissions)
      .filter(([, perms]) => perms.includes(permission))
      .map(([field]) => field);
  }

  /**
   * Get all defined field permissions.
   */
  static getAllFieldPermissions(): Record<string, WebhookPermission[]> {
    return { ...this.fieldPermissions };
  }

  /**
   * Add or update permission requirements for a field.
   */
  static setFieldPermissions(
    fieldName: string,
    permissions: WebhookPermission[],
  ): void {
    this.fieldPermissions[fieldName] = [...permissions];
  }
}
