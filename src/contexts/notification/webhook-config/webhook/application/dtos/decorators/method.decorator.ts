// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { applyDecorators } from '@nestjs/common';
import { ApiProperty } from '@nestjs/swagger';
import { IsEnum, IsOptional, IsNotEmpty } from 'class-validator';
import {
  WebhookMethodValues,
  WebhookMethodValue,
} from '../../../domain/value-objects';

export { WebhookMethodValues, WebhookMethodValue };
/**
 * Options for property decorators (enum)
 */
interface PropOptions {
  required?: boolean;
}

/**
 * Property decorator for Webhook Method with required option
 * @param {Object} options - Options for the decorator
 * @returns {PropertyDecorator}
 */
export function ApiWebhookMethod(options: PropOptions = {}) {
  const { required = false } = options;
  return applyDecorators(
    ApiProperty({
      description: `Unique identifier for the Slack application as registered in the Slack App Directory. Used for app-specific API calls and identifying the source of bot interactions. Values include post, put.`,
      type: String,
      enum: WebhookMethodValues,
      required,
    }),
    IsEnum(WebhookMethodValues),
    required ? IsNotEmpty() : IsOptional(),
  );
}
