// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { applyDecorators } from '@nestjs/common';
import { ApiProperty } from '@nestjs/swagger';
import { IsEnum, IsOptional, IsNotEmpty } from 'class-validator';
import {
  WebhookStatusValues,
  WebhookStatusValue,
} from '../../../domain/value-objects';

export { WebhookStatusValues, WebhookStatusValue };
/**
 * Options for property decorators (enum)
 */
interface PropOptions {
  required?: boolean;
}

/**
 * Property decorator for Webhook Status with required option
 * @param {Object} options - Options for the decorator
 * @returns {PropertyDecorator}
 */
export function ApiWebhookStatus(options: PropOptions = {}) {
  const { required = true } = options;
  return applyDecorators(
    ApiProperty({
      description: `Global toggle for Slack integration functionality. When false, all notifications to this workspace are disabled, useful for maintenance or temporary suspension. Values include active, paused, disabled.`,
      type: String,
      enum: WebhookStatusValues,
      required,
    }),
    IsEnum(WebhookStatusValues),
    required ? IsNotEmpty() : IsOptional(),
  );
}
