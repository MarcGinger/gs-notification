// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

/**
 * Abstract Use Case Contracts for Webhook Operations
 *
 * These abstract classes are used instead of interfaces to work with NestJS DI.
 * TypeScript interfaces are erased at runtime and cannot be injected.
 */

import { Result, DomainError } from 'src/shared/errors';
import type { IUserToken } from 'src/shared/security';
import type { UpsertWebhookProps } from '../../domain/props';
import type {
  DetailWebhookResponse,
  WebhookPageResponse,
  ListWebhookFilterRequest,
} from '../dtos';

export abstract class IUpsertWebhookUseCase {
  abstract execute(params: {
    user: IUserToken;
    id: string;
    props: UpsertWebhookProps;
    correlationId: string;
    authorizationReason: string;
  }): Promise<Result<DetailWebhookResponse, DomainError>>;
}

export abstract class IGetWebhookUseCase {
  abstract execute(params: {
    user: IUserToken;
    id: string;
    correlationId: string;
  }): Promise<Result<DetailWebhookResponse, DomainError>>;
}
export abstract class IListWebhookUseCase {
  abstract execute(params: {
    user: IUserToken;
    filter?: ListWebhookFilterRequest;
    correlationId: string;
  }): Promise<Result<WebhookPageResponse, DomainError>>;
}
