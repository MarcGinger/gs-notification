// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DomainError, Result, ok, err } from 'src/shared/errors';
import {
  createIntegerVO,
  createIntegerVOErrors,
  IntegerVOInstance,
  createDateTimeVO,
  createDateTimeVOErrors,
  DateTimeVOInstance,
} from 'src/shared/domain/value-objects';
import { Clock } from 'src/shared/domain/clock';
import { AuthErrors } from '../errors/auth.errors';

/**
 * Auth Instance Value Objects
 *
 * Instance VOs for Auth aggregate lifecycle management.
 * Includes creation timestamps, update timestamps, and version tracking.
 * Follows the Clock injection pattern for deterministic testing.
 */

// ===========================================
// Auth Created At Instance VO
// ===========================================

const AuthCreatedAtErrors = createDateTimeVOErrors(
  AuthErrors.CREATED_AT_REQUIRED,
  'Auth Created At',
);

export const AuthCreatedAt = createDateTimeVO({
  name: 'AuthCreatedAt',
  allowFuture: false, // Cannot create auth in the future
  allowPast: true, // Can create with past timestamps (data migration)
  customValidation: (date: Date): Result<void, DomainError> => {
    // Optional: Add domain-specific creation time validation
    // For example, cannot be more than 1 year in the past
    const oneYearAgo = new Date();
    oneYearAgo.setFullYear(oneYearAgo.getFullYear() - 1);

    if (date < oneYearAgo) {
      return err({
        ...AuthErrors.CREATED_AT_REQUIRED,
        detail: 'Auth creation date cannot be more than 1 year in the past',
        context: { value: date, maxPastDate: oneYearAgo },
      } as DomainError);
    }

    return ok(undefined);
  },
  errors: AuthCreatedAtErrors,
});

export type AuthCreatedAt = DateTimeVOInstance;

// ===========================================
// Auth Updated At Instance VO
// ===========================================

const AuthUpdatedAtErrors = createDateTimeVOErrors(
  AuthErrors.UPDATED_AT_REQUIRED,
  'Auth Updated At',
);

export const AuthUpdatedAt = createDateTimeVO({
  name: 'AuthUpdatedAt',
  allowFuture: false, // Cannot update auth in the future
  allowPast: true, // Can update with past timestamps (data correction)
  errors: AuthUpdatedAtErrors,
});

export type AuthUpdatedAt = DateTimeVOInstance;

// ===========================================
// Auth Version Instance VO
// ===========================================

const AuthVersionErrors = createIntegerVOErrors(
  AuthErrors.INVALID_CODE_DATA, // Reusing existing error until specific version error is added
  'Auth Version',
);

export const AuthVersion = createIntegerVO({
  name: 'AuthVersion',
  min: 0, // Version starts at 0
  allowNegative: false,
  errors: AuthVersionErrors,
});

export type AuthVersion = IntegerVOInstance;

// ===========================================
// Clock-Based Helper Functions (Explicit Dependencies)
// ===========================================

/**
 * Create AuthCreatedAt from Clock (explicit dependency)
 * Deterministic and testable - pass a fake clock in tests
 */
export const createdAtNow = (
  clock: Clock,
): Result<AuthCreatedAt, DomainError> => AuthCreatedAt.create(clock.now());

/**
 * Create AuthUpdatedAt from Clock (explicit dependency)
 * Deterministic and testable - pass a fake clock in tests
 */
export const updatedAtNow = (
  clock: Clock,
): Result<AuthUpdatedAt, DomainError> => AuthUpdatedAt.create(clock.now());

/**
 * Bump version by 1 (domain business rule)
 */
export const bumpVersion = (
  currentVersion: AuthVersion,
): Result<AuthVersion, DomainError> =>
  AuthVersion.create(currentVersion.value + 1);

/**
 * Create initial version (starts at 1 for new aggregates)
 */
export const initialVersion = (): Result<AuthVersion, DomainError> =>
  AuthVersion.create(1);

// ===========================================
// Direct Creation Functions (Non-Clock Based)
// ===========================================

/**
 * Create AuthCreatedAt from Date
 */
export const createAuthCreatedAt = (
  date: Date,
): Result<AuthCreatedAt, DomainError> => AuthCreatedAt.create(date);

/**
 * Create AuthUpdatedAt from Date
 */
export const createAuthUpdatedAt = (
  date: Date,
): Result<AuthUpdatedAt, DomainError> => AuthUpdatedAt.create(date);

/**
 * Create AuthVersion from number
 */
export const createAuthVersion = (
  version: number,
): Result<AuthVersion, DomainError> => AuthVersion.create(version);

// ===========================================
// Domain Invariants & Validation
// ===========================================

/**
 * Validate that updatedAt is not before createdAt (monotonic time)
 */
export const validateMonotonicTime = (
  createdAt: AuthCreatedAt,
  updatedAt: AuthUpdatedAt,
): Result<void, DomainError> => {
  if (updatedAt.value < createdAt.value) {
    return err({
      ...AuthErrors.UPDATED_AT_REQUIRED,
      detail: 'Updated at cannot be before created at',
      context: {
        createdAt: createdAt.value.toISOString(),
        updatedAt: updatedAt.value.toISOString(),
      },
    } as DomainError);
  }
  return ok(undefined);
};

// ===========================================
// Legacy Compatibility (DEPRECATED)
// ===========================================

/**
 * @deprecated Use createdAtNow(clock) instead for better testability
 * This function hides the Date dependency and makes testing harder
 */
export function createAuthCreatedAtNow(): Result<AuthCreatedAt, DomainError> {
  return AuthCreatedAt.create(new Date());
}

/**
 * @deprecated Use updatedAtNow(clock) instead for better testability
 * This function hides the Date dependency and makes testing harder
 */
export function createAuthUpdatedAtNow(): Result<AuthUpdatedAt, DomainError> {
  return AuthUpdatedAt.create(new Date());
}
