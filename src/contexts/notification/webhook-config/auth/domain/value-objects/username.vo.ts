// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { AuthErrors } from '../errors/auth.errors';

/**
 * Username Value Object
 * Represents a validated username with business rules
 */
export const AuthUsername = createStringVO({
  name: 'Username',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 255,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(AuthErrors.INVALID_USERNAME, 'Username'),
});

/**
 * Public instance type for Username
 */
export type AuthUsername = StringVOInstance;

// Convenience creators
export const createAuthUsername = (s: string) => AuthUsername.create(s);
export const authUsernameFrom = (v: unknown) => AuthUsername.from(v);
