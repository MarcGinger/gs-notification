// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { AuthErrors } from '../errors/auth.errors';

/**
 * Password Value Object
 * Represents a validated password with business rules
 */
export const AuthPassword = createStringVO({
  name: 'Password',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 255,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(AuthErrors.INVALID_PASSWORD, 'Password'),
});

/**
 * Public instance type for Password
 */
export type AuthPassword = StringVOInstance;

// Convenience creators
export const createAuthPassword = (s: string) => AuthPassword.create(s);
export const authPasswordFrom = (v: unknown) => AuthPassword.from(v);
