// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  BooleanVOInstance,
  createBooleanVO,
  createBooleanVOErrors,
} from 'src/shared/domain/value-objects';
import { AuthErrors } from '..//errors/auth.errors';

/**
 * VerifyTls Boolean Value Object
 * Represents a validated verifyTls with business rules
 */
export const AuthVerifyTls = createBooleanVO({
  name: 'VerifyTls',
  treatNullAsFalse: true,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createBooleanVOErrors(AuthErrors.INVALID_VERIFY_TLS, 'VerifyTls'),
});

/** Public instance type for VerifyTls */
export type AuthVerifyTls = BooleanVOInstance;

// Convenience creators
export const createAuthVerifyTls = (v: boolean) => AuthVerifyTls.create(v);
export const authVerifyTlsFrom = (v: unknown) => AuthVerifyTls.from(v);
