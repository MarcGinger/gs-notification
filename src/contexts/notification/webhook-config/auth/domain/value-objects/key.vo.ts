// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { AuthErrors } from '../errors/auth.errors';

/**
 * Key Value Object
 * Represents a validated key with business rules
 */
export const AuthKey = createStringVO({
  name: 'Key',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 255,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(AuthErrors.INVALID_KEY, 'Key'),
});

/**
 * Public instance type for Key
 */
export type AuthKey = StringVOInstance;

// Convenience creators
export const createAuthKey = (s: string) => AuthKey.create(s);
export const authKeyFrom = (v: unknown) => AuthKey.from(v);
