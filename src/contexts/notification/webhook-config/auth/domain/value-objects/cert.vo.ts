// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { AuthErrors } from '../errors/auth.errors';

/**
 * Cert Value Object
 * Represents a validated cert with business rules
 */
export const AuthCert = createStringVO({
  name: 'Cert',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 255,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(AuthErrors.INVALID_CERT, 'Cert'),
});

/**
 * Public instance type for Cert
 */
export type AuthCert = StringVOInstance;

// Convenience creators
export const createAuthCert = (s: string) => AuthCert.create(s);
export const authCertFrom = (v: unknown) => AuthCert.from(v);
