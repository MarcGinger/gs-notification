// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { AuthErrors } from '../errors/auth.errors';

/**
 * TokenUrl Value Object
 * Represents a validated tokenUrl with business rules
 */
export const AuthTokenUrl = createStringVO({
  name: 'TokenUrl',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 500,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(AuthErrors.INVALID_TOKEN_URL, 'TokenUrl'),
});

/**
 * Public instance type for TokenUrl
 */
export type AuthTokenUrl = StringVOInstance;

// Convenience creators
export const createAuthTokenUrl = (s: string) => AuthTokenUrl.create(s);
export const authTokenUrlFrom = (v: unknown) => AuthTokenUrl.from(v);
