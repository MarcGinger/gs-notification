// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { AuthErrors } from '../errors/auth.errors';

/**
 * Ca Value Object
 * Represents a validated ca with business rules
 */
export const AuthCa = createStringVO({
  name: 'Ca',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 255,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(AuthErrors.INVALID_CA, 'Ca'),
});

/**
 * Public instance type for Ca
 */
export type AuthCa = StringVOInstance;

// Convenience creators
export const createAuthCa = (s: string) => AuthCa.create(s);
export const authCaFrom = (v: unknown) => AuthCa.from(v);
