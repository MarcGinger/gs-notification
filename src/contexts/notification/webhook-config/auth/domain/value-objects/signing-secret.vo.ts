// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { AuthErrors } from '../errors/auth.errors';

/**
 * SigningSecret Value Object
 * Represents a validated signingSecret with business rules
 */
export const AuthSigningSecret = createStringVO({
  name: 'SigningSecret',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 255,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    AuthErrors.INVALID_SIGNING_SECRET,
    'SigningSecret',
  ),
});

/**
 * Public instance type for SigningSecret
 */
export type AuthSigningSecret = StringVOInstance;

// Convenience creators
export const createAuthSigningSecret = (s: string) =>
  AuthSigningSecret.create(s);
export const authSigningSecretFrom = (v: unknown) => AuthSigningSecret.from(v);
