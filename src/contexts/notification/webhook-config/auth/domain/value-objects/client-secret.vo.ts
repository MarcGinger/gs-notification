// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { AuthErrors } from '../errors/auth.errors';

/**
 * ClientSecret Value Object
 * Represents a validated clientSecret with business rules
 */
export const AuthClientSecret = createStringVO({
  name: 'ClientSecret',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 255,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    AuthErrors.INVALID_CLIENT_SECRET,
    'ClientSecret',
  ),
});

/**
 * Public instance type for ClientSecret
 */
export type AuthClientSecret = StringVOInstance;

// Convenience creators
export const createAuthClientSecret = (s: string) => AuthClientSecret.create(s);
export const authClientSecretFrom = (v: unknown) => AuthClientSecret.from(v);
