// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { AuthErrors } from '../errors/auth.errors';

/**
 * WebhookId Value Object
 * Represents a validated webhookId with business rules
 */
export const AuthWebhookId = createStringVO({
  name: 'WebhookId',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,
  minLength: 10,
  maxLength: 32,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(AuthErrors.INVALID_WEBHOOK_ID, 'WebhookId'),
});

/**
 * Public instance type for WebhookId
 */
export type AuthWebhookId = StringVOInstance;

// Convenience creators
export const createAuthWebhookId = (s: string) => AuthWebhookId.create(s);
export const authWebhookIdFrom = (v: unknown) => AuthWebhookId.from(v);
