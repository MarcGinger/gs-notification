// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  EnumVOInstance,
  createDisplayNameHelper,
  createEnumTypeGuards,
  createEnumVO,
  createEnumVOErrors,
  createStateTransitions,
} from 'src/shared/domain/value-objects';
import { Result, DomainError } from 'src/shared/errors';
import { AuthErrors } from '../errors/auth.errors';

// ============================================================================
// SINGLE SOURCE OF TRUTH
// ============================================================================

/**
 * AuthSignatureAlgorithm allowed values
 * Canonical definition used across all layers (API, Domain, DB)
 */
export const AuthSignatureAlgorithmValues = ['sha256', 'sha1'] as const;
export type AuthSignatureAlgorithmValue =
  (typeof AuthSignatureAlgorithmValues)[number];

// ============================================================================
// VALUE OBJECT - Domain Layer
// ============================================================================

/**
 * AuthSignatureAlgorithm Value Object
 *
 * Provides validation, business rules, and rich domain behavior
 * for auth signatureAlgorithm enumerated values.
 *
 * @example
 * ```typescript
 * const signatureAlgorithm = AuthSignatureAlgorithm.create('sha256');
 * if (signatureAlgorithm.ok) {
 *   console.log(signatureAlgorithm.value.canTransitionTo('sha1'));
 * }
 * ```
 */
export const AuthSignatureAlgorithm = createEnumVO({
  name: 'AuthSignatureAlgorithm',
  values: AuthSignatureAlgorithmValues,

  errors: createEnumVOErrors(
    AuthErrors.INVALID_SIGNATURE_ALGORITHM,
    'AuthSignatureAlgorithm',
  ),
});

export type AuthSignatureAlgorithm =
  EnumVOInstance<AuthSignatureAlgorithmValue>;

// ============================================================================
// CONVENIENCE FACTORIES
// ============================================================================

export const createAuthSignatureAlgorithm = (
  value: AuthSignatureAlgorithmValue | undefined,
) =>
  AuthSignatureAlgorithm.create(value) as Result<
    AuthSignatureAlgorithm,
    DomainError
  >;
export const authSignatureAlgorithmFrom = (value: unknown) =>
  AuthSignatureAlgorithm.from(value) as Result<
    AuthSignatureAlgorithm,
    DomainError
  >;

// ============================================================================
// BUSINESS LOGIC HELPERS - Domain Behavior
// ============================================================================

/**
 * Valid state transitions for auth signatureAlgorithm
 */
const AUTH_SIGNATURE_ALGORITHM_TRANSITIONS = {
  sha256: ['sha1'] as const,
  sha1: ['sha256'] as const,
} as const;

/**
 * Display names for auth signatureAlgorithm values
 */
const AUTH_SIGNATURE_ALGORITHM_DISPLAY_NAMES = {
  sha256: 'Sha256',
  sha1: 'Sha1',
} as const;

/**
 * State machine helpers for AuthSignatureAlgorithm transitions
 */
const AuthSignatureAlgorithmTransitions = createStateTransitions(
  AUTH_SIGNATURE_ALGORITHM_TRANSITIONS,
);

/**
 * Type guards for AuthSignatureAlgorithm values
 */
const AuthSignatureAlgorithmGuards = createEnumTypeGuards(
  AuthSignatureAlgorithmValues,
  'AuthSignatureAlgorithm',
  (value, validValues) => ({
    ...AuthErrors.INVALID_SIGNATURE_ALGORITHM,
    detail: `AuthSignatureAlgorithm must be one of: ${validValues.join(', ')}, received: ${String(value)}`,
    context: { value, validValues },
  }),
);

/**
 * Display name helpers for AuthSignatureAlgorithm
 */
const AuthSignatureAlgorithmDisplay = createDisplayNameHelper(
  AUTH_SIGNATURE_ALGORITHM_DISPLAY_NAMES,
);

/**
 * Business logic functions for AuthSignatureAlgorithm
 */
export const AuthSignatureAlgorithmLogic = {
  /**
   * Check if transition from one state to another is valid
   */
  canTransition: (
    from: AuthSignatureAlgorithmValue,
    to: AuthSignatureAlgorithmValue,
  ): boolean => AuthSignatureAlgorithmTransitions.canTransition(from, to),

  /**
   * Get all valid next states from current state
   */
  getValidTransitions: (
    from: AuthSignatureAlgorithmValue,
  ): readonly AuthSignatureAlgorithmValue[] =>
    AuthSignatureAlgorithmTransitions.getValidTransitions(from),

  /**
   * Get human-readable display name
   */
  getDisplayName: (value: AuthSignatureAlgorithmValue): string =>
    AuthSignatureAlgorithmDisplay.getDisplayName(value),

  /**
   * Get all available values with metadata
   */
  getAllValues(): Array<{
    value: AuthSignatureAlgorithmValue;
    displayName: string;
    validTransitions: readonly AuthSignatureAlgorithmValue[];
  }> {
    return AuthSignatureAlgorithmValues.map((value) => ({
      value,
      displayName: AuthSignatureAlgorithmLogic.getDisplayName(value),
      validTransitions: AuthSignatureAlgorithmLogic.getValidTransitions(value),
    }));
  },
} as const;

// ============================================================================
// TYPE GUARDS - Runtime Type Checking
// ============================================================================

/**
 * Type guard to check if value is valid AuthSignatureAlgorithm
 */
export const isAuthSignatureAlgorithmValue = (
  value: unknown,
): value is AuthSignatureAlgorithmValue =>
  AuthSignatureAlgorithmGuards.isValid(value);

/**
 * Assert that value is valid AuthSignatureAlgorithm (throws if invalid)
 */
export const assertAuthSignatureAlgorithmValue = (
  value: unknown,
): asserts value is AuthSignatureAlgorithmValue =>
  AuthSignatureAlgorithmGuards.assert(value);
