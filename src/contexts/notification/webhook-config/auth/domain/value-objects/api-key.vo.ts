// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { AuthErrors } from '../errors/auth.errors';

/**
 * ApiKey Value Object
 * Represents a validated apiKey with business rules
 */
export const AuthApiKey = createStringVO({
  name: 'ApiKey',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 255,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(AuthErrors.INVALID_API_KEY, 'ApiKey'),
});

/**
 * Public instance type for ApiKey
 */
export type AuthApiKey = StringVOInstance;

// Convenience creators
export const createAuthApiKey = (s: string) => AuthApiKey.create(s);
export const authApiKeyFrom = (v: unknown) => AuthApiKey.from(v);
