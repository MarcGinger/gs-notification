// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  PermissionRiskLevel,
  PermissionTenantScope,
  PermissionOperationType,
  BasePermissionMeta,
} from 'src/shared/domain/permissions';
import {
  createPermissionRegistry,
  createRoleHierarchy,
  assertRegistryComplete,
} from 'src/shared/domain/permissions/utils';
import { createDomainPermissionHelpers } from 'src/shared/domain/permissions/domain-permission-helpers.factory';

export enum AuthPermission {
  DOMAIN_AUTH_CREATE = 'DOMAIN_AUTH_CREATE',
  DOMAIN_AUTH_READ = 'DOMAIN_AUTH_READ',
  DOMAIN_AUTH_UPDATE = 'DOMAIN_AUTH_UPDATE',
  DOMAIN_AUTH_DELETE = 'DOMAIN_AUTH_DELETE',
  DOMAIN_AUTH_ADMIN = 'DOMAIN_AUTH_ADMIN',
  DOMAIN_AUTH_BULK_IMPORT = 'DOMAIN_AUTH_BULK_IMPORT',
  DOMAIN_AUTH_BULK_EXPORT = 'DOMAIN_AUTH_BULK_EXPORT',
  DOMAIN_AUTH_AUDIT_LOG = 'DOMAIN_AUTH_AUDIT_LOG',
}

// Permission registry using shared utilities
export const AuthPermissionRegistry = createPermissionRegistry<
  string,
  Record<string, BasePermissionMeta>
>({
  [AuthPermission.DOMAIN_AUTH_CREATE]: {
    description: 'Create auth records',
    riskLevel: PermissionRiskLevel.MEDIUM,
    requiresJustification: false,
    relatedPermissions: [],
    policyPath: 'TENANT.auth.auth_create',
    category: 'Core Operations',
    auditRequired: true,
    name: 'DOMAIN_AUTH_CREATE',
    action: 'auth_create',
    tags: ['crud', 'create'],
    operationType: PermissionOperationType.CRUD,
    policyRuleId: 'auth_auth_create',
    tenantScope: PermissionTenantScope.TENANT,
    i18nKey: 'permissions.auth.auth_create',
  },
  [AuthPermission.DOMAIN_AUTH_READ]: {
    description: 'Read auth records',
    riskLevel: PermissionRiskLevel.LOW,
    requiresJustification: false,
    relatedPermissions: [],
    policyPath: 'TENANT.auth.auth_read',
    category: 'Core Operations',
    auditRequired: false,
    name: 'DOMAIN_AUTH_READ',
    action: 'auth_read',
    tags: ['crud', 'read'],
    operationType: PermissionOperationType.CRUD,
    policyRuleId: 'auth_auth_read',
    tenantScope: PermissionTenantScope.TENANT,
    i18nKey: 'permissions.auth.auth_read',
  },
  [AuthPermission.DOMAIN_AUTH_UPDATE]: {
    description: 'Update auth records',
    riskLevel: PermissionRiskLevel.MEDIUM,
    requiresJustification: false,
    relatedPermissions: [AuthPermission.DOMAIN_AUTH_READ],
    policyPath: 'TENANT.auth.auth_update',
    category: 'Core Operations',
    auditRequired: true,
    name: 'DOMAIN_AUTH_UPDATE',
    action: 'auth_update',
    tags: ['crud', 'update'],
    operationType: PermissionOperationType.CRUD,
    policyRuleId: 'auth_auth_update',
    tenantScope: PermissionTenantScope.TENANT,
    i18nKey: 'permissions.auth.auth_update',
  },
  [AuthPermission.DOMAIN_AUTH_DELETE]: {
    description: 'Delete auth records',
    riskLevel: PermissionRiskLevel.HIGH,
    requiresJustification: true,
    relatedPermissions: [
      AuthPermission.DOMAIN_AUTH_READ,
      AuthPermission.DOMAIN_AUTH_UPDATE,
    ],
    policyPath: 'TENANT.auth.auth_delete',
    category: 'Core Operations',
    auditRequired: true,
    name: 'DOMAIN_AUTH_DELETE',
    action: 'auth_delete',
    tags: ['crud', 'delete'],
    operationType: PermissionOperationType.CRUD,
    policyRuleId: 'auth_auth_delete',
    tenantScope: PermissionTenantScope.TENANT,
    i18nKey: 'permissions.auth.auth_delete',
  },
  [AuthPermission.DOMAIN_AUTH_ADMIN]: {
    description: 'Full administrative access to auth',
    riskLevel: PermissionRiskLevel.CRITICAL,
    requiresJustification: true,
    relatedPermissions: [
      AuthPermission.DOMAIN_AUTH_CREATE,
      AuthPermission.DOMAIN_AUTH_READ,
      AuthPermission.DOMAIN_AUTH_UPDATE,
      AuthPermission.DOMAIN_AUTH_DELETE,
    ],
    policyPath: 'TENANT.auth.auth_admin',
    category: 'Administrative',
    auditRequired: true,
    name: 'DOMAIN_AUTH_ADMIN',
    action: 'auth_admin',
    tags: ['admin', 'full-access'],
    operationType: PermissionOperationType.ADMIN,
    policyRuleId: 'auth_auth_admin',
    tenantScope: PermissionTenantScope.TENANT,
    i18nKey: 'permissions.auth.auth_admin',
  },
  [AuthPermission.DOMAIN_AUTH_BULK_IMPORT]: {
    description: 'Bulk import auth records',
    riskLevel: PermissionRiskLevel.HIGH,
    requiresJustification: true,
    relatedPermissions: [],
    policyPath: 'TENANT.auth.auth_bulk_import',
    category: 'Bulk Operations',
    auditRequired: true,
    name: 'DOMAIN_AUTH_BULK_IMPORT',
    action: 'auth_bulk_import',
    tags: ['bulk', 'import'],
    operationType: PermissionOperationType.BULK,
    policyRuleId: 'auth_auth_bulk_import',
    tenantScope: PermissionTenantScope.TENANT,
    i18nKey: 'permissions.auth.auth_bulk_import',
  },
  [AuthPermission.DOMAIN_AUTH_BULK_EXPORT]: {
    description: 'Bulk export auth records',
    riskLevel: PermissionRiskLevel.HIGH,
    requiresJustification: true,
    relatedPermissions: [],
    policyPath: 'TENANT.auth.auth_bulk_export',
    category: 'Bulk Operations',
    auditRequired: true,
    name: 'DOMAIN_AUTH_BULK_EXPORT',
    action: 'auth_bulk_export',
    tags: ['bulk', 'export'],
    operationType: PermissionOperationType.BULK,
    policyRuleId: 'auth_auth_bulk_export',
    tenantScope: PermissionTenantScope.TENANT,
    i18nKey: 'permissions.auth.auth_bulk_export',
  },
  [AuthPermission.DOMAIN_AUTH_AUDIT_LOG]: {
    description: 'Access auth audit logs',
    riskLevel: PermissionRiskLevel.HIGH,
    requiresJustification: true,
    relatedPermissions: [],
    policyPath: 'TENANT.auth.auth_audit_log',
    category: 'Audit & Compliance',
    auditRequired: true,
    name: 'DOMAIN_AUTH_AUDIT_LOG',
    action: 'auth_audit_log',
    tags: ['audit', 'logs'],
    operationType: PermissionOperationType.SENSITIVE,
    policyRuleId: 'auth_auth_audit_log',
    tenantScope: PermissionTenantScope.TENANT,
    i18nKey: 'permissions.auth.auth_audit_log',
  },
});

// Role hierarchy using shared utilities
export const AuthPermissionHierarchy = createRoleHierarchy<
  string,
  Record<string, readonly string[]>
>({
  AuthViewer: [AuthPermission.DOMAIN_AUTH_READ],
  AuthEditor: [
    AuthPermission.DOMAIN_AUTH_CREATE,
    AuthPermission.DOMAIN_AUTH_READ,
    AuthPermission.DOMAIN_AUTH_UPDATE,
  ],
  AuthManager: [
    AuthPermission.DOMAIN_AUTH_CREATE,
    AuthPermission.DOMAIN_AUTH_READ,
    AuthPermission.DOMAIN_AUTH_UPDATE,
    AuthPermission.DOMAIN_AUTH_DELETE,
    AuthPermission.DOMAIN_AUTH_BULK_IMPORT,
    AuthPermission.DOMAIN_AUTH_BULK_EXPORT,
    AuthPermission.DOMAIN_AUTH_AUDIT_LOG,
  ],
  AuthAdmin: [
    AuthPermission.DOMAIN_AUTH_CREATE,
    AuthPermission.DOMAIN_AUTH_READ,
    AuthPermission.DOMAIN_AUTH_UPDATE,
    AuthPermission.DOMAIN_AUTH_DELETE,
    AuthPermission.DOMAIN_AUTH_ADMIN,
    AuthPermission.DOMAIN_AUTH_BULK_IMPORT,
    AuthPermission.DOMAIN_AUTH_BULK_EXPORT,
    AuthPermission.DOMAIN_AUTH_AUDIT_LOG,
  ],
});

// Runtime completeness validation
const ALL_AUTH_PERMISSIONS = Object.values(AuthPermission);
assertRegistryComplete(ALL_AUTH_PERMISSIONS, AuthPermissionRegistry);

/**
 * Auth-specific field permission matrix for field-level security
 */
const AUTH_FIELD_PERMISSION_MATRIX = {
  metadata: [AuthPermission.DOMAIN_AUTH_ADMIN],
  bulkImport: [AuthPermission.DOMAIN_AUTH_BULK_IMPORT],
  bulkExport: [AuthPermission.DOMAIN_AUTH_BULK_EXPORT],
} as const;

/**
 * Factory-generated AuthPermissionHelpers with full functionality
 * This includes all methods needed by the authorization system:
 * - getRiskLevel, requiresJustification, isHighRisk
 * - getPolicyPath, requiresAudit (needed by auth.resource.ts)
 * - getByRiskLevel, expandWithRelated, etc.
 * - Field-level permission logic with FIELD_PERMISSION_MATRIX
 */
export const AuthPermissionHelpers = createDomainPermissionHelpers({
  domain: 'auth',
  permissions: AuthPermission,
  registry: AuthPermissionRegistry,
  fieldMatrix: AUTH_FIELD_PERMISSION_MATRIX,
});

// Add domain-specific convenience methods
export const AuthPermissionUtils = {
  /**
   * Get minimum permissions for basic auth management
   */
  getBasicManagementPermissions(): AuthPermission[] {
    return [
      AuthPermission.DOMAIN_AUTH_READ,
      AuthPermission.DOMAIN_AUTH_CREATE,
      AuthPermission.DOMAIN_AUTH_UPDATE,
    ];
  },

  /**
   * Check if user can perform bulk operations
   */
  canPerformBulkOperations(userPermissions: AuthPermission[]): boolean {
    return [
      AuthPermission.DOMAIN_AUTH_BULK_IMPORT,
      AuthPermission.DOMAIN_AUTH_BULK_EXPORT,
    ].some((perm) => userPermissions.includes(perm));
  },
} as const;
