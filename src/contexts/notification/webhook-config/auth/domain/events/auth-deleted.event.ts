// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { EventMetadata } from 'src/shared/domain/events';

/**
 * Auth Deleted Event Payload
 * Contains identifying information about the deleted auth
 */
export interface AuthDeletedEventPayload {
  webhookId: string;
  deletedAt: Date;
  version: number;
  metadata: EventMetadata;
}

/**
 * Auth Deleted Domain Event
 *
 * Emitted when a auth is successfully deleted.
 * Contains identifying information and deletion metadata.
 */
export class AuthDeletedEvent {
  public readonly eventType = 'NotificationWebhookConfigAuthDeleted.v1';
  public readonly eventVersion = 'v1';

  constructor(public readonly payload: AuthDeletedEventPayload) {}

  // Factory method with security context
  static create(
    data: Omit<AuthDeletedEventPayload, 'metadata'>,
    baseMetadata: EventMetadata,
  ): AuthDeletedEvent {
    const metadata: EventMetadata = {
      ...baseMetadata,
      eventVersion: '1.0',
      source: 'notification.domain.auth',
    };

    return new AuthDeletedEvent({
      ...data,
      metadata,
    });
  }

  get webhookId(): string {
    return this.payload.webhookId;
  }

  get deletedAt(): Date {
    return this.payload.deletedAt;
  }

  get version(): number {
    return this.payload.version;
  }

  get metadata(): EventMetadata {
    return this.payload.metadata;
  }
}
