// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Module } from '@nestjs/common';
import { RouterModule, Routes } from '@nestjs/core';
import { AuthModule } from '../../auth.module';
import { AuthProjectorModule } from '../../auth-projector.module';
/**
 * Auth Router Configuration
 *
 * Configures nested routing for auth endpoints to follow
 * the module monolith structure: /notification/webhook-config/api/v1/auths
 *
 * This replaces flat routing (/api/v1/auths) with contextual routing
 * that reflects the bounded context hierarchy.
 */

// Define the routing structure for the auth bounded context
const authRoutes: Routes = [
  {
    path: 'notification',
    children: [
      {
        path: 'webhook-config',
        children: [
          {
            path: 'auths',
            module: AuthModule,
          },
          {
            path: 'health',
            module: AuthProjectorModule,
          },
        ],
      },
    ],
  },
];

/**
 * Auth Router Module
 *
 * Provides nested routing configuration for auth endpoints.
 * This module should be imported in the main app module to enable
 * the nested route structure.
 *
 * Routes configured:
 * - /notification/webhook-config/api/v1/auths/:webhookId (GET, PUT)
 * - /notification/webhook-config/health/projectors/auth (GET)
 * - /notification/webhook-config/health/projectors/auth/metrics (GET)
 */
@Module({
  imports: [AuthModule, AuthProjectorModule, RouterModule.register(authRoutes)],
  exports: [AuthModule, AuthProjectorModule],
})
export class AuthRouterModule {}
