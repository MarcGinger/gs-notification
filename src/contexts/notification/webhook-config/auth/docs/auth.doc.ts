// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DocumentBuilder, SwaggerModule } from '@nestjs/swagger';
import { INestApplication } from '@nestjs/common';
import { SwaggerConfigUtil } from 'src/docs/swagger-config.util';
import { AuthModule } from '../auth.module';
/**
 * Auth documentation endpoints interface
 */
export interface AuthDocumentationUrls {
  auth: string;
}

/**
 * Auth Documentation
 * This module handles the Swagger documentation for auths
 *
 */
export class AuthDocumentation {
  static setup(app: INestApplication, port: string | number): void {
    const config = new DocumentBuilder()
      .addBearerAuth(
        { type: 'http', scheme: 'bearer', bearerFormat: 'JWT' },
        'bearer',
      )
      .setTitle('📱 Auth Management API')
      .setDescription(
        `
## bounded context: notification
[← Back to documentation](/api/docs/notification)

### application: webhook-config
[← Back to documentation](/api/docs/notification/webhook-config)
`,
      )
      .setVersion('1.0.0')
      .addTag('Auths', `Configuration for auth table`);

    // Add dynamic server configuration
    SwaggerConfigUtil.addServers(config, port);

    const document = SwaggerModule.createDocument(app, config.build(), {
      include: [AuthModule],
      extraModels: [],
    });

    SwaggerModule.setup(
      'api/docs/notification/webhook-config/auth',
      app,
      document,
    );
  }

  /**
   * Setup all auth-related documentation endpoints
   * This method centralizes the setup of all auth module documentation
   */
  static setupAll(
    app: INestApplication,
    port: string | number,
  ): AuthDocumentationUrls {
    // Setup main auth documentation
    AuthDocumentation.setup(app, port);

    // Setup sub-module documentation

    // Return all endpoint URLs
    return {
      auth: AuthDocumentation.getEndpoint(port),
    };
  }

  static getEndpoint(port: string | number): string {
    return `${SwaggerConfigUtil.getServerUrl(port)}/api/docs/notification/webhook-config/auth`;
  }
}
