// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { CommandHandler, ICommandHandler } from '@nestjs/cqrs';
import { Inject } from '@nestjs/common';
import { Result, DomainError } from 'src/shared/errors';
import { APP_LOGGER, componentLogger, Logger } from 'src/shared/logging';
import { CommandHandlerUtil } from 'src/shared/application';
import { DetailAuthResponse } from '../dtos';
import { UpsertAuthUseCase } from '../use-cases';
import { UpsertAuthCommand } from '../commands';

/**
 * Enhanced Upsert Auth Handler with security context support
 */
@CommandHandler(UpsertAuthCommand)
export class UpsertAuthHandler implements ICommandHandler<UpsertAuthCommand> {
  private readonly logger: Logger;

  constructor(
    @Inject(UpsertAuthUseCase)
    private readonly upsertAuthUseCase: UpsertAuthUseCase,
    @Inject(APP_LOGGER) moduleLogger: Logger,
  ) {
    this.logger = componentLogger(moduleLogger, 'UpsertAuthHandler');
  }

  /**
   * Executes the upsert auth command with enhanced security and validation
   */
  async execute(
    command: UpsertAuthCommand,
  ): Promise<Result<DetailAuthResponse, DomainError>> {
    const commandName = 'UpsertAuthCommand';

    // Log command execution start
    CommandHandlerUtil.logCommandStart(this.logger, commandName, command, {
      application: 'webhook-config',
      component: 'UpsertAuthHandler',
      extractCommandData: (cmd: UpsertAuthCommand) => ({
        propsKeys: Object.keys(cmd.props || {}),
      }),
    });

    // Transform command to use case parameters
    const result = await this.upsertAuthUseCase.execute({
      user: command.user,
      webhookId: command.webhookId,
      props: command.props,
      correlationId: command.correlationId,
      authorizationReason: 'CQRS Command Handler',
    });

    // Log result with enhanced context
    if (result.ok) {
      CommandHandlerUtil.logCommandSuccess(
        this.logger,
        commandName,
        command,
        result.value,
        {
          application: 'webhook-config',
          component: 'UpsertAuthHandler',
        },
      );
    } else {
      // Use shared error handling with custom context
      CommandHandlerUtil.logValidationError(
        this.logger,
        commandName,
        result.error,
        command,
        {
          application: 'webhook-config',
          component: 'UpsertAuthHandler',
          extractErrorContext: (cmd: UpsertAuthCommand) => ({
            inputStructure: {
              hasProps: !!cmd.props,
              propsKeys: cmd.props ? Object.keys(cmd.props) : [],
            },
          }),
        },
      );
    }

    return result;
  }
}
