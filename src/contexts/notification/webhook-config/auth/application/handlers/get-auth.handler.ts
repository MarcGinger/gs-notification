// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { QueryHandler, IQueryHandler } from '@nestjs/cqrs';
import { DetailAuthResponse } from '../dtos';
import { GetAuthUseCase } from '../use-cases';
import { GetAuthQuery } from '../queries';
import { Result, DomainError } from 'src/shared/errors';

@QueryHandler(GetAuthQuery)
export class GetAuthHandler implements IQueryHandler<GetAuthQuery> {
  constructor(private readonly getAuthUseCase: GetAuthUseCase) {}

  async execute(
    query: GetAuthQuery,
  ): Promise<Result<DetailAuthResponse | null, DomainError>> {
    return this.getAuthUseCase.execute({
      user: query.user,
      webhookId: query.webhookId,
      correlationId: query.correlationId || 'query-handler',
    });
  }
}
