// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { AuthPermission } from '../../domain/permissions/auth.permissions';

/**
 * Field-level permission matrix for Auth updates.
 * Maps auth fields to the required permissions for modification.
 */
export class AuthFieldPermissionMatrix {
  private static readonly fieldPermissions: Record<string, AuthPermission[]> = {
    // Basic auth information fields - require standard update permission
    name: [AuthPermission.DOMAIN_AUTH_UPDATE],
    description: [AuthPermission.DOMAIN_AUTH_UPDATE],
    category: [AuthPermission.DOMAIN_AUTH_UPDATE],

    // Status fields - may require special permissions or admin rights
    active: [
      AuthPermission.DOMAIN_AUTH_UPDATE,
      AuthPermission.DOMAIN_AUTH_ADMIN,
    ],
    status: [
      AuthPermission.DOMAIN_AUTH_UPDATE,
      AuthPermission.DOMAIN_AUTH_ADMIN,
    ],

    // Financial fields - typically require higher permissions
    fees: [AuthPermission.DOMAIN_AUTH_UPDATE],
    rates: [AuthPermission.DOMAIN_AUTH_UPDATE],
    pricing: [AuthPermission.DOMAIN_AUTH_UPDATE],

    // Configuration fields
    channelCodes: [AuthPermission.DOMAIN_AUTH_UPDATE],
    railCodes: [AuthPermission.DOMAIN_AUTH_UPDATE],
    configuration: [AuthPermission.DOMAIN_AUTH_UPDATE],

    // Administrative fields - require admin permissions
    tenant: [AuthPermission.DOMAIN_AUTH_ADMIN],
    auditInfo: [AuthPermission.DOMAIN_AUTH_ADMIN],
    metadata: [AuthPermission.DOMAIN_AUTH_UPDATE],
  };

  /**
   * Get required permissions for a specific field.
   * Returns empty array if field has no special permission requirements.
   */
  static getRequiredPermissions(fieldName: string): AuthPermission[] {
    return this.fieldPermissions[fieldName] || [];
  }

  /**
   * Check if a field has specific permission requirements.
   */
  static hasSpecialPermissions(fieldName: string): boolean {
    return fieldName in this.fieldPermissions;
  }

  /**
   * Get all fields that require a specific permission.
   */
  static getFieldsForPermission(permission: AuthPermission): string[] {
    return Object.entries(this.fieldPermissions)
      .filter(([, perms]) => perms.includes(permission))
      .map(([field]) => field);
  }

  /**
   * Get all defined field permissions.
   */
  static getAllFieldPermissions(): Record<string, AuthPermission[]> {
    return { ...this.fieldPermissions };
  }

  /**
   * Add or update permission requirements for a field.
   */
  static setFieldPermissions(
    fieldName: string,
    permissions: AuthPermission[],
  ): void {
    this.fieldPermissions[fieldName] = [...permissions];
  }
}
