// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { AuthDomainState } from '../../domain/state';
import { DetailAuthResponse } from '../dtos';

/**
 * Auth DTO Assembler
 *
 * Converts AuthDomainState (pure VOs) to transport DTOs.
 * Bridges the gap between rich domain objects and API response format.
 * Uses existing generated DTOs and follows established patterns.
 */

/**
 * Assembles DTOs from AuthDomainState for API transport
 */
export class AuthDtoAssembler {
  /**
   * Convert AuthDomainState to detailed response DTO
   *
   * @param domainState - Rich domain state with VOs
   * @returns Clean DTO for API response
   */
  static toDetailResponse(domainState: AuthDomainState): DetailAuthResponse {
    const dto = new DetailAuthResponse();

    // Extract primitive values from VOs
    dto.webhookId = domainState.webhookId.value;
    dto.type = domainState.type.value;
    dto.verifyTls = domainState.verifyTls?.value;
    dto.signingSecret = domainState.signingSecret?.value;
    dto.signatureAlgorithm = domainState.signatureAlgorithm?.value;
    dto.username = domainState.username?.value;
    dto.password = domainState.password?.value;
    dto.apiKey = domainState.apiKey?.value;
    dto.apiKeyHeader = domainState.apiKeyHeader?.value;
    dto.apiKeyQueryParam = domainState.apiKeyQueryParam?.value;
    dto.tokenUrl = domainState.tokenUrl?.value;
    dto.clientId = domainState.clientId?.value;
    dto.clientSecret = domainState.clientSecret?.value;
    dto.scope = domainState.scope?.value;
    dto.cert = domainState.cert?.value;
    dto.key = domainState.key?.value;
    dto.ca = domainState.ca?.value;

    return dto;
  }
  /**
   * Convert array of AuthDomainState to array of detail DTOs
   *
   * @param domainStates - Array of rich domain states
   * @returns Array of clean DTOs for detail API response
   */
  static toDetailResponseArray(
    domainStates: AuthDomainState[],
  ): DetailAuthResponse[] {
    return domainStates.map((domainState) =>
      this.toDetailResponse(domainState),
    );
  }
}
