// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { AuthSnapshotProps } from '../../domain/props';
import { DetailAuthResponse } from '../dtos';

/**
 * Auth Snapshot to DTO Mappers
 * Reusable mapping functions for converting domain snapshots to DTOs
 */

/**
 * Map auth snapshot to DTO response
 */
export const mapAuthSnapshotToDto = (
  snapshot: AuthSnapshotProps,
): DetailAuthResponse => {
  return {
    webhookId: snapshot.webhookId,
    type: snapshot.type,
    verifyTls: snapshot.verifyTls,
    signingSecret: snapshot.signingSecret,
    signatureAlgorithm: snapshot.signatureAlgorithm,
    username: snapshot.username,
    password: snapshot.password,
    apiKey: snapshot.apiKey,
    apiKeyHeader: snapshot.apiKeyHeader,
    apiKeyQueryParam: snapshot.apiKeyQueryParam,
    tokenUrl: snapshot.tokenUrl,
    clientId: snapshot.clientId,
    clientSecret: snapshot.clientSecret,
    scope: snapshot.scope,
    cert: snapshot.cert,
    key: snapshot.key,
    ca: snapshot.ca,
  };
};
