// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Module } from '@nestjs/common';
import { RouterModule, Routes } from '@nestjs/core';
import { SecureTestModule } from '../../secure-test.module';
import { SecureTestProjectorModule } from '../../secure-test-projector.module';
/**
 * Secure test Router Configuration
 *
 * Configures nested routing for secure-test endpoints to follow
 * the module monolith structure: /notification/webhook-config/api/v1/secure-tests
 *
 * This replaces flat routing (/api/v1/secure-tests) with contextual routing
 * that reflects the bounded context hierarchy.
 */

// Define the routing structure for the secure-test bounded context
const secureTestRoutes: Routes = [
  {
    path: 'notification',
    children: [
      {
        path: 'webhook-config',
        children: [
          {
            path: 'secure-tests',
            module: SecureTestModule,
          },
          {
            path: 'health',
            module: SecureTestProjectorModule,
          },
        ],
      },
    ],
  },
];

/**
 * Secure test Router Module
 *
 * Provides nested routing configuration for secure-test endpoints.
 * This module should be imported in the main app module to enable
 * the nested route structure.
 *
 * Routes configured:
 * - /notification/webhook-config/api/v1/secure-tests/:id (GET, PUT)
 * - /notification/webhook-config/health/projectors/secureTest (GET)
 * - /notification/webhook-config/health/projectors/secure-test/metrics (GET)
 */
@Module({
  imports: [
    SecureTestModule,
    SecureTestProjectorModule,
    RouterModule.register(secureTestRoutes),
  ],
  exports: [SecureTestModule, SecureTestProjectorModule],
})
export class SecureTestRouterModule {}
