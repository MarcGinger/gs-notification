// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  Controller,
  Get,
  Post,
  Body,
  Put,
  HttpStatus,
  HttpCode,
  Param,
  UseInterceptors,
  UseGuards,
} from '@nestjs/common';
import {
  ApiTags,
  ApiOperation,
  ApiResponse,
  ApiCreatedResponse,
  ApiBody,
  ApiHeader,
  ApiParam,
  ApiOkResponse,
  ApiBearerAuth,
} from '@nestjs/swagger';
import { IdempotencyKey } from 'src/shared/interfaces/decorators';
import { IdempotencyKeyPipe } from 'src/shared/interfaces/pipes';
import { SecureTestApplicationService } from '../../../application/services';
import { CurrentUser, IUserToken, JwtAuthGuard } from 'src/shared/security';
import {
  DetailSecureTestResponse,
  CreateSecureTestRequest,
  UpdateSecureTestRequest,
} from '../../../application/dtos';
import { Result, ResultInterceptor, DomainError } from 'src/shared/errors';
import {
  SecureTestReadResource,
  SecureTestCreateResource,
  SecureTestUpdateResource,
} from '../../secure-test.resource';
import { ApiCommonErrors } from 'src/shared/interfaces/http';

@Controller()
@ApiTags('SecureTests')
@ApiBearerAuth()
@UseGuards(JwtAuthGuard)
@UseInterceptors(ResultInterceptor)
export class SecureTestController {
  constructor(
    private readonly secureTestApplicationService: SecureTestApplicationService,
  ) {}

  @Get(':id')
  @SecureTestReadResource()
  @ApiOperation({
    summary: 'Get Secure test by ID',
    description:
      'Retrieves a single Secure test by its unique identifier. Requires READ permission (LOW risk).',
  })
  @ApiParam({
    name: 'id',
    type: 'string',
    description: 'SecureTest unique identifier',
  })
  @ApiOkResponse({
    description: 'SecureTest details retrieved successfully',
    type: DetailSecureTestResponse,
  })
  @ApiCommonErrors()
  async get(
    @CurrentUser() user: IUserToken,
    @Param('id') id: string,
  ): Promise<Result<DetailSecureTestResponse, DomainError>> {
    const result = await this.secureTestApplicationService.getSecureTestById(
      user,
      id,
    );

    return result;
  }
  // ========================================
  // Core CRUD Operations
  // ========================================

  @Post()
  @SecureTestCreateResource()
  @HttpCode(HttpStatus.CREATED)
  @ApiOperation({
    summary: 'Create a new Secure test',
    description:
      'Creates a new Secure test in the catalog. Requires CREATE permission (MEDIUM risk).',
  })
  @ApiHeader({
    name: 'Idempotency-Key',
    required: false,
    description:
      'Ensures the request is processed only once. Accepts both Idempotency-Key and x-idempotency-key headers.',
  })
  @ApiCreatedResponse({
    type: DetailSecureTestResponse,
    description: 'SecureTest successfully created',
    headers: {
      Location: {
        description: 'URI of the newly created resource',
        schema: {
          type: 'string',
          example: '/secure-tests/123e4567-e89b-12d3-a456-426614174000',
        },
      },
    },
  })
  @ApiCommonErrors()
  @ApiBody({ type: CreateSecureTestRequest })
  async create(
    @CurrentUser() user: IUserToken,
    @Body() createSecureTestRequest: CreateSecureTestRequest,
    @IdempotencyKey(new IdempotencyKeyPipe())
    idempotencyKey: string | undefined,
  ): Promise<Result<DetailSecureTestResponse, DomainError>> {
    // idempotencyKey is validated by the IdempotencyKeyPipe

    // Call service with options including idempotency key
    const result = await this.secureTestApplicationService.createSecureTest(
      user,
      createSecureTestRequest,
      {
        idempotencyKey,
      },
    );

    return result;
  }

  @Put(':id')
  @SecureTestUpdateResource()
  @ApiOperation({
    summary: 'Update a Secure test',
    description:
      'Updates an existing Secure test with new data. Supports partial updates. Requires UPDATE permission (MEDIUM risk).',
  })
  @ApiParam({
    name: 'id',
    type: 'string',
    description: 'SecureTest unique identifier',
  })
  @ApiResponse({
    status: HttpStatus.OK,
    description: 'SecureTest updated successfully',
    type: DetailSecureTestResponse,
  })
  @ApiCommonErrors({
    include422: true,
    extra: [
      { status: 429, description: 'Too Many Requests' },
      { status: 503, description: 'Upstream dependency unavailable' },
    ],
  })
  @ApiBody({
    type: UpdateSecureTestRequest,
    description:
      'SecureTest update data. Only provided fields will be updated.',
  })
  async update(
    @CurrentUser() user: IUserToken,
    @Param('id') id: string,
    @Body() updateSecureTestRequest: UpdateSecureTestRequest,
  ): Promise<Result<DetailSecureTestResponse, DomainError>> {
    const result = await this.secureTestApplicationService.updateSecureTest(
      user,
      id,
      updateSecureTestRequest,
    );

    return result;
  }
}
