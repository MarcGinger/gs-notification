// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { SecureTestPermission } from '../../domain/permissions/secure-test.permissions';

/**
 * Field-level permission matrix for SecureTest updates.
 * Maps secureTest fields to the required permissions for modification.
 */
export class SecureTestFieldPermissionMatrix {
  private static readonly fieldPermissions: Record<
    string,
    SecureTestPermission[]
  > = {
    // Basic secureTest information fields - require standard update permission
    name: [SecureTestPermission.DOMAIN_SECURE_TEST_UPDATE],
    description: [SecureTestPermission.DOMAIN_SECURE_TEST_UPDATE],
    category: [SecureTestPermission.DOMAIN_SECURE_TEST_UPDATE],

    // Status fields - may require special permissions or admin rights
    active: [
      SecureTestPermission.DOMAIN_SECURE_TEST_UPDATE,
      SecureTestPermission.DOMAIN_SECURE_TEST_ADMIN,
    ],
    status: [
      SecureTestPermission.DOMAIN_SECURE_TEST_UPDATE,
      SecureTestPermission.DOMAIN_SECURE_TEST_ADMIN,
    ],

    // Financial fields - typically require higher permissions
    fees: [SecureTestPermission.DOMAIN_SECURE_TEST_UPDATE],
    rates: [SecureTestPermission.DOMAIN_SECURE_TEST_UPDATE],
    pricing: [SecureTestPermission.DOMAIN_SECURE_TEST_UPDATE],

    // Configuration fields
    channelCodes: [SecureTestPermission.DOMAIN_SECURE_TEST_UPDATE],
    railCodes: [SecureTestPermission.DOMAIN_SECURE_TEST_UPDATE],
    configuration: [SecureTestPermission.DOMAIN_SECURE_TEST_UPDATE],

    // Administrative fields - require admin permissions
    tenant: [SecureTestPermission.DOMAIN_SECURE_TEST_ADMIN],
    auditInfo: [SecureTestPermission.DOMAIN_SECURE_TEST_ADMIN],
    metadata: [SecureTestPermission.DOMAIN_SECURE_TEST_UPDATE],
  };

  /**
   * Get required permissions for a specific field.
   * Returns empty array if field has no special permission requirements.
   */
  static getRequiredPermissions(fieldName: string): SecureTestPermission[] {
    return this.fieldPermissions[fieldName] || [];
  }

  /**
   * Check if a field has specific permission requirements.
   */
  static hasSpecialPermissions(fieldName: string): boolean {
    return fieldName in this.fieldPermissions;
  }

  /**
   * Get all fields that require a specific permission.
   */
  static getFieldsForPermission(permission: SecureTestPermission): string[] {
    return Object.entries(this.fieldPermissions)
      .filter(([, perms]) => perms.includes(permission))
      .map(([field]) => field);
  }

  /**
   * Get all defined field permissions.
   */
  static getAllFieldPermissions(): Record<string, SecureTestPermission[]> {
    return { ...this.fieldPermissions };
  }

  /**
   * Add or update permission requirements for a field.
   */
  static setFieldPermissions(
    fieldName: string,
    permissions: SecureTestPermission[],
  ): void {
    this.fieldPermissions[fieldName] = [...permissions];
  }
}
