// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { applyDecorators } from '@nestjs/common';
import { ApiProperty } from '@nestjs/swagger';
import { IsEnum, IsOptional, IsNotEmpty } from 'class-validator';
import {
  SecureTestSignatureAlgorithmValues,
  SecureTestSignatureAlgorithmValue,
} from '../../../domain/value-objects';

export {
  SecureTestSignatureAlgorithmValues,
  SecureTestSignatureAlgorithmValue,
};
/**
 * Options for property decorators (enum)
 */
interface PropOptions {
  required?: boolean;
}

/**
 * Property decorator for SecureTest SignatureAlgorithm with required option
 * @param {Object} options - Options for the decorator
 * @returns {PropertyDecorator}
 */
export function ApiSecureTestSignatureAlgorithm(options: PropOptions = {}) {
  const { required = false } = options;
  return applyDecorators(
    ApiProperty({
      description: `Cryptographic algorithm for HMAC signatures - sha256 (recommended) or sha1 (legacy). Only used with &#x27;hmac&#x27; authentication type. Values include sha256, sha1.`,
      type: String,
      enum: SecureTestSignatureAlgorithmValues,
      required,
    }),
    IsEnum(SecureTestSignatureAlgorithmValues),
    required ? IsNotEmpty() : IsOptional(),
  );
}
