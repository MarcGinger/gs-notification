// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

// Framework imports
import { Injectable, Logger, Inject } from '@nestjs/common';

// Shared utilities and infrastructure
import { Result, DomainError, ok, err, withContext } from 'src/shared/errors';
import {
  AuthorizationPort,
  buildAuthorizationContext,
} from 'src/shared/security/authorization.port';
import type { AuthorizationRequest } from 'src/shared/security/authorization.port';

// Domain types and errors
import { SecureTestPermission } from '../../domain/permissions/secure-test.permissions';
import { SecureTestErrors } from '../../domain/errors/secure-test.errors';
import { WEBHOOK_CONFIG_DI_TOKENS } from '../../../webhook-config.constants';

// Application layer
import { SecureTestFieldPermissionMatrix } from '../security/secure-test-field-permission-matrix';
import {
  SecureTestAuthContext,
  CrudOperation,
  BatchOperation,
} from '../types/secure-test-auth-context';

/**
 * Domain-specific authorization service for SecureTest operations.
 * Provides SecureTest-aware convenience methods and field-level permission checking.
 *
 * Improvements:
 * - Uses typed SecureTestAuthContext for better ABAC support
 * - Includes tenant/role information in actor context
 * - Optimized field-level checks with anyOf pattern
 * - Proper error handling with domain error factories
 * - Chunked batch processing for performance
 */
@Injectable()
export class SecureTestAuthorizationService {
  private readonly logger = new Logger(SecureTestAuthorizationService.name);

  constructor(
    @Inject(WEBHOOK_CONFIG_DI_TOKENS.AUTHORIZATION_SERVICE)
    private readonly authorizationPort: AuthorizationPort<SecureTestPermission>,
  ) {}

  /**
   * Helper to create consistent logging context
   */
  private createLogContext(
    operation: string,
    correlationId: string,
    userId?: string,
    additionalContext?: Record<string, unknown>,
  ): Record<string, unknown> {
    return {
      serviceName: 'SecureTestAuthorizationService',
      component: 'SecureTestAuthorizationService',
      operation,
      correlationId,
      userId,
      ...additionalContext,
    };
  }

  /**
   * Check if user can read a specific secureTest
   */
  async canReadSecureTest(
    userId: string,
    secureTests: string,
    correlationId: string,
    context?: SecureTestAuthContext,
  ): Promise<Result<boolean, DomainError>> {
    if (!secureTests) {
      return err(
        withContext(SecureTestErrors.PERMISSION_DENIED, {
          secureTests: secureTests || '',
          userId,
          correlationId,
          operation: 'read',
        }),
      );
    }

    const actor = {
      userId,
      tenant: context?.tenant,
      roles: context?.roles,
    };

    const request: AuthorizationRequest<SecureTestPermission> = {
      domain: 'secure_test',
      permissions: [SecureTestPermission.DOMAIN_SECURE_TEST_READ],
      actor,
      resource: { type: 'secure_test', id: secureTests },
      context: buildAuthorizationContext(correlationId, {
        metadata: context?.metadata,
      }),
    };

    const result = await this.authorizationPort.check(request);
    return result.ok ? ok(result.value.allowed) : err(result.error);
  }

  /**
   * Check if user can create secureTests
   */
  async canCreateSecureTest(
    userId: string,
    correlationId: string,
    context?: SecureTestAuthContext,
  ): Promise<Result<boolean, DomainError>> {
    const actor = {
      userId,
      tenant: context?.tenant,
      roles: context?.roles,
    };

    const request: AuthorizationRequest<SecureTestPermission> = {
      domain: 'secure_test',
      permissions: [SecureTestPermission.DOMAIN_SECURE_TEST_CREATE],
      actor,
      resource: { type: 'secure_test' },
      context: buildAuthorizationContext(correlationId, {
        metadata: context?.metadata,
      }),
    };

    const result = await this.authorizationPort.check(request);
    return result.ok ? ok(result.value.allowed) : err(result.error);
  }

  /**
   * Check if user can update a specific secureTest
   */
  async canUpdateSecureTest(
    userId: string,
    secureTests: string,
    correlationId: string,
    context?: SecureTestAuthContext,
  ): Promise<Result<boolean, DomainError>> {
    if (!secureTests) {
      return err(
        withContext(SecureTestErrors.PERMISSION_DENIED, {
          secureTests: secureTests || '',
          userId,
          correlationId,
          operation: 'update',
        }),
      );
    }

    const actor = {
      userId,
      tenant: context?.tenant,
      roles: context?.roles,
    };

    const request: AuthorizationRequest<SecureTestPermission> = {
      domain: 'secure_test',
      permissions: [SecureTestPermission.DOMAIN_SECURE_TEST_UPDATE],
      actor,
      resource: { type: 'secure_test', id: secureTests },
      context: buildAuthorizationContext(correlationId, {
        metadata: context?.metadata,
      }),
    };

    const result = await this.authorizationPort.check(request);
    return result.ok ? ok(result.value.allowed) : err(result.error);
  }

  /**
   * Check if user can delete a specific secureTest
   */
  async canDeleteSecureTest(
    userId: string,
    secureTests: string,
    correlationId: string,
    context?: SecureTestAuthContext,
  ): Promise<Result<boolean, DomainError>> {
    if (!secureTests) {
      return err(
        withContext(SecureTestErrors.PERMISSION_DENIED, {
          secureTests: secureTests || '',
          userId,
          correlationId,
          operation: 'delete',
        }),
      );
    }

    const actor = {
      userId,
      tenant: context?.tenant,
      roles: context?.roles,
    };

    const request: AuthorizationRequest<SecureTestPermission> = {
      domain: 'secure_test',
      permissions: [SecureTestPermission.DOMAIN_SECURE_TEST_DELETE],
      actor,
      resource: { type: 'secure_test', id: secureTests },
      context: buildAuthorizationContext(correlationId, {
        metadata: context?.metadata,
      }),
    };

    const result = await this.authorizationPort.check(request);
    return result.ok ? ok(result.value.allowed) : err(result.error);
  }

  /**
   * Check if user can perform admin operations on secureTests
   */
  async canAdministerSecureTest(
    userId: string,
    correlationId: string,
    secureTests?: string,
    context?: SecureTestAuthContext,
  ): Promise<Result<boolean, DomainError>> {
    const actor = {
      userId,
      tenant: context?.tenant,
      roles: context?.roles,
    };

    const request: AuthorizationRequest<SecureTestPermission> = {
      domain: 'secure_test',
      permissions: [SecureTestPermission.DOMAIN_SECURE_TEST_ADMIN],
      actor,
      resource: { type: 'secure_test', id: secureTests },
      context: buildAuthorizationContext(correlationId, {
        metadata: context?.metadata,
      }),
    };

    const result = await this.authorizationPort.check(request);
    return result.ok ? ok(result.value.allowed) : err(result.error);
  }

  /**
   * Check field-level permissions for secureTest updates.
   * Returns which fields the user can modify based on the permission matrix.
   *
   * Optimizations:
   * - Uses anyOf pattern to reduce OPA calls
   * - Parallel processing of field checks
   * - Proper actor context with tenant/role information
   */
  async checkFieldPermissions(
    userId: string,
    secureTests: string,
    requestedFields: string[],
    correlationId: string,
    context?: SecureTestAuthContext,
  ): Promise<
    Result<{ allowedFields: string[]; deniedFields: string[] }, DomainError>
  > {
    if (!secureTests) {
      return err(
        withContext(SecureTestErrors.PERMISSION_DENIED, {
          secureTests: secureTests || '',
          userId,
          correlationId,
          operation: 'checkFieldPermissions',
        }),
      );
    }

    try {
      const actor = {
        userId,
        tenant: context?.tenant,
        roles: context?.roles,
      };

      // Process fields in parallel for better performance
      const results = await Promise.all(
        requestedFields.map(async (field) => {
          const requiredPermissions =
            SecureTestFieldPermissionMatrix.getRequiredPermissions(field);

          // Use basic UPDATE permission if no special permissions required
          const permissions = requiredPermissions.length
            ? requiredPermissions
            : [SecureTestPermission.DOMAIN_SECURE_TEST_UPDATE];

          const request: AuthorizationRequest<SecureTestPermission> = {
            domain: 'secure_test',
            permissions,
            anyOf: true, // User needs ANY of the required permissions
            actor,
            resource: { type: 'secure_test', id: secureTests },
            context: buildAuthorizationContext(correlationId, {
              metadata: { ...context?.metadata, field },
            }),
          };

          const result = await this.authorizationPort.check(request);
          return { field, allowed: result.ok && result.value.allowed };
        }),
      );

      const allowedFields = results
        .filter((r) => r.allowed)
        .map((r) => r.field);
      const deniedFields = results
        .filter((r) => !r.allowed)
        .map((r) => r.field);

      return ok({ allowedFields, deniedFields });
    } catch (error: unknown) {
      this.logger.error(
        'Error checking field permissions',
        this.createLogContext('checkFieldPermissions', correlationId, userId, {
          secureTests,
          requestedFields,
          error: error instanceof Error ? error.message : String(error),
        }),
      );

      return err(
        withContext(SecureTestErrors.AUTHORIZATION_FAILED, {
          secureTests: secureTests || '',
          userId,
          correlationId,
          operation: 'checkFieldPermissions',
          reason: error instanceof Error ? error.message : String(error),
        }),
      );
    }
  }

  /**
   * Comprehensive authorization check for secureTest operations.
   * Validates both operation-level and field-level permissions.
   *
   * Improvements:
   * - Proper type safety with CrudOperation
   * - Explicit guards for required secureTest IDs
   * - Uses typed SecureTestAuthContext
   */
  async authorizeSecureTestOperation(
    userId: string,
    operation: CrudOperation,
    correlationId: string,
    secureTests?: string,
    fields?: string[],
    context?: SecureTestAuthContext,
  ): Promise<
    Result<
      {
        authorized: boolean;
        allowedFields?: string[];
        deniedFields?: string[];
      },
      DomainError
    >
  > {
    try {
      // Validate required secureTests for operations that need it
      if (['read', 'update', 'delete'].includes(operation) && !secureTests) {
        return err(
          withContext(SecureTestErrors.PERMISSION_DENIED, {
            secureTests: secureTests || '',
            userId,
            correlationId,
            operation: String(operation),
          }),
        );
      }

      // First check operation-level permission
      let operationResult: Result<boolean, DomainError>;

      switch (operation) {
        case 'read':
          operationResult = await this.canReadSecureTest(
            userId,
            secureTests!,
            correlationId,
            context,
          );
          break;
        case 'create':
          operationResult = await this.canCreateSecureTest(
            userId,
            correlationId,
            context,
          );
          break;
        case 'update':
          operationResult = await this.canUpdateSecureTest(
            userId,
            secureTests!,
            correlationId,
            context,
          );
          break;
        case 'delete':
          operationResult = await this.canDeleteSecureTest(
            userId,
            secureTests!,
            correlationId,
            context,
          );
          break;
        default:
          return err(
            withContext(SecureTestErrors.NOT_IMPLEMENTED, {
              secureTests: secureTests || '',
              userId,
              correlationId,
              operation: String(operation),
            }),
          );
      }

      if (!operationResult.ok) {
        return err(operationResult.error);
      }

      if (!operationResult.value) {
        return ok({ authorized: false });
      }

      // For update operations with field specifications, check field-level permissions
      if (
        operation === 'update' &&
        fields &&
        fields.length > 0 &&
        secureTests
      ) {
        const fieldResult = await this.checkFieldPermissions(
          userId,
          secureTests,
          fields,
          correlationId,
          context,
        );

        if (!fieldResult.ok) {
          return err(fieldResult.error);
        }

        const { allowedFields, deniedFields } = fieldResult.value;

        // Operation is authorized, but some fields might be denied
        return ok({
          authorized: allowedFields.length > 0,
          allowedFields,
          deniedFields,
        });
      }

      // Simple operation authorization without field-level checks
      return ok({ authorized: true });
    } catch (error: unknown) {
      this.logger.error(
        'Error in comprehensive secureTest authorization',
        this.createLogContext('comprehensiveCheck', correlationId, userId, {
          operation,
          secureTests,
          fields,
          error: error instanceof Error ? error.message : String(error),
        }),
      );

      return err(
        withContext(SecureTestErrors.AUTHORIZATION_FAILED, {
          secureTests: secureTests || '',
          userId,
          correlationId,
          operation: String(operation),
          reason: error instanceof Error ? error.message : String(error),
        }),
      );
    }
  }

  /**
   * Batch authorization check for multiple secureTests.
   * Useful for list operations where you need to filter results.
   *
   * Optimizations:
   * - Chunked processing to avoid overwhelming OPA
   * - Typed SecureTestAuthContext and BatchOperation
   * - Proper actor context with tenant/role information
   */
  async authorizeSecureTestList(
    userId: string,
    secureTestss: string[],
    correlationId: string,
    operation: BatchOperation = 'read',
    context?: SecureTestAuthContext,
  ): Promise<Result<{ authorized: string[]; denied: string[] }, DomainError>> {
    try {
      const authorized: string[] = [];
      const denied: string[] = [];

      // Chunk processing to avoid stampeding OPA with large lists
      const chunk = <T>(arr: T[], size: number): T[][] =>
        Array.from({ length: Math.ceil(arr.length / size) }, (_, i) =>
          arr.slice(i * size, (i + 1) * size),
        );

      // Process in chunks of 50 to balance performance and resource usage
      for (const batch of chunk(secureTestss, 50)) {
        const results = await Promise.allSettled(
          batch.map(async (secureTests) => {
            let result: Result<boolean, DomainError>;

            switch (operation) {
              case 'read':
                result = await this.canReadSecureTest(
                  userId,
                  secureTests,
                  correlationId,
                  context,
                );
                break;
              case 'update':
                result = await this.canUpdateSecureTest(
                  userId,
                  secureTests,
                  correlationId,
                  context,
                );
                break;
              case 'delete':
                result = await this.canDeleteSecureTest(
                  userId,
                  secureTests,
                  correlationId,
                  context,
                );
                break;
              default:
                result = err(
                  withContext(SecureTestErrors.NOT_IMPLEMENTED, {
                    secureTests,
                    userId,
                    correlationId,
                    operation: String(operation),
                  }),
                );
                break;
            }

            return { secureTests, result };
          }),
        );

        // Process results from this chunk
        results.forEach((promiseResult, index) => {
          const secureTests = batch[index];

          if (promiseResult.status === 'fulfilled') {
            const { result } = promiseResult.value;
            if (result.ok && result.value) {
              authorized.push(secureTests);
            } else {
              denied.push(secureTests);
            }
          } else {
            // Promise rejected, treat as denied
            denied.push(secureTests);
            this.logger.warn(
              'Authorization check failed for secureTest',
              this.createLogContext(
                'batchAuthorization',
                correlationId,
                userId,
                {
                  secureTests,
                  error: String(promiseResult.reason),
                },
              ),
            );
          }
        });
      }

      return ok({ authorized, denied });
    } catch (error: unknown) {
      this.logger.error(
        'Error in batch secureTest authorization',
        this.createLogContext('batchAuthorization', correlationId, userId, {
          secureTestss: secureTestss.slice(0, 10), // Log only first 10 for brevity
          operation,
          error: error instanceof Error ? error.message : String(error),
        }),
      );

      return err(
        withContext(SecureTestErrors.AUTHORIZATION_FAILED, {
          secureTests: '',
          userId,
          correlationId,
          operation: String(operation),
          reason: error instanceof Error ? error.message : String(error),
        }),
      );
    }
  }
}
