// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { QueryHandler, IQueryHandler } from '@nestjs/cqrs';
import { DetailSecureTestResponse } from '../dtos';
import { GetSecureTestUseCase } from '../use-cases';
import { GetSecureTestQuery } from '../queries';
import { Result, DomainError } from 'src/shared/errors';

@QueryHandler(GetSecureTestQuery)
export class GetSecureTestHandler implements IQueryHandler<GetSecureTestQuery> {
  constructor(private readonly getSecureTestUseCase: GetSecureTestUseCase) {}

  async execute(
    query: GetSecureTestQuery,
  ): Promise<Result<DetailSecureTestResponse | null, DomainError>> {
    return this.getSecureTestUseCase.execute({
      user: query.user,
      id: query.id,
      correlationId: query.correlationId || 'query-handler',
    });
  }
}
