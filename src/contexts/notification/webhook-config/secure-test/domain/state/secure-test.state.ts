// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  SecureTestCreatedAt,
  SecureTestUpdatedAt,
  SecureTestVersion,
  SecureTestDescription,
  SecureTestId,
  SecureTestName,
  SecureTestSignatureAlgorithm,
  SecureTestType,
  SecureTestSecretRef,
} from '../value-objects';

/**
 * SecureTest Domain State
 *
 * Domain state interface that uses value objects to model business concepts
 * while maintaining compatibility with entity operations. Business fields are
 * modeled as value objects with proper validation and domain rules.
 *
 * Domain-First State Interface
 *
 * This interface represents the domain model where business concepts
 * are properly encapsulated as value objects when appropriate.
 */

export interface SecureTestDomainState {
  id: SecureTestId;
  name: SecureTestName;
  description?: SecureTestDescription;
  type: SecureTestType;
  signingSecretRef?: SecureTestSecretRef;
  signatureAlgorithm?: SecureTestSignatureAlgorithm;
  usernameRef?: SecureTestSecretRef;
  passwordRef?: SecureTestSecretRef;
  version: SecureTestVersion;
  createdAt: SecureTestCreatedAt;
  updatedAt: SecureTestUpdatedAt;
}

export type SecureTestDomainStateOptional = Partial<SecureTestDomainState>;
