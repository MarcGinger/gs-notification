// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { EventMetadata } from 'src/shared/domain/events';

/**
 * SecureTest Deleted Event Payload
 * Contains identifying information about the deleted secureTest
 */
export interface SecureTestDeletedEventPayload {
  id: string;
  deletedAt: Date;
  version: number;
  metadata: EventMetadata;
}

/**
 * SecureTest Deleted Domain Event
 *
 * Emitted when a secureTest is successfully deleted.
 * Contains identifying information and deletion metadata.
 */
export class SecureTestDeletedEvent {
  public readonly eventType = 'NotificationWebhookConfigSecureTestDeleted.v1';
  public readonly eventVersion = 'v1';

  constructor(public readonly payload: SecureTestDeletedEventPayload) {}

  // Factory method with security context
  static create(
    data: Omit<SecureTestDeletedEventPayload, 'metadata'>,
    baseMetadata: EventMetadata,
  ): SecureTestDeletedEvent {
    const metadata: EventMetadata = {
      ...baseMetadata,
      eventVersion: '1.0',
      source: 'notification.domain.secure-test',
    };

    return new SecureTestDeletedEvent({
      ...data,
      metadata,
    });
  }

  get id(): string {
    return this.payload.id;
  }

  get deletedAt(): Date {
    return this.payload.deletedAt;
  }

  get version(): number {
    return this.payload.version;
  }

  get metadata(): EventMetadata {
    return this.payload.metadata;
  }
}
