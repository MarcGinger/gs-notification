// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import type {
  SecureTestSignatureAlgorithmValue,
  SecureTestTypeValue,
} from '../value-objects';

/**
 * SecureTestSnapshotProps - Persistence Layer Interface
 *
 * IMPORTANT: This interface maintains the original field names for persistence
 * compatibility. The domain entity layer still uses the original structure,
 * while the application props (SecureTestProps) use SecretRef for security.
 *
 * Migration Strategy:
 * - Entity Domain State: signingSecret (string) - internal domain state
 * - Application Props: signingSecretRef (SecretRef) - API layer with security
 * - Snapshot Props: signingSecret (string) - persistence compatibility
 */
export interface SecureTestSnapshotProps {
  id: string;
  name: string;
  description?: string;
  type: SecureTestTypeValue;
  signingSecret?: string;
  signatureAlgorithm?: SecureTestSignatureAlgorithmValue;
  username?: string;
  password?: string;
  createdAt: Date;
  updatedAt: Date;
  version: number;
}
