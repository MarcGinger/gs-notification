// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { extractPropertyValue } from 'src/shared/utilities/value-object.util';
import { SecureTestOptionsConfiguration } from '../value-objects';

/**
 * options Configuration Data Extractors
 *
 * Pure domain functions that extract structured data from options value objects.
 * These functions understand domain structure but have no knowledge of DTOs or application concerns.
 */

/**
 * Extract options configuration data from domain value object
 *
 * This function knows the internal structure of SecureTestOptionsConfiguration
 * but remains pure domain logic without DTO knowledge.
 */
export const extractOptionsConfigurationData = <T>(
  config: SecureTestOptionsConfiguration,
): T => {
  return {
    name: extractPropertyValue<string>(config, 'name'),
  } as T;
};
