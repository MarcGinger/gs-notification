// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { SecureTestErrors } from '../errors/secure-test.errors';

/**
 * Password Value Object
 * Represents a validated password with business rules
 */
export const SecureTestPassword = createStringVO({
  name: 'Password',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 255,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(SecureTestErrors.INVALID_PASSWORD, 'Password'),
});

/**
 * Public instance type for Password
 */
export type SecureTestPassword = StringVOInstance;

// Convenience creators
export const createSecureTestPassword = (s: string) =>
  SecureTestPassword.create(s);
export const secureTestPasswordFrom = (v: unknown) =>
  SecureTestPassword.from(v);
