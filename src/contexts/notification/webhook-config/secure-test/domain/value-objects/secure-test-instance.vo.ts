// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DomainError, Result, ok, err } from 'src/shared/errors';
import {
  createIntegerVO,
  createIntegerVOErrors,
  IntegerVOInstance,
  createDateTimeVO,
  createDateTimeVOErrors,
  DateTimeVOInstance,
} from 'src/shared/domain/value-objects';
import { Clock } from 'src/shared/domain/clock';
import { SecureTestErrors } from '../errors/secure-test.errors';

/**
 * SecureTest Instance Value Objects
 *
 * Instance VOs for SecureTest aggregate lifecycle management.
 * Includes creation timestamps, update timestamps, and version tracking.
 * Follows the Clock injection pattern for deterministic testing.
 */

// ===========================================
// SecureTest Created At Instance VO
// ===========================================

const SecureTestCreatedAtErrors = createDateTimeVOErrors(
  SecureTestErrors.CREATED_AT_REQUIRED,
  'SecureTest Created At',
);

export const SecureTestCreatedAt = createDateTimeVO({
  name: 'SecureTestCreatedAt',
  allowFuture: false, // Cannot create secure_test in the future
  allowPast: true, // Can create with past timestamps (data migration)
  customValidation: (date: Date): Result<void, DomainError> => {
    // Optional: Add domain-specific creation time validation
    // For example, cannot be more than 1 year in the past
    const oneYearAgo = new Date();
    oneYearAgo.setFullYear(oneYearAgo.getFullYear() - 1);

    if (date < oneYearAgo) {
      return err({
        ...SecureTestErrors.CREATED_AT_REQUIRED,
        detail:
          'SecureTest creation date cannot be more than 1 year in the past',
        context: { value: date, maxPastDate: oneYearAgo },
      } as DomainError);
    }

    return ok(undefined);
  },
  errors: SecureTestCreatedAtErrors,
});

export type SecureTestCreatedAt = DateTimeVOInstance;

// ===========================================
// SecureTest Updated At Instance VO
// ===========================================

const SecureTestUpdatedAtErrors = createDateTimeVOErrors(
  SecureTestErrors.UPDATED_AT_REQUIRED,
  'SecureTest Updated At',
);

export const SecureTestUpdatedAt = createDateTimeVO({
  name: 'SecureTestUpdatedAt',
  allowFuture: false, // Cannot update secure_test in the future
  allowPast: true, // Can update with past timestamps (data correction)
  errors: SecureTestUpdatedAtErrors,
});

export type SecureTestUpdatedAt = DateTimeVOInstance;

// ===========================================
// SecureTest Version Instance VO
// ===========================================

const SecureTestVersionErrors = createIntegerVOErrors(
  SecureTestErrors.INVALID_CODE_DATA, // Reusing existing error until specific version error is added
  'SecureTest Version',
);

export const SecureTestVersion = createIntegerVO({
  name: 'SecureTestVersion',
  min: 0, // Version starts at 0
  allowNegative: false,
  errors: SecureTestVersionErrors,
});

export type SecureTestVersion = IntegerVOInstance;

// ===========================================
// Clock-Based Helper Functions (Explicit Dependencies)
// ===========================================

/**
 * Create SecureTestCreatedAt from Clock (explicit dependency)
 * Deterministic and testable - pass a fake clock in tests
 */
export const createdAtNow = (
  clock: Clock,
): Result<SecureTestCreatedAt, DomainError> =>
  SecureTestCreatedAt.create(clock.now());

/**
 * Create SecureTestUpdatedAt from Clock (explicit dependency)
 * Deterministic and testable - pass a fake clock in tests
 */
export const updatedAtNow = (
  clock: Clock,
): Result<SecureTestUpdatedAt, DomainError> =>
  SecureTestUpdatedAt.create(clock.now());

/**
 * Bump version by 1 (domain business rule)
 */
export const bumpVersion = (
  currentVersion: SecureTestVersion,
): Result<SecureTestVersion, DomainError> =>
  SecureTestVersion.create(currentVersion.value + 1);

/**
 * Create initial version (starts at 1 for new aggregates)
 */
export const initialVersion = (): Result<SecureTestVersion, DomainError> =>
  SecureTestVersion.create(1);

// ===========================================
// Direct Creation Functions (Non-Clock Based)
// ===========================================

/**
 * Create SecureTestCreatedAt from Date
 */
export const createSecureTestCreatedAt = (
  date: Date,
): Result<SecureTestCreatedAt, DomainError> => SecureTestCreatedAt.create(date);

/**
 * Create SecureTestUpdatedAt from Date
 */
export const createSecureTestUpdatedAt = (
  date: Date,
): Result<SecureTestUpdatedAt, DomainError> => SecureTestUpdatedAt.create(date);

/**
 * Create SecureTestVersion from number
 */
export const createSecureTestVersion = (
  version: number,
): Result<SecureTestVersion, DomainError> => SecureTestVersion.create(version);

// ===========================================
// Domain Invariants & Validation
// ===========================================

/**
 * Validate that updatedAt is not before createdAt (monotonic time)
 */
export const validateMonotonicTime = (
  createdAt: SecureTestCreatedAt,
  updatedAt: SecureTestUpdatedAt,
): Result<void, DomainError> => {
  if (updatedAt.value < createdAt.value) {
    return err({
      ...SecureTestErrors.UPDATED_AT_REQUIRED,
      detail: 'Updated at cannot be before created at',
      context: {
        createdAt: createdAt.value.toISOString(),
        updatedAt: updatedAt.value.toISOString(),
      },
    } as DomainError);
  }
  return ok(undefined);
};

// ===========================================
// Legacy Compatibility (DEPRECATED)
// ===========================================

/**
 * @deprecated Use createdAtNow(clock) instead for better testability
 * This function hides the Date dependency and makes testing harder
 */
export function createSecureTestCreatedAtNow(): Result<
  SecureTestCreatedAt,
  DomainError
> {
  return SecureTestCreatedAt.create(new Date());
}

/**
 * @deprecated Use updatedAtNow(clock) instead for better testability
 * This function hides the Date dependency and makes testing harder
 */
export function createSecureTestUpdatedAtNow(): Result<
  SecureTestUpdatedAt,
  DomainError
> {
  return SecureTestUpdatedAt.create(new Date());
}
