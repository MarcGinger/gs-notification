// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  RecordVOInstance,
  createRecordVO,
  createRecordVOErrors,
} from 'src/shared/domain/value-objects';
import { OptionsErrors } from '../../errors/options.errors';
import { OptionsProps } from '../../props';
import { OptionsName } from './';

/**
 * SecureTestOptionsConfiguration Value Object
 *
 * Embedded configuration for options settings specific to a options.
 * This is owned by the Options aggregate and not shared across options.
 * Replaces the separate SecureTestOptionsConfigurationAggregate for options-owned configurations.
 *
 * Composed of individual value objects for each field to ensure
 * proper validation and separation of concerns.
 */
export const SecureTestOptionsConfiguration = createRecordVO({
  name: 'SecureTestOptionsConfiguration',

  // Basic validation will be handled at the application level
  // Advanced validation can be added via refinements if needed
  errors: createRecordVOErrors(
    OptionsErrors.INVALID_OPTIONS_CONFIGURATION_DATA,
    'SecureTestOptionsConfiguration',
  ),
});

/** Public instance type for SecureTestOptionsConfiguration */
export type SecureTestOptionsConfiguration = RecordVOInstance;

// Convenience creators using composed value objects
export const createOptionsConfiguration = (config: { name: OptionsName }) =>
  SecureTestOptionsConfiguration.create(config);

export const optionsConfigurationFrom = (v: unknown) =>
  SecureTestOptionsConfiguration.from(v);

/**
 * Converts SecureTestOptionsConfiguration to OptionsProps
 */
export const optionsConfigurationToSnapshotProps = (
  config: SecureTestOptionsConfiguration,
): OptionsProps => {
  return config.value as unknown as OptionsProps;
};
