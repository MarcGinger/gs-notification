// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { OptionsErrors } from '../../errors/options.errors';

/**
 * Name Value Object
 * Represents a validated name with business rules
 */
export const OptionsName = createStringVO({
  name: 'Name',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  maxLength: 255,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(OptionsErrors.INVALID_NAME, 'Name'),
});

/**
 * Public instance type for Name
 */
export type OptionsName = StringVOInstance;

// Convenience creators
export const createOptionsName = (s: string) => OptionsName.create(s);
export const optionsNameFrom = (v: unknown) => OptionsName.from(v);
