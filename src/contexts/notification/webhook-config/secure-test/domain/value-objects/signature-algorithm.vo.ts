// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  EnumVOInstance,
  createDisplayNameHelper,
  createEnumTypeGuards,
  createEnumVO,
  createEnumVOErrors,
  createStateTransitions,
} from 'src/shared/domain/value-objects';
import { Result, DomainError } from 'src/shared/errors';
import { SecureTestErrors } from '../errors/secure-test.errors';

// ============================================================================
// SINGLE SOURCE OF TRUTH
// ============================================================================

/**
 * SecureTestSignatureAlgorithm allowed values
 * Canonical definition used across all layers (API, Domain, DB)
 */
export const SecureTestSignatureAlgorithmValues = ['sha256', 'sha1'] as const;
export type SecureTestSignatureAlgorithmValue =
  (typeof SecureTestSignatureAlgorithmValues)[number];

// ============================================================================
// VALUE OBJECT - Domain Layer
// ============================================================================

/**
 * SecureTestSignatureAlgorithm Value Object
 *
 * Provides validation, business rules, and rich domain behavior
 * for secureTest signatureAlgorithm enumerated values.
 *
 * @example
 * ```typescript
 * const signatureAlgorithm = SecureTestSignatureAlgorithm.create('sha256');
 * if (signatureAlgorithm.ok) {
 *   console.log(signatureAlgorithm.value.canTransitionTo('sha1'));
 * }
 * ```
 */
export const SecureTestSignatureAlgorithm = createEnumVO({
  name: 'SecureTestSignatureAlgorithm',
  values: SecureTestSignatureAlgorithmValues,

  errors: createEnumVOErrors(
    SecureTestErrors.INVALID_SIGNATURE_ALGORITHM,
    'SecureTestSignatureAlgorithm',
  ),
});

export type SecureTestSignatureAlgorithm =
  EnumVOInstance<SecureTestSignatureAlgorithmValue>;

// ============================================================================
// CONVENIENCE FACTORIES
// ============================================================================

export const createSecureTestSignatureAlgorithm = (
  value: SecureTestSignatureAlgorithmValue | undefined,
) =>
  SecureTestSignatureAlgorithm.create(value) as Result<
    SecureTestSignatureAlgorithm,
    DomainError
  >;
export const secureTestSignatureAlgorithmFrom = (value: unknown) =>
  SecureTestSignatureAlgorithm.from(value) as Result<
    SecureTestSignatureAlgorithm,
    DomainError
  >;

// ============================================================================
// BUSINESS LOGIC HELPERS - Domain Behavior
// ============================================================================

/**
 * Valid state transitions for secureTest signatureAlgorithm
 */
const SECURE_TEST_SIGNATURE_ALGORITHM_TRANSITIONS = {
  sha256: ['sha1'] as const,
  sha1: ['sha256'] as const,
} as const;

/**
 * Display names for secureTest signatureAlgorithm values
 */
const SECURE_TEST_SIGNATURE_ALGORITHM_DISPLAY_NAMES = {
  sha256: 'Sha256',
  sha1: 'Sha1',
} as const;

/**
 * State machine helpers for SecureTestSignatureAlgorithm transitions
 */
const SecureTestSignatureAlgorithmTransitions = createStateTransitions(
  SECURE_TEST_SIGNATURE_ALGORITHM_TRANSITIONS,
);

/**
 * Type guards for SecureTestSignatureAlgorithm values
 */
const SecureTestSignatureAlgorithmGuards = createEnumTypeGuards(
  SecureTestSignatureAlgorithmValues,
  'SecureTestSignatureAlgorithm',
  (value, validValues) => ({
    ...SecureTestErrors.INVALID_SIGNATURE_ALGORITHM,
    detail: `SecureTestSignatureAlgorithm must be one of: ${validValues.join(', ')}, received: ${String(value)}`,
    context: { value, validValues },
  }),
);

/**
 * Display name helpers for SecureTestSignatureAlgorithm
 */
const SecureTestSignatureAlgorithmDisplay = createDisplayNameHelper(
  SECURE_TEST_SIGNATURE_ALGORITHM_DISPLAY_NAMES,
);

/**
 * Business logic functions for SecureTestSignatureAlgorithm
 */
export const SecureTestSignatureAlgorithmLogic = {
  /**
   * Check if transition from one state to another is valid
   */
  canTransition: (
    from: SecureTestSignatureAlgorithmValue,
    to: SecureTestSignatureAlgorithmValue,
  ): boolean => SecureTestSignatureAlgorithmTransitions.canTransition(from, to),

  /**
   * Get all valid next states from current state
   */
  getValidTransitions: (
    from: SecureTestSignatureAlgorithmValue,
  ): readonly SecureTestSignatureAlgorithmValue[] =>
    SecureTestSignatureAlgorithmTransitions.getValidTransitions(from),

  /**
   * Get human-readable display name
   */
  getDisplayName: (value: SecureTestSignatureAlgorithmValue): string =>
    SecureTestSignatureAlgorithmDisplay.getDisplayName(value),

  /**
   * Get all available values with metadata
   */
  getAllValues(): Array<{
    value: SecureTestSignatureAlgorithmValue;
    displayName: string;
    validTransitions: readonly SecureTestSignatureAlgorithmValue[];
  }> {
    return SecureTestSignatureAlgorithmValues.map((value) => ({
      value,
      displayName: SecureTestSignatureAlgorithmLogic.getDisplayName(value),
      validTransitions:
        SecureTestSignatureAlgorithmLogic.getValidTransitions(value),
    }));
  },
} as const;

// ============================================================================
// TYPE GUARDS - Runtime Type Checking
// ============================================================================

/**
 * Type guard to check if value is valid SecureTestSignatureAlgorithm
 */
export const isSecureTestSignatureAlgorithmValue = (
  value: unknown,
): value is SecureTestSignatureAlgorithmValue =>
  SecureTestSignatureAlgorithmGuards.isValid(value);

/**
 * Assert that value is valid SecureTestSignatureAlgorithm (throws if invalid)
 */
export const assertSecureTestSignatureAlgorithmValue = (
  value: unknown,
): asserts value is SecureTestSignatureAlgorithmValue =>
  SecureTestSignatureAlgorithmGuards.assert(value);
