// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { SecureTestErrors } from '../errors/secure-test.errors';

/**
 * SigningSecret Value Object
 * Represents a validated signingSecret with business rules
 */
export const SecureTestSigningSecret = createStringVO({
  name: 'SigningSecret',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 255,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    SecureTestErrors.INVALID_SIGNING_SECRET,
    'SigningSecret',
  ),
});

/**
 * Public instance type for SigningSecret
 */
export type SecureTestSigningSecret = StringVOInstance;

// Convenience creators
export const createSecureTestSigningSecret = (s: string) =>
  SecureTestSigningSecret.create(s);
export const secureTestSigningSecretFrom = (v: unknown) =>
  SecureTestSigningSecret.from(v);
