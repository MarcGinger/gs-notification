// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { SecureTestOptionsConfiguration } from './options';
import { CreateOptionsProps, UpdateOptionsProps } from '../props';

/**
 * SecureTestOptions - uses OptionsConfiguration directly
 * Since options is a single configuration object, not a collection
 */
export const SecureTestOptions = {
  create: (config: Record<string, CreateOptionsProps>) =>
    SecureTestOptionsConfiguration.from(config),
  update: (config: Record<string, UpdateOptionsProps>) =>
    SecureTestOptionsConfiguration.from(config),
  from: (v: unknown) => SecureTestOptionsConfiguration.from(v),
};

/** Public instance type for SecureTestOptions */
export type SecureTestOptions = SecureTestOptionsConfiguration;

// Convenience creators for SecureTestOptions
export const createSecureTestOptions = SecureTestOptions.create;
export const secureTestoptionsFrom = SecureTestOptions.from;
