// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { SecureTestErrors } from '../errors/secure-test.errors';

/**
 * Description Value Object
 * Represents a validated description with business rules
 */
export const SecureTestDescription = createStringVO({
  name: 'Description',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    SecureTestErrors.INVALID_DESCRIPTION,
    'Description',
  ),
});

/**
 * Public instance type for Description
 */
export type SecureTestDescription = StringVOInstance;

// Convenience creators
export const createSecureTestDescription = (s: string) =>
  SecureTestDescription.create(s);
export const secureTestDescriptionFrom = (v: unknown) =>
  SecureTestDescription.from(v);
