// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { SecureTestErrors } from '../errors/secure-test.errors';

/**
 * Name Value Object
 * Represents a validated name with business rules
 */
export const SecureTestName = createStringVO({
  name: 'Name',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  maxLength: 64,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(SecureTestErrors.INVALID_NAME, 'Name'),
});

/**
 * Public instance type for Name
 */
export type SecureTestName = StringVOInstance;

// Convenience creators
export const createSecureTestName = (s: string) => SecureTestName.create(s);
export const secureTestNameFrom = (v: unknown) => SecureTestName.from(v);
