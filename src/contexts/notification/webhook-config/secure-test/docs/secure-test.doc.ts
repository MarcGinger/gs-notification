// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DocumentBuilder, SwaggerModule } from '@nestjs/swagger';
import { INestApplication } from '@nestjs/common';
import { SwaggerConfigUtil } from 'src/docs/swagger-config.util';
import { SecureTestModule } from '../secure-test.module';
/**
 * SecureTest documentation endpoints interface
 */
export interface SecureTestDocumentationUrls {
  secureTest: string;
}

/**
 * SecureTest Documentation
 * This module handles the Swagger documentation for secure_tests
 *
 */
export class SecureTestDocumentation {
  static setup(app: INestApplication, port: string | number): void {
    const config = new DocumentBuilder()
      .addBearerAuth(
        { type: 'http', scheme: 'bearer', bearerFormat: 'JWT' },
        'bearer',
      )
      .setTitle('📱 SecureTest Management API')
      .setDescription(
        `
## bounded context: notification
[← Back to documentation](/api/docs/notification)

### application: webhook-config
[← Back to documentation](/api/docs/notification/webhook-config)
`,
      )
      .setVersion('1.0.0')
      .addTag('SecureTests', `Configuration for secure_test table`);

    // Add dynamic server configuration
    SwaggerConfigUtil.addServers(config, port);

    const document = SwaggerModule.createDocument(app, config.build(), {
      include: [SecureTestModule],
      extraModels: [],
    });

    SwaggerModule.setup(
      'api/docs/notification/webhook-config/secure-test',
      app,
      document,
    );
  }

  /**
   * Setup all secureTest-related documentation endpoints
   * This method centralizes the setup of all secureTest module documentation
   */
  static setupAll(
    app: INestApplication,
    port: string | number,
  ): SecureTestDocumentationUrls {
    // Setup main secureTest documentation
    SecureTestDocumentation.setup(app, port);

    // Setup sub-module documentation

    // Return all endpoint URLs
    return {
      secureTest: SecureTestDocumentation.getEndpoint(port),
    };
  }

  static getEndpoint(port: string | number): string {
    return `${SwaggerConfigUtil.getServerUrl(port)}/api/docs/notification/webhook-config/secure-test`;
  }
}
