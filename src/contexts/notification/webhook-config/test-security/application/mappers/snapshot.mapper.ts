// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { TestSecuritySnapshotProps } from '../../domain/props';
import { DetailTestSecurityResponse } from '../dtos';

/**
 * TestSecurity Snapshot to DTO Mappers
 * Reusable mapping functions for converting domain snapshots to DTOs
 */

/**
 * Map testSecurity snapshot to DTO response
 */
export const mapTestSecuritySnapshotToDto = (
  snapshot: TestSecuritySnapshotProps,
): DetailTestSecurityResponse => {
  return {
    id: snapshot.id,
    name: snapshot.name,
    description: snapshot.description,
  };
};
