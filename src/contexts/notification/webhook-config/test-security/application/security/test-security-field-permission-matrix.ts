// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { TestSecurityPermission } from '../../domain/permissions/test-security.permissions';

/**
 * Field-level permission matrix for TestSecurity updates.
 * Maps testSecurity fields to the required permissions for modification.
 */
export class TestSecurityFieldPermissionMatrix {
  private static readonly fieldPermissions: Record<
    string,
    TestSecurityPermission[]
  > = {
    // Basic testSecurity information fields - require standard update permission
    name: [TestSecurityPermission.DOMAIN_TEST_SECURITY_UPDATE],
    description: [TestSecurityPermission.DOMAIN_TEST_SECURITY_UPDATE],
    category: [TestSecurityPermission.DOMAIN_TEST_SECURITY_UPDATE],

    // Status fields - may require special permissions or admin rights
    active: [
      TestSecurityPermission.DOMAIN_TEST_SECURITY_UPDATE,
      TestSecurityPermission.DOMAIN_TEST_SECURITY_ADMIN,
    ],
    status: [
      TestSecurityPermission.DOMAIN_TEST_SECURITY_UPDATE,
      TestSecurityPermission.DOMAIN_TEST_SECURITY_ADMIN,
    ],

    // Financial fields - typically require higher permissions
    fees: [TestSecurityPermission.DOMAIN_TEST_SECURITY_UPDATE],
    rates: [TestSecurityPermission.DOMAIN_TEST_SECURITY_UPDATE],
    pricing: [TestSecurityPermission.DOMAIN_TEST_SECURITY_UPDATE],

    // Configuration fields
    channelCodes: [TestSecurityPermission.DOMAIN_TEST_SECURITY_UPDATE],
    railCodes: [TestSecurityPermission.DOMAIN_TEST_SECURITY_UPDATE],
    configuration: [TestSecurityPermission.DOMAIN_TEST_SECURITY_UPDATE],

    // Administrative fields - require admin permissions
    tenant: [TestSecurityPermission.DOMAIN_TEST_SECURITY_ADMIN],
    auditInfo: [TestSecurityPermission.DOMAIN_TEST_SECURITY_ADMIN],
    metadata: [TestSecurityPermission.DOMAIN_TEST_SECURITY_UPDATE],
  };

  /**
   * Get required permissions for a specific field.
   * Returns empty array if field has no special permission requirements.
   */
  static getRequiredPermissions(fieldName: string): TestSecurityPermission[] {
    return this.fieldPermissions[fieldName] || [];
  }

  /**
   * Check if a field has specific permission requirements.
   */
  static hasSpecialPermissions(fieldName: string): boolean {
    return fieldName in this.fieldPermissions;
  }

  /**
   * Get all fields that require a specific permission.
   */
  static getFieldsForPermission(permission: TestSecurityPermission): string[] {
    return Object.entries(this.fieldPermissions)
      .filter(([, perms]) => perms.includes(permission))
      .map(([field]) => field);
  }

  /**
   * Get all defined field permissions.
   */
  static getAllFieldPermissions(): Record<string, TestSecurityPermission[]> {
    return { ...this.fieldPermissions };
  }

  /**
   * Add or update permission requirements for a field.
   */
  static setFieldPermissions(
    fieldName: string,
    permissions: TestSecurityPermission[],
  ): void {
    this.fieldPermissions[fieldName] = [...permissions];
  }
}
