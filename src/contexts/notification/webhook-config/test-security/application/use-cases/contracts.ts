// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

/**
 * Abstract Use Case Contracts for TestSecurity Operations
 *
 * These abstract classes are used instead of interfaces to work with NestJS DI.
 * TypeScript interfaces are erased at runtime and cannot be injected.
 */

import { Result, DomainError } from 'src/shared/errors';
import type { IUserToken } from 'src/shared/security';
import type {
  CreateTestSecurityProps,
  UpdateTestSecurityProps,
} from '../../domain/props';
import type {
  DetailTestSecurityResponse,
  TestSecurityPageResponse,
  ListTestSecurityFilterRequest,
} from '../dtos';

export abstract class ICreateTestSecurityUseCase {
  abstract execute(params: {
    user: IUserToken;
    props: CreateTestSecurityProps;
    correlationId: string;
    authorizationReason: string;
  }): Promise<Result<DetailTestSecurityResponse, DomainError>>;
}

export abstract class IUpdateTestSecurityUseCase {
  abstract execute(params: {
    user: IUserToken;
    id: string;
    props: UpdateTestSecurityProps;
    correlationId: string;
    authorizationReason: string;
  }): Promise<Result<DetailTestSecurityResponse, DomainError>>;
}

export abstract class IDeleteTestSecurityUseCase {
  abstract execute(params: {
    user: IUserToken;
    id: string;
    correlationId: string;
    authorizationReason: string;
  }): Promise<Result<void, DomainError>>;
}

export abstract class IGetTestSecurityUseCase {
  abstract execute(params: {
    user: IUserToken;
    id: string;
    correlationId: string;
  }): Promise<Result<DetailTestSecurityResponse, DomainError>>;
}
export abstract class IListTestSecurityUseCase {
  abstract execute(params: {
    user: IUserToken;
    filter?: ListTestSecurityFilterRequest;
    correlationId: string;
  }): Promise<Result<TestSecurityPageResponse, DomainError>>;
}
