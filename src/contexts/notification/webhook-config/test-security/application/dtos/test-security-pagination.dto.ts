// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

/**
 * TestSecurity list request DTO with pagination and filtering
 * Combines standardized pagination with test-security-specific filters
 * Now extends shared base classes for consistency across domains
 */

import {
  BaseSortableListFilterRequest,
  SortFieldConfig,
  PaginationMetaResponse,
} from 'src/shared/application/dtos';
import { ApiListOf } from 'src/shared/application/decorators';
import { ApiPropertyOptional } from '@nestjs/swagger';
import { ApiTestSecurityListMeta, ApiTestSecurityName } from './decorators';
import { ListTestSecurityResponse } from './';
/**
 * TestSecurity list request DTO with pagination and filtering
 * Combines standardized pagination with test-security-specific filters
 * Now extends shared base classes for consistency across domains
 *
 * Available sort fields:
 * - name: Sort by name (default: asc)
 *
 * Example sortBy usage: {"code": "asc", "name": "desc"}
 */
export class ListTestSecurityFilterRequest extends BaseSortableListFilterRequest {
  @ApiTestSecurityName({ required: false })
  name?: string;

  /**
   * Override sortBy with test-security-specific schema for better Swagger documentation
   */
  @ApiPropertyOptional({
    type: 'object',
    description: 'Sort criteria with field names and directions',
    example: { code: 'asc', name: 'asc' },
    default: { code: 'asc', name: 'asc' },
    properties: {
      Name: {
        type: 'string',
        enum: ['asc', 'desc'],
        description: 'Sort by name',
        default: 'asc',
      },
    },
    additionalProperties: false,
  })
  sortBy?: Record<string, 'asc' | 'desc'> = undefined;
  /**
   * Define available sort fields for testSecurities
   * This method ensures type-safe sorting validation
   *
   * Available sort fields:
   * - name: TestSecurity name (sortable, default: asc)
   *
   * @returns Configuration object defining sortable fields and their properties
   */
  getSortFieldConfig(): SortFieldConfig {
    return {
      name: {
        description: 'TestSecurity name',
        sortable: true,
        defaultDirection: 'asc' as const,
      },
    };
  }
}

/**
 * TestSecurity page response DTO with metadata for pagination
 * Extends the generic PagedResponse pattern while maintaining API compatibility
 */
export class TestSecurityPageResponse {
  @ApiListOf(ListTestSecurityResponse)
  readonly data: ListTestSecurityResponse[];

  @ApiTestSecurityListMeta()
  readonly meta: PaginationMetaResponse;

  constructor(data: ListTestSecurityResponse[], meta: PaginationMetaResponse) {
    this.data = data;
    this.meta = meta;
  }

  /**
   * Static factory method for creating response instances
   */
  static create(
    data: ListTestSecurityResponse[],
    meta: PaginationMetaResponse,
  ): TestSecurityPageResponse {
    return new TestSecurityPageResponse(data, meta);
  }

  /**
   * Check if response has data
   */
  get isEmpty(): boolean {
    return this.data.length === 0;
  }

  /**
   * Get total count if available
   */
  get totalCount(): number | undefined {
    return this.meta.totalItems;
  }
}
