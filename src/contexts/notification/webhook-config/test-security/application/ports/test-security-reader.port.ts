// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Result, DomainError } from 'src/shared/errors';
import { RepositoryOptions } from 'src/shared/infrastructure/repositories';
import { ActorContext } from 'src/shared/application/context';
import { Option } from 'src/shared/domain/types';
import { TestSecuritySnapshotProps } from '../../domain/props';
import { TestSecurityId } from '../../domain/value-objects';

/**
 * Token for injecting ITestSecurityReader port implementation
 */
export const TEST_SECURITY_READER_TOKEN = 'ITestSecurityReader' as const;

/**
 * TestSecurity Reader Port - Interface Segregation Principle Implementation
 *
 * Handles basic read operations for TestSecurity aggregates.
 * This interface focuses solely on simple retrieval operations.
 *
 * ISP Benefits:
 * - Clients only depend on methods they actually use
 * - Easier to mock and test specific read scenarios
 * - Can be implemented independently from write operations
 * - Supports read-only implementations (e.g., reporting services)
 *
 * @domain Notification Context - TestSecurity Reader Port
 * @layer Application Ports
 * @pattern Interface Segregation Principle
 */
export interface ITestSecurityReader {
  /**
   * Find a TestSecurity by its unique identifier
   * @param actor - The authenticated user context
   * @param id - The unique identifier of the TestSecurity
   * @param options - Optional repository options
   * @returns Result containing the TestSecurity snapshot or null if not found
   */
  findById(
    actor: ActorContext,
    id: TestSecurityId,
    options?: RepositoryOptions,
  ): Promise<Result<Option<TestSecuritySnapshotProps>, DomainError>>;

  /**
   * Check if a testSecurity exists by ID (for write-path validation)
   * @param actor - The authenticated user context
   * @param id - The unique identifier of the TestSecurity
   * @param options - Optional repository options
   * @returns Result containing boolean indicating existence
   */
  exists(
    actor: ActorContext,
    id: TestSecurityId,
    options?: RepositoryOptions,
  ): Promise<Result<boolean, DomainError>>;

  /**
   * Get testSecurity version for optimistic concurrency control
   * @param actor - The authenticated user context
   * @param id - The unique identifier of the TestSecurity
   * @param options - Optional repository options
   * @returns Result containing version number or null if not found
   */
  getVersion(
    actor: ActorContext,
    id: TestSecurityId,
    options?: RepositoryOptions,
  ): Promise<Result<Option<number>, DomainError>>;

  /**
   * Get minimal testSecurity data for write-path operations
   * @param actor - The authenticated user context
   * @param id - The unique identifier of the TestSecurity
   * @param options - Optional repository options
   * @returns Result containing minimal testSecurity data or null if not found
   */
  getMinimal(
    actor: ActorContext,
    id: TestSecurityId,
    options?: RepositoryOptions,
  ): Promise<Result<Option<{ id: string; version: number }>, DomainError>>;
}
