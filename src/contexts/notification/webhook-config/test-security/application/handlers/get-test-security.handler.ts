// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { QueryHandler, IQueryHandler } from '@nestjs/cqrs';
import { DetailTestSecurityResponse } from '../dtos';
import { GetTestSecurityUseCase } from '../use-cases';
import { GetTestSecurityQuery } from '../queries';
import { Result, DomainError } from 'src/shared/errors';

@QueryHandler(GetTestSecurityQuery)
export class GetTestSecurityHandler
  implements IQueryHandler<GetTestSecurityQuery>
{
  constructor(
    private readonly getTestSecurityUseCase: GetTestSecurityUseCase,
  ) {}

  async execute(
    query: GetTestSecurityQuery,
  ): Promise<Result<DetailTestSecurityResponse | null, DomainError>> {
    return this.getTestSecurityUseCase.execute({
      user: query.user,
      id: query.id,
      correlationId: query.correlationId || 'query-handler',
    });
  }
}
