// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { CommandHandler, ICommandHandler } from '@nestjs/cqrs';
import { Inject } from '@nestjs/common';
import { Result, DomainError } from 'src/shared/errors';
import { DeleteTestSecurityUseCase } from '../use-cases';
import { DeleteTestSecurityCommand } from '../commands';

@CommandHandler(DeleteTestSecurityCommand)
export class DeleteTestSecurityHandler
  implements ICommandHandler<DeleteTestSecurityCommand>
{
  constructor(
    @Inject(DeleteTestSecurityUseCase)
    private readonly deleteTestSecurityUseCase: DeleteTestSecurityUseCase,
  ) {}

  async execute(
    command: DeleteTestSecurityCommand,
  ): Promise<Result<void, DomainError>> {
    return await this.deleteTestSecurityUseCase.execute({
      user: command.user,
      id: command.id,
      correlationId: command.correlationId,
      authorizationReason: 'CQRS Command Handler',
    });
  }
}
