// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { QueryHandler, IQueryHandler } from '@nestjs/cqrs';
import { Injectable } from '@nestjs/common';
import { DomainError, Result } from 'src/shared/errors';
import { ListTestSecurityQuery } from '../queries';
import { ListTestSecurityUseCase } from '../use-cases';
import { TestSecurityPageResponse } from '../dtos';

/**
 * ✅ Enhanced CQRS Query Handler with use case delegation
 * Follows the pattern: Handler → Use Case → Repository
 */
@Injectable()
@QueryHandler(ListTestSecurityQuery)
export class ListTestSecurityHandler
  implements IQueryHandler<ListTestSecurityQuery>
{
  constructor(
    private readonly listTestSecurityUseCase: ListTestSecurityUseCase,
  ) {}

  /**
   * Handle the list testSecurity query by delegating to the use case
   */
  async execute(
    query: ListTestSecurityQuery,
  ): Promise<Result<TestSecurityPageResponse, DomainError>> {
    // ✅ Delegate to use case for business logic
    return await this.listTestSecurityUseCase.execute({
      user: query.user,
      filter: query.filter,
      correlationId: query.correlationId || 'unknown',
    });
  }
}
