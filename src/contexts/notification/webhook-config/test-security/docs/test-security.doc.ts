// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DocumentBuilder, SwaggerModule } from '@nestjs/swagger';
import { INestApplication } from '@nestjs/common';
import { SwaggerConfigUtil } from 'src/docs/swagger-config.util';
import { TestSecurityModule } from '../test-security.module';
/**
 * TestSecurity documentation endpoints interface
 */
export interface TestSecurityDocumentationUrls {
  testSecurity: string;
}

/**
 * TestSecurity Documentation
 * This module handles the Swagger documentation for testSecurities
 *
 */
export class TestSecurityDocumentation {
  static setup(app: INestApplication, port: string | number): void {
    const config = new DocumentBuilder()
      .addBearerAuth(
        { type: 'http', scheme: 'bearer', bearerFormat: 'JWT' },
        'bearer',
      )
      .setTitle('📱 TestSecurity Management API')
      .setDescription(
        `
## bounded context: notification
[← Back to documentation](/api/docs/notification)

### application: webhook-config
[← Back to documentation](/api/docs/notification/webhook-config)
`,
      )
      .setVersion('1.0.0')
      .addTag('TestSecurities', `Configuration for testSecurity table`);

    // Add dynamic server configuration
    SwaggerConfigUtil.addServers(config, port);

    const document = SwaggerModule.createDocument(app, config.build(), {
      include: [TestSecurityModule],
      extraModels: [],
    });

    SwaggerModule.setup(
      'api/docs/notification/webhook-config/test-security',
      app,
      document,
    );
  }

  /**
   * Setup all testSecurity-related documentation endpoints
   * This method centralizes the setup of all testSecurity module documentation
   */
  static setupAll(
    app: INestApplication,
    port: string | number,
  ): TestSecurityDocumentationUrls {
    // Setup main testSecurity documentation
    TestSecurityDocumentation.setup(app, port);

    // Setup sub-module documentation

    // Return all endpoint URLs
    return {
      testSecurity: TestSecurityDocumentation.getEndpoint(port),
    };
  }

  static getEndpoint(port: string | number): string {
    return `${SwaggerConfigUtil.getServerUrl(port)}/api/docs/notification/webhook-config/test-security`;
  }
}
