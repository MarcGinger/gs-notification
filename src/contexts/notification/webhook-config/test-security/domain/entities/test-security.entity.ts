// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { EntityIdBase } from 'src/shared/domain/entities';
import { Result, ok, err, DomainError } from 'src/shared/errors';
import { TestSecuritySnapshotProps } from '../props';
import { TestSecurityDomainState } from '../state';
import { TestSecurityErrors } from '../errors/test-security.errors';
import {
  TestSecurityCreatedAt,
  TestSecurityUpdatedAt,
  TestSecurityVersion,
  TestSecurityDescription,
  TestSecurityId,
  TestSecurityName,
} from '../value-objects';

/**
 * Domain Entity: TestSecurity
 *
 * Represents the core TestSecurity entity in the notification domain.
 * Encapsulates testSecurity data, identity, and basic entity behavior.
 *
 * This entity follows DDD principles:
 * - Identity: Id as unique identifier
 * - Immutability: Changes create new instances
 * - Encapsulation: Private state with controlled access
 * - Business validation: Domain rules enforced
 *
 * @domain Notification Context - TestSecurity Entity
 * @layer Domain Entities
 */

/**
 * TestSecurity Entity
 *
 * Core domain entity representing a testSecurity in the notification.
 * Handles testSecurity identity, validation, and state management.
 */
export class TestSecurityEntity extends EntityIdBase<
  TestSecurityDomainState,
  TestSecurityId
> {
  private static clock: { now: () => Date } = { now: () => new Date() };

  public static setClock(c: { now: () => Date }) {
    this.clock = c;
  }

  /**
   * Helper method to create updatedAt VO from date
   */
  private static createUpdatedAt(
    date?: Date,
  ): Result<TestSecurityUpdatedAt, DomainError> {
    const targetDate = date || TestSecurityEntity.clock.now();
    const result = TestSecurityUpdatedAt.from(targetDate);
    if (!result.ok) {
      return err(result.error);
    }
    return ok(result.value);
  }

  /**
   * Helper method to create version VO from number
   */
  private static createVersion(
    versionNumber?: number,
    currentVersion?: TestSecurityVersion,
  ): Result<TestSecurityVersion, DomainError> {
    const targetVersion = versionNumber ?? currentVersion?.value ?? 1;
    const result = TestSecurityVersion.from(targetVersion);
    if (!result.ok) {
      return err(result.error);
    }
    return ok(result.value);
  }

  /**
   * Helper method to create updated entity with common logic
   */
  private createUpdatedEntity(
    fieldUpdates: Partial<TestSecurityDomainState>,
    updatedAt?: Date,
    version?: number,
  ): Result<TestSecurityEntity, DomainError> {
    const updatedAtResult = TestSecurityEntity.createUpdatedAt(updatedAt);
    if (!updatedAtResult.ok) {
      return err(updatedAtResult.error);
    }

    const versionResult = TestSecurityEntity.createVersion(
      version,
      this.props.version,
    );
    if (!versionResult.ok) {
      return err(versionResult.error);
    }

    const updatedProps: TestSecurityDomainState = {
      ...this.props,
      ...fieldUpdates,
      updatedAt: updatedAtResult.value,
      version: versionResult.value,
    };

    return TestSecurityEntity.create(updatedProps);
  }

  private constructor(props: TestSecurityDomainState) {
    super(props, props.id);
  }

  /**
   * Factory method to create a new testSecurity entity
   *
   * @param props - TestSecurity properties
   * @returns Result containing TestSecurityEntity or DomainError
   */
  public static create(
    props: TestSecurityDomainState,
  ): Result<TestSecurityEntity, DomainError> {
    // Validate required properties
    const validationResult = TestSecurityEntity.validate(props);
    if (validationResult.ok === false) {
      return err(validationResult.error);
    }

    return ok(new TestSecurityEntity(props));
  }

  /**
   * Factory method to reconstitute a testSecurity entity from persistence
   *
   * @param props - TestSecurity properties from database
   * @returns TestSecurityEntity instance
   */
  public static reconstitute(
    props: TestSecurityDomainState,
  ): TestSecurityEntity {
    return new TestSecurityEntity(props);
  }

  /**
   * Factory method to reconstitute entity from snapshot
   *
   * @param snapshot - TestSecurity snapshot properties
   * @returns Result containing TestSecurityEntity or DomainError
   */
  public static fromSnapshot(
    snapshot: TestSecuritySnapshotProps,
  ): Result<TestSecurityEntity, DomainError> {
    const idResult = TestSecurityId.from(snapshot.id);
    if (!idResult.ok) {
      return err(idResult.error);
    }
    const nameResult = TestSecurityName.from(snapshot.name);
    if (!nameResult.ok) {
      return err(nameResult.error);
    }
    const descriptionResult = TestSecurityDescription.from(
      snapshot.description,
    );
    if (!descriptionResult.ok) {
      return err(descriptionResult.error);
    }
    const createdAtResult = TestSecurityCreatedAt.from(snapshot.createdAt);
    if (!createdAtResult.ok) {
      return err(createdAtResult.error);
    }

    const updatedAtResult = TestSecurityUpdatedAt.from(snapshot.updatedAt);
    if (!updatedAtResult.ok) {
      return err(updatedAtResult.error);
    }

    const versionResult = TestSecurityVersion.from(snapshot.version);
    if (!versionResult.ok) {
      return err(versionResult.error);
    }

    const props: TestSecurityDomainState = {
      id: idResult.value,
      name: nameResult.value,
      description: descriptionResult.value,
      createdAt: createdAtResult.value,
      updatedAt: updatedAtResult.value,
      version: versionResult.value,
    };

    return TestSecurityEntity.create(props);
  }

  /**
   * Validates testSecurity entity properties
   *
   * @param props - Properties to validate
   * @returns Validation result
   */
  private static validate(
    props: TestSecurityDomainState,
  ): Result<void, DomainError> {
    // Basic validation
    if (!props.id) {
      return err(TestSecurityErrors.INVALID_ID_DATA);
    }
    if (!props.name) {
      return err(TestSecurityErrors.INVALID_NAME_DATA);
    }

    return ok(undefined);
  }

  // ======================
  // Getters (Public API)
  // ======================

  public get id(): TestSecurityId {
    return this.props.id;
  }

  public get name(): TestSecurityName {
    return this.props.name;
  }

  public get description(): TestSecurityDescription | undefined {
    return this.props.description;
  }

  public get createdAt(): TestSecurityCreatedAt {
    return this.props.createdAt;
  }

  public get updatedAt(): TestSecurityUpdatedAt {
    return this.props.updatedAt;
  }

  public get version(): TestSecurityVersion {
    return this.props.version;
  }

  // ======================
  // Simple Update Methods (No Business Logic)
  // ======================

  /**
   * Creates a new entity with updated name (pure state transition)
   *
   * @param name - New Name value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<TestSecurityEntity, DomainError>
   */
  public withName(
    name: TestSecurityName,
    updatedAt?: Date,
    version?: number,
  ): Result<TestSecurityEntity, DomainError> {
    return this.createUpdatedEntity({ name }, updatedAt, version);
  }

  /**
   * Creates a new entity with updated description (pure state transition)
   *
   * @param description - New description value
   * @param updatedAt - Optional timestamp (uses clock if not provided)
   * @returns Result<TestSecurityEntity, DomainError>
   */
  public withDescription(
    description: TestSecurityDescription,
    updatedAt?: Date,
    version?: number,
  ): Result<TestSecurityEntity, DomainError> {
    return this.createUpdatedEntity({ description }, updatedAt, version);
  }

  // ======================
  // Query Methods
  // ======================

  /**
   * Checks if two testSecurities are the same entity
   *
   * @param other - Other testSecurity to compare
   */
  public sameAs(other: TestSecurityEntity): boolean {
    return this.props.id.equals(other.props.id);
  }

  /**
   * Gets a snapshot of current entity state for serialization
   */
  public toSnapshot(): TestSecuritySnapshotProps {
    return {
      id: this.props.id.value,
      name: this.props.name.value,
      description: this.props.description?.value,
      createdAt: this.props.createdAt.value,
      updatedAt: this.props.updatedAt.value,
      version: this.props.version.value,
    };
  }

  /**
   * Gets the current domain state with value objects
   *
   * Provides access to the rich domain state for application layer
   * without requiring infrastructure mappers. Maintains clean architecture.
   *
   * @returns Current domain state with value objects
   */
  public getDomainState(): TestSecurityDomainState {
    return this.props;
  }
}
