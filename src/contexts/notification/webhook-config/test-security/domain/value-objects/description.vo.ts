// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { TestSecurityErrors } from '../errors/test-security.errors';

/**
 * Description Value Object
 * Represents a validated description with business rules
 */
export const TestSecurityDescription = createStringVO({
  name: 'Description',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    TestSecurityErrors.INVALID_DESCRIPTION,
    'Description',
  ),
});

/**
 * Public instance type for Description
 */
export type TestSecurityDescription = StringVOInstance;

// Convenience creators
export const createTestSecurityDescription = (s: string) =>
  TestSecurityDescription.create(s);
export const testSecurityDescriptionFrom = (v: unknown) =>
  TestSecurityDescription.from(v);
