// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { TestSecurityErrors } from '../errors/test-security.errors';

/**
 * Name Value Object
 * Represents a validated name with business rules
 */
export const TestSecurityName = createStringVO({
  name: 'Name',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(TestSecurityErrors.INVALID_NAME, 'Name'),
});

/**
 * Public instance type for Name
 */
export type TestSecurityName = StringVOInstance;

// Convenience creators
export const createTestSecurityName = (s: string) => TestSecurityName.create(s);
export const testSecurityNameFrom = (v: unknown) => TestSecurityName.from(v);
