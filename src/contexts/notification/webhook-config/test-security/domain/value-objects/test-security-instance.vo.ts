// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DomainError, Result, ok, err } from 'src/shared/errors';
import {
  createIntegerVO,
  createIntegerVOErrors,
  IntegerVOInstance,
  createDateTimeVO,
  createDateTimeVOErrors,
  DateTimeVOInstance,
} from 'src/shared/domain/value-objects';
import { Clock } from 'src/shared/domain/clock';
import { TestSecurityErrors } from '../errors/test-security.errors';

/**
 * TestSecurity Instance Value Objects
 *
 * Instance VOs for TestSecurity aggregate lifecycle management.
 * Includes creation timestamps, update timestamps, and version tracking.
 * Follows the Clock injection pattern for deterministic testing.
 */

// ===========================================
// TestSecurity Created At Instance VO
// ===========================================

const TestSecurityCreatedAtErrors = createDateTimeVOErrors(
  TestSecurityErrors.CREATED_AT_REQUIRED,
  'TestSecurity Created At',
);

export const TestSecurityCreatedAt = createDateTimeVO({
  name: 'TestSecurityCreatedAt',
  allowFuture: false, // Cannot create testSecurity in the future
  allowPast: true, // Can create with past timestamps (data migration)
  customValidation: (date: Date): Result<void, DomainError> => {
    // Optional: Add domain-specific creation time validation
    // For example, cannot be more than 1 year in the past
    const oneYearAgo = new Date();
    oneYearAgo.setFullYear(oneYearAgo.getFullYear() - 1);

    if (date < oneYearAgo) {
      return err({
        ...TestSecurityErrors.CREATED_AT_REQUIRED,
        detail:
          'TestSecurity creation date cannot be more than 1 year in the past',
        context: { value: date, maxPastDate: oneYearAgo },
      } as DomainError);
    }

    return ok(undefined);
  },
  errors: TestSecurityCreatedAtErrors,
});

export type TestSecurityCreatedAt = DateTimeVOInstance;

// ===========================================
// TestSecurity Updated At Instance VO
// ===========================================

const TestSecurityUpdatedAtErrors = createDateTimeVOErrors(
  TestSecurityErrors.UPDATED_AT_REQUIRED,
  'TestSecurity Updated At',
);

export const TestSecurityUpdatedAt = createDateTimeVO({
  name: 'TestSecurityUpdatedAt',
  allowFuture: false, // Cannot update testSecurity in the future
  allowPast: true, // Can update with past timestamps (data correction)
  errors: TestSecurityUpdatedAtErrors,
});

export type TestSecurityUpdatedAt = DateTimeVOInstance;

// ===========================================
// TestSecurity Version Instance VO
// ===========================================

const TestSecurityVersionErrors = createIntegerVOErrors(
  TestSecurityErrors.INVALID_CODE_DATA, // Reusing existing error until specific version error is added
  'TestSecurity Version',
);

export const TestSecurityVersion = createIntegerVO({
  name: 'TestSecurityVersion',
  min: 0, // Version starts at 0
  allowNegative: false,
  errors: TestSecurityVersionErrors,
});

export type TestSecurityVersion = IntegerVOInstance;

// ===========================================
// Clock-Based Helper Functions (Explicit Dependencies)
// ===========================================

/**
 * Create TestSecurityCreatedAt from Clock (explicit dependency)
 * Deterministic and testable - pass a fake clock in tests
 */
export const createdAtNow = (
  clock: Clock,
): Result<TestSecurityCreatedAt, DomainError> =>
  TestSecurityCreatedAt.create(clock.now());

/**
 * Create TestSecurityUpdatedAt from Clock (explicit dependency)
 * Deterministic and testable - pass a fake clock in tests
 */
export const updatedAtNow = (
  clock: Clock,
): Result<TestSecurityUpdatedAt, DomainError> =>
  TestSecurityUpdatedAt.create(clock.now());

/**
 * Bump version by 1 (domain business rule)
 */
export const bumpVersion = (
  currentVersion: TestSecurityVersion,
): Result<TestSecurityVersion, DomainError> =>
  TestSecurityVersion.create(currentVersion.value + 1);

/**
 * Create initial version (starts at 1 for new aggregates)
 */
export const initialVersion = (): Result<TestSecurityVersion, DomainError> =>
  TestSecurityVersion.create(1);

// ===========================================
// Direct Creation Functions (Non-Clock Based)
// ===========================================

/**
 * Create TestSecurityCreatedAt from Date
 */
export const createTestSecurityCreatedAt = (
  date: Date,
): Result<TestSecurityCreatedAt, DomainError> =>
  TestSecurityCreatedAt.create(date);

/**
 * Create TestSecurityUpdatedAt from Date
 */
export const createTestSecurityUpdatedAt = (
  date: Date,
): Result<TestSecurityUpdatedAt, DomainError> =>
  TestSecurityUpdatedAt.create(date);

/**
 * Create TestSecurityVersion from number
 */
export const createTestSecurityVersion = (
  version: number,
): Result<TestSecurityVersion, DomainError> =>
  TestSecurityVersion.create(version);

// ===========================================
// Domain Invariants & Validation
// ===========================================

/**
 * Validate that updatedAt is not before createdAt (monotonic time)
 */
export const validateMonotonicTime = (
  createdAt: TestSecurityCreatedAt,
  updatedAt: TestSecurityUpdatedAt,
): Result<void, DomainError> => {
  if (updatedAt.value < createdAt.value) {
    return err({
      ...TestSecurityErrors.UPDATED_AT_REQUIRED,
      detail: 'Updated at cannot be before created at',
      context: {
        createdAt: createdAt.value.toISOString(),
        updatedAt: updatedAt.value.toISOString(),
      },
    } as DomainError);
  }
  return ok(undefined);
};

// ===========================================
// Legacy Compatibility (DEPRECATED)
// ===========================================

/**
 * @deprecated Use createdAtNow(clock) instead for better testability
 * This function hides the Date dependency and makes testing harder
 */
export function createTestSecurityCreatedAtNow(): Result<
  TestSecurityCreatedAt,
  DomainError
> {
  return TestSecurityCreatedAt.create(new Date());
}

/**
 * @deprecated Use updatedAtNow(clock) instead for better testability
 * This function hides the Date dependency and makes testing harder
 */
export function createTestSecurityUpdatedAtNow(): Result<
  TestSecurityUpdatedAt,
  DomainError
> {
  return TestSecurityUpdatedAt.create(new Date());
}
