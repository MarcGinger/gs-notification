// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { EventMetadata } from 'src/shared/domain/events';

/**
 * TestSecurity Deleted Event Payload
 * Contains identifying information about the deleted testSecurity
 */
export interface TestSecurityDeletedEventPayload {
  id: string;
  deletedAt: Date;
  version: number;
  metadata: EventMetadata;
}

/**
 * TestSecurity Deleted Domain Event
 *
 * Emitted when a testSecurity is successfully deleted.
 * Contains identifying information and deletion metadata.
 */
export class TestSecurityDeletedEvent {
  public readonly eventType = 'NotificationWebhookConfigTestSecurityDeleted.v1';
  public readonly eventVersion = 'v1';

  constructor(public readonly payload: TestSecurityDeletedEventPayload) {}

  // Factory method with security context
  static create(
    data: Omit<TestSecurityDeletedEventPayload, 'metadata'>,
    baseMetadata: EventMetadata,
  ): TestSecurityDeletedEvent {
    const metadata: EventMetadata = {
      ...baseMetadata,
      eventVersion: '1.0',
      source: 'notification.domain.test-security',
    };

    return new TestSecurityDeletedEvent({
      ...data,
      metadata,
    });
  }

  get id(): string {
    return this.payload.id;
  }

  get deletedAt(): Date {
    return this.payload.deletedAt;
  }

  get version(): number {
    return this.payload.version;
  }

  get metadata(): EventMetadata {
    return this.payload.metadata;
  }
}
