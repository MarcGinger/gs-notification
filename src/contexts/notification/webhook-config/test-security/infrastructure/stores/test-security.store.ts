// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

/**
 * Simple Generic Store
 *
 * Direct, no-nonsense storage for any projections.
 * Used by both projectors (write) and repositories (read).
 */

import { BaseProjection, SimpleStore } from 'src/shared/infrastructure/stores';
import { DetailTestSecurityResponse } from '../../application/dtos';

/**
 * TestSecurity projection data
 */
export interface TestSecurityProjection
  extends DetailTestSecurityResponse,
    BaseProjection {
  lastStreamRevision: string;
}

/**
 * TestSecurity-specific store instance - shared across projectors and repositories
 */
export const testSecurityStore = new SimpleStore<TestSecurityProjection>(
  5000,
  'testSecurity',
);
