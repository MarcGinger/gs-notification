// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  Controller,
  Get,
  Query,
  Post,
  Body,
  Put,
  Delete,
  HttpStatus,
  HttpCode,
  Param,
  UseInterceptors,
  UseGuards,
} from '@nestjs/common';
import {
  ApiTags,
  ApiOperation,
  ApiResponse,
  ApiNoContentResponse,
  ApiCreatedResponse,
  ApiBody,
  ApiHeader,
  ApiParam,
  ApiOkResponse,
  ApiBearerAuth,
} from '@nestjs/swagger';
import { IdempotencyKey } from 'src/shared/interfaces/decorators';
import { IdempotencyKeyPipe } from 'src/shared/interfaces/pipes';
import { TestSecurityApplicationService } from '../../../application/services';
import { CurrentUser, IUserToken, JwtAuthGuard } from 'src/shared/security';
import {
  DetailTestSecurityResponse,
  CreateTestSecurityRequest,
  UpdateTestSecurityRequest,
  TestSecurityPageResponse,
  ListTestSecurityFilterRequest,
  ListTestSecurityResponse,
} from '../../../application/dtos';
import { Result, ResultInterceptor, DomainError } from 'src/shared/errors';
import { PaginatedResponse } from 'src/shared/application/dtos';
import {
  TestSecurityReadResource,
  TestSecurityCreateResource,
  TestSecurityUpdateResource,
  TestSecurityDeleteResource,
} from '../../test-security.resource';
import { ApiCommonErrors } from 'src/shared/interfaces/http';

@Controller()
@ApiTags('TestSecurities')
@ApiBearerAuth()
@UseGuards(JwtAuthGuard)
@UseInterceptors(ResultInterceptor)
export class TestSecurityController {
  constructor(
    private readonly testSecurityApplicationService: TestSecurityApplicationService,
  ) {}

  @Get()
  @TestSecurityReadResource()
  @ApiOperation({
    summary: 'List Test securities',
    description:
      'Retrieves a list of Test securities with optional filtering. Supports pagination and filtering by name or category. Requires READ permission (LOW risk).',
  })
  @ApiOkResponse({
    description: 'List of Test securities retrieved successfully',
    type: TestSecurityPageResponse,
  })
  @ApiCommonErrors()
  async list(
    @CurrentUser() user: IUserToken,
    @Query() pageRequest?: ListTestSecurityFilterRequest,
  ): Promise<Result<PaginatedResponse<ListTestSecurityResponse>, DomainError>> {
    const result = await this.testSecurityApplicationService.listTestSecurities(
      user,
      pageRequest,
    );
    return result;
  }

  @Get(':id')
  @TestSecurityReadResource()
  @ApiOperation({
    summary: 'Get Test security by ID',
    description:
      'Retrieves a single Test security by its unique identifier. Requires READ permission (LOW risk).',
  })
  @ApiParam({
    name: 'id',
    type: 'string',
    description: 'TestSecurity unique identifier (UUID format)',
    format: 'uuid',
    example: '123e4567-e89b-12d3-a456-426614174000',
  })
  @ApiOkResponse({
    description: 'TestSecurity details retrieved successfully',
    type: DetailTestSecurityResponse,
  })
  @ApiCommonErrors()
  async get(
    @CurrentUser() user: IUserToken,
    @Param('id') id: string,
  ): Promise<Result<DetailTestSecurityResponse, DomainError>> {
    const result =
      await this.testSecurityApplicationService.getTestSecurityById(user, id);

    return result;
  }
  // ========================================
  // Core CRUD Operations
  // ========================================

  @Post()
  @TestSecurityCreateResource()
  @HttpCode(HttpStatus.CREATED)
  @ApiOperation({
    summary: 'Create a new Test security',
    description:
      'Creates a new Test security in the catalog. Requires CREATE permission (MEDIUM risk).',
  })
  @ApiHeader({
    name: 'Idempotency-Key',
    required: false,
    description:
      'Ensures the request is processed only once. Accepts both Idempotency-Key and x-idempotency-key headers.',
  })
  @ApiCreatedResponse({
    type: DetailTestSecurityResponse,
    description: 'TestSecurity successfully created',
    headers: {
      Location: {
        description: 'URI of the newly created resource',
        schema: {
          type: 'string',
          example: '/test-securities/123e4567-e89b-12d3-a456-426614174000',
        },
      },
    },
  })
  @ApiCommonErrors()
  @ApiBody({ type: CreateTestSecurityRequest })
  async create(
    @CurrentUser() user: IUserToken,
    @Body() createTestSecurityRequest: CreateTestSecurityRequest,
    @IdempotencyKey(new IdempotencyKeyPipe())
    idempotencyKey: string | undefined,
  ): Promise<Result<DetailTestSecurityResponse, DomainError>> {
    // idempotencyKey is validated by the IdempotencyKeyPipe

    // Call service with options including idempotency key
    const result = await this.testSecurityApplicationService.createTestSecurity(
      user,
      createTestSecurityRequest,
      {
        idempotencyKey,
      },
    );

    return result;
  }

  @Put(':id')
  @TestSecurityUpdateResource()
  @ApiOperation({
    summary: 'Update a Test security',
    description:
      'Updates an existing Test security with new data. Supports partial updates. Requires UPDATE permission (MEDIUM risk).',
  })
  @ApiParam({
    name: 'id',
    type: 'string',
    description: 'TestSecurity unique identifier (UUID format)',
    format: 'uuid',
    example: '123e4567-e89b-12d3-a456-426614174000',
  })
  @ApiResponse({
    status: HttpStatus.OK,
    description: 'TestSecurity updated successfully',
    type: DetailTestSecurityResponse,
  })
  @ApiCommonErrors({
    include422: true,
    extra: [
      { status: 429, description: 'Too Many Requests' },
      { status: 503, description: 'Upstream dependency unavailable' },
    ],
  })
  @ApiBody({
    type: UpdateTestSecurityRequest,
    description:
      'TestSecurity update data. Only provided fields will be updated.',
  })
  async update(
    @CurrentUser() user: IUserToken,
    @Param('id') id: string,
    @Body() updateTestSecurityRequest: UpdateTestSecurityRequest,
  ): Promise<Result<DetailTestSecurityResponse, DomainError>> {
    const result = await this.testSecurityApplicationService.updateTestSecurity(
      user,
      id,
      updateTestSecurityRequest,
    );

    return result;
  }

  @Delete(':id')
  @TestSecurityDeleteResource()
  @HttpCode(HttpStatus.NO_CONTENT)
  @ApiOperation({
    summary: 'Delete a Test security',
    description:
      'Soft-deletes a Test security (marks as deleted). Requires DELETE permission (HIGH risk, justification required).',
  })
  @ApiParam({
    name: 'id',
    type: 'string',
    description: 'TestSecurity unique identifier (UUID format)',
    format: 'uuid',
    example: '123e4567-e89b-12d3-a456-426614174000',
  })
  @ApiCommonErrors()
  @ApiNoContentResponse({
    description: 'TestSecurity deleted successfully. No content returned.',
  })
  async remove(
    @CurrentUser() user: IUserToken,
    @Param('id') id: string,
  ): Promise<Result<void, DomainError>> {
    const result = await this.testSecurityApplicationService.deleteTestSecurity(
      user,
      id,
    );
    return result;
  }
}
