// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Module } from '@nestjs/common';
import { RouterModule, Routes } from '@nestjs/core';
import { TestSecurityModule } from '../../test-security.module';
import { TestSecurityProjectorModule } from '../../test-security-projector.module';
/**
 * Test security Router Configuration
 *
 * Configures nested routing for test-security endpoints to follow
 * the module monolith structure: /notification/webhook-config/api/v1/test-securities
 *
 * This replaces flat routing (/api/v1/test-securities) with contextual routing
 * that reflects the bounded context hierarchy.
 */

// Define the routing structure for the test-security bounded context
const testSecurityRoutes: Routes = [
  {
    path: 'notification',
    children: [
      {
        path: 'webhook-config',
        children: [
          {
            path: 'test-securities',
            module: TestSecurityModule,
          },
          {
            path: 'health',
            module: TestSecurityProjectorModule,
          },
        ],
      },
    ],
  },
];

/**
 * Test security Router Module
 *
 * Provides nested routing configuration for test-security endpoints.
 * This module should be imported in the main app module to enable
 * the nested route structure.
 *
 * Routes configured:
 * - /notification/webhook-config/api/v1/test-securities (GET, POST)
 * - /notification/webhook-config/api/v1/test-securities/:id (GET, PUT, DELETE)
 * - /notification/webhook-config/health/projectors/testSecurity (GET)
 * - /notification/webhook-config/health/projectors/test-security/metrics (GET)
 */
@Module({
  imports: [
    TestSecurityModule,
    TestSecurityProjectorModule,
    RouterModule.register(testSecurityRoutes),
  ],
  exports: [TestSecurityModule, TestSecurityProjectorModule],
})
export class TestSecurityRouterModule {}
