// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Module } from '@nestjs/common';
import { RouterModule, Routes } from '@nestjs/core';
import { AuthtypeModule } from '../../authtype.module';
import { AuthtypeProjectorModule } from '../../authtype-projector.module';
/**
 * Authtype Router Configuration
 *
 * Configures nested routing for authtype endpoints to follow
 * the module monolith structure: /notification/webhook-config/api/v1/authtypes
 *
 * This replaces flat routing (/api/v1/authtypes) with contextual routing
 * that reflects the bounded context hierarchy.
 */

// Define the routing structure for the authtype bounded context
const authtypeRoutes: Routes = [
  {
    path: 'notification',
    children: [
      {
        path: 'webhook-config',
        children: [
          {
            path: 'authtypes',
            module: AuthtypeModule,
          },
          {
            path: 'health',
            module: AuthtypeProjectorModule,
          },
        ],
      },
    ],
  },
];

/**
 * Authtype Router Module
 *
 * Provides nested routing configuration for authtype endpoints.
 * This module should be imported in the main app module to enable
 * the nested route structure.
 *
 * Routes configured:
 * - /notification/webhook-config/api/v1/authtypes/:id (GET, PUT, DELETE)
 * - /notification/webhook-config/health/projectors/authtype (GET)
 * - /notification/webhook-config/health/projectors/authtype/metrics (GET)
 */
@Module({
  imports: [
    AuthtypeModule,
    AuthtypeProjectorModule,
    RouterModule.register(authtypeRoutes),
  ],
  exports: [AuthtypeModule, AuthtypeProjectorModule],
})
export class AuthtypeRouterModule {}
