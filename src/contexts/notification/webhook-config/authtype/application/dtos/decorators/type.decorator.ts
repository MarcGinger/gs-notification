// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { applyDecorators } from '@nestjs/common';
import { ApiProperty } from '@nestjs/swagger';
import { IsEnum, IsOptional, IsNotEmpty } from 'class-validator';
import {
  AuthtypeTypeValues,
  AuthtypeTypeValue,
} from '../../../domain/value-objects';

export { AuthtypeTypeValues, AuthtypeTypeValue };
/**
 * Options for property decorators (enum)
 */
interface PropOptions {
  required?: boolean;
}

/**
 * Property decorator for Authtype Type with required option
 * @param {Object} options - Options for the decorator
 * @returns {PropertyDecorator}
 */
export function ApiAuthtypeType(options: PropOptions = {}) {
  const { required = true } = options;
  return applyDecorators(
    ApiProperty({
      description: `Authentication method type - none (no auth), hmac (signature-based), basic (username/password), api-key (API key), oauth2-client-credentials (OAuth2 CC flow), or mtls (mutual TLS). Values include none, hmac, basic, api-key, oauth2-client-credentials, mtls.`,
      type: String,
      enum: AuthtypeTypeValues,
      required,
    }),
    IsEnum(AuthtypeTypeValues),
    required ? IsNotEmpty() : IsOptional(),
  );
}
