// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { AuthtypeDomainState } from '../../domain/state';
import { DetailAuthtypeResponse } from '../dtos';

/**
 * Authtype DTO Assembler
 *
 * Converts AuthtypeDomainState (pure VOs) to transport DTOs.
 * Bridges the gap between rich domain objects and API response format.
 * Uses existing generated DTOs and follows established patterns.
 */

/**
 * Assembles DTOs from AuthtypeDomainState for API transport
 */
export class AuthtypeDtoAssembler {
  /**
   * Convert AuthtypeDomainState to detailed response DTO
   *
   * @param domainState - Rich domain state with VOs
   * @returns Clean DTO for API response
   */
  static toDetailResponse(
    domainState: AuthtypeDomainState,
  ): DetailAuthtypeResponse {
    const dto = new DetailAuthtypeResponse();

    // Extract primitive values from VOs
    dto.id = domainState.id.value;
    dto.webhookId = domainState.webhookId.value;
    dto.type = domainState.type.value;
    dto.signingSecretRef = domainState.signingSecretRef?.value;
    dto.signatureAlgorithm = domainState.signatureAlgorithm?.value;
    dto.usernameRef = domainState.usernameRef?.value;
    dto.passwordRef = domainState.passwordRef?.value;
    dto.apiKeyRef = domainState.apiKeyRef?.value;
    dto.apiKeyHeader = domainState.apiKeyHeader?.value;
    dto.apiKeyQueryParam = domainState.apiKeyQueryParam?.value;
    dto.tokenUrl = domainState.tokenUrl?.value;
    dto.clientIdRef = domainState.clientIdRef?.value;
    dto.clientSecretRef = domainState.clientSecretRef?.value;
    dto.scope = domainState.scope?.value;
    dto.certRef = domainState.certRef?.value;
    dto.keyRef = domainState.keyRef?.value;
    dto.caRef = domainState.caRef?.value;

    return dto;
  }
  /**
   * Convert array of AuthtypeDomainState to array of detail DTOs
   *
   * @param domainStates - Array of rich domain states
   * @returns Array of clean DTOs for detail API response
   */
  static toDetailResponseArray(
    domainStates: AuthtypeDomainState[],
  ): DetailAuthtypeResponse[] {
    return domainStates.map((domainState) =>
      this.toDetailResponse(domainState),
    );
  }
}
