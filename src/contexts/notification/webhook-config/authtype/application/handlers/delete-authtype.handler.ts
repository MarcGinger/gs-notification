// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { CommandHandler, ICommandHandler } from '@nestjs/cqrs';
import { Inject } from '@nestjs/common';
import { Result, DomainError } from 'src/shared/errors';
import { DeleteAuthtypeUseCase } from '../use-cases';
import { DeleteAuthtypeCommand } from '../commands';

@CommandHandler(DeleteAuthtypeCommand)
export class DeleteAuthtypeHandler
  implements ICommandHandler<DeleteAuthtypeCommand>
{
  constructor(
    @Inject(DeleteAuthtypeUseCase)
    private readonly deleteAuthtypeUseCase: DeleteAuthtypeUseCase,
  ) {}

  async execute(
    command: DeleteAuthtypeCommand,
  ): Promise<Result<void, DomainError>> {
    return await this.deleteAuthtypeUseCase.execute({
      user: command.user,
      id: command.id,
      correlationId: command.correlationId,
      authorizationReason: 'CQRS Command Handler',
    });
  }
}
