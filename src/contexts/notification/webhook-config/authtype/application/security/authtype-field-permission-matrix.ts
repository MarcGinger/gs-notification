// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { AuthtypePermission } from '../../domain/permissions/authtype.permissions';

/**
 * Field-level permission matrix for Authtype updates.
 * Maps authtype fields to the required permissions for modification.
 */
export class AuthtypeFieldPermissionMatrix {
  private static readonly fieldPermissions: Record<
    string,
    AuthtypePermission[]
  > = {
    // Basic authtype information fields - require standard update permission
    name: [AuthtypePermission.DOMAIN_AUTHTYPE_UPDATE],
    description: [AuthtypePermission.DOMAIN_AUTHTYPE_UPDATE],
    category: [AuthtypePermission.DOMAIN_AUTHTYPE_UPDATE],

    // Status fields - may require special permissions or admin rights
    active: [
      AuthtypePermission.DOMAIN_AUTHTYPE_UPDATE,
      AuthtypePermission.DOMAIN_AUTHTYPE_ADMIN,
    ],
    status: [
      AuthtypePermission.DOMAIN_AUTHTYPE_UPDATE,
      AuthtypePermission.DOMAIN_AUTHTYPE_ADMIN,
    ],

    // Financial fields - typically require higher permissions
    fees: [AuthtypePermission.DOMAIN_AUTHTYPE_UPDATE],
    rates: [AuthtypePermission.DOMAIN_AUTHTYPE_UPDATE],
    pricing: [AuthtypePermission.DOMAIN_AUTHTYPE_UPDATE],

    // Configuration fields
    channelCodes: [AuthtypePermission.DOMAIN_AUTHTYPE_UPDATE],
    railCodes: [AuthtypePermission.DOMAIN_AUTHTYPE_UPDATE],
    configuration: [AuthtypePermission.DOMAIN_AUTHTYPE_UPDATE],

    // Administrative fields - require admin permissions
    tenant: [AuthtypePermission.DOMAIN_AUTHTYPE_ADMIN],
    auditInfo: [AuthtypePermission.DOMAIN_AUTHTYPE_ADMIN],
    metadata: [AuthtypePermission.DOMAIN_AUTHTYPE_UPDATE],
  };

  /**
   * Get required permissions for a specific field.
   * Returns empty array if field has no special permission requirements.
   */
  static getRequiredPermissions(fieldName: string): AuthtypePermission[] {
    return this.fieldPermissions[fieldName] || [];
  }

  /**
   * Check if a field has specific permission requirements.
   */
  static hasSpecialPermissions(fieldName: string): boolean {
    return fieldName in this.fieldPermissions;
  }

  /**
   * Get all fields that require a specific permission.
   */
  static getFieldsForPermission(permission: AuthtypePermission): string[] {
    return Object.entries(this.fieldPermissions)
      .filter(([, perms]) => perms.includes(permission))
      .map(([field]) => field);
  }

  /**
   * Get all defined field permissions.
   */
  static getAllFieldPermissions(): Record<string, AuthtypePermission[]> {
    return { ...this.fieldPermissions };
  }

  /**
   * Add or update permission requirements for a field.
   */
  static setFieldPermissions(
    fieldName: string,
    permissions: AuthtypePermission[],
  ): void {
    this.fieldPermissions[fieldName] = [...permissions];
  }
}
