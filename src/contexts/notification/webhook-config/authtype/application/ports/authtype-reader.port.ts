// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Result, DomainError } from 'src/shared/errors';
import { RepositoryOptions } from 'src/shared/infrastructure/repositories';
import { ActorContext } from 'src/shared/application/context';
import { Option } from 'src/shared/domain/types';
import { AuthtypeSnapshotProps } from '../../domain/props';
import { AuthtypeId } from '../../domain/value-objects';

/**
 * Token for injecting IAuthtypeReader port implementation
 */
export const AUTHTYPE_READER_TOKEN = 'IAuthtypeReader' as const;

/**
 * Authtype Reader Port - Interface Segregation Principle Implementation
 *
 * Handles basic read operations for Authtype aggregates.
 * This interface focuses solely on simple retrieval operations.
 *
 * ISP Benefits:
 * - Clients only depend on methods they actually use
 * - Easier to mock and test specific read scenarios
 * - Can be implemented independently from write operations
 * - Supports read-only implementations (e.g., reporting services)
 *
 * @domain Notification Context - Authtype Reader Port
 * @layer Application Ports
 * @pattern Interface Segregation Principle
 */
export interface IAuthtypeReader {
  /**
   * Find a Authtype by its unique identifier
   * @param actor - The authenticated user context
   * @param id - The unique identifier of the Authtype
   * @param options - Optional repository options
   * @returns Result containing the Authtype snapshot or null if not found
   */
  findById(
    actor: ActorContext,
    id: AuthtypeId,
    options?: RepositoryOptions,
  ): Promise<Result<Option<AuthtypeSnapshotProps>, DomainError>>;

  /**
   * Check if a authtype exists by ID (for write-path validation)
   * @param actor - The authenticated user context
   * @param id - The unique identifier of the Authtype
   * @param options - Optional repository options
   * @returns Result containing boolean indicating existence
   */
  exists(
    actor: ActorContext,
    id: AuthtypeId,
    options?: RepositoryOptions,
  ): Promise<Result<boolean, DomainError>>;

  /**
   * Get authtype version for optimistic concurrency control
   * @param actor - The authenticated user context
   * @param id - The unique identifier of the Authtype
   * @param options - Optional repository options
   * @returns Result containing version number or null if not found
   */
  getVersion(
    actor: ActorContext,
    id: AuthtypeId,
    options?: RepositoryOptions,
  ): Promise<Result<Option<number>, DomainError>>;

  /**
   * Get minimal authtype data for write-path operations
   * @param actor - The authenticated user context
   * @param id - The unique identifier of the Authtype
   * @param options - Optional repository options
   * @returns Result containing minimal authtype data or null if not found
   */
  getMinimal(
    actor: ActorContext,
    id: AuthtypeId,
    options?: RepositoryOptions,
  ): Promise<Result<Option<{ id: string; version: number }>, DomainError>>;
}
