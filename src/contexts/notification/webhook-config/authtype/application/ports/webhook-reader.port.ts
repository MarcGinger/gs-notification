// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Result, DomainError } from 'src/shared/errors';
import { ActorContext } from 'src/shared/application/context';
import { RepositoryOptions } from 'src/shared/infrastructure/repositories';
import { Option } from 'src/shared/domain/types';
import { WebhookReference } from './references';

export const WEBHOOK_REFERENCE_READER_TOKEN = 'IWebhookReader' as const;

/**
 * Webhook Reader Port (Bounded Context: Notification)
 *
 * Handles webhook reference data lookups for FK validation.
 * Clean ownership of webhook-related operations with swappable implementations.
 */
export interface IWebhookReader {
  /**
   * Find valid Webhook Ids for Webhook configuration
   * @param actor - The authenticated user context
   * @param options - Optional repository options
   * @returns Result containing available Webhook Ids
   */
  findValidWebhookIds(
    actor: ActorContext,
    options?: RepositoryOptions,
  ): Promise<Result<string[], DomainError>>;

  /**
   * Find Webhook configuration by Webhook id
   * @param actor - The authenticated user context
   * @param  - The   to lookup
   * @param options - Optional repository options
   * @returns Result containing Webhook configuration or null if not found
   */
  findWebhookById(
    actor: ActorContext,
    id: string,
    options?: RepositoryOptions,
  ): Promise<Result<Option<WebhookReference>, DomainError>>;
}
