// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  EnumVOInstance,
  createDisplayNameHelper,
  createEnumTypeGuards,
  createEnumVO,
  createEnumVOErrors,
  createStateTransitions,
} from 'src/shared/domain/value-objects';
import { Result, DomainError } from 'src/shared/errors';
import { AuthtypeErrors } from '../errors/authtype.errors';

// ============================================================================
// SINGLE SOURCE OF TRUTH
// ============================================================================

/**
 * AuthtypeType allowed values
 * Canonical definition used across all layers (API, Domain, DB)
 */
export const AuthtypeTypeValues = [
  'none',
  'hmac',
  'basic',
  'apiKey',
  'oauth2ClientCredentials',
  'mtls',
] as const;
export type AuthtypeTypeValue = (typeof AuthtypeTypeValues)[number];

// ============================================================================
// VALUE OBJECT - Domain Layer
// ============================================================================

/**
 * AuthtypeType Value Object
 *
 * Provides validation, business rules, and rich domain behavior
 * for authtype type enumerated values.
 *
 * @example
 * ```typescript
 * const type = AuthtypeType.create('none');
 * if (type.ok) {
 *   console.log(type.value.canTransitionTo('hmac'));
 * }
 * ```
 */
export const AuthtypeType = createEnumVO({
  name: 'AuthtypeType',
  values: AuthtypeTypeValues,
  required: true,
  errors: createEnumVOErrors(AuthtypeErrors.INVALID_TYPE, 'AuthtypeType'),
});

export type AuthtypeType = EnumVOInstance<AuthtypeTypeValue>;

// ============================================================================
// CONVENIENCE FACTORIES
// ============================================================================

export const createAuthtypeType = (value: AuthtypeTypeValue | undefined) =>
  AuthtypeType.create(value) as Result<AuthtypeType, DomainError>;
export const authtypeTypeFrom = (value: unknown) =>
  AuthtypeType.from(value) as Result<AuthtypeType, DomainError>;

// ============================================================================
// BUSINESS LOGIC HELPERS - Domain Behavior
// ============================================================================

/**
 * Valid state transitions for authtype type
 */
const AUTHTYPE_TYPE_TRANSITIONS = {
  none: ['hmac', 'basic', 'apiKey', 'oauth2ClientCredentials', 'mtls'] as const,
  hmac: ['none', 'basic', 'apiKey', 'oauth2ClientCredentials', 'mtls'] as const,
  basic: ['none', 'hmac', 'apiKey', 'oauth2ClientCredentials', 'mtls'] as const,
  apiKey: ['none', 'hmac', 'basic', 'oauth2ClientCredentials', 'mtls'] as const,
  oauth2ClientCredentials: ['none', 'hmac', 'basic', 'apiKey', 'mtls'] as const,
  mtls: ['none', 'hmac', 'basic', 'apiKey', 'oauth2ClientCredentials'] as const,
} as const;

/**
 * Display names for authtype type values
 */
const AUTHTYPE_TYPE_DISPLAY_NAMES = {
  none: 'None',
  hmac: 'Hmac',
  basic: 'Basic',
  apiKey: 'Api Key',
  oauth2ClientCredentials: 'Oauth2client Credentials',
  mtls: 'Mtls',
} as const;

/**
 * State machine helpers for AuthtypeType transitions
 */
const AuthtypeTypeTransitions = createStateTransitions(
  AUTHTYPE_TYPE_TRANSITIONS,
);

/**
 * Type guards for AuthtypeType values
 */
const AuthtypeTypeGuards = createEnumTypeGuards(
  AuthtypeTypeValues,
  'AuthtypeType',
  (value, validValues) => ({
    ...AuthtypeErrors.INVALID_TYPE,
    detail: `AuthtypeType must be one of: ${validValues.join(', ')}, received: ${String(value)}`,
    context: { value, validValues },
  }),
);

/**
 * Display name helpers for AuthtypeType
 */
const AuthtypeTypeDisplay = createDisplayNameHelper(
  AUTHTYPE_TYPE_DISPLAY_NAMES,
);

/**
 * Business logic functions for AuthtypeType
 */
export const AuthtypeTypeLogic = {
  /**
   * Check if transition from one state to another is valid
   */
  canTransition: (from: AuthtypeTypeValue, to: AuthtypeTypeValue): boolean =>
    AuthtypeTypeTransitions.canTransition(from, to),

  /**
   * Get all valid next states from current state
   */
  getValidTransitions: (
    from: AuthtypeTypeValue,
  ): readonly AuthtypeTypeValue[] =>
    AuthtypeTypeTransitions.getValidTransitions(from),

  /**
   * Get human-readable display name
   */
  getDisplayName: (value: AuthtypeTypeValue): string =>
    AuthtypeTypeDisplay.getDisplayName(value),

  /**
   * Get all available values with metadata
   */
  getAllValues(): Array<{
    value: AuthtypeTypeValue;
    displayName: string;
    validTransitions: readonly AuthtypeTypeValue[];
  }> {
    return AuthtypeTypeValues.map((value) => ({
      value,
      displayName: AuthtypeTypeLogic.getDisplayName(value),
      validTransitions: AuthtypeTypeLogic.getValidTransitions(value),
    }));
  },
} as const;

// ============================================================================
// TYPE GUARDS - Runtime Type Checking
// ============================================================================

/**
 * Type guard to check if value is valid AuthtypeType
 */
export const isAuthtypeTypeValue = (
  value: unknown,
): value is AuthtypeTypeValue => AuthtypeTypeGuards.isValid(value);

/**
 * Assert that value is valid AuthtypeType (throws if invalid)
 */
export const assertAuthtypeTypeValue = (
  value: unknown,
): asserts value is AuthtypeTypeValue => AuthtypeTypeGuards.assert(value);
