// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { AuthtypeErrors } from '../errors/authtype.errors';

/**
 * SigningSecretRef Value Object
 * Represents a validated signingSecretRef with business rules
 */
export const AuthtypeSigningSecretRef = createStringVO({
  name: 'SigningSecretRef',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 255,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    AuthtypeErrors.INVALID_SIGNING_SECRET_REF,
    'SigningSecretRef',
  ),
});

/**
 * Public instance type for SigningSecretRef
 */
export type AuthtypeSigningSecretRef = StringVOInstance;

// Convenience creators
export const createAuthtypeSigningSecretRef = (s: string) =>
  AuthtypeSigningSecretRef.create(s);
export const authtypeSigningSecretRefFrom = (v: unknown) =>
  AuthtypeSigningSecretRef.from(v);
