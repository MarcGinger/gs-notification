// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { AuthtypeErrors } from '../errors/authtype.errors';

/**
 * ApiKeyRef Value Object
 * Represents a validated apiKeyRef with business rules
 */
export const AuthtypeApiKeyRef = createStringVO({
  name: 'ApiKeyRef',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 255,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(AuthtypeErrors.INVALID_API_KEY_REF, 'ApiKeyRef'),
});

/**
 * Public instance type for ApiKeyRef
 */
export type AuthtypeApiKeyRef = StringVOInstance;

// Convenience creators
export const createAuthtypeApiKeyRef = (s: string) =>
  AuthtypeApiKeyRef.create(s);
export const authtypeApiKeyRefFrom = (v: unknown) => AuthtypeApiKeyRef.from(v);
