// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { AuthtypeErrors } from '../errors/authtype.errors';

/**
 * CertRef Value Object
 * Represents a validated certRef with business rules
 */
export const AuthtypeCertRef = createStringVO({
  name: 'CertRef',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 255,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(AuthtypeErrors.INVALID_CERT_REF, 'CertRef'),
});

/**
 * Public instance type for CertRef
 */
export type AuthtypeCertRef = StringVOInstance;

// Convenience creators
export const createAuthtypeCertRef = (s: string) => AuthtypeCertRef.create(s);
export const authtypeCertRefFrom = (v: unknown) => AuthtypeCertRef.from(v);
