// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DomainError, Result, ok, err } from 'src/shared/errors';
import {
  createIntegerVO,
  createIntegerVOErrors,
  IntegerVOInstance,
  createDateTimeVO,
  createDateTimeVOErrors,
  DateTimeVOInstance,
} from 'src/shared/domain/value-objects';
import { Clock } from 'src/shared/domain/clock';
import { AuthtypeErrors } from '../errors/authtype.errors';

/**
 * Authtype Instance Value Objects
 *
 * Instance VOs for Authtype aggregate lifecycle management.
 * Includes creation timestamps, update timestamps, and version tracking.
 * Follows the Clock injection pattern for deterministic testing.
 */

// ===========================================
// Authtype Created At Instance VO
// ===========================================

const AuthtypeCreatedAtErrors = createDateTimeVOErrors(
  AuthtypeErrors.CREATED_AT_REQUIRED,
  'Authtype Created At',
);

export const AuthtypeCreatedAt = createDateTimeVO({
  name: 'AuthtypeCreatedAt',
  allowFuture: false, // Cannot create authtype in the future
  allowPast: true, // Can create with past timestamps (data migration)
  customValidation: (date: Date): Result<void, DomainError> => {
    // Optional: Add domain-specific creation time validation
    // For example, cannot be more than 1 year in the past
    const oneYearAgo = new Date();
    oneYearAgo.setFullYear(oneYearAgo.getFullYear() - 1);

    if (date < oneYearAgo) {
      return err({
        ...AuthtypeErrors.CREATED_AT_REQUIRED,
        detail: 'Authtype creation date cannot be more than 1 year in the past',
        context: { value: date, maxPastDate: oneYearAgo },
      } as DomainError);
    }

    return ok(undefined);
  },
  errors: AuthtypeCreatedAtErrors,
});

export type AuthtypeCreatedAt = DateTimeVOInstance;

// ===========================================
// Authtype Updated At Instance VO
// ===========================================

const AuthtypeUpdatedAtErrors = createDateTimeVOErrors(
  AuthtypeErrors.UPDATED_AT_REQUIRED,
  'Authtype Updated At',
);

export const AuthtypeUpdatedAt = createDateTimeVO({
  name: 'AuthtypeUpdatedAt',
  allowFuture: false, // Cannot update authtype in the future
  allowPast: true, // Can update with past timestamps (data correction)
  errors: AuthtypeUpdatedAtErrors,
});

export type AuthtypeUpdatedAt = DateTimeVOInstance;

// ===========================================
// Authtype Version Instance VO
// ===========================================

const AuthtypeVersionErrors = createIntegerVOErrors(
  AuthtypeErrors.INVALID_CODE_DATA, // Reusing existing error until specific version error is added
  'Authtype Version',
);

export const AuthtypeVersion = createIntegerVO({
  name: 'AuthtypeVersion',
  min: 0, // Version starts at 0
  allowNegative: false,
  errors: AuthtypeVersionErrors,
});

export type AuthtypeVersion = IntegerVOInstance;

// ===========================================
// Clock-Based Helper Functions (Explicit Dependencies)
// ===========================================

/**
 * Create AuthtypeCreatedAt from Clock (explicit dependency)
 * Deterministic and testable - pass a fake clock in tests
 */
export const createdAtNow = (
  clock: Clock,
): Result<AuthtypeCreatedAt, DomainError> =>
  AuthtypeCreatedAt.create(clock.now());

/**
 * Create AuthtypeUpdatedAt from Clock (explicit dependency)
 * Deterministic and testable - pass a fake clock in tests
 */
export const updatedAtNow = (
  clock: Clock,
): Result<AuthtypeUpdatedAt, DomainError> =>
  AuthtypeUpdatedAt.create(clock.now());

/**
 * Bump version by 1 (domain business rule)
 */
export const bumpVersion = (
  currentVersion: AuthtypeVersion,
): Result<AuthtypeVersion, DomainError> =>
  AuthtypeVersion.create(currentVersion.value + 1);

/**
 * Create initial version (starts at 1 for new aggregates)
 */
export const initialVersion = (): Result<AuthtypeVersion, DomainError> =>
  AuthtypeVersion.create(1);

// ===========================================
// Direct Creation Functions (Non-Clock Based)
// ===========================================

/**
 * Create AuthtypeCreatedAt from Date
 */
export const createAuthtypeCreatedAt = (
  date: Date,
): Result<AuthtypeCreatedAt, DomainError> => AuthtypeCreatedAt.create(date);

/**
 * Create AuthtypeUpdatedAt from Date
 */
export const createAuthtypeUpdatedAt = (
  date: Date,
): Result<AuthtypeUpdatedAt, DomainError> => AuthtypeUpdatedAt.create(date);

/**
 * Create AuthtypeVersion from number
 */
export const createAuthtypeVersion = (
  version: number,
): Result<AuthtypeVersion, DomainError> => AuthtypeVersion.create(version);

// ===========================================
// Domain Invariants & Validation
// ===========================================

/**
 * Validate that updatedAt is not before createdAt (monotonic time)
 */
export const validateMonotonicTime = (
  createdAt: AuthtypeCreatedAt,
  updatedAt: AuthtypeUpdatedAt,
): Result<void, DomainError> => {
  if (updatedAt.value < createdAt.value) {
    return err({
      ...AuthtypeErrors.UPDATED_AT_REQUIRED,
      detail: 'Updated at cannot be before created at',
      context: {
        createdAt: createdAt.value.toISOString(),
        updatedAt: updatedAt.value.toISOString(),
      },
    } as DomainError);
  }
  return ok(undefined);
};

// ===========================================
// Legacy Compatibility (DEPRECATED)
// ===========================================

/**
 * @deprecated Use createdAtNow(clock) instead for better testability
 * This function hides the Date dependency and makes testing harder
 */
export function createAuthtypeCreatedAtNow(): Result<
  AuthtypeCreatedAt,
  DomainError
> {
  return AuthtypeCreatedAt.create(new Date());
}

/**
 * @deprecated Use updatedAtNow(clock) instead for better testability
 * This function hides the Date dependency and makes testing harder
 */
export function createAuthtypeUpdatedAtNow(): Result<
  AuthtypeUpdatedAt,
  DomainError
> {
  return AuthtypeUpdatedAt.create(new Date());
}
