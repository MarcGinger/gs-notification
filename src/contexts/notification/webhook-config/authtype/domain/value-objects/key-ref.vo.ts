// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { AuthtypeErrors } from '../errors/authtype.errors';

/**
 * KeyRef Value Object
 * Represents a validated keyRef with business rules
 */
export const AuthtypeKeyRef = createStringVO({
  name: 'KeyRef',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 255,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(AuthtypeErrors.INVALID_KEY_REF, 'KeyRef'),
});

/**
 * Public instance type for KeyRef
 */
export type AuthtypeKeyRef = StringVOInstance;

// Convenience creators
export const createAuthtypeKeyRef = (s: string) => AuthtypeKeyRef.create(s);
export const authtypeKeyRefFrom = (v: unknown) => AuthtypeKeyRef.from(v);
