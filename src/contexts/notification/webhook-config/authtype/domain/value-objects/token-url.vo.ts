// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { AuthtypeErrors } from '../errors/authtype.errors';

/**
 * TokenUrl Value Object
 * Represents a validated tokenUrl with business rules
 */
export const AuthtypeTokenUrl = createStringVO({
  name: 'TokenUrl',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 500,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(AuthtypeErrors.INVALID_TOKEN_URL, 'TokenUrl'),
});

/**
 * Public instance type for TokenUrl
 */
export type AuthtypeTokenUrl = StringVOInstance;

// Convenience creators
export const createAuthtypeTokenUrl = (s: string) => AuthtypeTokenUrl.create(s);
export const authtypeTokenUrlFrom = (v: unknown) => AuthtypeTokenUrl.from(v);
