// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { AuthtypeErrors } from '../errors/authtype.errors';

/**
 * ApiKeyHeader Value Object
 * Represents a validated apiKeyHeader with business rules
 */
export const AuthtypeApiKeyHeader = createStringVO({
  name: 'ApiKeyHeader',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 100,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    AuthtypeErrors.INVALID_API_KEY_HEADER,
    'ApiKeyHeader',
  ),
});

/**
 * Public instance type for ApiKeyHeader
 */
export type AuthtypeApiKeyHeader = StringVOInstance;

// Convenience creators
export const createAuthtypeApiKeyHeader = (s: string) =>
  AuthtypeApiKeyHeader.create(s);
export const authtypeApiKeyHeaderFrom = (v: unknown) =>
  AuthtypeApiKeyHeader.from(v);
