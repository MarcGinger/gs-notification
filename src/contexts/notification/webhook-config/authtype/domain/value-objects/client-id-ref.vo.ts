// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { AuthtypeErrors } from '../errors/authtype.errors';

/**
 * ClientIdRef Value Object
 * Represents a validated clientIdRef with business rules
 */
export const AuthtypeClientIdRef = createStringVO({
  name: 'ClientIdRef',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 255,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    AuthtypeErrors.INVALID_CLIENT_ID_REF,
    'ClientIdRef',
  ),
});

/**
 * Public instance type for ClientIdRef
 */
export type AuthtypeClientIdRef = StringVOInstance;

// Convenience creators
export const createAuthtypeClientIdRef = (s: string) =>
  AuthtypeClientIdRef.create(s);
export const authtypeClientIdRefFrom = (v: unknown) =>
  AuthtypeClientIdRef.from(v);
