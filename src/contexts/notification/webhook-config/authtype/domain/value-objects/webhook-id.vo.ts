// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { AuthtypeErrors } from '../errors/authtype.errors';

/**
 * WebhookId Value Object
 * Represents a validated webhookId with business rules
 */
export const AuthtypeWebhookId = createStringVO({
  name: 'WebhookId',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,
  minLength: 10,
  maxLength: 32,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(AuthtypeErrors.INVALID_WEBHOOK_ID, 'WebhookId'),
});

/**
 * Public instance type for WebhookId
 */
export type AuthtypeWebhookId = StringVOInstance;

// Convenience creators
export const createAuthtypeWebhookId = (s: string) =>
  AuthtypeWebhookId.create(s);
export const authtypeWebhookIdFrom = (v: unknown) => AuthtypeWebhookId.from(v);
