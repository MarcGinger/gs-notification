// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { AuthtypeErrors } from '../errors/authtype.errors';

/**
 * PasswordRef Value Object
 * Represents a validated passwordRef with business rules
 */
export const AuthtypePasswordRef = createStringVO({
  name: 'PasswordRef',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 255,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    AuthtypeErrors.INVALID_PASSWORD_REF,
    'PasswordRef',
  ),
});

/**
 * Public instance type for PasswordRef
 */
export type AuthtypePasswordRef = StringVOInstance;

// Convenience creators
export const createAuthtypePasswordRef = (s: string) =>
  AuthtypePasswordRef.create(s);
export const authtypePasswordRefFrom = (v: unknown) =>
  AuthtypePasswordRef.from(v);
