// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { AuthtypeErrors } from '../errors/authtype.errors';

/**
 * UsernameRef Value Object
 * Represents a validated usernameRef with business rules
 */
export const AuthtypeUsernameRef = createStringVO({
  name: 'UsernameRef',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 255,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    AuthtypeErrors.INVALID_USERNAME_REF,
    'UsernameRef',
  ),
});

/**
 * Public instance type for UsernameRef
 */
export type AuthtypeUsernameRef = StringVOInstance;

// Convenience creators
export const createAuthtypeUsernameRef = (s: string) =>
  AuthtypeUsernameRef.create(s);
export const authtypeUsernameRefFrom = (v: unknown) =>
  AuthtypeUsernameRef.from(v);
