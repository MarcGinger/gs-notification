// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { AuthtypeErrors } from '../errors/authtype.errors';

/**
 * Scope Value Object
 * Represents a validated scope with business rules
 */
export const AuthtypeScope = createStringVO({
  name: 'Scope',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 500,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(AuthtypeErrors.INVALID_SCOPE, 'Scope'),
});

/**
 * Public instance type for Scope
 */
export type AuthtypeScope = StringVOInstance;

// Convenience creators
export const createAuthtypeScope = (s: string) => AuthtypeScope.create(s);
export const authtypeScopeFrom = (v: unknown) => AuthtypeScope.from(v);
