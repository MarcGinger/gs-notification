// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { EventMetadata } from 'src/shared/domain/events';

/**
 * Config Deleted Event Payload
 * Contains identifying information about the deleted config
 */
export interface ConfigDeletedEventPayload {
  webhookId: string;
  deletedAt: Date;
  version: number;
  metadata: EventMetadata;
}

/**
 * Config Deleted Domain Event
 *
 * Emitted when a config is successfully deleted.
 * Contains identifying information and deletion metadata.
 */
export class ConfigDeletedEvent {
  public readonly eventType = 'NotificationSlackConfigConfigDeleted.v1';
  public readonly eventVersion = 'v1';

  constructor(public readonly payload: ConfigDeletedEventPayload) {}

  // Factory method with security context
  static create(
    data: Omit<ConfigDeletedEventPayload, 'metadata'>,
    baseMetadata: EventMetadata,
  ): ConfigDeletedEvent {
    const metadata: EventMetadata = {
      ...baseMetadata,
      eventVersion: '1.0',
      source: 'notification.domain.config',
    };

    return new ConfigDeletedEvent({
      ...data,
      metadata,
    });
  }

  get webhookId(): string {
    return this.payload.webhookId;
  }

  get deletedAt(): Date {
    return this.payload.deletedAt;
  }

  get version(): number {
    return this.payload.version;
  }

  get metadata(): EventMetadata {
    return this.payload.metadata;
  }
}
