// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { ConfigErrors } from '../errors/config.errors';

/**
 * WebhookId Value Object
 * Represents a validated webhookId with business rules
 */
export const ConfigWebhookId = createStringVO({
  name: 'WebhookId',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 32,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(ConfigErrors.INVALID_WEBHOOK_ID, 'WebhookId'),
});

/**
 * Public instance type for WebhookId
 */
export type ConfigWebhookId = StringVOInstance;

// Convenience creators
export const createConfigWebhookId = (s: string) => ConfigWebhookId.create(s);
export const configWebhookIdFrom = (v: unknown) => ConfigWebhookId.from(v);
