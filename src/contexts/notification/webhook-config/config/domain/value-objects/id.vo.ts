// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { ConfigErrors } from '../errors/config.errors';

/**
 * Id Value Object
 * Represents a validated id with business rules
 */
export const ConfigId = createStringVO({
  name: 'Id',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  maxLength: 32,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(ConfigErrors.INVALID_ID, 'Id'),
});

/**
 * Public instance type for Id
 */
export type ConfigId = StringVOInstance;

// Convenience creators
export const createConfigId = (s: string) => ConfigId.create(s);
export const configIdFrom = (v: unknown) => ConfigId.from(v);
