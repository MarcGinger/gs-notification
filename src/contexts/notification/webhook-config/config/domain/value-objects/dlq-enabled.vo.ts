// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  BooleanVOInstance,
  createBooleanVO,
  createBooleanVOErrors,
} from 'src/shared/domain/value-objects';
import { ConfigErrors } from '..//errors/config.errors';

/**
 * DlqEnabled Boolean Value Object
 * Represents a validated dlqEnabled with business rules
 */
export const ConfigDlqEnabled = createBooleanVO({
  name: 'DlqEnabled',
  treatNullAsFalse: true,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createBooleanVOErrors(ConfigErrors.INVALID_DLQ_ENABLED, 'DlqEnabled'),
});

/** Public instance type for DlqEnabled */
export type ConfigDlqEnabled = BooleanVOInstance;

// Convenience creators
export const createConfigDlqEnabled = (v: boolean) =>
  ConfigDlqEnabled.create(v);
export const configDlqEnabledFrom = (v: unknown) => ConfigDlqEnabled.from(v);
