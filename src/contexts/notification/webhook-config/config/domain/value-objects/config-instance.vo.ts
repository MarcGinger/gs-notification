// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DomainError, Result, ok, err } from 'src/shared/errors';
import {
  createIntegerVO,
  createIntegerVOErrors,
  IntegerVOInstance,
  createDateTimeVO,
  createDateTimeVOErrors,
  DateTimeVOInstance,
} from 'src/shared/domain/value-objects';
import { Clock } from 'src/shared/domain/clock';
import { ConfigErrors } from '../errors/config.errors';

/**
 * Config Instance Value Objects
 *
 * Instance VOs for Config aggregate lifecycle management.
 * Includes creation timestamps, update timestamps, and version tracking.
 * Follows the Clock injection pattern for deterministic testing.
 */

// ===========================================
// Config Created At Instance VO
// ===========================================

const ConfigCreatedAtErrors = createDateTimeVOErrors(
  ConfigErrors.CREATED_AT_REQUIRED,
  'Config Created At',
);

export const ConfigCreatedAt = createDateTimeVO({
  name: 'ConfigCreatedAt',
  allowFuture: false, // Cannot create config in the future
  allowPast: true, // Can create with past timestamps (data migration)
  customValidation: (date: Date): Result<void, DomainError> => {
    // Optional: Add domain-specific creation time validation
    // For example, cannot be more than 1 year in the past
    const oneYearAgo = new Date();
    oneYearAgo.setFullYear(oneYearAgo.getFullYear() - 1);

    if (date < oneYearAgo) {
      return err({
        ...ConfigErrors.CREATED_AT_REQUIRED,
        detail: 'Config creation date cannot be more than 1 year in the past',
        context: { value: date, maxPastDate: oneYearAgo },
      } as DomainError);
    }

    return ok(undefined);
  },
  errors: ConfigCreatedAtErrors,
});

export type ConfigCreatedAt = DateTimeVOInstance;

// ===========================================
// Config Updated At Instance VO
// ===========================================

const ConfigUpdatedAtErrors = createDateTimeVOErrors(
  ConfigErrors.UPDATED_AT_REQUIRED,
  'Config Updated At',
);

export const ConfigUpdatedAt = createDateTimeVO({
  name: 'ConfigUpdatedAt',
  allowFuture: false, // Cannot update config in the future
  allowPast: true, // Can update with past timestamps (data correction)
  errors: ConfigUpdatedAtErrors,
});

export type ConfigUpdatedAt = DateTimeVOInstance;

// ===========================================
// Config Version Instance VO
// ===========================================

const ConfigVersionErrors = createIntegerVOErrors(
  ConfigErrors.INVALID_CODE_DATA, // Reusing existing error until specific version error is added
  'Config Version',
);

export const ConfigVersion = createIntegerVO({
  name: 'ConfigVersion',
  min: 0, // Version starts at 0
  allowNegative: false,
  errors: ConfigVersionErrors,
});

export type ConfigVersion = IntegerVOInstance;

// ===========================================
// Clock-Based Helper Functions (Explicit Dependencies)
// ===========================================

/**
 * Create ConfigCreatedAt from Clock (explicit dependency)
 * Deterministic and testable - pass a fake clock in tests
 */
export const createdAtNow = (
  clock: Clock,
): Result<ConfigCreatedAt, DomainError> => ConfigCreatedAt.create(clock.now());

/**
 * Create ConfigUpdatedAt from Clock (explicit dependency)
 * Deterministic and testable - pass a fake clock in tests
 */
export const updatedAtNow = (
  clock: Clock,
): Result<ConfigUpdatedAt, DomainError> => ConfigUpdatedAt.create(clock.now());

/**
 * Bump version by 1 (domain business rule)
 */
export const bumpVersion = (
  currentVersion: ConfigVersion,
): Result<ConfigVersion, DomainError> =>
  ConfigVersion.create(currentVersion.value + 1);

/**
 * Create initial version (starts at 1 for new aggregates)
 */
export const initialVersion = (): Result<ConfigVersion, DomainError> =>
  ConfigVersion.create(1);

// ===========================================
// Direct Creation Functions (Non-Clock Based)
// ===========================================

/**
 * Create ConfigCreatedAt from Date
 */
export const createConfigCreatedAt = (
  date: Date,
): Result<ConfigCreatedAt, DomainError> => ConfigCreatedAt.create(date);

/**
 * Create ConfigUpdatedAt from Date
 */
export const createConfigUpdatedAt = (
  date: Date,
): Result<ConfigUpdatedAt, DomainError> => ConfigUpdatedAt.create(date);

/**
 * Create ConfigVersion from number
 */
export const createConfigVersion = (
  version: number,
): Result<ConfigVersion, DomainError> => ConfigVersion.create(version);

// ===========================================
// Domain Invariants & Validation
// ===========================================

/**
 * Validate that updatedAt is not before createdAt (monotonic time)
 */
export const validateMonotonicTime = (
  createdAt: ConfigCreatedAt,
  updatedAt: ConfigUpdatedAt,
): Result<void, DomainError> => {
  if (updatedAt.value < createdAt.value) {
    return err({
      ...ConfigErrors.UPDATED_AT_REQUIRED,
      detail: 'Updated at cannot be before created at',
      context: {
        createdAt: createdAt.value.toISOString(),
        updatedAt: updatedAt.value.toISOString(),
      },
    } as DomainError);
  }
  return ok(undefined);
};

// ===========================================
// Legacy Compatibility (DEPRECATED)
// ===========================================

/**
 * @deprecated Use createdAtNow(clock) instead for better testability
 * This function hides the Date dependency and makes testing harder
 */
export function createConfigCreatedAtNow(): Result<
  ConfigCreatedAt,
  DomainError
> {
  return ConfigCreatedAt.create(new Date());
}

/**
 * @deprecated Use updatedAtNow(clock) instead for better testability
 * This function hides the Date dependency and makes testing harder
 */
export function createConfigUpdatedAtNow(): Result<
  ConfigUpdatedAt,
  DomainError
> {
  return ConfigUpdatedAt.create(new Date());
}
