// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { ConfigErrors } from '../errors/config.errors';

/**
 * TenantId Value Object
 * Represents a validated tenantId with business rules
 */
export const ConfigTenantId = createStringVO({
  name: 'TenantId',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  maxLength: 32,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(ConfigErrors.INVALID_TENANT_ID, 'TenantId'),
});

/**
 * Public instance type for TenantId
 */
export type ConfigTenantId = StringVOInstance;

// Convenience creators
export const createConfigTenantId = (s: string) => ConfigTenantId.create(s);
export const configTenantIdFrom = (v: unknown) => ConfigTenantId.from(v);
