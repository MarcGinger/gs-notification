// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  RecordVOInstance,
  createRecordVO,
  createRecordVOErrors,
} from 'src/shared/domain/value-objects';
import { ConfigErrors } from '../errors/config.errors';

/**
 * Metadata Record Value Object
 * Validates a plain object (Record<string, unknown>); deepFreeze.
 */
export const ConfigMetadata = createRecordVO({
  name: 'Metadata',
  // JSON key validation - must start with letter/underscore, contain only alphanumeric, underscore
  keyPattern: /^[a-zA-Z_][a-zA-Z0-9_]*$/,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createRecordVOErrors(ConfigErrors.INVALID_METADATA, 'Metadata'),
});

/** Public instance type for Metadata */
export type ConfigMetadata = RecordVOInstance;

// Convenience creators
export const createConfigMetadata = (v: Record<string, unknown>) =>
  ConfigMetadata.create(v);

export const configMetadataFrom = (v: unknown) => ConfigMetadata.from(v);
