// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  EnumVOInstance,
  createDisplayNameHelper,
  createEnumTypeGuards,
  createEnumVO,
  createEnumVOErrors,
  createStateTransitions,
} from 'src/shared/domain/value-objects';
import { Result, DomainError } from 'src/shared/errors';
import { ConfigErrors } from '../errors/config.errors';

// ============================================================================
// SINGLE SOURCE OF TRUTH
// ============================================================================

/**
 * ConfigSignatureAlgorithm allowed values
 * Canonical definition used across all layers (API, Domain, DB)
 */
export const ConfigSignatureAlgorithmValues = ['sha256', 'sha1'] as const;
export type ConfigSignatureAlgorithmValue =
  (typeof ConfigSignatureAlgorithmValues)[number];

// ============================================================================
// VALUE OBJECT - Domain Layer
// ============================================================================

/**
 * ConfigSignatureAlgorithm Value Object
 *
 * Provides validation, business rules, and rich domain behavior
 * for config signatureAlgorithm enumerated values.
 *
 * @example
 * ```typescript
 * const signatureAlgorithm = ConfigSignatureAlgorithm.create('sha256');
 * if (signatureAlgorithm.ok) {
 *   console.log(signatureAlgorithm.value.canTransitionTo('sha1'));
 * }
 * ```
 */
export const ConfigSignatureAlgorithm = createEnumVO({
  name: 'ConfigSignatureAlgorithm',
  values: ConfigSignatureAlgorithmValues,

  errors: createEnumVOErrors(
    ConfigErrors.INVALID_SIGNATURE_ALGORITHM,
    'ConfigSignatureAlgorithm',
  ),
});

export type ConfigSignatureAlgorithm =
  EnumVOInstance<ConfigSignatureAlgorithmValue>;

// ============================================================================
// CONVENIENCE FACTORIES
// ============================================================================

export const createConfigSignatureAlgorithm = (
  value: ConfigSignatureAlgorithmValue | undefined,
) =>
  ConfigSignatureAlgorithm.create(value) as Result<
    ConfigSignatureAlgorithm,
    DomainError
  >;
export const configSignatureAlgorithmFrom = (value: unknown) =>
  ConfigSignatureAlgorithm.from(value) as Result<
    ConfigSignatureAlgorithm,
    DomainError
  >;

// ============================================================================
// BUSINESS LOGIC HELPERS - Domain Behavior
// ============================================================================

/**
 * Valid state transitions for config signatureAlgorithm
 */
const CONFIG_SIGNATURE_ALGORITHM_TRANSITIONS = {
  sha256: ['sha1'] as const,
  sha1: ['sha256'] as const,
} as const;

/**
 * Display names for config signatureAlgorithm values
 */
const CONFIG_SIGNATURE_ALGORITHM_DISPLAY_NAMES = {
  sha256: 'Sha256',
  sha1: 'Sha1',
} as const;

/**
 * State machine helpers for ConfigSignatureAlgorithm transitions
 */
const ConfigSignatureAlgorithmTransitions = createStateTransitions(
  CONFIG_SIGNATURE_ALGORITHM_TRANSITIONS,
);

/**
 * Type guards for ConfigSignatureAlgorithm values
 */
const ConfigSignatureAlgorithmGuards = createEnumTypeGuards(
  ConfigSignatureAlgorithmValues,
  'ConfigSignatureAlgorithm',
  (value, validValues) => ({
    ...ConfigErrors.INVALID_SIGNATURE_ALGORITHM,
    detail: `ConfigSignatureAlgorithm must be one of: ${validValues.join(', ')}, received: ${String(value)}`,
    context: { value, validValues },
  }),
);

/**
 * Display name helpers for ConfigSignatureAlgorithm
 */
const ConfigSignatureAlgorithmDisplay = createDisplayNameHelper(
  CONFIG_SIGNATURE_ALGORITHM_DISPLAY_NAMES,
);

/**
 * Business logic functions for ConfigSignatureAlgorithm
 */
export const ConfigSignatureAlgorithmLogic = {
  /**
   * Check if transition from one state to another is valid
   */
  canTransition: (
    from: ConfigSignatureAlgorithmValue,
    to: ConfigSignatureAlgorithmValue,
  ): boolean => ConfigSignatureAlgorithmTransitions.canTransition(from, to),

  /**
   * Get all valid next states from current state
   */
  getValidTransitions: (
    from: ConfigSignatureAlgorithmValue,
  ): readonly ConfigSignatureAlgorithmValue[] =>
    ConfigSignatureAlgorithmTransitions.getValidTransitions(from),

  /**
   * Get human-readable display name
   */
  getDisplayName: (value: ConfigSignatureAlgorithmValue): string =>
    ConfigSignatureAlgorithmDisplay.getDisplayName(value),

  /**
   * Get all available values with metadata
   */
  getAllValues(): Array<{
    value: ConfigSignatureAlgorithmValue;
    displayName: string;
    validTransitions: readonly ConfigSignatureAlgorithmValue[];
  }> {
    return ConfigSignatureAlgorithmValues.map((value) => ({
      value,
      displayName: ConfigSignatureAlgorithmLogic.getDisplayName(value),
      validTransitions:
        ConfigSignatureAlgorithmLogic.getValidTransitions(value),
    }));
  },
} as const;

// ============================================================================
// TYPE GUARDS - Runtime Type Checking
// ============================================================================

/**
 * Type guard to check if value is valid ConfigSignatureAlgorithm
 */
export const isConfigSignatureAlgorithmValue = (
  value: unknown,
): value is ConfigSignatureAlgorithmValue =>
  ConfigSignatureAlgorithmGuards.isValid(value);

/**
 * Assert that value is valid ConfigSignatureAlgorithm (throws if invalid)
 */
export const assertConfigSignatureAlgorithmValue = (
  value: unknown,
): asserts value is ConfigSignatureAlgorithmValue =>
  ConfigSignatureAlgorithmGuards.assert(value);
