// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Injectable, Inject } from '@nestjs/common';
import { Result, ok, err, DomainError, withContext } from 'src/shared/errors';
import { Log, Logger } from 'src/shared/logging';
import { ActorContext } from 'src/shared/application/context';
import { Option } from 'src/shared/domain/types';
import { ConfigErrors } from '../../domain/errors';
import { WebhookConfigServiceConstants } from '../../../service-constants';
import {
  WEBHOOK_REFERENCE_READER_TOKEN,
  WebhookReference,
  IWebhookReader,
} from '../ports';

export interface ForeignKeyValidationContext {
  correlationId?: string;
  userId?: string;
  operation: string;
  component: string;
}

/**
 * Config Foreign Key Validation Service
 *
 * Provides centralized validation for foreign key references used in Config domain.
 * This service validates that referenced entities exist before Config creation or updates.
 *
 * Benefits:
 * - Single responsibility for foreign key validation
 * - Reusable across create and update use cases
 * - Consistent error handling and logging
 * - Fail-fast validation to avoid expensive operations
 */
@Injectable()
export class ConfigForeignKeyValidatorService {
  constructor(
    @Inject(WEBHOOK_REFERENCE_READER_TOKEN)
    private readonly webhookReader: IWebhookReader,
  ) {}
  /**
   * Validates webhookId existence
   * @param actor - User context for authorization
   * @param webhookId - Webhook id to validate
   * @param context - Validation context for logging and error handling
   * @param logger - Logger instance for structured logging
   * @returns Result indicating validation success or failure
   */
  async validateWebhookId(
    actor: ActorContext,
    webhookId: string | undefined,
    context: ForeignKeyValidationContext,
    logger: Logger,
  ): Promise<Result<WebhookReference | undefined, DomainError>> {
    if (!webhookId) {
      return ok(undefined);
    }
    Log.debug(logger, 'Validating webhookId foreign key', {
      application: WebhookConfigServiceConstants.SERVICE_NAME,
      component: context.component,
      method: 'validateWebhookId',
      correlationId: context.correlationId,
      userId: context.userId,
      webhookId,
      operation: context.operation,
    });

    const webhookIdResult = await this.webhookReader.findWebhookById(
      actor,
      webhookId,
    );

    if (!webhookIdResult.ok) {
      Log.error(logger, 'Webhook lookup failed during validation', {
        application: WebhookConfigServiceConstants.SERVICE_NAME,
        component: context.component,
        method: 'validateWebhookId',
        correlationId: context.correlationId,
        userId: context.userId,
        webhookId,
        operation: context.operation,
        error: webhookIdResult.error,
      });
      return err(webhookIdResult.error);
    }

    const webhook = webhookIdResult.value;

    if (Option.isNone(webhook)) {
      Log.warn(logger, 'Webhook not found during validation', {
        application: WebhookConfigServiceConstants.SERVICE_NAME,
        component: context.component,
        method: 'validateWebhookId',
        correlationId: context.correlationId,
        userId: context.userId,
        webhookId,
        operation: context.operation,
      });
      return err(
        withContext(ConfigErrors.INVALID_WEBHOOK_ID, {
          correlationId: context.correlationId,
          userId: context.userId,
          operation: context.operation,
          webhookId,
          reason: 'Webhook id does not exist in the system',
        }),
      );
    }

    Log.debug(logger, 'Webhook existence validation passed', {
      application: WebhookConfigServiceConstants.SERVICE_NAME,
      component: context.component,
      method: 'validateWebhookId',
      correlationId: context.correlationId,
      userId: context.userId,
      webhookId,
      operation: context.operation,
    });

    return ok(webhook.value);
  }

  /**
   * Validates all foreign keys for config props
   * @param actor - User context for authorization
   * @param props - Config properties containing foreign key fields
   * @param context - Validation context for logging and error handling
   * @param logger - Logger instance for structured logging
   * @returns Result indicating validation success or failure
   */
  async validateConfigForeignKeys(
    actor: ActorContext,
    props: {
      webhookId?: string;
    },
    context: ForeignKeyValidationContext,
    logger: Logger,
  ): Promise<
    Result<
      {
        webhookId?: WebhookReference;
      },
      DomainError
    >
  > {
    // Validate webhookId if provided
    const webhookIdValidation = await this.validateWebhookId(
      actor,
      props.webhookId,
      context,
      logger,
    );
    if (!webhookIdValidation.ok) {
      return webhookIdValidation;
    }

    return ok({
      webhookId: webhookIdValidation.value,
    });
  }

  /**
   * Generic alias for validateConfigForeignKeys to match shared runner interface
   * @param actor - Actor context for permissions and auditing
   * @param props - Props containing foreign key fields to validate
   * @param context - Validation context for logging and error handling
   * @param logger - Logger instance for structured logging
   * @returns Result indicating validation success or failure
   */
  async validateForeignKeys(
    actor: ActorContext,
    props: {
      webhookId?: string;
    },
    context: ForeignKeyValidationContext,
    logger: Logger,
  ): Promise<
    Result<
      {
        webhookId?: WebhookReference;
      },
      DomainError
    >
  > {
    return this.validateConfigForeignKeys(actor, props, context, logger);
  }
}
