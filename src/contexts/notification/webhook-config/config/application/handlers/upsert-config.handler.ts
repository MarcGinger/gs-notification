// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { CommandHandler, ICommandHandler } from '@nestjs/cqrs';
import { Inject } from '@nestjs/common';
import { Result, DomainError } from 'src/shared/errors';
import { APP_LOGGER, componentLogger, Logger } from 'src/shared/logging';
import { CommandHandlerUtil } from 'src/shared/application';
import { DetailConfigResponse } from '../dtos';
import { UpsertConfigUseCase } from '../use-cases';
import { UpsertConfigCommand } from '../commands';

/**
 * Enhanced Upsert Config Handler with security context support
 */
@CommandHandler(UpsertConfigCommand)
export class UpsertConfigHandler
  implements ICommandHandler<UpsertConfigCommand>
{
  private readonly logger: Logger;

  constructor(
    @Inject(UpsertConfigUseCase)
    private readonly upsertConfigUseCase: UpsertConfigUseCase,
    @Inject(APP_LOGGER) moduleLogger: Logger,
  ) {
    this.logger = componentLogger(moduleLogger, 'UpsertConfigHandler');
  }

  /**
   * Executes the upsert config command with enhanced security and validation
   */
  async execute(
    command: UpsertConfigCommand,
  ): Promise<Result<DetailConfigResponse, DomainError>> {
    const commandName = 'UpsertConfigCommand';

    // Log command execution start
    CommandHandlerUtil.logCommandStart(this.logger, commandName, command, {
      application: 'webhook-config',
      component: 'UpsertConfigHandler',
      extractCommandData: (cmd: UpsertConfigCommand) => ({
        propsKeys: Object.keys(cmd.props || {}),
      }),
    });

    // Transform command to use case parameters
    const result = await this.upsertConfigUseCase.execute({
      user: command.user,
      webhookId: command.webhookId,
      props: command.props,
      correlationId: command.correlationId,
      authorizationReason: 'CQRS Command Handler',
    });

    // Log result with enhanced context
    if (result.ok) {
      CommandHandlerUtil.logCommandSuccess(
        this.logger,
        commandName,
        command,
        result.value,
        {
          application: 'webhook-config',
          component: 'UpsertConfigHandler',
        },
      );
    } else {
      // Use shared error handling with custom context
      CommandHandlerUtil.logValidationError(
        this.logger,
        commandName,
        result.error,
        command,
        {
          application: 'webhook-config',
          component: 'UpsertConfigHandler',
          extractErrorContext: (cmd: UpsertConfigCommand) => ({
            inputStructure: {
              hasProps: !!cmd.props,
              propsKeys: cmd.props ? Object.keys(cmd.props) : [],
            },
          }),
        },
      );
    }

    return result;
  }
}
