// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { applyDecorators } from '@nestjs/common';
import { ApiProperty } from '@nestjs/swagger';
import { IsEnum, IsOptional, IsNotEmpty } from 'class-validator';
import {
  ConfigSignatureAlgorithmValues,
  ConfigSignatureAlgorithmValue,
} from '../../../domain/value-objects';

export { ConfigSignatureAlgorithmValues, ConfigSignatureAlgorithmValue };
/**
 * Options for property decorators (enum)
 */
interface PropOptions {
  required?: boolean;
}

/**
 * Property decorator for Config SignatureAlgorithm with required option
 * @param {Object} options - Options for the decorator
 * @returns {PropertyDecorator}
 */
export function ApiConfigSignatureAlgorithm(options: PropOptions = {}) {
  const { required = false } = options;
  return applyDecorators(
    ApiProperty({
      description: `Cryptographic algorithm for signing webhook payloads - sha256 (recommended for security) or sha1 (legacy). Used with signingSecretRef to verify request authenticity. Values include sha256, sha1.`,
      type: String,
      enum: ConfigSignatureAlgorithmValues,
      required,
    }),
    IsEnum(ConfigSignatureAlgorithmValues),
    required ? IsNotEmpty() : IsOptional(),
  );
}
