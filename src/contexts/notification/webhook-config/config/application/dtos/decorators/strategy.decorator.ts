// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { applyDecorators } from '@nestjs/common';
import { ApiProperty } from '@nestjs/swagger';
import { IsEnum, IsOptional, IsNotEmpty } from 'class-validator';
import {
  ConfigStrategyValues,
  ConfigStrategyValue,
} from '../../../domain/value-objects';

export { ConfigStrategyValues, ConfigStrategyValue };
/**
 * Options for property decorators (enum)
 */
interface PropOptions {
  required?: boolean;
}

/**
 * Property decorator for Config Strategy with required option
 * @param {Object} options - Options for the decorator
 * @returns {PropertyDecorator}
 */
export function ApiConfigStrategy(options: PropOptions = {}) {
  const { required = true } = options;
  return applyDecorators(
    ApiProperty({
      description: `Configuration strategy scope Values include per-webhook, per-tenant, global.`,
      type: String,
      enum: ConfigStrategyValues,
      required,
    }),
    IsEnum(ConfigStrategyValues),
    required ? IsNotEmpty() : IsOptional(),
  );
}
