// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { applyDecorators } from '@nestjs/common';
import { ApiProperty } from '@nestjs/swagger';
import { IsEnum, IsOptional, IsNotEmpty } from 'class-validator';
import {
  ConfigRetryStrategyValues,
  ConfigRetryStrategyValue,
} from '../../../domain/value-objects';

export { ConfigRetryStrategyValues, ConfigRetryStrategyValue };
/**
 * Options for property decorators (enum)
 */
interface PropOptions {
  required?: boolean;
}

/**
 * Property decorator for Config RetryStrategy with required option
 * @param {Object} options - Options for the decorator
 * @returns {PropertyDecorator}
 */
export function ApiConfigRetryStrategy(options: PropOptions = {}) {
  const { required = false } = options;
  return applyDecorators(
    ApiProperty({
      description: `Strategy for calculating retry delays - exponential (doubles delay), linear (constant increase), or fixed (same delay). Controls how aggressively to back off on failures. Values include exponential, linear, fixed.`,
      type: String,
      enum: ConfigRetryStrategyValues,
      required,
    }),
    IsEnum(ConfigRetryStrategyValues),
    required ? IsNotEmpty() : IsOptional(),
  );
}
