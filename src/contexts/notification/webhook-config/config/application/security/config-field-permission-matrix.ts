// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { ConfigPermission } from '../../domain/permissions/config.permissions';

/**
 * Field-level permission matrix for Config updates.
 * Maps config fields to the required permissions for modification.
 */
export class ConfigFieldPermissionMatrix {
  private static readonly fieldPermissions: Record<string, ConfigPermission[]> =
    {
      // Basic config information fields - require standard update permission
      name: [ConfigPermission.DOMAIN_CONFIG_UPDATE],
      description: [ConfigPermission.DOMAIN_CONFIG_UPDATE],
      category: [ConfigPermission.DOMAIN_CONFIG_UPDATE],

      // Status fields - may require special permissions or admin rights
      active: [
        ConfigPermission.DOMAIN_CONFIG_UPDATE,
        ConfigPermission.DOMAIN_CONFIG_ADMIN,
      ],
      status: [
        ConfigPermission.DOMAIN_CONFIG_UPDATE,
        ConfigPermission.DOMAIN_CONFIG_ADMIN,
      ],

      // Financial fields - typically require higher permissions
      fees: [ConfigPermission.DOMAIN_CONFIG_UPDATE],
      rates: [ConfigPermission.DOMAIN_CONFIG_UPDATE],
      pricing: [ConfigPermission.DOMAIN_CONFIG_UPDATE],

      // Configuration fields
      channelCodes: [ConfigPermission.DOMAIN_CONFIG_UPDATE],
      railCodes: [ConfigPermission.DOMAIN_CONFIG_UPDATE],
      configuration: [ConfigPermission.DOMAIN_CONFIG_UPDATE],

      // Administrative fields - require admin permissions
      tenant: [ConfigPermission.DOMAIN_CONFIG_ADMIN],
      auditInfo: [ConfigPermission.DOMAIN_CONFIG_ADMIN],
      metadata: [ConfigPermission.DOMAIN_CONFIG_UPDATE],
    };

  /**
   * Get required permissions for a specific field.
   * Returns empty array if field has no special permission requirements.
   */
  static getRequiredPermissions(fieldName: string): ConfigPermission[] {
    return this.fieldPermissions[fieldName] || [];
  }

  /**
   * Check if a field has specific permission requirements.
   */
  static hasSpecialPermissions(fieldName: string): boolean {
    return fieldName in this.fieldPermissions;
  }

  /**
   * Get all fields that require a specific permission.
   */
  static getFieldsForPermission(permission: ConfigPermission): string[] {
    return Object.entries(this.fieldPermissions)
      .filter(([, perms]) => perms.includes(permission))
      .map(([field]) => field);
  }

  /**
   * Get all defined field permissions.
   */
  static getAllFieldPermissions(): Record<string, ConfigPermission[]> {
    return { ...this.fieldPermissions };
  }

  /**
   * Add or update permission requirements for a field.
   */
  static setFieldPermissions(
    fieldName: string,
    permissions: ConfigPermission[],
  ): void {
    this.fieldPermissions[fieldName] = [...permissions];
  }
}
