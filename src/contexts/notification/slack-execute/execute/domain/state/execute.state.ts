// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  ExecuteCreatedAt,
  ExecuteUpdatedAt,
  ExecuteVersion,
  ExecuteChannelId,
  ExecuteData,
  ExecuteId,
  ExecuteRecipient,
  ExecuteStatus,
  ExecuteTemplateCode,
  ExecuteWorkspaceId,
} from '../value-objects';

/**
 * Execute Domain State
 *
 * Domain state interface that uses value objects to model business concepts
 * while maintaining compatibility with entity operations. Business fields are
 * modeled as value objects with proper validation and domain rules.
 *
 * Domain-First State Interface
 *
 * This interface represents the domain model where business concepts
 * are properly encapsulated as value objects when appropriate.
 */

export interface ExecuteDomainState {
  id: ExecuteId;
  workspaceId: ExecuteWorkspaceId;
  channelId: ExecuteChannelId;
  templateCode: ExecuteTemplateCode;
  recipient?: ExecuteRecipient;
  data?: ExecuteData;
  status?: ExecuteStatus;
  version: ExecuteVersion;
  createdAt: ExecuteCreatedAt;
  updatedAt: ExecuteUpdatedAt;
}

export type ExecuteDomainStateOptional = Partial<ExecuteDomainState>;
