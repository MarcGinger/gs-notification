// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { ExecuteErrors } from '../errors/execute.errors';

/**
 * ChannelId Value Object
 * Represents a validated channelId with business rules
 */
export const ExecuteChannelId = createStringVO({
  name: 'ChannelId',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  maxLength: 64,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(ExecuteErrors.INVALID_CHANNEL_ID, 'ChannelId'),
});

/**
 * Public instance type for ChannelId
 */
export type ExecuteChannelId = StringVOInstance;

// Convenience creators
export const createExecuteChannelId = (s: string) => ExecuteChannelId.create(s);
export const executeChannelIdFrom = (v: unknown) => ExecuteChannelId.from(v);
