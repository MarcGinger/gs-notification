// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { ExecuteErrors } from '../errors/execute.errors';

/**
 * Recipient Value Object
 * Represents a validated recipient with business rules
 */
export const ExecuteRecipient = createStringVO({
  name: 'Recipient',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 10,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(ExecuteErrors.INVALID_RECIPIENT, 'Recipient'),
});

/**
 * Public instance type for Recipient
 */
export type ExecuteRecipient = StringVOInstance;

// Convenience creators
export const createExecuteRecipient = (s: string) => ExecuteRecipient.create(s);
export const executeRecipientFrom = (v: unknown) => ExecuteRecipient.from(v);
