// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { ExecuteErrors } from '../errors/execute.errors';

/**
 * TemplateCode Value Object
 * Represents a validated templateCode with business rules
 */
export const ExecuteTemplateCode = createStringVO({
  name: 'TemplateCode',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  maxLength: 64,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    ExecuteErrors.INVALID_TEMPLATE_CODE,
    'TemplateCode',
  ),
});

/**
 * Public instance type for TemplateCode
 */
export type ExecuteTemplateCode = StringVOInstance;

// Convenience creators
export const createExecuteTemplateCode = (s: string) =>
  ExecuteTemplateCode.create(s);
export const executeTemplateCodeFrom = (v: unknown) =>
  ExecuteTemplateCode.from(v);
