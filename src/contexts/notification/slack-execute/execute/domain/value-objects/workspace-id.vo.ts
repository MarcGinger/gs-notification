// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { ExecuteErrors } from '../errors/execute.errors';

/**
 * WorkspaceId Value Object
 * Represents a validated workspaceId with business rules
 */
export const ExecuteWorkspaceId = createStringVO({
  name: 'WorkspaceId',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  maxLength: 64,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    ExecuteErrors.INVALID_WORKSPACE_ID,
    'WorkspaceId',
  ),
});

/**
 * Public instance type for WorkspaceId
 */
export type ExecuteWorkspaceId = StringVOInstance;

// Convenience creators
export const createExecuteWorkspaceId = (s: string) =>
  ExecuteWorkspaceId.create(s);
export const executeWorkspaceIdFrom = (v: unknown) =>
  ExecuteWorkspaceId.from(v);
