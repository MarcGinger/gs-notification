// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DomainError, Result, err, withContext, ok } from 'src/shared/errors';
import { Clock } from 'src/shared/domain/clock';
import { EventMetadata } from 'src/shared/domain/events';
import { ExecuteAggregate } from '../aggregates';
import { CreateExecuteProps } from '../props';
import { ExecuteDomainState } from '../state';
import {
  ExecuteCreatedAt,
  ExecuteUpdatedAt,
  ExecuteVersion,
  ExecuteId,
  ExecuteWorkspaceId,
  ExecuteChannelId,
  ExecuteTemplateCode,
  ExecuteRecipient,
  ExecuteData,
  createExecuteStatus,
} from '../value-objects';

/**
 * Enhanced execute entity factory with comprehensive validation and security context
 * Moved from application layer to properly separate business concerns
 */
export function createExecuteAggregateFromProps(
  props: CreateExecuteProps,
  metadata: EventMetadata,
  clock: Clock,
  // validatedEntities?: {
  //  channels?: ChannelReference[];
  // },
): Result<ExecuteAggregate, DomainError> {
  // Validate each property by creating value objects
  // Generate a validate identity column UUID
  const idResult = ExecuteId.generate();
  if (!idResult.ok) {
    return err(
      withContext(idResult.error, {
        ...idResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_execute',
      }),
    );
  }

  const workspaceIdResult = ExecuteWorkspaceId.from(props.workspaceId);
  if (!workspaceIdResult.ok) {
    return err(
      withContext(workspaceIdResult.error, {
        ...workspaceIdResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_execute',
        workspaceId: props.workspaceId,
      }),
    );
  }

  const channelIdResult = ExecuteChannelId.from(props.channelId);
  if (!channelIdResult.ok) {
    return err(
      withContext(channelIdResult.error, {
        ...channelIdResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_execute',
        channelId: props.channelId,
      }),
    );
  }

  const templateCodeResult = ExecuteTemplateCode.from(props.templateCode);
  if (!templateCodeResult.ok) {
    return err(
      withContext(templateCodeResult.error, {
        ...templateCodeResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_execute',
        templateCode: props.templateCode,
      }),
    );
  }

  const recipientResult = ExecuteRecipient.from(props.recipient);
  if (!recipientResult.ok) {
    return err(
      withContext(recipientResult.error, {
        ...recipientResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_execute',
        recipient: props.recipient,
      }),
    );
  }

  const dataResult = ExecuteData.from(props.data);
  if (!dataResult.ok) {
    return err(
      withContext(dataResult.error, {
        ...dataResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_execute',
        data: props.data,
      }),
    );
  }

  const statusResult = createExecuteStatus(props.status);
  if (!statusResult.ok) {
    return err(
      withContext(statusResult.error, {
        ...statusResult.error.context,
        correlationId: metadata.correlationId,
        userId: metadata.userId,
        operation: 'create_execute',
        status: props.status,
      }),
    );
  }

  const createdAtResult = ExecuteCreatedAt.create(clock.now());
  if (!createdAtResult.ok) {
    return err(createdAtResult.error);
  }

  const updatedAtResult = ExecuteUpdatedAt.create(clock.now());
  if (!updatedAtResult.ok) {
    return err(updatedAtResult.error);
  }

  const versionResult = ExecuteVersion.create(1); // Initial version is 1
  if (!versionResult.ok) {
    return err(versionResult.error);
  }

  // Create the entity properties with validated value objects
  const entityProps: ExecuteDomainState = {
    id: idResult.value,
    workspaceId: workspaceIdResult.value,
    channelId: channelIdResult.value,
    templateCode: templateCodeResult.value,
    recipient: recipientResult.value,
    data: dataResult.value,
    status: statusResult.value,
    createdAt: createdAtResult.value,
    updatedAt: updatedAtResult.value,
    version: versionResult.value,
  };

  // Create the entity using the constructor
  return ExecuteAggregate.create(entityProps, clock, metadata);
}
