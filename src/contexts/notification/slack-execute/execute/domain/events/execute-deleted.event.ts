// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { EventMetadata } from 'src/shared/domain/events';

/**
 * Execute Deleted Event Payload
 * Contains identifying information about the deleted execute
 */
export interface ExecuteDeletedEventPayload {
  id: string;
  deletedAt: Date;
  version: number;
  metadata: EventMetadata;
}

/**
 * Execute Deleted Domain Event
 *
 * Emitted when a execute is successfully deleted.
 * Contains identifying information and deletion metadata.
 */
export class ExecuteDeletedEvent {
  public readonly eventType = 'NotificationSlackExecuteExecuteDeleted.v1';
  public readonly eventVersion = 'v1';

  constructor(public readonly payload: ExecuteDeletedEventPayload) {}

  // Factory method with security context
  static create(
    data: Omit<ExecuteDeletedEventPayload, 'metadata'>,
    baseMetadata: EventMetadata,
  ): ExecuteDeletedEvent {
    const metadata: EventMetadata = {
      ...baseMetadata,
      eventVersion: '1.0',
      source: 'notification.domain.execute',
    };

    return new ExecuteDeletedEvent({
      ...data,
      metadata,
    });
  }

  get id(): string {
    return this.payload.id;
  }

  get deletedAt(): Date {
    return this.payload.deletedAt;
  }

  get version(): number {
    return this.payload.version;
  }

  get metadata(): EventMetadata {
    return this.payload.metadata;
  }
}
