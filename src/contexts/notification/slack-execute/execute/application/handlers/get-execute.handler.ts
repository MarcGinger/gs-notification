// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { QueryHandler, IQueryHandler } from '@nestjs/cqrs';
import { DetailExecuteResponse } from '../dtos';
import { GetExecuteUseCase } from '../use-cases';
import { GetExecuteQuery } from '../queries';
import { Result, DomainError } from 'src/shared/errors';

@QueryHandler(GetExecuteQuery)
export class GetExecuteHandler implements IQueryHandler<GetExecuteQuery> {
  constructor(private readonly getExecuteUseCase: GetExecuteUseCase) {}

  async execute(
    query: GetExecuteQuery,
  ): Promise<Result<DetailExecuteResponse | null, DomainError>> {
    return this.getExecuteUseCase.execute({
      user: query.user,
      id: query.id,
      correlationId: query.correlationId || 'query-handler',
    });
  }
}
