// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

/**
 * Abstract Use Case Contracts for Execute Operations
 *
 * These abstract classes are used instead of interfaces to work with NestJS DI.
 * TypeScript interfaces are erased at runtime and cannot be injected.
 */

import { Result, DomainError } from 'src/shared/errors';
import type { IUserToken } from 'src/shared/security';
import type {
  CreateExecuteProps,
  UpdateExecuteProps,
} from '../../domain/props';
import type { DetailExecuteResponse } from '../dtos';

export abstract class ICreateExecuteUseCase {
  abstract execute(params: {
    user: IUserToken;
    props: CreateExecuteProps;
    correlationId: string;
    authorizationReason: string;
  }): Promise<Result<DetailExecuteResponse, DomainError>>;
}

export abstract class IUpdateExecuteUseCase {
  abstract execute(params: {
    user: IUserToken;
    id: string;
    props: UpdateExecuteProps;
    correlationId: string;
    authorizationReason: string;
  }): Promise<Result<DetailExecuteResponse, DomainError>>;
}

export abstract class IGetExecuteUseCase {
  abstract execute(params: {
    user: IUserToken;
    id: string;
    correlationId: string;
  }): Promise<Result<DetailExecuteResponse, DomainError>>;
}
