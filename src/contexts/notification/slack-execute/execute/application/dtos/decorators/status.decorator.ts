// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { applyDecorators } from '@nestjs/common';
import { ApiProperty } from '@nestjs/swagger';
import { IsEnum, IsOptional, IsNotEmpty } from 'class-validator';
import {
  ExecuteStatusValues,
  ExecuteStatusValue,
} from '../../../domain/value-objects';

export { ExecuteStatusValues, ExecuteStatusValue };
/**
 * Options for property decorators (enum)
 */
interface PropOptions {
  required?: boolean;
}

/**
 * Property decorator for Execute Status with required option
 * @param {Object} options - Options for the decorator
 * @returns {PropertyDecorator}
 */
export function ApiExecuteStatus(options: PropOptions = {}) {
  const { required = false } = options;
  return applyDecorators(
    ApiProperty({
      description: `Current message lifecycle status. Values include requested, validated, queued, failed.`,
      type: String,
      enum: ExecuteStatusValues,
      required,
    }),
    IsEnum(ExecuteStatusValues),
    required ? IsNotEmpty() : IsOptional(),
  );
}
