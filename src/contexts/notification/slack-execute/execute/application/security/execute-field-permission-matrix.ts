// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { ExecutePermission } from '../../domain/permissions/execute.permissions';

/**
 * Field-level permission matrix for Execute updates.
 * Maps execute fields to the required permissions for modification.
 */
export class ExecuteFieldPermissionMatrix {
  private static readonly fieldPermissions: Record<
    string,
    ExecutePermission[]
  > = {
    // Basic execute information fields - require standard update permission
    name: [ExecutePermission.DOMAIN_EXECUTE_UPDATE],
    description: [ExecutePermission.DOMAIN_EXECUTE_UPDATE],
    category: [ExecutePermission.DOMAIN_EXECUTE_UPDATE],

    // Status fields - may require special permissions or admin rights
    active: [
      ExecutePermission.DOMAIN_EXECUTE_UPDATE,
      ExecutePermission.DOMAIN_EXECUTE_ADMIN,
    ],
    status: [
      ExecutePermission.DOMAIN_EXECUTE_UPDATE,
      ExecutePermission.DOMAIN_EXECUTE_ADMIN,
    ],

    // Financial fields - typically require higher permissions
    fees: [ExecutePermission.DOMAIN_EXECUTE_UPDATE],
    rates: [ExecutePermission.DOMAIN_EXECUTE_UPDATE],
    pricing: [ExecutePermission.DOMAIN_EXECUTE_UPDATE],

    // Configuration fields
    channelCodes: [ExecutePermission.DOMAIN_EXECUTE_UPDATE],
    railCodes: [ExecutePermission.DOMAIN_EXECUTE_UPDATE],
    configuration: [ExecutePermission.DOMAIN_EXECUTE_UPDATE],

    // Administrative fields - require admin permissions
    tenantId: [ExecutePermission.DOMAIN_EXECUTE_ADMIN],
    auditInfo: [ExecutePermission.DOMAIN_EXECUTE_ADMIN],
    metadata: [ExecutePermission.DOMAIN_EXECUTE_UPDATE],
  };

  /**
   * Get required permissions for a specific field.
   * Returns empty array if field has no special permission requirements.
   */
  static getRequiredPermissions(fieldName: string): ExecutePermission[] {
    return this.fieldPermissions[fieldName] || [];
  }

  /**
   * Check if a field has specific permission requirements.
   */
  static hasSpecialPermissions(fieldName: string): boolean {
    return fieldName in this.fieldPermissions;
  }

  /**
   * Get all fields that require a specific permission.
   */
  static getFieldsForPermission(permission: ExecutePermission): string[] {
    return Object.entries(this.fieldPermissions)
      .filter(([, perms]) => perms.includes(permission))
      .map(([field]) => field);
  }

  /**
   * Get all defined field permissions.
   */
  static getAllFieldPermissions(): Record<string, ExecutePermission[]> {
    return { ...this.fieldPermissions };
  }

  /**
   * Add or update permission requirements for a field.
   */
  static setFieldPermissions(
    fieldName: string,
    permissions: ExecutePermission[],
  ): void {
    this.fieldPermissions[fieldName] = [...permissions];
  }
}
