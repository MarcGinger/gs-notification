// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { ExecuteSnapshotProps } from '../../domain/props';
import { DetailExecuteResponse } from '../dtos';

/**
 * Execute Snapshot to DTO Mappers
 * Reusable mapping functions for converting domain snapshots to DTOs
 */

/**
 * Map execute snapshot to DTO response
 */
export const mapExecuteSnapshotToDto = (
  snapshot: ExecuteSnapshotProps,
): DetailExecuteResponse => {
  return {
    id: snapshot.id,
    workspaceId: snapshot.workspaceId,
    channelId: snapshot.channelId,
    templateCode: snapshot.templateCode,
    recipient: snapshot.recipient,
    data: snapshot.data,
    status: snapshot.status,
  };
};
