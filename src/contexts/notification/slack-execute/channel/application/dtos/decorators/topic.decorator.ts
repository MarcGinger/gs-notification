// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { applyDecorators } from '@nestjs/common';
import { ApiProperty } from '@nestjs/swagger';
import { IsOptional, IsNotEmpty, IsString, MaxLength } from 'class-validator';

/**
 * Options for property decorators
 */
interface PropOptions {
  required?: boolean;
}

/**
 * Property decorator for Channel Topic
 * @param {Object} options - Options for the decorator
 * @returns {PropertyDecorator}
 */
export function ApiChannelTopic(options: PropOptions = {}) {
  const { required = false } = options;

  return applyDecorators(
    ApiProperty({
      description: `Optional channel topic/description as set by channel administrators. Provides context about the channel's intended use and notification types.`,
      example: `System notifications and alerts`,
      type: String,
      required,
    }),
    IsString(),
    MaxLength(255),
    required ? IsNotEmpty() : IsOptional(),
  );
}
