// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { ChannelErrors } from '../errors/channel.errors';

/**
 * Purpose Value Object
 * Represents a validated purpose with business rules
 */
export const ChannelPurpose = createStringVO({
  name: 'Purpose',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 255,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(ChannelErrors.INVALID_PURPOSE, 'Purpose'),
});

/**
 * Public instance type for Purpose
 */
export type ChannelPurpose = StringVOInstance;

// Convenience creators
export const createChannelPurpose = (s: string) => ChannelPurpose.create(s);
export const channelPurposeFrom = (v: unknown) => ChannelPurpose.from(v);
