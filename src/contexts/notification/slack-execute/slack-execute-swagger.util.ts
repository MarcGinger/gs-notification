// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { INestApplication } from '@nestjs/common';
import { AppConfigUtil } from 'src/shared/config/app-config.util';

import { SlackExecuteDocumentation } from './slack-execute.doc';
import { ExecuteDocumentation } from './execute/docs/execute.doc';
import { TemplateDocumentation } from './template/docs/template.doc';
import { ChannelDocumentation } from './channel/docs/channel.doc';
import { WorkspaceDocumentation } from './workspace/docs/workspace.doc';

/**
 * Setup notification-slack-execute application/service Swagger documentation
 * Configures documentation for all aggregates within the notification_slack_execute domain
 */
export function setupNotificationSlackExecuteDocs(
  app: INestApplication,
  port: string | number,
): Record<string, string> {
  if (AppConfigUtil.isProduction()) {
    return {};
  }

  const boundedContextUrls = SlackExecuteDocumentation.setupAll(app, port);

  // Setup documentation for all notification_slack_execute aggregates
  const executeUrls = ExecuteDocumentation.setupAll(app, port);
  const templateUrls = TemplateDocumentation.setupAll(app, port);
  const channelUrls = ChannelDocumentation.setupAll(app, port);
  const workspaceUrls = WorkspaceDocumentation.setupAll(app, port);

  return {
    ...boundedContextUrls,
    ...executeUrls,
    ...templateUrls,
    ...channelUrls,
    ...workspaceUrls,
  };
}
