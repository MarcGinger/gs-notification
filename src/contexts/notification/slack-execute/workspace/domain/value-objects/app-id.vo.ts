// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { WorkspaceErrors } from '../errors/workspace.errors';

/**
 * AppId Value Object
 * Represents a validated appId with business rules
 */
export const WorkspaceAppId = createStringVO({
  name: 'AppId',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 64,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(WorkspaceErrors.INVALID_APP_ID, 'AppId'),
});

/**
 * Public instance type for AppId
 */
export type WorkspaceAppId = StringVOInstance;

// Convenience creators
export const createWorkspaceAppId = (s: string) => WorkspaceAppId.create(s);
export const workspaceAppIdFrom = (v: unknown) => WorkspaceAppId.from(v);
