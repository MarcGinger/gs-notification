// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

// slack-config PII Policy Registration
// Registers domain-specific PII policy with the shared policy provider

import { Injectable, OnModuleInit } from '@nestjs/common';
import {
  registerDomainPolicy,
  PIIPolicyBundle,
} from 'src/shared/services/compliance';

export const SLACK_CONFIG_POLICY: PIIPolicyBundle = {
  domain: 'slack-config',
  keywords: {
    include: [], // don't blanket-flag "name"
    exclude: [], // ensure no accidental catch-all
  },
  rules: [
    // Exact field paths that are PII
    // Explicit exceptions (non-PII)
  ],
};

@Injectable()
export class SlackConfigPolicyService implements OnModuleInit {
  onModuleInit() {
    // Register slack-config domain policy during module initialization
    registerDomainPolicy(SLACK_CONFIG_POLICY);
  }
}
