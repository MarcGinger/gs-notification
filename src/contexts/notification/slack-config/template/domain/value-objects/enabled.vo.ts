// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  BooleanVOInstance,
  createBooleanVO,
  createBooleanVOErrors,
} from 'src/shared/domain/value-objects';
import { TemplateErrors } from '..//errors/template.errors';

/**
 * Enabled Boolean Value Object
 * Represents a validated enabled with business rules
 */
export const TemplateEnabled = createBooleanVO({
  name: 'Enabled',
  treatNullAsFalse: false,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createBooleanVOErrors(TemplateErrors.INVALID_ENABLED, 'Enabled'),
});

/** Public instance type for Enabled */
export type TemplateEnabled = BooleanVOInstance;

// Convenience creators
export const createTemplateEnabled = (v: boolean) => TemplateEnabled.create(v);
export const templateEnabledFrom = (v: unknown) => TemplateEnabled.from(v);
