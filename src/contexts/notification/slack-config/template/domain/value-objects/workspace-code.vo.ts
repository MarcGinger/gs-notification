// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { TemplateErrors } from '../errors/template.errors';

/**
 * WorkspaceCode Value Object
 * Represents a validated workspaceCode with business rules
 */
export const TemplateWorkspaceCode = createStringVO({
  name: 'WorkspaceCode',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  maxLength: 32,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    TemplateErrors.INVALID_WORKSPACE_CODE,
    'WorkspaceCode',
  ),
});

/**
 * Public instance type for WorkspaceCode
 */
export type TemplateWorkspaceCode = StringVOInstance;

// Convenience creators
export const createTemplateWorkspaceCode = (s: string) =>
  TemplateWorkspaceCode.create(s);
export const templateWorkspaceCodeFrom = (v: unknown) =>
  TemplateWorkspaceCode.from(v);
