// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DomainError, Result, ok, err } from 'src/shared/errors';
import {
  createIntegerVO,
  createIntegerVOErrors,
  IntegerVOInstance,
  createDateTimeVO,
  createDateTimeVOErrors,
  DateTimeVOInstance,
} from 'src/shared/domain/value-objects';
import { Clock } from 'src/shared/domain/clock';
import { TemplateErrors } from '../errors/template.errors';

/**
 * Template Instance Value Objects
 *
 * Instance VOs for Template aggregate lifecycle management.
 * Includes creation timestamps, update timestamps, and version tracking.
 * Follows the Clock injection pattern for deterministic testing.
 */

// ===========================================
// Template Created At Instance VO
// ===========================================

const TemplateCreatedAtErrors = createDateTimeVOErrors(
  TemplateErrors.CREATED_AT_REQUIRED,
  'Template Created At',
);

export const TemplateCreatedAt = createDateTimeVO({
  name: 'TemplateCreatedAt',
  allowFuture: false, // Cannot create template in the future
  allowPast: true, // Can create with past timestamps (data migration)
  customValidation: (date: Date): Result<void, DomainError> => {
    // Optional: Add domain-specific creation time validation
    // For example, cannot be more than 1 year in the past
    const oneYearAgo = new Date();
    oneYearAgo.setFullYear(oneYearAgo.getFullYear() - 1);

    if (date < oneYearAgo) {
      return err({
        ...TemplateErrors.CREATED_AT_REQUIRED,
        detail: 'Template creation date cannot be more than 1 year in the past',
        context: { value: date, maxPastDate: oneYearAgo },
      } as DomainError);
    }

    return ok(undefined);
  },
  errors: TemplateCreatedAtErrors,
});

export type TemplateCreatedAt = DateTimeVOInstance;

// ===========================================
// Template Updated At Instance VO
// ===========================================

const TemplateUpdatedAtErrors = createDateTimeVOErrors(
  TemplateErrors.UPDATED_AT_REQUIRED,
  'Template Updated At',
);

export const TemplateUpdatedAt = createDateTimeVO({
  name: 'TemplateUpdatedAt',
  allowFuture: false, // Cannot update template in the future
  allowPast: true, // Can update with past timestamps (data correction)
  errors: TemplateUpdatedAtErrors,
});

export type TemplateUpdatedAt = DateTimeVOInstance;

// ===========================================
// Template Version Instance VO
// ===========================================

const TemplateVersionErrors = createIntegerVOErrors(
  TemplateErrors.INVALID_CODE_DATA, // Reusing existing error until specific version error is added
  'Template Version',
);

export const TemplateVersion = createIntegerVO({
  name: 'TemplateVersion',
  min: 0, // Version starts at 0
  allowNegative: false,
  errors: TemplateVersionErrors,
});

export type TemplateVersion = IntegerVOInstance;

// ===========================================
// Clock-Based Helper Functions (Explicit Dependencies)
// ===========================================

/**
 * Create TemplateCreatedAt from Clock (explicit dependency)
 * Deterministic and testable - pass a fake clock in tests
 */
export const createdAtNow = (
  clock: Clock,
): Result<TemplateCreatedAt, DomainError> =>
  TemplateCreatedAt.create(clock.now());

/**
 * Create TemplateUpdatedAt from Clock (explicit dependency)
 * Deterministic and testable - pass a fake clock in tests
 */
export const updatedAtNow = (
  clock: Clock,
): Result<TemplateUpdatedAt, DomainError> =>
  TemplateUpdatedAt.create(clock.now());

/**
 * Bump version by 1 (domain business rule)
 */
export const bumpVersion = (
  currentVersion: TemplateVersion,
): Result<TemplateVersion, DomainError> =>
  TemplateVersion.create(currentVersion.value + 1);

/**
 * Create initial version (starts at 1 for new aggregates)
 */
export const initialVersion = (): Result<TemplateVersion, DomainError> =>
  TemplateVersion.create(1);

// ===========================================
// Direct Creation Functions (Non-Clock Based)
// ===========================================

/**
 * Create TemplateCreatedAt from Date
 */
export const createTemplateCreatedAt = (
  date: Date,
): Result<TemplateCreatedAt, DomainError> => TemplateCreatedAt.create(date);

/**
 * Create TemplateUpdatedAt from Date
 */
export const createTemplateUpdatedAt = (
  date: Date,
): Result<TemplateUpdatedAt, DomainError> => TemplateUpdatedAt.create(date);

/**
 * Create TemplateVersion from number
 */
export const createTemplateVersion = (
  version: number,
): Result<TemplateVersion, DomainError> => TemplateVersion.create(version);

// ===========================================
// Domain Invariants & Validation
// ===========================================

/**
 * Validate that updatedAt is not before createdAt (monotonic time)
 */
export const validateMonotonicTime = (
  createdAt: TemplateCreatedAt,
  updatedAt: TemplateUpdatedAt,
): Result<void, DomainError> => {
  if (updatedAt.value < createdAt.value) {
    return err({
      ...TemplateErrors.UPDATED_AT_REQUIRED,
      detail: 'Updated at cannot be before created at',
      context: {
        createdAt: createdAt.value.toISOString(),
        updatedAt: updatedAt.value.toISOString(),
      },
    } as DomainError);
  }
  return ok(undefined);
};

// ===========================================
// Legacy Compatibility (DEPRECATED)
// ===========================================

/**
 * @deprecated Use createdAtNow(clock) instead for better testability
 * This function hides the Date dependency and makes testing harder
 */
export function createTemplateCreatedAtNow(): Result<
  TemplateCreatedAt,
  DomainError
> {
  return TemplateCreatedAt.create(new Date());
}

/**
 * @deprecated Use updatedAtNow(clock) instead for better testability
 * This function hides the Date dependency and makes testing harder
 */
export function createTemplateUpdatedAtNow(): Result<
  TemplateUpdatedAt,
  DomainError
> {
  return TemplateUpdatedAt.create(new Date());
}
