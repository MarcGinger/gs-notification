// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { TemplateErrors } from '../errors/template.errors';

/**
 * Name Value Object
 * Represents a validated name with business rules
 */
export const TemplateName = createStringVO({
  name: 'Name',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  maxLength: 255,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(TemplateErrors.INVALID_NAME, 'Name'),
});

/**
 * Public instance type for Name
 */
export type TemplateName = StringVOInstance;

// Convenience creators
export const createTemplateName = (s: string) => TemplateName.create(s);
export const templateNameFrom = (v: unknown) => TemplateName.from(v);
