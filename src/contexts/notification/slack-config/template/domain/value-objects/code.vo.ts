// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { TemplateErrors } from '../errors/template.errors';

/**
 * Code Value Object
 * Represents a validated code with business rules
 */
export const TemplateCode = createStringVO({
  name: 'Code',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,
  minLength: 10,
  maxLength: 11,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(TemplateErrors.INVALID_CODE, 'Code'),
});

/**
 * Public instance type for Code
 */
export type TemplateCode = StringVOInstance;

// Convenience creators
export const createTemplateCode = (s: string) => TemplateCode.create(s);
export const templateCodeFrom = (v: unknown) => TemplateCode.from(v);
