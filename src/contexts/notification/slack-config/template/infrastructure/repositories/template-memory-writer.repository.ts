// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Injectable, Inject } from '@nestjs/common';
import { Result, DomainError, err, ok } from 'src/shared/errors';
import { ActorContext } from 'src/shared/application/context';
import {
  SaveReceipt,
  handleRepositoryError,
} from 'src/shared/infrastructure/repositories';
import { RepositoryErrorFactory } from 'src/shared/domain/errors/repository.error';
import { APP_LOGGER, Log, componentLogger, Logger } from 'src/shared/logging';
import { Clock, CLOCK } from 'src/shared/infrastructure/time';
import { CorrelationUtil } from 'src/shared/utilities/correlation.util';
import {
  RepositoryLoggingUtil,
  RepositoryLoggingConfig,
} from 'src/shared/infrastructure/repositories';
import { TemplateAggregate } from '../../domain/aggregates';
import { TemplateCode } from '../../domain/value-objects';
import { ITemplateWriter } from '../../application/ports';
import { templateStore, TemplateProjection } from '../stores/template.store';

/**
 * Template Writer Repository - In-Memory Implementation
 *
 * Handles write operations (create, update, delete) for Template aggregates using in-memory projector
 * as the data store instead of EventStore DB.
 *
 * Benefits:
 * - Zero external database dependencies
 * - Ultra-fast write operations (LRU cache)
 * - Perfect for testing, prototyping, and shadow validation
 * - Maintains same interface as EventStore-based implementation
 * - Comprehensive logging and error handling
 *
 * ISP Benefits:
 * - Clients that only need to write data don't depend on read methods
 * - Easier to implement write-only scenarios
 * - Can be secured separately from read operations
 * - Supports command-only patterns in CQRS architectures
 *
 * @domain Notification Context - Template Writer Repository (In-Memory)
 * @layer Infrastructure
 * @pattern Repository Pattern + Interface Segregation Principle + In-Memory Store
 */
@Injectable()
export class TemplateWriterRepository implements ITemplateWriter {
  private readonly logger: Logger;
  private readonly loggingConfig: RepositoryLoggingConfig;

  constructor(
    @Inject(APP_LOGGER) baseLogger: Logger,
    @Inject(CLOCK) private readonly clock: Clock,
  ) {
    this.loggingConfig = {
      serviceName: 'NotificationSlackConfigService',
      component: 'TemplateWriterRepository',
    };
    this.logger = componentLogger(baseLogger, this.loggingConfig.component);
  }

  /**
   * Helper to create consistent logging context using shared utilities
   */
  private createLogContext(
    operation: string,
    correlationId: string,
    actor: ActorContext,
    additionalContext?: Record<string, unknown>,
  ): Record<string, unknown> {
    return RepositoryLoggingUtil.createLogContext(
      this.loggingConfig,
      this.clock,
      operation,
      correlationId,
      actor,
      additionalContext,
    );
  }

  /**
   * Helper to assess operation risk level using shared utilities
   */
  private assessOperationRisk(operation: string): 'LOW' | 'MEDIUM' | 'HIGH' {
    return RepositoryLoggingUtil.assessOperationRisk(operation);
  }

  /**
   * Save a Template aggregate (create or update) using In-Memory store approach
   * @param actor - The authenticated user context
   * @param template - The Template aggregate to save
   * @param expectedVersionFromCaller - Optional version for optimistic locking
   * @returns Result with SaveReceipt containing revision tracking or domain error
   */
  async save(
    actor: ActorContext,
    template: TemplateAggregate,
    expectedVersionFromCaller?: number,
  ): Promise<Result<SaveReceipt, DomainError>> {
    const operation = 'save';
    const riskLevel = this.assessOperationRisk(operation);
    const correlationId = CorrelationUtil.generateForOperation('template-save');

    const logContext = this.createLogContext(operation, correlationId, actor, {
      riskLevel,
      aggregateId: template.id.value,
      currentVersion: template.version,
      eventCount: template.uncommittedEvents?.length ?? 0,
      customCorrelationId: false,
      dataSource: 'in-memory-projector',
    });

    // Validate actor context with enhanced security logging
    const validation = RepositoryLoggingUtil.validateActorContext(
      this.logger,
      actor,
      logContext,
    );
    if (!validation.ok) {
      return Promise.resolve(validation);
    }

    // Guard tenant explicitly
    if (!actor.tenant) {
      return Promise.resolve(
        err(
          RepositoryErrorFactory.validationError('tenant', 'Missing tenant id'),
        ),
      );
    }

    // Log successful authorization
    RepositoryLoggingUtil.logAuthorizationSuccess(
      this.logger,
      operation,
      logContext,
      {
        operationType: 'aggregate_save',
        scope: 'in_memory_projection',
        aggregateId: template.id.value,
      },
    );

    const events = template.uncommittedEvents ?? [];

    // No-op save: return ok receipt (many apps "save" unchanged aggregates)
    if (events.length === 0) {
      Log.debug(this.logger, 'No uncommitted events; no-op save', {
        ...logContext,
        operationResult: 'no_op',
        queryDetails: {
          scope: 'shared_template_store',
          method: 'no_operation',
        },
      });

      const receipt: SaveReceipt = {
        stream: `template-${template.id.value}`,
        aggregateId: template.id.value,
        tenant: actor.tenant,
        eventCount: 0,
        newVersion: template.version,
        streamRevision: BigInt(template.version),
        timestampIso: this.clock.nowIso(),
        correlationId,
      };

      // Log no-op metrics
      RepositoryLoggingUtil.logQueryMetrics(
        this.logger,
        operation,
        logContext,
        {
          resultCount: 0,
          dataQuality: 'empty',
          sampleData: {
            operationType: 'no_op',
            reason: 'no_uncommitted_events',
          },
        },
      );

      return Promise.resolve(ok(receipt));
    }

    // prevVersion = version BEFORE current batch
    const prevVersion = template.version - events.length;

    // Optional optimistic lock from caller
    if (
      expectedVersionFromCaller !== undefined &&
      expectedVersionFromCaller !== prevVersion
    ) {
      Log.warn(this.logger, 'Pre-check optimistic lock failed', {
        ...logContext,
        expectedVersionFromCaller,
        prevVersion,
        concurrencyError: 'optimistic_lock_failure',
      });

      return Promise.resolve(
        err(
          RepositoryErrorFactory.concurrencyConflict(
            expectedVersionFromCaller,
            prevVersion,
          ),
        ),
      );
    }

    try {
      Log.debug(this.logger, 'Saving template to shared store', {
        ...logContext,
        eventCount: events.length,
        prevVersion,
        queryDetails: {
          scope: 'shared_template_store',
          method: 'templateStore.set',
        },
      });

      // Check if template already exists for version conflict detection
      const existingProjection = templateStore.get(
        actor.tenant,
        template.id.value,
      );
      if (existingProjection && expectedVersionFromCaller !== undefined) {
        if (existingProjection.version !== expectedVersionFromCaller) {
          return Promise.resolve(
            err(
              RepositoryErrorFactory.concurrencyConflict(
                expectedVersionFromCaller,
                existingProjection.version,
              ),
            ),
          );
        }
      }

      // Create projection from aggregate properties for store
      const projection: TemplateProjection = {
        // TemplateProps fields
        code: template.entity.code.value,
        workspaceCode: template.entity.workspaceCode.value,
        name: template.entity.name.value,
        description: template.entity.description?.value,
        contentBlocks: template.entity.contentBlocks.toArray(),
        variables: template.entity.variables?.toArray(),
        samplePayload: template.entity.samplePayload?.value,
        enabled: template.entity.enabled.value,
        // BaseProjection fields
        tenant: actor.tenant,
        version: template.version,
        createdAt: template.entity.createdAt.value,
        updatedAt: this.clock.now(),
        deletedAt: undefined,
        lastStreamRevision: template.version.toString(),
      };

      // Save to in-memory store
      templateStore.set(actor.tenant, template.id.value, projection);

      // Mark committed after successful store save
      template.markEventsAsCommitted();

      const receipt: SaveReceipt = {
        stream: `template-${template.id.value}`,
        aggregateId: template.id.value,
        tenant: actor.tenant,
        eventCount: events.length,
        newVersion: template.version,
        streamRevision: BigInt(template.version),
        timestampIso: this.clock.nowIso(),
        correlationId,
      };

      // Log successful save metrics using shared utility
      RepositoryLoggingUtil.logQueryMetrics(
        this.logger,
        operation,
        logContext,
        {
          resultCount: events.length,
          dataQuality: 'good',
          sampleData: {
            eventsProcessed: events.length,
            newVersion: receipt.newVersion,
            code: template.id.value,
          },
        },
      );

      return Promise.resolve(ok(receipt));
    } catch (error) {
      // Log operation error using shared utility
      RepositoryLoggingUtil.logOperationError(
        this.logger,
        operation,
        logContext,
        error as Error,
        'HIGH',
      );

      // Handle and return the classified error using shared utility
      return Promise.resolve(handleRepositoryError(error));
    }
  }

  /**
   * Delete a Template by its unique identifier using In-Memory store approach
   * @param actor - The authenticated user context
   * @param code - The unique identifier of the Template to delete
   * @returns Result with SaveReceipt containing revision tracking or domain error
   */
  async delete(
    actor: ActorContext,
    code: TemplateCode,
  ): Promise<Result<SaveReceipt, DomainError>> {
    const operation = 'delete';
    const correlationId =
      CorrelationUtil.generateForOperation('template-delete');

    const logContext = this.createLogContext(operation, correlationId, actor, {
      templateCode: code.value,
    });

    // Authorization success logging
    RepositoryLoggingUtil.logAuthorizationSuccess(
      this.logger,
      operation,
      logContext,
    );

    try {
      // Check if template exists
      if (!templateStore.has(actor.tenant || 'default-tenant', code.value)) {
        const notFoundError = RepositoryErrorFactory.notFound(
          'Template',
          code.value,
        );

        RepositoryLoggingUtil.logOperationError(
          this.logger,
          operation,
          logContext,
          new Error(`Template not found: ${code.value}`),
          'MEDIUM',
        );

        return err(notFoundError);
      }

      // Remove from In-Memory store
      templateStore.delete(actor.tenant || 'default-tenant', code.value);

      const receipt: SaveReceipt = {
        stream: `template-${code.value}`,
        aggregateId: code.value,
        tenant: actor.tenant || 'default-tenant',
        eventCount: 1,
        newVersion: 1, // Simple versioning for In-Memory
        streamRevision: BigInt(1),
        timestampIso: this.clock.nowIso(),
        correlationId,
      };

      RepositoryLoggingUtil.logQueryMetrics(
        this.logger,
        operation,
        logContext,
        {
          resultCount: 1,
          dataQuality: 'good',
        },
      );

      return ok(receipt);
    } catch (error) {
      RepositoryLoggingUtil.logOperationError(
        this.logger,
        operation,
        logContext,
        error as Error,
        'HIGH',
      );

      return handleRepositoryError(error);
    }
  }
}
