// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Result, DomainError } from 'src/shared/errors';
import { RepositoryOptions } from 'src/shared/infrastructure/repositories';
import { ActorContext } from 'src/shared/application/context';
import { Option } from 'src/shared/domain/types';
import { TemplateSnapshotProps } from '../../domain/props';
import { TemplateCode } from '../../domain/value-objects';

/**
 * Token for injecting ITemplateReader port implementation
 */
export const TEMPLATE_READER_TOKEN = 'ITemplateReader' as const;

/**
 * Template Reader Port - Interface Segregation Principle Implementation
 *
 * Handles basic read operations for Template aggregates.
 * This interface focuses solely on simple retrieval operations.
 *
 * ISP Benefits:
 * - Clients only depend on methods they actually use
 * - Easier to mock and test specific read scenarios
 * - Can be implemented independently from write operations
 * - Supports read-only implementations (e.g., reporting services)
 *
 * @domain Notification Context - Template Reader Port
 * @layer Application Ports
 * @pattern Interface Segregation Principle
 */
export interface ITemplateReader {
  /**
   * Find a Template by its unique identifier
   * @param actor - The authenticated user context
   * @param code - The unique identifier of the Template
   * @param options - Optional repository options
   * @returns Result containing the Template snapshot or null if not found
   */
  findById(
    actor: ActorContext,
    code: TemplateCode,
    options?: RepositoryOptions,
  ): Promise<Result<Option<TemplateSnapshotProps>, DomainError>>;

  /**
   * Check if a template exists by ID (for write-path validation)
   * @param actor - The authenticated user context
   * @param code - The unique identifier of the Template
   * @param options - Optional repository options
   * @returns Result containing boolean indicating existence
   */
  exists(
    actor: ActorContext,
    code: TemplateCode,
    options?: RepositoryOptions,
  ): Promise<Result<boolean, DomainError>>;

  /**
   * Get template version for optimistic concurrency control
   * @param actor - The authenticated user context
   * @param code - The unique identifier of the Template
   * @param options - Optional repository options
   * @returns Result containing version number or null if not found
   */
  getVersion(
    actor: ActorContext,
    code: TemplateCode,
    options?: RepositoryOptions,
  ): Promise<Result<Option<number>, DomainError>>;

  /**
   * Get minimal template data for write-path operations
   * @param actor - The authenticated user context
   * @param code - The unique identifier of the Template
   * @param options - Optional repository options
   * @returns Result containing minimal template data or null if not found
   */
  getMinimal(
    actor: ActorContext,
    code: TemplateCode,
    options?: RepositoryOptions,
  ): Promise<Result<Option<{ code: string; version: number }>, DomainError>>;
}
