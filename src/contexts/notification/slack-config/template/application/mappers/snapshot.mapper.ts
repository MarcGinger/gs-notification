// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { TemplateSnapshotProps } from '../../domain/props';
import { DetailTemplateResponse } from '../dtos';

/**
 * Template Snapshot to DTO Mappers
 * Reusable mapping functions for converting domain snapshots to DTOs
 */

/**
 * Map template snapshot to DTO response
 */
export const mapTemplateSnapshotToDto = (
  snapshot: TemplateSnapshotProps,
): DetailTemplateResponse => {
  return {
    code: snapshot.code,
    workspaceId: snapshot.workspaceId,
    name: snapshot.name,
    description: snapshot.description,
    contentBlocks: snapshot.contentBlocks,
    variables: snapshot.variables,
    samplePayload: snapshot.samplePayload,
    enabled: snapshot.enabled,
  };
};
