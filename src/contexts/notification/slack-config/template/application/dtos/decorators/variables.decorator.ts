// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { applyDecorators } from '@nestjs/common';
import { ApiProperty } from '@nestjs/swagger';
import { IsOptional, IsNotEmpty, IsObject, ValidateIf } from 'class-validator';

/**
 * Options for property decorators
 */
interface PropOptions {
  required?: boolean;
}

/**
 * Property decorator for Template Variables
 * @param {Object} options - Options for the decorator
 * @returns {PropertyDecorator}
 */
export function ApiTemplateVariables(options: PropOptions = {}) {
  const { required = false } = options;

  return applyDecorators(
    ApiProperty({
      description: `List of variable placeholders (["approver","productName"]).`,
      example: '{ "key1": "value1", "key2": 123 }',
      type: 'object',
      additionalProperties: true,
    }),
    IsObject(),
    ValidateIf((o, v) => v !== null),
    required ? IsNotEmpty() : IsOptional(),
  );
}
