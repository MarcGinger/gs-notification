// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { CommandHandler, ICommandHandler } from '@nestjs/cqrs';
import { Inject } from '@nestjs/common';
import { Result, DomainError } from 'src/shared/errors';
import { DeleteTemplateUseCase } from '../use-cases';
import { DeleteTemplateCommand } from '../commands';

@CommandHandler(DeleteTemplateCommand)
export class DeleteTemplateHandler
  implements ICommandHandler<DeleteTemplateCommand>
{
  constructor(
    @Inject(DeleteTemplateUseCase)
    private readonly deleteTemplateUseCase: DeleteTemplateUseCase,
  ) {}

  async execute(
    command: DeleteTemplateCommand,
  ): Promise<Result<void, DomainError>> {
    return await this.deleteTemplateUseCase.execute({
      user: command.user,
      code: command.code,
      correlationId: command.correlationId,
      authorizationReason: 'CQRS Command Handler',
    });
  }
}
