// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { AppConfigPermission } from '../../domain/permissions/app-config.permissions';

/**
 * Field-level permission matrix for AppConfig updates.
 * Maps appConfig fields to the required permissions for modification.
 */
export class AppConfigFieldPermissionMatrix {
  private static readonly fieldPermissions: Record<
    string,
    AppConfigPermission[]
  > = {
    // Basic appConfig information fields - require standard update permission
    name: [AppConfigPermission.DOMAIN_APP_CONFIG_UPDATE],
    description: [AppConfigPermission.DOMAIN_APP_CONFIG_UPDATE],
    category: [AppConfigPermission.DOMAIN_APP_CONFIG_UPDATE],

    // Status fields - may require special permissions or admin rights
    active: [
      AppConfigPermission.DOMAIN_APP_CONFIG_UPDATE,
      AppConfigPermission.DOMAIN_APP_CONFIG_ADMIN,
    ],
    status: [
      AppConfigPermission.DOMAIN_APP_CONFIG_UPDATE,
      AppConfigPermission.DOMAIN_APP_CONFIG_ADMIN,
    ],

    // Financial fields - typically require higher permissions
    fees: [AppConfigPermission.DOMAIN_APP_CONFIG_UPDATE],
    rates: [AppConfigPermission.DOMAIN_APP_CONFIG_UPDATE],
    pricing: [AppConfigPermission.DOMAIN_APP_CONFIG_UPDATE],

    // Configuration fields
    channelCodes: [AppConfigPermission.DOMAIN_APP_CONFIG_UPDATE],
    railCodes: [AppConfigPermission.DOMAIN_APP_CONFIG_UPDATE],
    configuration: [AppConfigPermission.DOMAIN_APP_CONFIG_UPDATE],

    // Administrative fields - require admin permissions
    tenantId: [AppConfigPermission.DOMAIN_APP_CONFIG_ADMIN],
    auditInfo: [AppConfigPermission.DOMAIN_APP_CONFIG_ADMIN],
    metadata: [AppConfigPermission.DOMAIN_APP_CONFIG_UPDATE],
  };

  /**
   * Get required permissions for a specific field.
   * Returns empty array if field has no special permission requirements.
   */
  static getRequiredPermissions(fieldName: string): AppConfigPermission[] {
    return this.fieldPermissions[fieldName] || [];
  }

  /**
   * Check if a field has specific permission requirements.
   */
  static hasSpecialPermissions(fieldName: string): boolean {
    return fieldName in this.fieldPermissions;
  }

  /**
   * Get all fields that require a specific permission.
   */
  static getFieldsForPermission(permission: AppConfigPermission): string[] {
    return Object.entries(this.fieldPermissions)
      .filter(([, perms]) => perms.includes(permission))
      .map(([field]) => field);
  }

  /**
   * Get all defined field permissions.
   */
  static getAllFieldPermissions(): Record<string, AppConfigPermission[]> {
    return { ...this.fieldPermissions };
  }

  /**
   * Add or update permission requirements for a field.
   */
  static setFieldPermissions(
    fieldName: string,
    permissions: AppConfigPermission[],
  ): void {
    this.fieldPermissions[fieldName] = [...permissions];
  }
}
