// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { applyDecorators } from '@nestjs/common';
import { ApiProperty } from '@nestjs/swagger';
import { IsInt, IsOptional, IsNotEmpty } from 'class-validator';
/**
 * Options for property decorators
 */
interface PropOptions {
  required?: boolean;
}

/**
 * Property decorator for AppConfig RetryBackoffSeconds
 * @param {Object} options - Options for the decorator
 * @returns {PropertyDecorator}
 */
export function ApiAppConfigRetryBackoffSeconds(options: PropOptions = {}) {
  const { required = true } = options;

  return applyDecorators(
    ApiProperty({
      description: `Initial delay in seconds between retry attempts. Actual delay may use exponential backoff strategy to avoid overwhelming Slack API rate limits.`,
      example: 5,
      type: Number,
      required,
    }),
    IsInt(),
    required ? IsNotEmpty() : IsOptional(),
  );
}
