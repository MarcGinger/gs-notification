// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { applyDecorators } from '@nestjs/common';
import { ApiProperty } from '@nestjs/swagger';
import { IsOptional, IsNotEmpty, IsString, MaxLength } from 'class-validator';

/**
 * Options for property decorators
 */
interface PropOptions {
  required?: boolean;
}

/**
 * Property decorator for AppConfig DefaultLocale
 * @param {Object} options - Options for the decorator
 * @returns {PropertyDecorator}
 */
export function ApiAppConfigDefaultLocale(options: PropOptions = {}) {
  const { required = true } = options;

  return applyDecorators(
    ApiProperty({
      description: `Default language/locale for message content when user-specific locale is not available. Follows ISO 639-1 language codes combined with ISO 3166-1 country codes.`,
      example: `en_US`,
      type: String,
      required,
    }),
    IsString(),
    MaxLength(16),
    required ? IsNotEmpty() : IsOptional(),
  );
}
