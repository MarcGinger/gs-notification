// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { QueryHandler, IQueryHandler } from '@nestjs/cqrs';
import { DetailAppConfigResponse } from '../dtos';
import { GetAppConfigUseCase } from '../use-cases';
import { GetAppConfigQuery } from '../queries';
import { Result, DomainError } from 'src/shared/errors';

@QueryHandler(GetAppConfigQuery)
export class GetAppConfigHandler implements IQueryHandler<GetAppConfigQuery> {
  constructor(private readonly getAppConfigUseCase: GetAppConfigUseCase) {}

  async execute(
    query: GetAppConfigQuery,
  ): Promise<Result<DetailAppConfigResponse | null, DomainError>> {
    return this.getAppConfigUseCase.execute({
      user: query.user,
      code: query.code,
      correlationId: query.correlationId || 'query-handler',
    });
  }
}
