// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { CommandHandler, ICommandHandler } from '@nestjs/cqrs';
import { Inject } from '@nestjs/common';
import { Result, DomainError } from 'src/shared/errors';
import { DeleteAppConfigUseCase } from '../use-cases';
import { DeleteAppConfigCommand } from '../commands';

@CommandHandler(DeleteAppConfigCommand)
export class DeleteAppConfigHandler
  implements ICommandHandler<DeleteAppConfigCommand>
{
  constructor(
    @Inject(DeleteAppConfigUseCase)
    private readonly deleteAppConfigUseCase: DeleteAppConfigUseCase,
  ) {}

  async execute(
    command: DeleteAppConfigCommand,
  ): Promise<Result<void, DomainError>> {
    return await this.deleteAppConfigUseCase.execute({
      user: command.user,
      id: command.id,
      correlationId: command.correlationId,
      authorizationReason: 'CQRS Command Handler',
    });
  }
}
