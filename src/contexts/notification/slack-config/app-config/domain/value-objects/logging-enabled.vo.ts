// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  BooleanVOInstance,
  createBooleanVO,
  createBooleanVOErrors,
} from 'src/shared/domain/value-objects';
import { AppConfigErrors } from '..//errors/app-config.errors';

/**
 * LoggingEnabled Boolean Value Object
 * Represents a validated loggingEnabled with business rules
 */
export const AppConfigLoggingEnabled = createBooleanVO({
  name: 'LoggingEnabled',
  treatNullAsFalse: false,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createBooleanVOErrors(
    AppConfigErrors.INVALID_LOGGING_ENABLED,
    'LoggingEnabled',
  ),
});

/** Public instance type for LoggingEnabled */
export type AppConfigLoggingEnabled = BooleanVOInstance;

// Convenience creators
export const createAppConfigLoggingEnabled = (v: boolean) =>
  AppConfigLoggingEnabled.create(v);
export const appConfigLoggingEnabledFrom = (v: unknown) =>
  AppConfigLoggingEnabled.from(v);
