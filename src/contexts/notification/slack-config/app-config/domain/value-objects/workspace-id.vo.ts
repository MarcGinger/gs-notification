// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { AppConfigErrors } from '../errors/app-config.errors';

/**
 * WorkspaceId Value Object
 * Represents a validated workspaceId with business rules
 */
export const AppConfigWorkspaceId = createStringVO({
  name: 'WorkspaceId',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  maxLength: 64,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    AppConfigErrors.INVALID_WORKSPACE_ID,
    'WorkspaceId',
  ),
});

/**
 * Public instance type for WorkspaceId
 */
export type AppConfigWorkspaceId = StringVOInstance;

// Convenience creators
export const createAppConfigWorkspaceId = (s: string) =>
  AppConfigWorkspaceId.create(s);
export const appConfigWorkspaceIdFrom = (v: unknown) =>
  AppConfigWorkspaceId.from(v);
