// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { AppConfigErrors } from '../errors/app-config.errors';

/**
 * DefaultLocale Value Object
 * Represents a validated defaultLocale with business rules
 */
export const AppConfigDefaultLocale = createStringVO({
  name: 'DefaultLocale',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  maxLength: 16,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    AppConfigErrors.INVALID_DEFAULT_LOCALE,
    'DefaultLocale',
  ),
});

/**
 * Public instance type for DefaultLocale
 */
export type AppConfigDefaultLocale = StringVOInstance;

// Convenience creators
export const createAppConfigDefaultLocale = (s: string) =>
  AppConfigDefaultLocale.create(s);
export const appConfigDefaultLocaleFrom = (v: unknown) =>
  AppConfigDefaultLocale.from(v);
