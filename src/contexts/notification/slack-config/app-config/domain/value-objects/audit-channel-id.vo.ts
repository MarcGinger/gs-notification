// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { AppConfigErrors } from '../errors/app-config.errors';

/**
 * AuditChannelId Value Object
 * Represents a validated auditChannelId with business rules
 */
export const AppConfigAuditChannelId = createStringVO({
  name: 'AuditChannelId',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 64,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    AppConfigErrors.INVALID_AUDIT_CHANNEL_ID,
    'AuditChannelId',
  ),
});

/**
 * Public instance type for AuditChannelId
 */
export type AppConfigAuditChannelId = StringVOInstance;

// Convenience creators
export const createAppConfigAuditChannelId = (s: string) =>
  AppConfigAuditChannelId.create(s);
export const appConfigAuditChannelIdFrom = (v: unknown) =>
  AppConfigAuditChannelId.from(v);
