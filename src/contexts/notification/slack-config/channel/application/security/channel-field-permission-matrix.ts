// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { ChannelPermission } from '../../domain/permissions/channel.permissions';

/**
 * Field-level permission matrix for Channel updates.
 * Maps channel fields to the required permissions for modification.
 */
export class ChannelFieldPermissionMatrix {
  private static readonly fieldPermissions: Record<
    string,
    ChannelPermission[]
  > = {
    // Basic channel information fields - require standard update permission
    name: [ChannelPermission.DOMAIN_CHANNEL_UPDATE],
    description: [ChannelPermission.DOMAIN_CHANNEL_UPDATE],
    category: [ChannelPermission.DOMAIN_CHANNEL_UPDATE],

    // Status fields - may require special permissions or admin rights
    active: [
      ChannelPermission.DOMAIN_CHANNEL_UPDATE,
      ChannelPermission.DOMAIN_CHANNEL_ADMIN,
    ],
    status: [
      ChannelPermission.DOMAIN_CHANNEL_UPDATE,
      ChannelPermission.DOMAIN_CHANNEL_ADMIN,
    ],

    // Financial fields - typically require higher permissions
    fees: [ChannelPermission.DOMAIN_CHANNEL_UPDATE],
    rates: [ChannelPermission.DOMAIN_CHANNEL_UPDATE],
    pricing: [ChannelPermission.DOMAIN_CHANNEL_UPDATE],

    // Configuration fields
    channelCodes: [ChannelPermission.DOMAIN_CHANNEL_UPDATE],
    railCodes: [ChannelPermission.DOMAIN_CHANNEL_UPDATE],
    configuration: [ChannelPermission.DOMAIN_CHANNEL_UPDATE],

    // Administrative fields - require admin permissions
    tenant: [ChannelPermission.DOMAIN_CHANNEL_ADMIN],
    auditInfo: [ChannelPermission.DOMAIN_CHANNEL_ADMIN],
    metadata: [ChannelPermission.DOMAIN_CHANNEL_UPDATE],
  };

  /**
   * Get required permissions for a specific field.
   * Returns empty array if field has no special permission requirements.
   */
  static getRequiredPermissions(fieldName: string): ChannelPermission[] {
    return this.fieldPermissions[fieldName] || [];
  }

  /**
   * Check if a field has specific permission requirements.
   */
  static hasSpecialPermissions(fieldName: string): boolean {
    return fieldName in this.fieldPermissions;
  }

  /**
   * Get all fields that require a specific permission.
   */
  static getFieldsForPermission(permission: ChannelPermission): string[] {
    return Object.entries(this.fieldPermissions)
      .filter(([, perms]) => perms.includes(permission))
      .map(([field]) => field);
  }

  /**
   * Get all defined field permissions.
   */
  static getAllFieldPermissions(): Record<string, ChannelPermission[]> {
    return { ...this.fieldPermissions };
  }

  /**
   * Add or update permission requirements for a field.
   */
  static setFieldPermissions(
    fieldName: string,
    permissions: ChannelPermission[],
  ): void {
    this.fieldPermissions[fieldName] = [...permissions];
  }
}
