// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { QueryHandler, IQueryHandler } from '@nestjs/cqrs';
import { DetailChannelResponse } from '../dtos';
import { GetChannelUseCase } from '../use-cases';
import { GetChannelQuery } from '../queries';
import { Result, DomainError } from 'src/shared/errors';

@QueryHandler(GetChannelQuery)
export class GetChannelHandler implements IQueryHandler<GetChannelQuery> {
  constructor(private readonly getChannelUseCase: GetChannelUseCase) {}

  async execute(
    query: GetChannelQuery,
  ): Promise<Result<DetailChannelResponse | null, DomainError>> {
    return this.getChannelUseCase.execute({
      user: query.user,
      id: query.id,
      correlationId: query.correlationId || 'query-handler',
    });
  }
}
