// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { DomainError, Result, ok, err } from 'src/shared/errors';
import {
  createIntegerVO,
  createIntegerVOErrors,
  IntegerVOInstance,
  createDateTimeVO,
  createDateTimeVOErrors,
  DateTimeVOInstance,
} from 'src/shared/domain/value-objects';
import { Clock } from 'src/shared/domain/clock';
import { ChannelErrors } from '../errors/channel.errors';

/**
 * Channel Instance Value Objects
 *
 * Instance VOs for Channel aggregate lifecycle management.
 * Includes creation timestamps, update timestamps, and version tracking.
 * Follows the Clock injection pattern for deterministic testing.
 */

// ===========================================
// Channel Created At Instance VO
// ===========================================

const ChannelCreatedAtErrors = createDateTimeVOErrors(
  ChannelErrors.CREATED_AT_REQUIRED,
  'Channel Created At',
);

export const ChannelCreatedAt = createDateTimeVO({
  name: 'ChannelCreatedAt',
  allowFuture: false, // Cannot create channel in the future
  allowPast: true, // Can create with past timestamps (data migration)
  customValidation: (date: Date): Result<void, DomainError> => {
    // Optional: Add domain-specific creation time validation
    // For example, cannot be more than 1 year in the past
    const oneYearAgo = new Date();
    oneYearAgo.setFullYear(oneYearAgo.getFullYear() - 1);

    if (date < oneYearAgo) {
      return err({
        ...ChannelErrors.CREATED_AT_REQUIRED,
        detail: 'Channel creation date cannot be more than 1 year in the past',
        context: { value: date, maxPastDate: oneYearAgo },
      } as DomainError);
    }

    return ok(undefined);
  },
  errors: ChannelCreatedAtErrors,
});

export type ChannelCreatedAt = DateTimeVOInstance;

// ===========================================
// Channel Updated At Instance VO
// ===========================================

const ChannelUpdatedAtErrors = createDateTimeVOErrors(
  ChannelErrors.UPDATED_AT_REQUIRED,
  'Channel Updated At',
);

export const ChannelUpdatedAt = createDateTimeVO({
  name: 'ChannelUpdatedAt',
  allowFuture: false, // Cannot update channel in the future
  allowPast: true, // Can update with past timestamps (data correction)
  errors: ChannelUpdatedAtErrors,
});

export type ChannelUpdatedAt = DateTimeVOInstance;

// ===========================================
// Channel Version Instance VO
// ===========================================

const ChannelVersionErrors = createIntegerVOErrors(
  ChannelErrors.INVALID_CODE_DATA, // Reusing existing error until specific version error is added
  'Channel Version',
);

export const ChannelVersion = createIntegerVO({
  name: 'ChannelVersion',
  min: 0, // Version starts at 0
  allowNegative: false,
  errors: ChannelVersionErrors,
});

export type ChannelVersion = IntegerVOInstance;

// ===========================================
// Clock-Based Helper Functions (Explicit Dependencies)
// ===========================================

/**
 * Create ChannelCreatedAt from Clock (explicit dependency)
 * Deterministic and testable - pass a fake clock in tests
 */
export const createdAtNow = (
  clock: Clock,
): Result<ChannelCreatedAt, DomainError> =>
  ChannelCreatedAt.create(clock.now());

/**
 * Create ChannelUpdatedAt from Clock (explicit dependency)
 * Deterministic and testable - pass a fake clock in tests
 */
export const updatedAtNow = (
  clock: Clock,
): Result<ChannelUpdatedAt, DomainError> =>
  ChannelUpdatedAt.create(clock.now());

/**
 * Bump version by 1 (domain business rule)
 */
export const bumpVersion = (
  currentVersion: ChannelVersion,
): Result<ChannelVersion, DomainError> =>
  ChannelVersion.create(currentVersion.value + 1);

/**
 * Create initial version (starts at 1 for new aggregates)
 */
export const initialVersion = (): Result<ChannelVersion, DomainError> =>
  ChannelVersion.create(1);

// ===========================================
// Direct Creation Functions (Non-Clock Based)
// ===========================================

/**
 * Create ChannelCreatedAt from Date
 */
export const createChannelCreatedAt = (
  date: Date,
): Result<ChannelCreatedAt, DomainError> => ChannelCreatedAt.create(date);

/**
 * Create ChannelUpdatedAt from Date
 */
export const createChannelUpdatedAt = (
  date: Date,
): Result<ChannelUpdatedAt, DomainError> => ChannelUpdatedAt.create(date);

/**
 * Create ChannelVersion from number
 */
export const createChannelVersion = (
  version: number,
): Result<ChannelVersion, DomainError> => ChannelVersion.create(version);

// ===========================================
// Domain Invariants & Validation
// ===========================================

/**
 * Validate that updatedAt is not before createdAt (monotonic time)
 */
export const validateMonotonicTime = (
  createdAt: ChannelCreatedAt,
  updatedAt: ChannelUpdatedAt,
): Result<void, DomainError> => {
  if (updatedAt.value < createdAt.value) {
    return err({
      ...ChannelErrors.UPDATED_AT_REQUIRED,
      detail: 'Updated at cannot be before created at',
      context: {
        createdAt: createdAt.value.toISOString(),
        updatedAt: updatedAt.value.toISOString(),
      },
    } as DomainError);
  }
  return ok(undefined);
};

// ===========================================
// Legacy Compatibility (DEPRECATED)
// ===========================================

/**
 * @deprecated Use createdAtNow(clock) instead for better testability
 * This function hides the Date dependency and makes testing harder
 */
export function createChannelCreatedAtNow(): Result<
  ChannelCreatedAt,
  DomainError
> {
  return ChannelCreatedAt.create(new Date());
}

/**
 * @deprecated Use updatedAtNow(clock) instead for better testability
 * This function hides the Date dependency and makes testing harder
 */
export function createChannelUpdatedAtNow(): Result<
  ChannelUpdatedAt,
  DomainError
> {
  return ChannelUpdatedAt.create(new Date());
}
