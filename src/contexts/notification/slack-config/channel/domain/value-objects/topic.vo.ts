// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { ChannelErrors } from '../errors/channel.errors';

/**
 * Topic Value Object
 * Represents a validated topic with business rules
 */
export const ChannelTopic = createStringVO({
  name: 'Topic',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 255,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(ChannelErrors.INVALID_TOPIC, 'Topic'),
});

/**
 * Public instance type for Topic
 */
export type ChannelTopic = StringVOInstance;

// Convenience creators
export const createChannelTopic = (s: string) => ChannelTopic.create(s);
export const channelTopicFrom = (v: unknown) => ChannelTopic.from(v);
