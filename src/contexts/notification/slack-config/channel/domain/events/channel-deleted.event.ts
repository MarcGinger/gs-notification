// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { EventMetadata } from 'src/shared/domain/events';

/**
 * Channel Deleted Event Payload
 * Contains identifying information about the deleted channel
 */
export interface ChannelDeletedEventPayload {
  code: string;
  deletedAt: Date;
  version: number;
  metadata: EventMetadata;
}

/**
 * Channel Deleted Domain Event
 *
 * Emitted when a channel is successfully deleted.
 * Contains identifying information and deletion metadata.
 */
export class ChannelDeletedEvent {
  public readonly eventType = 'NotificationSlackConfigChannelDeleted.v1';
  public readonly eventVersion = 'v1';

  constructor(public readonly payload: ChannelDeletedEventPayload) {}

  // Factory method with security context
  static create(
    data: Omit<ChannelDeletedEventPayload, 'metadata'>,
    baseMetadata: EventMetadata,
  ): ChannelDeletedEvent {
    const metadata: EventMetadata = {
      ...baseMetadata,
      eventVersion: '1.0',
      source: 'notification.domain.channel',
    };

    return new ChannelDeletedEvent({
      ...data,
      metadata,
    });
  }

  get code(): string {
    return this.payload.code;
  }

  get deletedAt(): Date {
    return this.payload.deletedAt;
  }

  get version(): number {
    return this.payload.version;
  }

  get metadata(): EventMetadata {
    return this.payload.metadata;
  }
}
