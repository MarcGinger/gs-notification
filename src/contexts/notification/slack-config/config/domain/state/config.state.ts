// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  ConfigCreatedAt,
  ConfigUpdatedAt,
  ConfigVersion,
  ConfigAuditChannelId,
  ConfigDefaultLocale,
  ConfigLoggingEnabled,
  ConfigMaxRetryAttempts,
  ConfigMetadata,
  ConfigRetryBackoffSeconds,
  ConfigWorkspaceCode,
} from '../value-objects';

/**
 * Config Domain State
 *
 * Domain state interface that uses value objects to model business concepts
 * while maintaining compatibility with entity operations. Business fields are
 * modeled as value objects with proper validation and domain rules.
 *
 * Domain-First State Interface
 *
 * This interface represents the domain model where business concepts
 * are properly encapsulated as value objects when appropriate.
 */

export interface ConfigDomainState {
  workspaceCode: ConfigWorkspaceCode;
  maxRetryAttempts: ConfigMaxRetryAttempts;
  retryBackoffSeconds: ConfigRetryBackoffSeconds;
  defaultLocale: ConfigDefaultLocale;
  loggingEnabled: ConfigLoggingEnabled;
  auditChannelId?: ConfigAuditChannelId;
  metadata?: ConfigMetadata;
  version: ConfigVersion;
  createdAt: ConfigCreatedAt;
  updatedAt: ConfigUpdatedAt;
}

export type ConfigDomainStateOptional = Partial<ConfigDomainState>;
