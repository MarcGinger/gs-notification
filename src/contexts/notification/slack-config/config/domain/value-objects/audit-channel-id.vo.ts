// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { ConfigErrors } from '../errors/config.errors';

/**
 * AuditChannelId Value Object
 * Represents a validated auditChannelId with business rules
 */
export const ConfigAuditChannelId = createStringVO({
  name: 'AuditChannelId',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 64,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    ConfigErrors.INVALID_AUDIT_CHANNEL_ID,
    'AuditChannelId',
  ),
});

/**
 * Public instance type for AuditChannelId
 */
export type ConfigAuditChannelId = StringVOInstance;

// Convenience creators
export const createConfigAuditChannelId = (s: string) =>
  ConfigAuditChannelId.create(s);
export const configAuditChannelIdFrom = (v: unknown) =>
  ConfigAuditChannelId.from(v);
