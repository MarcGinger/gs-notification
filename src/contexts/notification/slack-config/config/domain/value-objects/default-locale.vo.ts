// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { ConfigErrors } from '../errors/config.errors';

/**
 * DefaultLocale Value Object
 * Represents a validated defaultLocale with business rules
 */
export const ConfigDefaultLocale = createStringVO({
  name: 'DefaultLocale',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  maxLength: 16,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    ConfigErrors.INVALID_DEFAULT_LOCALE,
    'DefaultLocale',
  ),
});

/**
 * Public instance type for DefaultLocale
 */
export type ConfigDefaultLocale = StringVOInstance;

// Convenience creators
export const createConfigDefaultLocale = (s: string) =>
  ConfigDefaultLocale.create(s);
export const configDefaultLocaleFrom = (v: unknown) =>
  ConfigDefaultLocale.from(v);
