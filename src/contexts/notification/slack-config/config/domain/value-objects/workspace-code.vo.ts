// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { ConfigErrors } from '../errors/config.errors';

/**
 * WorkspaceCode Value Object
 * Represents a validated workspaceCode with business rules
 */
export const ConfigWorkspaceCode = createStringVO({
  name: 'WorkspaceCode',
  trim: true,
  caseTransform: 'none',
  allowEmpty: false,

  maxLength: 32,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    ConfigErrors.INVALID_WORKSPACE_CODE,
    'WorkspaceCode',
  ),
});

/**
 * Public instance type for WorkspaceCode
 */
export type ConfigWorkspaceCode = StringVOInstance;

// Convenience creators
export const createConfigWorkspaceCode = (s: string) =>
  ConfigWorkspaceCode.create(s);
export const configWorkspaceCodeFrom = (v: unknown) =>
  ConfigWorkspaceCode.from(v);
