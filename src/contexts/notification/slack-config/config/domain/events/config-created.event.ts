// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

/**
 * Config Created Event Payload
 * Contains only business facts - no envelope metadata, timestamps, or versioning
 */
export interface ConfigCreatedEventPayload {
  workspaceCode: string;
  maxRetryAttempts: number;
  retryBackoffSeconds: number;
  defaultLocale: string;
  loggingEnabled: boolean;
  auditChannelId?: string;
  metadata?: Record<string, unknown>;
}

/**
 * Config Created Domain Event
 *
 * Emitted when a new config is successfully created.
 * Contains complete config information and creation metadata.
 * Enhanced with security metadata and PII protection capabilities.
 */
export class ConfigCreatedEvent {
  public readonly eventType = 'NotificationSlackConfigConfigCreated.v1';
  public readonly eventVersion = 'v1';

  constructor(public readonly payload: ConfigCreatedEventPayload) {}

  // Factory method - no metadata needed, just business data
  static create(data: ConfigCreatedEventPayload): ConfigCreatedEvent {
    return new ConfigCreatedEvent(data);
  }

  get workspaceCode(): string {
    return this.payload.workspaceCode;
  }

  get maxRetryAttempts(): number {
    return this.payload.maxRetryAttempts;
  }

  get retryBackoffSeconds(): number {
    return this.payload.retryBackoffSeconds;
  }

  get defaultLocale(): string {
    return this.payload.defaultLocale;
  }

  get loggingEnabled(): boolean {
    return this.payload.loggingEnabled;
  }

  get auditChannelId(): string | undefined {
    return this.payload.auditChannelId;
  }

  get metadata(): Record<string, unknown> | undefined {
    return this.payload.metadata;
  }
}
