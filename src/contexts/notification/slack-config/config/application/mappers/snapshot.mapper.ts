// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { ConfigSnapshotProps } from '../../domain/props';
import { DetailConfigResponse } from '../dtos';

/**
 * Config Snapshot to DTO Mappers
 * Reusable mapping functions for converting domain snapshots to DTOs
 */

/**
 * Map config snapshot to DTO response
 */
export const mapConfigSnapshotToDto = (
  snapshot: ConfigSnapshotProps,
): DetailConfigResponse => {
  return {
    workspaceCode: snapshot.workspaceCode,
    maxRetryAttempts: snapshot.maxRetryAttempts,
    retryBackoffSeconds: snapshot.retryBackoffSeconds,
    defaultLocale: snapshot.defaultLocale,
    loggingEnabled: snapshot.loggingEnabled,
    auditChannelId: snapshot.auditChannelId,
    metadata: snapshot.metadata,
  };
};
