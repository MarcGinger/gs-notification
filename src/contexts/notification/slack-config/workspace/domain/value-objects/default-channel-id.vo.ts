// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { WorkspaceErrors } from '../errors/workspace.errors';

/**
 * DefaultChannelId Value Object
 * Represents a validated defaultChannelId with business rules
 */
export const WorkspaceDefaultChannelId = createStringVO({
  name: 'DefaultChannelId',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  maxLength: 64,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    WorkspaceErrors.INVALID_DEFAULT_CHANNEL_ID,
    'DefaultChannelId',
  ),
});

/**
 * Public instance type for DefaultChannelId
 */
export type WorkspaceDefaultChannelId = StringVOInstance;

// Convenience creators
export const createWorkspaceDefaultChannelId = (s: string) =>
  WorkspaceDefaultChannelId.create(s);
export const workspaceDefaultChannelIdFrom = (v: unknown) =>
  WorkspaceDefaultChannelId.from(v);
