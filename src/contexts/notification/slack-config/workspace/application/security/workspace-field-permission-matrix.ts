// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { WorkspacePermission } from '../../domain/permissions/workspace.permissions';

/**
 * Field-level permission matrix for Workspace updates.
 * Maps workspace fields to the required permissions for modification.
 */
export class WorkspaceFieldPermissionMatrix {
  private static readonly fieldPermissions: Record<
    string,
    WorkspacePermission[]
  > = {
    // Basic workspace information fields - require standard update permission
    name: [WorkspacePermission.DOMAIN_WORKSPACE_UPDATE],
    description: [WorkspacePermission.DOMAIN_WORKSPACE_UPDATE],
    category: [WorkspacePermission.DOMAIN_WORKSPACE_UPDATE],

    // Status fields - may require special permissions or admin rights
    active: [
      WorkspacePermission.DOMAIN_WORKSPACE_UPDATE,
      WorkspacePermission.DOMAIN_WORKSPACE_ADMIN,
    ],
    status: [
      WorkspacePermission.DOMAIN_WORKSPACE_UPDATE,
      WorkspacePermission.DOMAIN_WORKSPACE_ADMIN,
    ],

    // Financial fields - typically require higher permissions
    fees: [WorkspacePermission.DOMAIN_WORKSPACE_UPDATE],
    rates: [WorkspacePermission.DOMAIN_WORKSPACE_UPDATE],
    pricing: [WorkspacePermission.DOMAIN_WORKSPACE_UPDATE],

    // Configuration fields
    channelCodes: [WorkspacePermission.DOMAIN_WORKSPACE_UPDATE],
    railCodes: [WorkspacePermission.DOMAIN_WORKSPACE_UPDATE],
    configuration: [WorkspacePermission.DOMAIN_WORKSPACE_UPDATE],

    // Administrative fields - require admin permissions
    tenantId: [WorkspacePermission.DOMAIN_WORKSPACE_ADMIN],
    auditInfo: [WorkspacePermission.DOMAIN_WORKSPACE_ADMIN],
    metadata: [WorkspacePermission.DOMAIN_WORKSPACE_UPDATE],
  };

  /**
   * Get required permissions for a specific field.
   * Returns empty array if field has no special permission requirements.
   */
  static getRequiredPermissions(fieldName: string): WorkspacePermission[] {
    return this.fieldPermissions[fieldName] || [];
  }

  /**
   * Check if a field has specific permission requirements.
   */
  static hasSpecialPermissions(fieldName: string): boolean {
    return fieldName in this.fieldPermissions;
  }

  /**
   * Get all fields that require a specific permission.
   */
  static getFieldsForPermission(permission: WorkspacePermission): string[] {
    return Object.entries(this.fieldPermissions)
      .filter(([, perms]) => perms.includes(permission))
      .map(([field]) => field);
  }

  /**
   * Get all defined field permissions.
   */
  static getAllFieldPermissions(): Record<string, WorkspacePermission[]> {
    return { ...this.fieldPermissions };
  }

  /**
   * Add or update permission requirements for a field.
   */
  static setFieldPermissions(
    fieldName: string,
    permissions: WorkspacePermission[],
  ): void {
    this.fieldPermissions[fieldName] = [...permissions];
  }
}
