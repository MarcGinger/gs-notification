// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { applyDecorators } from '@nestjs/common';
import { ApiProperty } from '@nestjs/swagger';
import { IsBoolean, IsOptional, IsNotEmpty } from 'class-validator';

/**
 * Options for property decorators (boolean)
 */
interface PropOptions {
  required?: boolean;
}

/**
 * Property decorator for Workspace Enabled
 * @param {Object} options - Options for the decorator
 * @returns {PropertyDecorator}
 */
export function ApiWorkspaceEnabled(options: PropOptions = {}) {
  const { required = true } = options;

  return applyDecorators(
    ApiProperty({
      description: `Global toggle for Slack integration functionality. When false, all notifications to this workspace are disabled, useful for maintenance or temporary suspension.`,
      example: true,
      type: Boolean,
      required,
    }),
    IsBoolean(),
    required ? IsNotEmpty() : IsOptional(),
  );
}
