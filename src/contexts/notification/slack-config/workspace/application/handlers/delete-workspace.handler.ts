// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { CommandHandler, ICommandHandler } from '@nestjs/cqrs';
import { Inject } from '@nestjs/common';
import { Result, DomainError } from 'src/shared/errors';
import { DeleteWorkspaceUseCase } from '../use-cases';
import { DeleteWorkspaceCommand } from '../commands';

@CommandHandler(DeleteWorkspaceCommand)
export class DeleteWorkspaceHandler
  implements ICommandHandler<DeleteWorkspaceCommand>
{
  constructor(
    @Inject(DeleteWorkspaceUseCase)
    private readonly deleteWorkspaceUseCase: DeleteWorkspaceUseCase,
  ) {}

  async execute(
    command: DeleteWorkspaceCommand,
  ): Promise<Result<void, DomainError>> {
    return await this.deleteWorkspaceUseCase.execute({
      user: command.user,
      id: command.id,
      correlationId: command.correlationId,
      authorizationReason: 'CQRS Command Handler',
    });
  }
}
