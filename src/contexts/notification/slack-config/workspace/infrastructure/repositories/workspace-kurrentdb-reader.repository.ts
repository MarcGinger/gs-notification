// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { Injectable, Inject } from '@nestjs/common';
import { APP_LOGGER, Log, componentLogger, Logger } from 'src/shared/logging';
import { Clock, CLOCK } from 'src/shared/infrastructure/time';
import {
  RepositoryLoggingUtil,
  RepositoryLoggingConfig,
  handleRepositoryError,
  RepositoryOptions,
} from 'src/shared/infrastructure/repositories';
import { Result, DomainError, err, ok } from 'src/shared/errors';
import { Option } from 'src/shared/domain/types';
import { ActorContext } from 'src/shared/application/context';
import { EventStoreService } from 'src/shared/infrastructure/eventstore';
import { RepositoryErrorFactory } from 'src/shared/domain/errors/repository.error';
import { CorrelationUtil } from 'src/shared/utilities';
import { WorkspaceSnapshotProps } from '../../domain/props';
import { WorkspaceProjectionKeys } from '../../workspace-projection-keys';
import { WorkspaceCode } from '../../domain/value-objects';
import { IWorkspaceReader } from '../../application/ports';
import { WorkspaceFieldValidatorUtil } from '../utilities/workspace-field-validator.util';

/**
 * Workspace Reader Repository - EventStore Implementation
 *
 * Bounded Context: Notification/Workspace
 * Handles basic read operations for Workspace aggregates using EventStore DB (ESDB)
 * as the source of truth with event replay and snapshot reconstruction.
 *
 * Benefits:
 * - EventStore as single source of truth
 * - Full event replay capability for audit and debugging
 * - Consistent reads from same data source as writes
 * - Supports temporal queries and point-in-time reconstruction
 * - Comprehensive logging and error handling
 *
 * @domain Notification Context - Workspace Reader Repository (EventStore)
 * @layer Infrastructure
 * @pattern Repository Pattern + Event Sourcing
 */
@Injectable()
export class WorkspaceReaderRepository implements IWorkspaceReader {
  private readonly logger: Logger;
  private readonly loggingConfig: RepositoryLoggingConfig;

  constructor(
    private readonly eventStore: EventStoreService,
    @Inject(APP_LOGGER) baseLogger: Logger,
    @Inject(CLOCK) private readonly clock: Clock,
  ) {
    this.loggingConfig = {
      serviceName: 'WorkspaceConfigService',
      component: 'WorkspaceReaderRepository',
    };
    this.logger = componentLogger(baseLogger, this.loggingConfig.component);

    Log.info(
      this.logger,
      'WorkspaceReaderRepository initialized with EventStore backend',
      {
        component: this.loggingConfig.component,
        eventStoreConnected: true,
        sourceOfTruth: 'eventstore',
      },
    );
  }

  /**
   * Generate EventStore stream name for workspace aggregate
   * Uses same pattern as WorkspaceWriter for consistency
   */
  private generateStreamName(tenant: string, code: string): string {
    return `${WorkspaceProjectionKeys.getEventStoreStreamPrefix()}-${tenant}-${code}`;
  }

  /**
   * Get Workspace aggregate from EventStore last event (complete aggregate state)
   */
  private async getWorkspaceFromLastEvent(
    tenant: string,
    workspaceCode: string,
  ): Promise<WorkspaceSnapshotProps | null> {
    try {
      const streamName = this.generateStreamName(tenant, workspaceCode);

      // Read the last event from the stream (contains complete aggregate state)
      let lastEvent: { data?: unknown; type?: string } | null = null;
      const iter = this.eventStore.readStream(streamName, {
        direction: 'backwards',
        maxCount: 1,
      });

      for await (const resolvedEvent of iter) {
        if (resolvedEvent.event) {
          lastEvent = resolvedEvent.event;
          break; // We only need the last event
        }
      }

      if (!lastEvent) {
        return null;
      }

      // Extract complete aggregate data from the last event
      if (!lastEvent.data || typeof lastEvent.data !== 'object') {
        Log.warn(
          this.logger,
          'Last event missing required data for reconstruction',
          {
            method: 'getWorkspaceFromLastEvent',
            streamName,
            eventType: lastEvent.type || 'unknown',
          },
        );
        return null;
      }

      // The last event contains the complete aggregate state - extract it directly
      const aggregateData = lastEvent.data as Record<string, any>;

      // Use WorkspaceFieldValidatorUtil to create validated workspace snapshot
      const projectorData =
        WorkspaceFieldValidatorUtil.createWorkspaceProjectorDataFromEventData(
          aggregateData,
        );

      // Parse object fields using safeParseJSON utility

      // Convert projector data to snapshot props with proper type conversion
      const workspaceSnapshot: WorkspaceSnapshotProps = {
        ...projectorData,
      };

      return workspaceSnapshot;
    } catch (error) {
      Log.error(this.logger, 'Failed to get Workspace from EventStore', {
        method: 'getWorkspaceFromLastEvent',
        error: (error as Error).message,
        tenant,
        workspaceCode,
      });
      return null;
    }
  }

  /**
   * Helper to create consistent logging context using shared utilities
   */
  private createLogContext(
    operation: string,
    correlationId: string,
    actor: ActorContext,
    additionalContext?: Record<string, unknown>,
  ): Record<string, unknown> {
    return RepositoryLoggingUtil.createLogContext(
      this.loggingConfig,
      this.clock,
      operation,
      correlationId,
      actor,
      additionalContext,
    );
  }

  /**
   * Helper to assess operation risk level using shared utilities
   */
  private assessOperationRisk(operation: string): 'LOW' | 'MEDIUM' | 'HIGH' {
    return RepositoryLoggingUtil.assessOperationRisk(operation);
  }

  /**
   * Find a Workspace by its unique identifier
   * @param actor - The authenticated user context
   * @param code - The unique identifier of the Workspace
   * @param options - Optional repository options
   * @returns Result containing the Workspace snapshot or null if not found
   */
  async findById(
    actor: ActorContext,
    code: WorkspaceCode,
    options?: RepositoryOptions,
  ): Promise<Result<Option<WorkspaceSnapshotProps>, DomainError>> {
    const operation = 'findById';
    const riskLevel = this.assessOperationRisk(operation);
    const correlationId =
      options?.correlationId ??
      CorrelationUtil.generateForOperation('workspace-find-by-id');

    const logContext = this.createLogContext(operation, correlationId, actor, {
      riskLevel,
      targetCode: code.value,
      customCorrelationId: !!options?.correlationId,
      source: options?.source,
      requestId: options?.requestId,
      dataSource: 'eventstore',
    });

    // Validate actor context with enhanced security logging
    const validation = RepositoryLoggingUtil.validateActorContext(
      this.logger,
      actor,
      logContext,
    );
    if (!validation.ok) return Promise.resolve(err(validation.error));

    // Guard tenant explicitly
    if (!actor.tenant) {
      return Promise.resolve(
        err(
          RepositoryErrorFactory.validationError('tenant', 'Missing tenant id'),
        ),
      );
    }

    // Log successful authorization
    RepositoryLoggingUtil.logAuthorizationSuccess(
      this.logger,
      operation,
      logContext,
      { queryType: 'workspace_lookup', scope: 'eventstore_stream' },
    );

    try {
      Log.debug(this.logger, 'Finding workspace by code in EventStore', {
        ...logContext,
        queryDetails: {
          scope: 'eventstore_stream',
          method: 'getWorkspaceFromLastEvent',
          tenant: actor.tenant,
          code: code.value,
        },
      });

      // Get workspace from EventStore last event
      const workspaceSnapshot = await this.getWorkspaceFromLastEvent(
        actor.tenant,
        code.value,
      );

      if (!workspaceSnapshot) {
        RepositoryLoggingUtil.logQueryMetrics(
          this.logger,
          operation,
          logContext,
          {
            resultCount: 0,
            dataQuality: 'empty',
          },
        );
        return ok(Option.none());
      }

      // Log query metrics using shared utility
      RepositoryLoggingUtil.logQueryMetrics(
        this.logger,
        operation,
        logContext,
        {
          resultCount: 1,
          dataQuality: 'good',
          sampleData: {
            code: workspaceSnapshot.code,
            version: workspaceSnapshot.version,
          },
        },
      );

      return ok(Option.some(workspaceSnapshot));
    } catch (error) {
      // Log operation error using shared utility
      RepositoryLoggingUtil.logOperationError(
        this.logger,
        operation,
        logContext,
        error as Error,
        'HIGH',
      );

      // Handle and return the classified error using shared utility
      return Promise.resolve(handleRepositoryError(error));
    }
  }

  /**
   * Check if a workspace exists by code (for write-path validation)
   * @param actor - The authenticated user context
   * @param code - The unique identifier of the Workspace
   * @param options - Optional repository options
   * @returns Result containing boolean indicating existence
   */
  async exists(
    actor: ActorContext,
    code: WorkspaceCode,
    options?: RepositoryOptions,
  ): Promise<Result<boolean, DomainError>> {
    const operation = 'exists';
    const riskLevel = this.assessOperationRisk(operation);
    const correlationId =
      options?.correlationId ??
      CorrelationUtil.generateForOperation('workspace-exists-check');

    const logContext = this.createLogContext(operation, correlationId, actor, {
      riskLevel,
      targetCode: code.value,
      customCorrelationId: !!options?.correlationId,
      source: options?.source,
      requestId: options?.requestId,
      dataSource: 'eventstore',
    });

    // Validate actor context
    const validation = RepositoryLoggingUtil.validateActorContext(
      this.logger,
      actor,
      logContext,
    );
    if (!validation.ok) return Promise.resolve(err(validation.error));

    // Log successful authorization
    RepositoryLoggingUtil.logAuthorizationSuccess(
      this.logger,
      operation,
      logContext,
      { queryType: 'existence_check', scope: 'eventstore_stream' },
    );

    try {
      Log.debug(this.logger, 'Checking workspace existence in EventStore', {
        ...logContext,
        queryDetails: {
          scope: 'eventstore_stream',
          method: 'getWorkspaceFromLastEvent',
          tenant: actor.tenant,
          code: code.value,
        },
      });

      // Check existence by reading last event from EventStore
      const workspaceSnapshot = await this.getWorkspaceFromLastEvent(
        actor.tenant,
        code.value,
      );
      const exists = workspaceSnapshot !== null;

      // Log query metrics using shared utility
      RepositoryLoggingUtil.logQueryMetrics(
        this.logger,
        operation,
        logContext,
        {
          resultCount: exists ? 1 : 0,
          dataQuality: 'good',
          sampleData: { exists, code: code.value },
        },
      );

      return ok(exists);
    } catch (error) {
      // Log operation error using shared utility
      RepositoryLoggingUtil.logOperationError(
        this.logger,
        operation,
        logContext,
        error as Error,
        'HIGH',
      );

      // Handle and return the classified error using shared utility
      return Promise.resolve(handleRepositoryError(error));
    }
  }

  /**
   * Get workspace version for optimistic concurrency control
   * @param actor - The authenticated user context
   * @param code - The unique identifier of the Workspace
   * @param options - Optional repository options
   * @returns Result containing version number or null if not found
   */
  async getVersion(
    actor: ActorContext,
    code: WorkspaceCode,
    options?: RepositoryOptions,
  ): Promise<Result<Option<number>, DomainError>> {
    const operation = 'getVersion';
    const riskLevel = this.assessOperationRisk(operation);
    const correlationId =
      options?.correlationId ??
      CorrelationUtil.generateForOperation('workspace-get-version');

    const logContext = this.createLogContext(operation, correlationId, actor, {
      riskLevel,
      targetCode: code.value,
      customCorrelationId: !!options?.correlationId,
      source: options?.source,
      requestId: options?.requestId,
      dataSource: 'eventstore',
    });

    // Validate actor context
    const validation = RepositoryLoggingUtil.validateActorContext(
      this.logger,
      actor,
      logContext,
    );
    if (!validation.ok) return Promise.resolve(err(validation.error));

    // Log successful authorization
    RepositoryLoggingUtil.logAuthorizationSuccess(
      this.logger,
      operation,
      logContext,
      { queryType: 'version_lookup', scope: 'eventstore_stream' },
    );

    try {
      Log.debug(this.logger, 'Getting workspace version from EventStore', {
        ...logContext,
        queryDetails: {
          scope: 'eventstore_stream',
          method: 'getWorkspaceFromLastEvent',
          tenant: actor.tenant,
          code: code.value,
        },
      });

      // Get workspace from EventStore last event to retrieve version
      const workspaceSnapshot = await this.getWorkspaceFromLastEvent(
        actor.tenant,
        code.value,
      );

      if (!workspaceSnapshot) {
        RepositoryLoggingUtil.logQueryMetrics(
          this.logger,
          operation,
          logContext,
          {
            resultCount: 0,
            dataQuality: 'empty',
          },
        );
        return ok(Option.none());
      }

      const version = workspaceSnapshot.version;

      // Log query metrics using shared utility
      RepositoryLoggingUtil.logQueryMetrics(
        this.logger,
        operation,
        logContext,
        {
          resultCount: 1,
          dataQuality: 'good',
          sampleData: { code: code.value, version },
        },
      );

      return ok(Option.some(version));
    } catch (error) {
      // Log operation error using shared utility
      RepositoryLoggingUtil.logOperationError(
        this.logger,
        operation,
        logContext,
        error as Error,
        'HIGH',
      );

      // Handle and return the classified error using shared utility
      return Promise.resolve(handleRepositoryError(error));
    }
  }

  /**
   * Get minimal workspace data for write-path operations
   * @param actor - The authenticated user context
   * @param code - The unique identifier of the Workspace
   * @param options - Optional repository options
   * @returns Result containing minimal workspace data or null if not found
   */
  async getMinimal(
    actor: ActorContext,
    code: WorkspaceCode,
    options?: RepositoryOptions,
  ): Promise<Result<Option<{ code: string; version: number }>, DomainError>> {
    const operation = 'getMinimal';
    const riskLevel = this.assessOperationRisk(operation);
    const correlationId =
      options?.correlationId ??
      CorrelationUtil.generateForOperation('workspace-get-minimal');

    const logContext = this.createLogContext(operation, correlationId, actor, {
      riskLevel,
      targetCode: code.value,
      customCorrelationId: !!options?.correlationId,
      source: options?.source,
      requestId: options?.requestId,
      dataSource: 'eventstore',
    });

    // Validate actor context
    const validation = RepositoryLoggingUtil.validateActorContext(
      this.logger,
      actor,
      logContext,
    );
    if (!validation.ok) return Promise.resolve(err(validation.error));

    // Log successful authorization
    RepositoryLoggingUtil.logAuthorizationSuccess(
      this.logger,
      operation,
      logContext,
      { queryType: 'minimal_lookup', scope: 'eventstore_stream' },
    );

    try {
      Log.debug(this.logger, 'Getting minimal workspace data from EventStore', {
        ...logContext,
        queryDetails: {
          scope: 'eventstore_stream',
          method: 'getWorkspaceFromLastEvent',
          tenant: actor.tenant,
          code: code.value,
        },
      });

      // Get workspace from EventStore last event to retrieve minimal data
      const workspaceSnapshot = await this.getWorkspaceFromLastEvent(
        actor.tenant,
        code.value,
      );

      if (!workspaceSnapshot) {
        RepositoryLoggingUtil.logQueryMetrics(
          this.logger,
          operation,
          logContext,
          {
            resultCount: 0,
            dataQuality: 'empty',
          },
        );
        return ok(Option.none());
      }

      const minimal = {
        code: workspaceSnapshot.code,
        version: workspaceSnapshot.version,
      };

      // Log query metrics using shared utility
      RepositoryLoggingUtil.logQueryMetrics(
        this.logger,
        operation,
        logContext,
        {
          resultCount: 1,
          dataQuality: 'good',
          sampleData: { ...minimal },
        },
      );

      return ok(Option.some(minimal));
    } catch (error) {
      // Log operation error using shared utility
      RepositoryLoggingUtil.logOperationError(
        this.logger,
        operation,
        logContext,
        error as Error,
        'HIGH',
      );

      // Handle and return the classified error using shared utility
      return handleRepositoryError(error);
    }
  }
}
