// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { INestApplication } from '@nestjs/common';
import { AppConfigUtil } from 'src/shared/config/app-config.util';
import { NotificationDocumentation } from './notification.doc';
import { setupNotificationSlackConfigDocs } from './slack-config/slack-config-swagger.util';

/**
 * Setup notification application/service Swagger documentation
 * Configures documentation for all aggregates within the notification
 */
export function setupNotificationConfigDocs(
  app: INestApplication,
  port: string | number,
): Record<string, string> {
  if (AppConfigUtil.isProduction()) {
    return {};
  }

  const notificationUrls = NotificationDocumentation.setupAll(app, port);
  const notificationSlackConfigUrls = setupNotificationSlackConfigDocs(
    app,
    port,
  );

  return {
    ...notificationUrls,
    ...notificationSlackConfigUrls,
  };
}
