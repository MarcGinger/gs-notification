// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { SlackConfigServiceConstants } from '../service-constants';
/**
 * Domain value object for Template projection keys
 * Centralizes all key patterns to prevent duplication and ensure consistency
 * Follows DDD principle of explicit domain concepts
 */
export class TemplateProjectionKeys {
  // EventStore DB patterns
  static readonly ESDB_BOUNDED_CONTEXT =
    SlackConfigServiceConstants.MODULE_NAMESPACE;
  static readonly ESDB_AGGREGATE_NAME = 'template';
  static readonly ESDB_VERSION = 'v1';

  /**
   * Get EventStore stream prefix for individual streams
   * Format: <serviceName>.<aggregate>.<version>
   * Example: slack-config.template.v1
   */
  static getEventStoreStreamPrefix(): string {
    return `${this.ESDB_BOUNDED_CONTEXT}.${this.ESDB_AGGREGATE_NAME}.${this.ESDB_VERSION}`;
  }

  /**
   * Get EventStore category projection pattern for catchup
   * Format: $ce-<boundedContext>.<aggregate>.<version>
   * Example: $ce-slack-config.template.v1
   */
  static getEventStoreCategoryPattern(): string {
    return `$ce-${this.getEventStoreStreamPrefix()}`;
  }

  /**
   * Get individual EventStore stream name for specific tenant and code
   * Format: <boundedContext>.<aggregate>.<version>-{tenant}-{code}
   * Example: slack-config.template.v1-tenant-12345
   */
  static getEventStoreStreamName(tenant: string, code: string): string {
    return `${this.getEventStoreStreamPrefix()}-${tenant}-${code}`;
  }

  /**
   * Extract tenant and code from EventStore stream name
   * Validates format: <boundedContext>.<aggregate>.<version>-{tenant}-{code}
   */
  static extractFromStreamName(
    streamName: string,
  ): { tenant: string; code: string } | null {
    const prefix = this.getEventStoreStreamPrefix();
    const pattern = new RegExp(
      `^${prefix.replace(/\./g, '\\.')}-([^-]+)-(.+)$`,
    );
    const match = streamName.match(pattern);
    if (!match) return null;

    const [, tenant, code] = match;
    return { tenant, code };
  }

  /**
   * Validate if stream name matches template pattern
   */
  static isTemplateStream(streamName: string): boolean {
    return this.extractFromStreamName(streamName) !== null;
  }

  /**
   * Get stream pattern for tenant-specific catchup
   * Format: <boundedContext>.<aggregate>.<version>-{tenant}-*
   */
  static getTenantStreamPattern(tenant: string): string {
    return `${this.getEventStoreStreamPrefix()}-${tenant}-*`;
  }

  /**
   * Get global stream pattern for all tenants
   * Format: <boundedContext>.<aggregate>.<version>-*
   */
  static getGlobalStreamPattern(): string {
    return `${this.getEventStoreStreamPrefix()}-*`;
  }

  // Redis projection key patterns
  static readonly REDIS_KEY_PREFIX = 'notification-slack.slack';
  static readonly REDIS_VERSION = 'v1';
  static readonly PROJECTOR_NAME = 'template-projector';
  static readonly SUBSCRIPTION_GROUP = 'template-projection';

  // ⚠️ IMPORTANT: Cache optimization keys use different format
  // The CacheOptimizationUtils class uses notification-slack.slack:pp:ver:{tenant}:template:{templateId}
  // for version hints. These are NOT migrated and serve different purposes:
  // - Business keys (this class): Human-readable, descriptive, versioned
  // - Cache keys (CacheOptimizationUtils): Compact, performance-optimized, module-namespaced
  // See docs/REDIS_KEY_ARCHITECTURE.md for complete explanation

  /**
   * Get Redis key for template projection with cluster-safe hash tags
   * Format: notification-slack.slack:v1:{tenantId}:template:{templateId}
   */
  static getRedisTemplateKey(tenantId: string, templateId: string): string {
    return `${this.REDIS_KEY_PREFIX}:${this.REDIS_VERSION}:{${tenantId}}:template:${templateId}`;
  }

  /**
   * Get Redis key for template index with cluster-safe hash tags
   * Format: notification-slack.slack:v1:{tenantId}:idx:template:list
   */
  static getRedisTemplateIndexKey(tenantId: string): string {
    return `${this.REDIS_KEY_PREFIX}:${this.REDIS_VERSION}:{${tenantId}}:idx:template:list`;
  }

  /**
   * Get Redis key for category-based template index with cluster-safe hash tags
   * Format: notification-slack.slack:v1:{tenantId}:idx:template:by-category:{category}
   */
  static getRedisCategoryIndexKey(tenantId: string, category: string): string {
    return `${this.REDIS_KEY_PREFIX}:${this.REDIS_VERSION}:{${tenantId}}:idx:template:by-category:${category}`;
  }

  /**
   * Get Redis key pattern for all templates in a tenant
   * Format: notification-slack.slack:v1:{tenantId}:template:*
   */
  static getRedisTenantTemplatePattern(tenantId: string): string {
    return `${this.REDIS_KEY_PREFIX}:${this.REDIS_VERSION}:{${tenantId}}:template:*`;
  }

  /**
   * Get Redis key pattern for all template projections
   * Format: notification-slack.slack:v1:*:template:*
   */
  static getRedisAllTemplatesPattern(): string {
    return `${this.REDIS_KEY_PREFIX}:${this.REDIS_VERSION}:*:template:*`;
  }

  /**
   * Get Redis key pattern for all projections in a specific tenant
   * Format: notification-slack.slack:v1:{tenantId}:*
   */
  static getRedisTenantPattern(tenantId: string): string {
    return `${this.REDIS_KEY_PREFIX}:${this.REDIS_VERSION}:{${tenantId}}:*`;
  }

  /**
   * Get Redis key pattern for all slack-config projections
   * Format: notification-slack.slack:*
   */
  static getRedisSlackConfigPattern(): string {
    return `${this.REDIS_KEY_PREFIX}:*`;
  }

  /**
   * Get new namespaced cache key format (current format)
   * Format: notification-slack.slack:pp:ver:{tenantId}:template:{templateId}
   */
  static getNamespacedVersionHintKey(
    tenantId: string,
    templateId: string,
  ): string {
    return `notification-slack.slack:pp:ver:{${tenantId}}:template:${templateId}`;
  }

  /**
   * Get current namespaced key patterns (should not be cleaned up)
   */
  static getCurrentKeyPatterns(): string[] {
    return [
      'notification-slack.slack:v1:*:template:*', // Business data keys
      'notification-slack.slack:pp:ver:*:template:*', // Namespaced version hints
      'notification-slack.slack:pd:dup:*', // Namespaced dedup keys
    ];
  }
}
