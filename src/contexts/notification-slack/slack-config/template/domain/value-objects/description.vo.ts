// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  StringVOInstance,
  createStringVO,
  createStringVOErrors,
} from 'src/shared/domain/value-objects';
import { TemplateErrors } from '../errors/template.errors';

/**
 * Description Value Object
 * Represents a validated description with business rules
 */
export const TemplateDescription = createStringVO({
  name: 'Description',
  trim: true,
  caseTransform: 'none',
  allowEmpty: true,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createStringVOErrors(
    TemplateErrors.INVALID_DESCRIPTION,
    'Description',
  ),
});

/**
 * Public instance type for Description
 */
export type TemplateDescription = StringVOInstance;

// Convenience creators
export const createTemplateDescription = (s: string) =>
  TemplateDescription.create(s);
export const templateDescriptionFrom = (v: unknown) =>
  TemplateDescription.from(v);
