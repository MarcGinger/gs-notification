// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import {
  BooleanVOInstance,
  createBooleanVO,
  createBooleanVOErrors,
} from 'src/shared/domain/value-objects';
import { WorkspaceErrors } from '..//errors/workspace.errors';

/**
 * Enabled Boolean Value Object
 * Represents a validated enabled with business rules
 */
export const WorkspaceEnabled = createBooleanVO({
  name: 'Enabled',
  treatNullAsFalse: false,

  // Optional: declarative refinements for advanced validation
  // refinements: [
  // ],

  errors: createBooleanVOErrors(WorkspaceErrors.INVALID_ENABLED, 'Enabled'),
});

/** Public instance type for Enabled */
export type WorkspaceEnabled = BooleanVOInstance;

// Convenience creators
export const createWorkspaceEnabled = (v: boolean) =>
  WorkspaceEnabled.create(v);
export const workspaceEnabledFrom = (v: unknown) => WorkspaceEnabled.from(v);
