// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { EventMetadata } from 'src/shared/domain/events';

/**
 * Workspace Deleted Event Payload
 * Contains identifying information about the deleted workspace
 */
export interface WorkspaceDeletedEventPayload {
  id: string;
  deletedAt: Date;
  version: number;
  metadata: EventMetadata;
}

/**
 * Workspace Deleted Domain Event
 *
 * Emitted when a workspace is successfully deleted.
 * Contains identifying information and deletion metadata.
 */
export class WorkspaceDeletedEvent {
  public readonly eventType = 'NotificationSlackConfigWorkspaceDeleted.v1';
  public readonly eventVersion = 'v1';

  constructor(public readonly payload: WorkspaceDeletedEventPayload) {}

  // Factory method with security context
  static create(
    data: Omit<WorkspaceDeletedEventPayload, 'metadata'>,
    baseMetadata: EventMetadata,
  ): WorkspaceDeletedEvent {
    const metadata: EventMetadata = {
      ...baseMetadata,
      eventVersion: '1.0',
      source: 'notification-slack.domain.workspace',
    };

    return new WorkspaceDeletedEvent({
      ...data,
      metadata,
    });
  }

  get id(): string {
    return this.payload.id;
  }

  get deletedAt(): Date {
    return this.payload.deletedAt;
  }

  get version(): number {
    return this.payload.version;
  }

  get metadata(): EventMetadata {
    return this.payload.metadata;
  }
}
