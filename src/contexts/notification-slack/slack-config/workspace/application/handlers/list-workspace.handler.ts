// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { QueryHandler, IQueryHandler } from '@nestjs/cqrs';
import { Injectable } from '@nestjs/common';
import { DomainError, Result } from 'src/shared/errors';
import { ListWorkspaceQuery } from '../queries';
import { ListWorkspaceUseCase } from '../use-cases';
import { WorkspacePageResponse } from '../dtos';

/**
 * ✅ Enhanced CQRS Query Handler with use case delegation
 * Follows the pattern: Handler → Use Case → Repository
 */
@Injectable()
@QueryHandler(ListWorkspaceQuery)
export class ListWorkspaceHandler implements IQueryHandler<ListWorkspaceQuery> {
  constructor(private readonly listWorkspaceUseCase: ListWorkspaceUseCase) {}

  /**
   * Handle the list workspace query by delegating to the use case
   */
  async execute(
    query: ListWorkspaceQuery,
  ): Promise<Result<WorkspacePageResponse, DomainError>> {
    // ✅ Delegate to use case for business logic
    return await this.listWorkspaceUseCase.execute({
      user: query.user,
      filter: query.filter,
      correlationId: query.correlationId || 'unknown',
    });
  }
}
