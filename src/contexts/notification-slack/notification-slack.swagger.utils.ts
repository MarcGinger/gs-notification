// @generated by gen v1.0.0 hash:regen
// REMOVE THIS COMMENT TO STOP AUTOMATIC UPDATES TO THIS BLOCK

import { INestApplication } from '@nestjs/common';
import { AppConfigUtil } from 'src/shared/config/app-config.util';
import { NotificationSlackDocumentation } from './notification-slack.doc';
import { setupNotificationSlackSlackConfigDocs } from './slack-config/slack-config-swagger.util';

/**
 * Setup notification-slack application/service Swagger documentation
 * Configures documentation for all aggregates within the notification_slack
 */
export function setupNotificationSlackConfigDocs(
  app: INestApplication,
  port: string | number,
): Record<string, string> {
  if (AppConfigUtil.isProduction()) {
    return {};
  }

  const notificationSlackUrls = NotificationSlackDocumentation.setupAll(
    app,
    port,
  );
  const notificationSlackSlackConfigUrls =
    setupNotificationSlackSlackConfigDocs(app, port);

  return {
    ...notificationSlackUrls,
    ...notificationSlackSlackConfigUrls,
  };
}
